# 接缝带定义（权威）

> 状态定稿 2026-08。本文是接缝带几何的**唯一权威定义**，代码与文档统一用词以本文为准。
> 实现入口：`Source/Generation/SeamlessPolygonGeometry.cs`（`BuildSeamBand` / `SeamBandInfo`）。

## 0. 背景与动机

旧架构中"接缝带"没有显式定义：传送点带 = 距 void 边界切比雪夫 1..2 格（`ComputeVoidBand(N=SeamOverlap)`），
offset = 源图多边形镜像假设 `round(2·(midA−centerA) − SeamOverlap·unit)`。两图多边形各自用自己
tile 中心的切平面基独立投影，同一条世界共享边在两端局部坐标系中的内切距不等（顶点方向角间隔
差 1° ≈ 内切距差 ~1 格），镜像假设把 A 的内切距强加给 C → **系统性 ±1 格渲染错位**（2026-08 实测：
聚焦 C 时 C 的接缝带比 A 背景内偏一格；赤道正北侧同样复现，排除地块角度因素）。

本次重构把接缝带显式定义，并把偏差吸收职责从"offset 固定收缩 2 格"移交"3 圈接缝带"。

## 1. 术语体系

以**离散边为锚**的方向词：**接缝带内** = 核心侧（朝地图中心）；**接缝带外** = void 侧。

**禁止**用"六边形外/内"指格集合——离散边格横跨连续边，内外归属有歧义。"格中心在多边形内/外"
（`ContainsPointAt(格中心)`）仅作为无歧义的标量判定使用。

## 2. 定义

从核心区向外的圈层顺序：

```
核心区 → 带内圈 → 离散边圈 → 带外圈 → void（接缝带外）
        └──────── 接缝带 B ────────┘
```

| 术语 | 定义 |
|---|---|
| **连续边** | 多边形顶点（等边模型——目标边长 0.5S、半径 = 0.5S/相邻顶点方向平均弦长，正六边形下精确回落 0.5S、正五边形 ≈0.4253S；2026-08 修正勿回退为半径恒定——旧模型五边形边长 +17.6% 且五-六共享边两图投影不等长、offset 中点对齐后向边端发散 ~11 格超带吸收能力，`BuildPolygonVertices`）环连成的 n 条浮点线段 `(verts[j], verts[(j+1)%n])`。N=5/6 通用。 |
| **离散边 D（离散边圈）** | 格方块 `[x,x+1]×[z,z+1]` 与任一连续边线段**相交**（slab 法，含边界/角点接触）的格集合。横跨连续边，不参与内外分类。恰好经过四格公共角时 2×2 格都入 D。 |
| **接缝带 B** | `Cheb(D, 1)` = 离散边 + 切比雪夫内外各一圈，约 3 圈厚。对角接触被切比雪夫膨胀填满，**无洞**。 |
| **带内圈** | `B − D` 中格中心在多边形内的格。无传送点。 |
| **带外圈** | `B − D` 中格中心在多边形外的格。**实地形**（不再是 void），属传送圈。 |
| **传送圈** | `D ∪ 带外圈`（外侧 2 圈）。传送点铺设范围（`Standable` 过滤后）。 |
| **void（接缝带外）** | 格中心在多边形外且 `∉ B`。铺 `RimExodus_Void`。 |
| **核心区** | 格中心在多边形内且 `∉ B` 的格。 |
| **过渡带 T** | 接缝带内（核心侧）距 B ≥1 的**全部**格（膨胀到核心区穷尽）。SeamOverride 混合枚举范围（运行时 strip 无命中的格自然跳过）。 |
| **接缝带外条带** | 接缝带外（void 侧）**全部**格（膨胀到方形边穷尽）。void 区，其原生三层快照是新图混合的参考数据——**全深存储**（权重衰减轴需要：接近源六边形高 → 源方形边 0）。 |

### void 判定的防孤岛等价性

新判定"中心在外且 ∉B"取代旧"中心或 4 角任一在内→非 void"的角检测。凸多边形下，角在内的格
必属 `{中心在内} ∪ D`（格方块与多边形有面积交集 ⟺ 中心在内或某条边线段穿过格方块）⊆
`{中心在内} ∪ B`，故两者防孤岛等价；差集恰为带外圈（有意从 void 变实地形）。

### 传送圈为何只有外侧 2 圈

传送落点 = `spot − offset`，中点对齐的取整残差 ±1 格由对侧 3 圈带吸收：pawn 由内向外正常
移动必先踩**离散边圈** spot（更靠内），落点理想在对侧离散边圈，偏移最多落到对侧带内圈/带外圈
——均为实地形，不进 void。若带内圈也有传送点，站带内圈传送的落点偏移可能越过对侧带外圈落进
void。极端组合（站带外圈 + 对端近 1 格）落点 void 由 `Walkable` 检查严格拒绝（不做回退——
寻路已排除不可站立 spot，正常移动流不触发）。

## 3. offset：连续边中点对齐

```
offset(A→C) = round(midA_jA − midC_jC)
```

- 契约不变：`neighborLocal + offset = myLocal`；`midNew + offset = midSource` 即两端连续边中点平移后重合。
- jA/jC = 共享边在各自多边形中的边索引（`WorldTileGeometry.FindNeighborIndex`，"边 j ↔ 邻居 j"同构）。
- **双端几何参与**（旧镜像假设只用源端——系统性 ±1 格错位的根因，勿回退）。
- **无收缩**（旧 SeamOverlap 收缩已废弃；偏差吸收移交 3 圈带）。
- 天然对称：`round(−x) = −round(x)`，任一端计算结果一致；登记时算一次取负。
- 取整残差 ≤1 格/分量；渲染保持整数 TRS 平移（浮点平移会引入半格纹理错位，不做），
  残差由"带外圈实地形 + 重叠带地形完全一致 + 聚焦图覆盖"掩盖。
- 旧常量 `SeamOverlap` 改名 `RoadAnchorInset = 2`，仅剩道路锚点内偏语义。

## 4. SeamOverride：接缝条带快照参考

### 数据（`SeamStripData`，平行三层快照：terrain 层 + building 层 + roof 层）

每图捕获一次（genStep 392 末尾，混合完成后）。三层同点位、同来源、同查询：

- `B ∪ T` 格 → **最终实况**（重叠区参考源，"完全一致"用）；
- 接缝带外条带格 → **原生快照**（391 同点位备份 `baseTerrainSnapshot`/`baseBuildingSnapshot`/`baseRoofSnapshot`；衰减区参考源）——三层都取原生（外条带 = 对端生成时 snapshot 的一部分：对端 void 之下的自然地形/山体，T·1 复刻即山体延续"假设成真"；曾试改存"清除后实际(null)"误杀真实延续山体，2026-08 回退勿复犯）；
- building 层存岩石体 BuildingDef（自然岩石 + 矿石 isResourceRock，null=无；判据 `def.building.isNaturalRock`——`naturalTerrain` 只被引擎赋给非矿石天然岩（TerrainDefGenerator_Stone），2026-08 前的 naturalTerrain 判据把压埋钢铁等矿石全漏成"无岩体"）：照抄区 spawn 用对端 def（跨缝岩色/矿脉连续；原版岩石地形阈值 elevation≥0.61 与 Building 阈值 >0.7 不同的中间带状态正确继承）；`SyncRockBuildingTo` 三分支——无岩 spawn / 有岩 Destroy(Vanish) / **都有岩但 def 不同 → 替换（Vanish+spawn）**（矿 lump 边界两侧 def 必不同，不替换则跨缝"铁矿紧邻花岗岩"断裂残留；2026-08 修复）；
- roof 层存 RoofDef（null=无）：照抄区屋顶照抄对端（岩壁带原生岩顶 Thick/Thin，不裸顶）。

存储：地块图挂 `MapParent_SeamlessTile.seamStrip`（WorldObject，序列化）；锚点图挂
`SeamlessTileManager.anchorSeamStrip`。

### 清理时序（全部归 391，勿在更早步骤预清）

order 200 的 `Patch_GenStepRocksFromGrid` 已删除（2026-08）。原生数据（含接缝带外条带的
岩体/屋顶）在 391 `BackupSnapshotAndApplyVoid` 先落三层快照，再由 `ApplyPolygonTerrain`
依次清 roof → rock（实体）→ terrain，最后 pathGrid 全量刷新

**基础三层快照的序列化（2026-08，卸载恢复支撑）**：原生 parent 图的三层快照以紧凑格式随
`SeamlessTileManager.baseSnapshotData`（`SeamlessBaseSnapshotData`）序列化——terrain 全格
ushort+去重 def 表、building/roof 稀疏（非空格 cellIndex+def 表索引）。开关
`serializeBaseSnapshots`（默认开）只门控**捕获**（保存时从内存数组捕入），已捕获数据照存
（清除 = 设置 UI 关开关二次确认流程的显式动作）；读档无条件还原内存数组——旧档无数据 = null
维持原降级，有数据则读档后 `DescribeCellReport` self 卷积参考可用（原"读档后回落当前"降级顺带消失）。
**地块图不序列化**（parent 为 RimExodus 类型、卸载恢复时必删，序列化纯浪费体积）。消费方 =
卸载前恢复原版兼容模式（`SeamlessUninstallRestore`，恢复仅回填 void 格——接缝带混合差异属观感、
原版可加载，不做全图归一）。（直写 topGrid 不触发重算，
不刷则后续步骤的 Standable/Walkable 读旧缓存——2026-08 动物站 void 的历史 bug 根因之一，
详见 doc/地图生成步骤.md 反转节）。历史教训：曾在 200 预清 void 格岩体，外条带原生
岩体在备份前丢失 → 新图照抄区岩壁整齐切断。

### 混合规则

SeamOverride 只做三件事：**按卷积权重覆盖、随机化边缘（dither）、道路修复（保护）**。规则轴 = **本端圈层**（对端只提供数据，不参与规则判定——错位天然免疫）：

- **c ∈ B_C（接缝带三圈）∪ T·1（过渡带 depth=1）→ 三层字面照抄对端参考格**：`a = c − offset`（镜像格 = 同一空间位置；两图接缝带在缝两侧空间重叠 3 圈，无缝条件即本侧带 = 对端同位置值。2026-08 曾试参考内移对端边法向 1 格，实测整缝系统性偏 1 格已回退，勿再加参考偏移），邻居条带快照有数据即抄地形、岩体（对端 def——跨缝岩色连续）、屋顶（对端原生岩顶，防裸顶岩壁；地形未变时岩体/屋顶同步仍执行；无任何地形例外）。**T·1 并入照抄区（2026-08 用户逻辑）**：本图 T depth=1 ↔ 对侧 OuterStrip depth=1 同一条空间带（距缝切比雪夫 2），对端在此复刻本侧原生岩体，兜住先生成侧 void rock 的 own-snapshot 假设。历史版本按对端圈层（B_A/T_A/外条带/核心区）分四条规则 + 错位补偿补丁，已废弃勿回退。**参考语义（2026-08 定稿）**：照抄与卷积的一切参考只认渲染映射格 a = c − offset——地图偏移带旋转（北邻逆时针转角），切比雪夫圈对应 ≠ 渲染映射对应，按圈找对侧地块必然错位；参考值 = 对端生成时 snapshot 的渲染映射格值（B∪T 段=混合后定格实况 / 外条带段=391 清 void 前原生快照，T·1 复刻对端 void 之下的自然山体延续）。（2026-08 教训勿复犯：曾把外条带岩体/屋顶参考改存"清除后实际(null)"以图消灭孤儿岩墙，实测误杀对端带内有延续岩的真实山体，已回退；"一侧无石一侧整条"原症状未复现于该路径，待复现再查。）
- **c ∈ void_C → 不在枚举范围**：直接用自己的 void。
- **c ∈ T_C depth≥2（过渡带）→ 卷积权重覆盖（源地图轴权重；depth=1 已并入照抄区，见上条）**：w = `dSq/(dSq+dOut)`——dOut = a 距源接缝带切比雪夫深度（B/T 区=0），dSq = a 到源方形边切比雪夫距离。**接近源六边形权重高，到源方形边渐近 0**——混合范围截止边界（源方形边）恰是权重归零处，自然闭合无锐利边缘；×乘性 dither（端点 0/1 不动）打散等值线。self（C 当前 topGrid 3×3，越界 clamp）+ 各参考（快照 3×3，缺格跳过）加权 → 众数（平局 defName 稳定决胜）覆写；**岩体/屋顶跟随主导参考（w 最大）的 def**——与照抄区语义统一（地形走卷积混合，离散层跟随参考；地形驱动 spawn 会在参考无岩体处生成本端岩体，2026-08 用户实测不一致后修正）。历史教训勿回退：按"本端固定深度衰减 + 外条带限深"会让数据边界处权重残值戛然而止，源方形边在 C 上投影成一条直线。
- **顶点楔形区**（B_C 多邻居命中）：卷积合成（各 w=1，Σw ≥ 1 时 self 权重 0）——替代旧"串行后写者胜"。
- **唯一例外 = 道路保护**：路是 1-2 格窄结构，3×3 众数卷积必然抹掉（窗口内少数派），且路由 A* 权威生成——①本格 IsRoad/bridge 跳过；②`SeamlessRoadPaths` ±3 格切比雪夫缓冲兜底无 tag 路面（Gravel 等）。
- **水体无例外（用户定夺 2026-08，勿回退为裸 IsWater 或"不可走水体"判定）**：对端是水体本端就是水体——不能走 pawn 自然绕路（与地图中央的水体同构）；河/海走廊位置由 river patch 端点对齐权威保证，SeamOverride 的逐格混合负责接缝两侧地形一致，两者互补。历史的水体例外（本格 IsWater 跳过、卷积采样跳水）是旧 offset ±2 格系统误差下"防水蔓延"的补丁，中点对齐后不成立（其间还因 `HasTag("Water")` 前缀匹配误伤 WaterMarshy 的 Marsh 造成接缝断裂）。
- **单向**：只改 C。先生成者原生，后生成者服从（先生成时对端不存在是自然序）。

### 旧候选枚举废弃

旧方案从 A 全图 snapshot 枚举候选（`cCell = aCell + offset` 落 C 六边形内）——A 方形角落格
绕六边形顶点投影进 C 侧向楔形区的污染（`A(249,249)→C(59,146)` 实测数据点）由此结构性消失：
只枚举 C 的带格，每格反向找邻居参考。

## 5. 地图生命周期（滚动软休眠，2026-08 已实现；权威文档 = `doc/地图滚动休眠.md`）

| 状态 | 现状对应 | 接缝参考数据 |
|---|---|---|
| 已加载（活跃） | 图在 `Find.Maps`（全量模拟、随存档序列化、读档恢复不重生成） | 活图存储位（生成期写入） |
| 软休眠 | **已实现（2026-08）**：Map 对象保留在 `Find.Maps` 不卸载、照常全量序列化（建筑/物品完好），只停 tick / 摘邻接显示 / 关访问入口；距玩家 ≥2 跳由 `SeamlessDormancyGovernor` 自动进入，走近自动唤醒 | `TryGetNeighborSeamStrip` 刻意不过滤休眠（数据查询 ≠ 邻接交互，快照不因休眠失效） |
| 完全卸载 | `RemoveTileMap`（销毁 Map **和** WorldObject = "从未出现过"）：governor 距离 ≥3 跳自动执行（家园/锚点图永不删除），Dev Remove All Tile Maps / Force Delete 同路径 | 无 |

统一取数入口 `SeamlessTileGraph.TryGetNeighborSeamStrip(worldTile)`：①活图/休眠图 → 活图存储位；
②`Find.World.worldObjects.MapParentAt(tile)` 上的 `MapParent_SeamlessTile` → WorldObject 快照（历史"硬卸载（卸 Map 留 WorldObject）"的预埋路径，软休眠不卸 Map 故通常不走，保留零成本）；
③都无 → 不参考。

**读档缺口修复**：旧 `baseTerrainSnapshot` 非序列化且读档无重建 → 读档后新生成图的接缝混合静默
失效。序列化的 `SeamStripData` 解决（外条带原生值跨读档可用）。

## 6. 参数表

| 参数 | 值 | 位置 | 语义 |
|---|---|---|---|
| `SeamStripInnerDepth` | 6 | `SeamlessPolygonGeometry` | 条带快照 B∪T（最终实况区）存储限深（只服务错位 ≤3 格的浅层照抄参考）；外条带不限深（存到源方形边） |
| `RoadAnchorInset` | 2 | `SeamlessTileManager`（旧名 SeamOverlap） | 道路接缝锚点内偏格数（河流锚点 0 不变） |
| `RoadGuardRadius` | 3 | `SeamlessSeamOverride` | 道路 A* 路径保护缓冲 |
| `seamOverrideNoiseAmplitude` | 0.15 | 设置（ModSettings） | 衰减区权重 dither 幅度（乘性；完全一致区不受影响） |

## 7. 消费方总览

| 系统 | 使用的定义 |
|---|---|
| void 铺设（`ApplyPolygonTerrain`，391） | void = 接缝带外 |
| ~~清岩（`Patch_GenStepRocksFromGrid`，200）~~ | **已删除（2026-08）**——清岩归 391 的 `ClearThingsOnCells`（快照备份之后）；**接缝带 B 上的岩石保留**（岩石属带的自然地形，混合时由 SyncRockBuilding 按最终地形增删——曾扩到 void∪B 导致锚点图接缝带岩石永久缺失，已回退） |
| 传送点（`PlaceEnterSpotsAllNeighbors`） | 传送圈 = D ∪ 带外圈，**无任何可通行性过滤**（spot 是纯逻辑连接：能不能走由地形运行时决定，不能走 pawn 自然绕路；地形变化后即时可用） |
| 边缘行为（`SeamlessEdgeCells` → ExitMapGrid/CellFinder/Reachability/预加载排除） | 复用传送点格集合（自动跟随）。**exit grid（2026-08 视觉优化）**：`IsExitCell` 标记集拓宽为 Band 全 3 圈 ∪ 传送点格（两侧撤离带镜像连续，切图不跳变），渲染保持原版单色浅绿；接缝位置由 `Patch_MapInterface_SeamOutline` 每帧沿多边形边（连续边 = 接缝带几何中心线）画绿色线段勾勒——两侧线段即中点对齐基准，对不齐 = 投影角度细微偏差、正常（首版三色分圈方案因过浅缺乏区分度已回退） |
| SeamOverride（392） | 混合范围 B∪T、邻居参考规则、条带快照 |
| 条带快照捕获（392 末） | B∪T + 外条带 |
| void 边界岩隐形 link（`SeamlessVoidRockLink`，392 末，2026-08） | **范围 = 邻接接缝带的 void（OuterStripDepth==1 圈，距缝切比雪夫 2）**铺 `RimExodus_VoidRockLink`（LinkGrid Rock flag，边界岩 atlas 选连续片），无本地岩石邻接过滤。判据 = 参考源镜像格 3×3（切比雪夫 1）窗口有岩（对角参与——岩石 sprite 角部会显示石壁）：归属邻居已生成 → 其条带快照 building 层镜像格 a = c − offset（不内移）；未生成 → own baseBuildingSnapshot 镜像位（原生岩体延伸假设，覆盖初始家园图无邻居场景）。**复刻闭环**：own-snapshot 假设由照抄区 T·1 兜住（对端字面照抄本侧原生岩体，见混合规则——2026-08 实测确认该复刻是设计语义，含真实山体延续）。挂点必须在 392 末（混合后）——T·1 照抄 spawn 的岩石在混合后才存在。已知限制：判据假设与实际不符/对侧岩石后被开采 → 该处仍显示连续（视觉滞后，接受） |
| 预加载带/禁建带（`SeamlessBorderLookup`） | `ComputeVoidBand` 读 void 实况（自动适应新 void 形状） |
| 道路/河流锚点（`ComputeSeamCellForEdge`） | **统一锚点工具**：边中点沿外法向最外非 void 格（新 void 边界内侧第一格）再内偏 offsetCells（道路 `RoadAnchorInset=0` 贴边）；基于带几何，与带宽无关 |
| 建筑选址（`Patches_BuildingPlacement`） | 多边形几何判定（不变；带外圈格仍被六边形外判据拒绝建筑） |
| 渲染（`SeamlessTileRenderer`） | 整数 offset TRS 平移（不变；新 offset 公式消系统误差） |
| 归属裁决（`SeamlessTileRegistry`） | `IsCellInPolygon` 可见区判定（不变） |
