- [已处置待游戏内回归 2026-09-07] 手动休眠/删除仍有玩家阵营 Pawn 的地图时增加风险确认：休眠有人时确认一次；删除保留原确认并追加第二次确认；统一覆盖殖民者、奴隶与玩家动物。
- 取得报告者日志（开 `logCaravanExit` 开关，看 `[caravan-exit]`）确定caravan无法正确撤离的原因
- [已处置待游戏内回归 2026-09-05] Vehicle wrecks from Vehicle Framework Tier 3 spawn on the edge of the map, leaving most (if not all) seamless maps completely empty of repairable wrecks. I DO see them spawn at the very edge of the map, but they're out of the actual playable area and can't be reached - they disappear on crossing.（根因定案：残骸生成走 VEF 的 ObjectSpawns 系统，其逐格判据不知道六边形裁切且无通行性检查 → 方形边缘 void 环是全图最大"合格空格池"（植被群系里核心区几乎全被植物/岩石占据），物体系统性落 void；且增量分帧路径不调用 `MapGenerator.GenerateMap` 方法本体 → VEF 的 Harmony Postfix 在地块图上根本不跑（零刷出）。修复两件：`Source/Compat/SeamlessVEFCompat.cs` 格级 CanSpawnAt Postfix 把 void ∪ 接缝带判不可刷（覆盖全部触发路径）+ `Source/Generation/GenerateMapPostfixReplay.cs` 通用复放 GenerateMap postfix（增量路径补齐，一切挂该方法的第三方 Postfix 受益不止 VEF）。回归清单见 AGENTS.md「邻居预加载与异步加载」节两新条 + README 兼容表 VEF 行；启动确认行 = "VEF compat: bound ObjectSpawns cell filter"。）

It would be cool if some objects were tagged as persistent, like walls and constructables near the borders, so you could have an actual seamless transition.


[已处置待游戏内回归 2026-09-05] 优化袭击生成逻辑，让袭击生成在已加载地图外边界，而非从当前地块对侧已加载地图的边界凭空出现。（调研定案：可行且大部分基础设施已在阶段5 就位——"敌对 NPC 从 B 行军到 A"与已实测的追击链同形状。落地 = `Source/Combat/SeamlessRaidOuterSpawn.cs` + `Patches_RaidOuterSpawn.cs` 4 patch[BOUND OK 94] + `Patch_CellFinder_TryFindRandomEdgeCellWith` 4 参 Prefix 的 ambient 分支；资格三分支照原规划（世界网格枚举、降频算活跃、全活跃 fallback = 任意 B-C 缝）；spot 朝未生成邻居也预铺 = 朝向格免几何锚点；跨图生成在 `RaidStrategyWorker.MakeLords` Postfix 整组迁移 + B 上重建 AssaultColony lord，行军交给现有推进链零新机制。用户定夺：宿主一视同仁含 POI 图 / 仅袭击类约束 / B 上有玩家接受涌现。邪教徒围攻机制特殊且难度顾虑 → v1 排除；发狂动物/食尸鬼仅同图约束。回归清单见 AGENTS.md「跨图索敌与射击」节袭击外缘生成条；设置 `raidOuterSpawnEnabled` 默认开。原规划全文如下留档：）

背景：当前步行类型的袭击（在地图边缘生成的敌人攻击，包括但不限于发狂的动物、异象的食尸鬼猎群、部落的步行袭击、海盗的步行袭击、异象的邪教徒围攻等等）都是直接生成在触发图的边缘——即使触发图的对应边界对面还有其他地图。这导致看起来步行袭击像是凭空出现的，不符合游戏设定

原则：我们的寻路都是不带多跳的，所以袭击者应该也不能多跳，也就是说袭击者要么在被袭击的图上出现，要么在被袭击的邻接图上出现，不会在需要跨越2次接缝的地块出现

预期逻辑：
假设地图为A触发了袭击，检查A检查周围第一圈地图B1-B6（也可能是B1-B5，考虑到有12个五边形地块。我们的实现不该显式区分具体数量，而是使用通用逻辑）和第二圈地图C1-Ck。
如果B1是活跃的而且其某条接缝对侧的Cx不活跃，那么B1和Cx的接缝就是可生成袭击的（游戏叙事是袭击者从玩家没看到的Cx进入了B1，然后袭击者从B1向A进军）
如果存在不活跃的B2，那么A和B2的接缝是可以生成袭击的（游戏叙事是袭击者从玩家没看到的B2直接进入了A）
如果第一圈所有B都是活跃的且第二圈所有C也都是活跃的，所有Bx和Cy的接缝都是可生成袭击的（fallback）
选择一个可生产袭击的接缝生成袭击

预期可能遇到的限制与预案：
邪教徒围攻的机制可能和其他聚集生成的步行袭击不同，需要确定其机制再决定做不做。而且邪教徒围攻在更大范围生成可能会带来显著的难度提升（原本玩家就需要绕地图一圈打邪教徒，绕多个地图一圈显然难度大幅上升），所以不能做也是可以接受的


- [已修复 2026-09-06，游戏内回归通过] @Touhoufanatic报告，排期0.1.10：ancient_smoke_vent在邻接地图上没有显示，按理说这应该是一个building（根因定案：报告观察到的实为 `AncientHeatVent` 等 `drawerType=RealtimeOnly` 的排放孔——其唯一绘制路径是聚焦图的 DynamicDrawManager（原版只在 CurrentMap 上跑），静态 mesh 被 `SectionLayer_Things.Regenerate` 的 RealtimeOnly 过滤结构性排除，而邻图背景只收集四个静态 section 层 + 手画 pawn/弹丸 → 跨缝整只缺失、聚焦才可见。修复 = `SeamlessTileRenderer.DrawNeighborRealtimeThings`（遍历邻图 `dynamicDrawManager.DrawThings` 只挑 RealtimeOnly、排除 pawn/弹丸防双画、视区+雾过滤与 pawn 通道同族）；烟/毒型排放孔是 MapMeshOnly 本体本就在 mesh 内正常显示，其烟柱 fleck 属邻图不画的动态粒子（已知缺口，是否补画待定夺）。诊断/定案过程与探针勘误见 AGENTS.md「void 渲染与邻居背景」节）
- [已修复 2026-09-06，待游戏内回归] @Icarus报告，排期0.1.10： 较小的地图上某些大型地标建筑（比如燃料精炼设施）可能会缺少一角（根因定案：地标主体选址 `MapGenUtility.TryGetStructureRect` 的第四级 fallback 直接全图随机采样、绕过我们的全部选址拦截，其校验只查"水占比 ≤25% 即放行"+方形边界——void 非水全过，小图上山地图清晰矩形耗尽时 46~81 见方的结构矩形压进 void 角落，sketch 落地时 void 上的部件被静默跳过 = 缺角。修复 = 该方法 Postfix 零拒绝改写（跨界矩形向中心滑动/中心锚定）+ GetClearRects 过滤从"整块删除跨边矩形"改"半平面裁剪进六边形内"——后者同时根治小图上 "GenStep_CrashedMechanoidPlatform did not find any valid rects" 这类废墟内容整块缺失（用户日志）。机制全述见 AGENTS.md「建筑选址避开六边形边」节）
- @Orizay报告，排期0.1.11：when using turrets that can shoot cross maps like VGE 1 gauss cannon the shoots gets stuck on the neighbor maps. For example if I have all the six maps generated around my home map and I shoot in to another map far away the shoots just get's stuck on the maps around my home map. Hope u can replicate a and fix the bug, overall I'm loving this mod so far.
- [已修复 2026-09-07，待游戏内回归] @@lophothedilo报告，排期0.1.11：Giddy-Up 2 - Continued https://steamcommunity.com/workshop/filedetails/?id=3674332861 骑乘动物跨过接缝时人和动物分离了（根因：Giddy-Up 的骑乘由两个独立 spawned Pawn + 扩展数据 + 坐骑 `Mounted` job 表示，RimExodus 原先只迁移实际寻路的骑手，坐骑留在旧图；旧 job driver 还缓存源图。修复：新增 `SeamlessTransferAssociations` 通用多 Pawn 关联随迁/恢复扩展点；`SeamlessGiddyUpCompat` 软反射捕获当前坐骑，让它走既有 `TryTransferPawn` 状态保留链，落地结束旧骑乘 job 后调用 Giddy-Up 自己的 `GoMount(Instant)` 重建双向数据、缓存和 job。未安装 Giddy-Up 时不注册、零强依赖。回归：玩家/敌对骑手跨缝后二者同图同格且仍为骑乘；连续往返；征召/非征召骑乘；普通 Pawn 与未装 Giddy-Up 零回归；启动行 `Giddy-Up 2 compat: mounted-pair transfer bridge registered`，Compat 日志有 `mounted pair restored`。）
- [已修复 2026-09-06，待游戏内回归]  @月吟裔德格  报告：暴雨、暴雪时，有邻接地图的情况下非常卡（无邻图不卡）。根因定案：不是天气效果/降频/音频的问题（均被原版 CurrentMap 门或 O(1) 早退排除），而是邻图背景渲染器的 section 重建方式——原版雨雪会让每张图持续产生 mesh 脏标记（降雪/融雪/雨冲污物，每 tick 每图约 1-2 个 17×17 区块变脏），降频邻图照常积脏，而这些脏标记只有我们的渲染器消费；旧实现每帧对每个邻图全图扫描、任何一个脏区块都触发**该区块全部渲染层的整体重建**（含最贵的逐格光照采样），无视区裁剪、无上限 → 雨雪期间每帧数毫秒到数十毫秒的重建风暴。修复 = 改用原版同款语义：只重建**视区内**区块中**真正变脏的层**（复用原版 `Section.TryUpdate`，传平移后的视区矩形）。顺带消除地图边缘裁剪平面每帧的 List 分配（GC 底噪）。机制全述与勿回退要点见 AGENTS.md「void 渲染与邻居背景」节邻图脏 section 视区重建条。
- [已修复 2026-09-06，待游戏内回归] 二轮（渲染修复后玩家实测"性能看起来仍然很糟糕"，同一场景）：tick profiler 显示 6 邻图全 0% 降频（things 全跳、pre/post 全 0）时每 tick 成本仍比无邻图高 1~1.5ms、全在 unaccounted 桶——根因 = **0% 凝固图 thing 的"跳过判定"空转**：DoTick 的 Harmony 前缀+后缀调度与门控判定对每个被跳过的 thing 照跑（6 图约 620 次/tick × ~1.5µs，skipped 不计 things 桶）。修复两件：①门控热路径收口（设置由 governor 每 tick 缓存一次、throttledMaps+fastRegions 双字典查询合并成单次 TryGetValue）；②**0% 凝固图把快速区外的 thing 摘出全局 tick 注册表**（这些物体的跳过判定结果恒为否，摘表纯等价变换、连空转判定都省掉；快速区内 thing 保留注册与判定照常全速——跨缝战斗保护，初版全摘误伤快速区已被玩家审阅抓出修正；与 Sleep/Wake 的摘表-重注册簿记精确互斥防双注册——RegisterThing 无去重，恢复=先整图归零再全注册）。配套剖析器 unaccounted 拆桶（world/gameComponents/other）+ `throttled-frozen` 状态标签（该图快速区外 thing 的 calls 归零是预期）。残余待重测：当前图各桶同 calls 耗时普涨 15-40%（things 159→285ms），疑 7 图常驻内存的 GC/CPU 缓存效应，待本轮落地后重测归因。机制全述见 AGENTS.md「分级休眠中间档」条⑧与 doc/地图滚动休眠.md 2.13 末节。
- [已实现 2026-09-06，待游戏内回归] 活跃威胁保活（用户需求，多轮设计对话定稿）：图上有活跃敌人（袭击者/被激怒的虫子[窄判]/被唤醒的机械族/蹒跚怪猎群，含敌对据点驻军——零排除定夺）时该图忽略降速（豁免降频/休眠/删除），敌人清零后经独立可配的复查间隔（默认 2 游戏秒）回落；右下角低优先持续告警列出保活地图（经纬度）。机制形态经两轮设计收敛：全事件触发方案（跨图移动/射击即时激活 + 撤离判断 + 袭击者离图追踪）因死亡无事件点、环境激怒零交互被否决，定稿为发现（Sweep）-清零（独立轮询）双频段自愈式轮询。设计对话与定夺记录（窄判/零排除/休眠图不注册）见 AGENTS.md「地图生命周期」节活跃威胁保活条；机制全文 = doc/地图滚动休眠.md 2.16；回归清单 = 同文件第五节第 15 条。
- [已修复 2026-09-06，待游戏内回归] 敌人撤退时消失在两边都有地图的接缝上（根因定案：撤离链首跳结构性到不了传送点——exit grid 2026-08 拓宽到接缝带全宽后，原版 JobDriver_Goto 的逐 tick pre-tick `IsExitCell` 检查让撤离者在踏入带内圈第一格就原生 despawn（转世界 pawn），而传送点只铺离散边圈∪带外圈、带内圈无点 → "踩点分派（对端已加载→传送续链）"永不触达，与对侧是否有图无关；2026-09-01 曾以 verbose 日志定案"实测主路径 = 带内圈原生离场"仅保留诊断日志未改行为，本轮取代。修复 = `Patch_JobDriver_Goto_TryExitMap_EvacuationDefer`（挂 private `JobDriver_Goto.TryExitMap`，pre-tick 与到站检查唯一汇聚点）：持 Evacuation 许可、目标为传送点、人未到点的 pawn 推迟原生离场到 spot——对端已加载（含降频）→ 传送续链可见跨缝；未加载/休眠 → 缝线上原生离场（Q4 不唤醒不变）。撤离链全体成员适用（用户定夺）：袭击者撤退/访客/商队/越狱囚犯/恐慌动物。安全网：无许可/已到点/重定向失败一律放行原版；6000t 许可超时兜底。验证器 OK 98；机制全述见 AGENTS.md「跨图边界行为」节首跳修复条与 doc/边界行为表.md 五。）
