[已修复 2026-09-08，待游戏内回归] drafted Pawn 的枪械 gizmo 跨图 hover/下令缺红白目标圈与指示标记、邻图 Pawn 开火时枪械不显示且无声：`SeamlessViewProjection.TryProject` 是地图间坐标唯一入口；hover 在 `GenDraw.DrawTargetHighlight` 公共绘制出口投影；Fleck/Mote 保留源图归属，由复合视图绘制阶段平移，允许显示在 CurrentMap 方形范围之外；Targeter 确认出口只生成一次 FeedbackShoot；邻图武器以投影 Pawn 为旋转支点；视野内邻图空间 one-shot 保留全部参数。回归：单/多选 gizmo hover 红白圈与下令标记位置正确，右键/移动反馈不重复；当前图方形外的邻图枪口焰/命中/爆炸 Fleck 与三个 aim mote；邻图静止持枪/瞄准/连射/后坐力；双向空间声音。
[已修复 2026-09-08，第二轮待游戏内回归] VVE 摩托艇可在深海航行、跨图失败：第一层根因是双侧代价场在 VF 可达校验之前固定按普通 Pawn 的 Normal PathGrid 把深海候选全部过滤；改用载具成本源后日志已进入 `Bridge issued ... rank 1/82`，暴露第二层同源门槛——真正传送在车辆专用落点解析前又调用普通 `arrivalCell.Walkable`，到缝后被拒。最终以可扩展成本源分派普通 Pawn（For(pawn)+WaterCellCost）与 VF 载具（对应 VehicleDef 的 VehiclePathGrid），非载具传送落点复用同一成本源，载具跳过普通 Walkable、直接走 VF 整车矩形准入；已飞行 Pawn 的 TransitTag Goto 保持飞行。回归：摩托艇在纯深海接缝双向跨越；陆地载具仍拒绝深水；飞行中的 Odyssey 鸟类跨浅水不增水面成本并可跨缝；未飞行鸟类不被自动起飞；普通智人保持浅水减速/深水禁行；混合陆水接缝选择符合各自路径成本。
[已修复 2026-09-08，第二轮待游戏内回归] 深海地图由纯沙修成深海后暴露原版陆地图生成假设：FindPlayerStartSpot 无 Heavy 地形红字、Animals/Scatterer 无落点警告、Fog 的接缝根被普通 Pawn Standable 门全部过滤；同时 CoastalEdgeFill 把 Ocean 自身的六条 Ocean 邻边当陆地海岸，画出河流状浅水条带。现增加默认开启、重启生效的内置海洋地图补全：只为空 Ocean Def 表补深海地形与基础晴雨雾雷雨；Ocean 本体跳过海岸补铺及陆地出生点/通用散布器/陆生动物；Fog 允许深海传送格发根。**首轮实测仍纯 Sand + 天气空表，根因是 Mod 构造器时点直接读 `BiomeDefOf.Ocean`，DefOf 尚未注入而静默返回；第二轮延迟到加载长事件收尾并改用 DefDatabase，核心 Def 缺失改常开 Warning。**关闭后全部 Ocean 专用行为退出供海洋 mod 接管，无缝框架保留。回归：新 Ocean 图为纯深海、无上述生成警告、从生成来源接缝正常揭雾；晴雨雾雷雨可轮换；开关关闭重启后不注入/不过滤第三方 Ocean 行为。
[已修复 2026-09-08，待游戏内回归] Ocean→海岸图生成时 `GenStep_Fog` 报 `outdoorFloodRoots=0`、海岸图不揭雾：目标图 PrimaryBiome 不是 Ocean，旧特例未放宽；来源边全是深海水，根选择器在调用原版洪水前用普通 Pawn `Standable` 全部过滤。现删除地形/Standable 预筛，来源边全部几何传送点直接交给原版 `FloodUnfog`（只被 MakeFog edifice 阻断）；fallback 从 `roots.Count==0` 改为实际 `cellsUnfogged==0`，保留整边岩壁时回退其他活跃边。回归：Ocean→海岸、海岸→Ocean 双向新图从来源边揭雾；整边实心岩壁仍回退；普通陆地接缝行为不变。
