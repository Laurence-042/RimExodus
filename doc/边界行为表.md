# 边界行为表（传送点 / 接缝带行为规范）

本文档定义 RimExodus 中"pawn 与接缝传送点交互"的完整行为规范：什么主体、在什么移动状态下、踩到/走到传送点格或边界带时应该发生什么。目的：①统一实现判据；②区分"原版原生行为"与"mod patch 强加行为"，控制 patch 数量。

**状态：定稿（2026-08，行为值经用户逐项确认；行为表由 `doc/gen_边界行为表.py` 生成）。** 原版机制结论均经反编译源码核查（`references/RimWorldDecompiled`），附路径:行号。

---

## 一、原版机制事实（已核查）

### 1.1 `exitMapOnArrival` 的全部设置点（全库仅 5 处赋值）

| # | 设置点 | 触发条件 | 来源 |
|---|---|---|---|
| 1 | `FloatMenuOptionProvider_DraftedMove.PawnGotoAction` | **玩家征召**右键/拖框 goto，且原始点击格（`clickCell`，非 gotoLoc）是 exit cell 时设为 `!pawn.IsColonyMech`（殖民地机械族例外） | `RimWorld/FloatMenuOptionProvider_DraftedMove.cs:106-110` |
| 2 | `JobGiver_ExitMap.TryGiveJob` | 所有"撤离 duty"的统一出口：构造 `Goto` + `exitMapOnArrival=true` + `failIfCantJoinOrCreateCaravan` | `Verse.AI/JobGiver_ExitMap.cs:79-84` |
| 3 | `JobGiver_PrisonerEscape` | 囚犯越狱：区域毗邻地图边缘（或 25 区域 BFS 内经自由通行的门可达边缘） | `RimWorld/JobGiver_PrisonerEscape.cs:15-22,26-57` |
| 4 | `GenGuest.GuestRelease` | 俘虏/访客被释放后离场 | `RimWorld/GenGuest.cs:41-79`（赋值 :77） |
| 5 | `Pawn_MindState.StartFleeingBecauseOfPawnAction` | **无阵营野生动物**恐慌逃跑、方向背离地图中心、50% 概率：`Flee` + flag | `Verse.AI/Pawn_MindState.cs:810-830`（赋值 :828） |

除上述外全库无其他赋值点（已 grep 全量核实）。

### 1.2 检查点（谁在消费这个 flag）

- `JobDriver_Goto`：pre-tick `exitMapOnArrival && IsExitCell(pos)` → `TryExitMap()`；到达时额外有 `OnEdge(map)` 兜底 → `pawn.ExitMap(allowedToJoinOrCreateCaravan: true, ...)`（`Verse.AI/JobDriver_Goto.cs:22-27,58,69-81`）。
- `JobDriver_Flee`：同构检查（`RimWorld/JobDriver_Flee.cs:33-48,54-57`），无条件组队/入队。
- `JobDriver_TakeAndExitMap`：**不读 flag**，tickAction 查 pawn 所在格 `IsExitCell`、到达时 `OnEdge || IsExitCell` 即 `ExitMap(allowedToJoinOrCreateCaravan:true)`（TargetB 只是引导目标；`RimWorld/JobDriver_TakeAndExitMap.cs:29-46`）。
- 组队链：`Pawn.ExitMap` → `CaravanExitMapUtility.CanExitMapAndJoinOrCreateCaravanNow`（前置：Spawned 且 `MapUsesExitGrid`；殖民者直接 true；非殖民者只能**加入现有**远行队，`CaravanExitMapUtility.cs:157-170`）→ `ExitMapAndJoinOrCreateCaravan`（加入或建队，建队时 `autoJoinable=true` 并仅唤醒可入队、未倒地、未征召的 pawn `CheckForJobOverride`，`:106-140`）。

### 1.5 RimExodus 现状几何事实（mod 侧核查；2026-08 接缝带重构后口径）

- **传送点铺在传送圈 = 离散边圈 ∪ 带外圈**（`BuildSeamBand` 纯几何、无任何可通行性过滤；`SeamlessEnterSpotPlacer.PlaceEnterSpotsAllNeighbors` 每格一个 `RimExodus_SeamlessEnterSpot`）。**带内圈（核心侧一圈）无传送点**——"接缝带内非传送点格"真实存在（与旧口径"spot 铺满整条接缝带（距 void 切比雪夫 ∈{1..2}）"不同，旧口径已废）。权威定义见 [接缝带定义](接缝带定义.md)。
- **exit cell = 传送圈传送点格**：`Patches_ExitMapGrid` 跳过原版 Rebuild、只把传送点格标为 exit cell。
- **预加载边界带是另一条更宽的带**：宽 15（`borderPreloadDistance`），"边界带内、传送圈外"（约距 void 3-15 格）的格真实存在。速查表由 `ComputeVoidBand(map, 15)`（读 void 实况多轮膨胀）构建（`SeamlessBorderLookup`）。征召 goto 到传送点格已排除预加载（`CheckPawnGoto` + `IsSeamEdgeCell` thingGrid O(1)——传送点格 = 撤离意图不触发对端生成，2026-08 落地）。

### 1.3 关键否定性事实（决定行为表结构）

- **原版不存在"未征召玩家 goto"**：`FloatMenuOptionProvider_DraftedMove` 是唯一构造玩家 Goto 菜单的 provider，且 `Undrafted => false`（`:14-15`）；`MultiPawnGotoController` 拖框也转调它（`RimWorld/MultiPawnGotoController.cs:155`）。因此"未征召殖民者踩传送点"只可能来自闲逛/工作/逃跑/跟随等 AI job——全部不带 flag。
- **玩家指令 job 统一带 `playerForced`**：`PawnGotoAction` 构造 job 时不显式设置，但 `Pawn_JobTracker.TryTakeOrderedJob` 入口统一 `job.playerForced = true`（`Verse.AI/Pawn_JobTracker.cs:897`）——征召 goto / 未征召玩家命令 / 拖框全部覆盖。RimExodus 现有预加载分支（`Patch_Pawn_JobTracker_StartJob` 查 playerForced）因此对玩家 goto 全量生效。
- **闲逛 job 是 `GotoWander` 且无 flag**：`JobGiver_Wander.cs:74-93`；目标格筛选（`RCellFinder.RandomWanderDestFor`，`RimWorld/RCellFinder.cs:255-300`）**不排除边缘/出口格**——动物和未征召 pawn 闲逛可以停在传送点格上，但原版不会因此离图。
- **跟随 job 无 flag**：`JobDriver_Follow`/`JobDriver_FollowClose` 均不含 flag。

### 1.4 各主体的离场路径（全部经 JobGiver_ExitMap 族）

撤离 duty 经 `LordToil_ExitMap`（`ExitDuty => DutyDefOf.ExitMapBest`）分配，最终都变成 `exitMapOnArrival=true` 的 Goto：

| 主体 | 离场路径 | 来源 |
|---|---|---|
| 敌方袭击者撤退 | `LordJob_AssaultColony` 等 → `LordToil_ExitMap` | `Verse.AI.Group/LordJob_AssaultColony.cs` |
| 盟友增援撤退 | ImmediateAttackFriendly → 非敌对路径 `LordJob_AssistColony`（图含两个 `LordToil_ExitMap`，已核实） | `RimWorld/RaidStrategyWorker_ImmediateAttack.cs:9-22`、`RimWorld/LordJob_AssistColony.cs:30-33` |
| 访客离场 | `LordJob_VisitColony` → `LordJob_TravelAndExit` → `LordToil_ExitMap` | `RimWorld/LordJob_VisitColony.cs` |
| 路过旅行者穿图 | `IncidentWorker_TravelerGroup` → `LordJob_TravelAndExit`（先走到穿图终点再撤离） | `RimWorld/IncidentWorker_TravelerGroup.cs:44-46`、`Verse.AI.Group/LordJob_TravelAndExit.cs:17-27` |
| 派系商队离场 | `LordJob_TradeWithColony` → `LordToil_ExitMapAndEscortCarriers` / `LordToil_ExitMap` | `RimWorld/LordJob_TradeWithColony.cs` |
| 敌方 mech 撤退 | 同袭击者（`LordJob_MechanoidsDefend`、GameEnding 转换） | `Verse.AI.Group/LordJob_MechanoidsDefend.cs` |
| 囚犯越狱 | `JobGiver_PrisonerEscape` 直发 flag job | 见 1.1#3 |
| 野性动物逃跑 | `Pawn_MindState`（50% 概率 Flee+flag） | 见 1.1#5 |
| **定居点 NPC** | **不离图**（`LordJob_DefendBase` 无 ExitMap toil） | `RimWorld/LordJob_DefendBase.cs` |

不经 flag 的直接 `ExitMap`（均 `allowedToJoinOrCreateCaravan:false`）：远行队出发按钮（`CaravanFormingUtility.ExitMapAndCreateCaravan` 流程）、Farskip、穿梭机/运输舱——与边界行走无关。

---

## 二、行为表

原则：

- 预加载原则上只能由玩家操作触发
- 在NPC在原版中会尝试从边缘撤离的场景中，如果对端地图未加载，NPC会正常撤离，如果对端地图已加载，NPC会进入对端地图后继续撤离

列说明：
- **flag** = 该移动场景下 job 的 `exitMapOnArrival` 预期值（注明原生来源）。
- **传送资格** = 该场景是否可获传送许可（Bridge/Evacuation/Pursue/Follow 四类 Grant，`SeamlessTransferGrants`；桥接自标识 = `job.dutyTag` TransitTag，无许可不传）。
- **踩点(已加载)** = 对端地图已加载（`hasArrival==true`）时 pawn 踩到传送点格的行为。
- **踩点(未生成)** = 对端未生成/生成中时 pawn 到达传送点格的行为。
- **预加载** = 该指令是否应触发邻居地图预加载。
- **原版行为** = 无 RimExodus 时此场景的结果（exit cell 为原版方形边缘带）。
- **patch 依赖**：`无需`（原版即如此）/`现有`（依赖现有 patch）/`已实现`（阶段5 传送许可登记制落地，注明实现锚点）。

<!-- GEN:行为表 BEGIN -->
全组合枚举（脚本 `doc/gen_边界行为表.py` 生成，改规则后重跑再生）。
判定只看 pawn 实际踩到传送点格且该移动的目标/意图指向跨图；
目标格非传送点的行，传送点一概不激活（踩点两列恒为"—"）。动物分野生/驯养，行为不同。

| # | 主体 | 移动来源 | 目标格 | flag | 玩家预期/场景分类 | 传送资格 | 踩传送点(已加载) | 踩传送点(未生成) | 预加载 | 原版行为 | patch 依赖 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 殖民者 | 玩家征召 goto（右键/拖框） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 有（DraftedMove 原生：点击格是 exit cell） | 玩家要求殖民者快速脱离地图成为远行队 | 无 | 原生撤离/组队（身份受限） | 原生撤离/组队（身份受限） | 否（已实现：CheckPawnGoto 排除集 = 接缝带 Band 全三圈 ∪ 传送点格（2026-08 扩，与 exit 标记集同口径）——征召 goto 接缝带任意格 = 撤离意图，不触发对端生成） | 出口格→撤离/组队 | 现有（ExitMapGrid 标传送点为出口格） |
| 2 | 殖民者 | 玩家征召 goto（右键/拖框） | 预加载边界带非传送点格（距 void 3-15 格） | 无（点击格非 exit cell 不设） | 玩家要求殖民者去开新图 | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 是（已生效：playerForced 分支；2026-08 修正：接缝带 Band 三圈非传送点格与 exit 标记集同口径排除——征召 goto 该处 = 撤离意图到站离场，白预加载对侧图的口径缺口已封） | 无事 | 现有（预加载已有） |
| 3 | 殖民者 | 跨图 goto（mod 注入桥接） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无 | 玩家要求殖民者去新地图 | 有 | **传送** + 续程 Goto | 不涉及（对端必须先生成才能 goto） | 不涉及（对端必须先生成才能 goto） | 不存在 | 已实现（点击重放 + 公共函数层：IsCrossMapOrderable 覆盖殖民者/殖民地机械族/玩家阵营驯养动物；Bridge 许可 TransitTag；选点 = 双侧代价场联合最优） |
| 4 | 殖民者 | AI 闲逛/工作移动 | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（GotoWander/工作 job 均不带） | 殖民者不该瞎逛到另一地图然后回不来 | 无 | 无事 | 无事 | 否 | 无事（可停在出口格但不离图） | 已实现（登记制：无许可不传 = 无事） |
| 5 | 殖民者 | AI 闲逛/工作移动 | 预加载边界带非传送点格（距 void 3-15 格） | 无（GotoWander/工作 job 均不带） | 殖民者不该瞎逛到另一地图然后回不来 | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事（可停在出口格但不离图） | 已实现（登记制：无许可不传 = 无事） |
| 6 | 殖民者 | AI 逃跑（flee，无 flag） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（普通 Flee 不设） | 原版 AI 因恐慌逃跑时不会跑出地图；按 NPC 原则不该跨图 | 无 | 无事 | 无事 | 否 | 无事 | 已实现（登记制：无许可不传 = 无事） |
| 7 | 殖民者 | AI 逃跑（flee，无 flag） | 预加载边界带非传送点格（距 void 3-15 格） | 无（普通 Flee 不设） | 原版 AI 因恐慌逃跑时不会跑出地图；按 NPC 原则不该跨图 | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事 | 已实现（登记制：无许可不传 = 无事） |
| 8 | 殖民地机械族 | 玩家征召 goto（右键/拖框） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（原版 `!IsColonyMech` 例外） | 保留原版例外（不自行离图）；跨图移动走跨图 goto | 无 | 无事 | 无事 | 否（已实现：同殖民者，CheckPawnGoto 排除传送点格） | 无事（mech 不设 flag，不撤离） | 无需（原版例外自动生效） |
| 9 | 殖民地机械族 | 玩家征召 goto（右键/拖框） | 预加载边界带非传送点格（距 void 3-15 格） | 无（点击格非 exit cell 不设） | 保留原版例外（不自行离图）；跨图移动走跨图 goto | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 是（已生效：playerForced 分支；2026-08 修正：接缝带 Band 三圈非传送点格与 exit 标记集同口径排除——征召 goto 该处 = 撤离意图到站离场，白预加载对侧图的口径缺口已封） | 无事（mech 不设 flag，不撤离） | 无需（原版例外自动生效） |
| 10 | 殖民地机械族 | 跨图 goto（mod 注入桥接） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无 | 玩家要求机械族去新地图 | 有 | **传送** + 续程 Goto | 不涉及（对端必须先生成才能 goto） | 不涉及（对端必须先生成才能 goto） | 不存在 | 已实现（点击重放 + 公共函数层：IsCrossMapOrderable 覆盖殖民者/殖民地机械族/玩家阵营驯养动物；Bridge 许可 TransitTag；选点 = 双侧代价场联合最优） |
| 11 | 殖民地机械族 | AI 闲逛/工作移动 | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（GotoWander/工作 job 均不带） | 同殖民者 | 无 | 无事 | 无事 | 否 | 无事（可停在出口格但不离图） | 已实现（登记制：无许可不传 = 无事） |
| 12 | 殖民地机械族 | AI 闲逛/工作移动 | 预加载边界带非传送点格（距 void 3-15 格） | 无（GotoWander/工作 job 均不带） | 同殖民者 | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事（可停在出口格但不离图） | 已实现（登记制：无许可不传 = 无事） |
| 13 | 敌方人形 | AI 闲逛/工作移动 | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（GotoWander/工作 job 均不带） | 等待突袭的敌人不该瞎逛到另一地图然后回不来 | 无 | 无事 | 无事 | 否 | 无事（可停在出口格但不离图） | 已实现（登记制：无许可不传 = 无事） |
| 14 | 敌方人形 | AI 闲逛/工作移动 | 预加载边界带非传送点格（距 void 3-15 格） | 无（GotoWander/工作 job 均不带） | 等待突袭的敌人不该瞎逛到另一地图然后回不来 | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事（可停在出口格但不离图） | 已实现（登记制：无许可不传 = 无事） |
| 15 | 敌方人形 | AI 战斗移动（追击/走位） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（AIFightEnemy 等不带） | 玩家不能通过跨图逃离追击，除非敌人和旧地图一起被清理 | 有 | **传送** | 无事 | 否 | 无事 | 已实现（2026-08 阶段5：TryRegisterCombatStepTransfer——NPC 战斗体+战斗 job（AttackMelee/AttackStatic/战斗 lord duty 下 Goto）+追击目标在对端图才即席 Pursue 许可；MarkPursuers 意图三判据——Attack 系 job / mindState.enemyTarget / 推进期 Goto 的 targetA=刚跨图者（还能跨缝射则原地打）；主动推进亦落地——GotoNearestHostile 跨图版取邻图最近 → StartPath 结构判据桥接过缝 → Bridge 落地收编袭击 lord（防无 lord 被 JobGiver_ExitMap 离场“过缝秒退”）） |
| 16 | 敌方人形 | AI 战斗移动（追击/走位） | 预加载边界带非传送点格（距 void 3-15 格） | 无（AIFightEnemy 等不带） | 玩家不能通过跨图逃离追击，除非敌人和旧地图一起被清理 | 有 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事 | 已实现（2026-08 阶段5：TryRegisterCombatStepTransfer——NPC 战斗体+战斗 job（AttackMelee/AttackStatic/战斗 lord duty 下 Goto）+追击目标在对端图才即席 Pursue 许可；MarkPursuers 意图三判据——Attack 系 job / mindState.enemyTarget / 推进期 Goto 的 targetA=刚跨图者（还能跨缝射则原地打）；主动推进亦落地——GotoNearestHostile 跨图版取邻图最近 → StartPath 结构判据桥接过缝 → Bridge 落地收编袭击 lord（防无 lord 被 JobGiver_ExitMap 离场“过缝秒退”）） |
| 17 | 敌方人形 | AI 逃跑（flee，无 flag） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（普通 Flee 不设） | 原版 AI 因恐慌逃跑时不会跑出地图；按 NPC 原则不该跨图 | 无 | 无事 | 无事 | 否 | 无事 | 已实现（登记制：无许可不传 = 无事） |
| 18 | 敌方人形 | AI 逃跑（flee，无 flag） | 预加载边界带非传送点格（距 void 3-15 格） | 无（普通 Flee 不设） | 原版 AI 因恐慌逃跑时不会跑出地图；按 NPC 原则不该跨图 | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事 | 已实现（登记制：无许可不传 = 无事） |
| 19 | 敌方人形 | 撤离 duty（袭击撤退/盟友·访客·商队·旅行者离场/囚犯越狱） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 有（JobGiver_ExitMap / JobGiver_PrisonerEscape） | 敌人需要能进入对端已加载地图继续跑，直到其到达没有加载对端地图的撤离点（视为跑出玩家视野） | 无 | **传送** + 对端续发撤离 job（继续跑，直到无加载对端的出口→原生撤离） | 原生撤离（视为跑出视野） | 否 | 出口格→撤离（NPC 只能加入现有队） | 已实现（Evacuation 许可：对端已加载→传送+续发撤离 job，VisitedTiles 防回弹；未生成→原生撤离） |
| 20 | 敌方人形 | 撤离 duty（袭击撤退/盟友·访客·商队·旅行者离场/囚犯越狱） | 预加载边界带非传送点格（距 void 3-15 格） | 有（JobGiver_ExitMap / JobGiver_PrisonerEscape） | 敌人需要能进入对端已加载地图继续跑，直到其到达没有加载对端地图的撤离点（视为跑出玩家视野） | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 出口格→撤离（NPC 只能加入现有队） | 已实现（Evacuation 许可：对端已加载→传送+续发撤离 job，VisitedTiles 防回弹；未生成→原生撤离） |
| 21 | 敌方机械族 | AI 闲逛/工作移动 | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（GotoWander/工作 job 均不带） | 同敌方人形 | 无 | 无事 | 无事 | 否 | 无事（可停在出口格但不离图） | 已实现（登记制：无许可不传 = 无事） |
| 22 | 敌方机械族 | AI 闲逛/工作移动 | 预加载边界带非传送点格（距 void 3-15 格） | 无（GotoWander/工作 job 均不带） | 同敌方人形 | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事（可停在出口格但不离图） | 已实现（登记制：无许可不传 = 无事） |
| 23 | 敌方机械族 | AI 战斗移动（追击/走位） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（AIFightEnemy 等不带） | 玩家不能通过跨图逃离追击，除非敌人和旧地图一起被清理 | 有 | **传送** | 无事 | 否 | 无事 | 已实现（2026-08 阶段5：TryRegisterCombatStepTransfer——NPC 战斗体+战斗 job（AttackMelee/AttackStatic/战斗 lord duty 下 Goto）+追击目标在对端图才即席 Pursue 许可；MarkPursuers 意图三判据——Attack 系 job / mindState.enemyTarget / 推进期 Goto 的 targetA=刚跨图者（还能跨缝射则原地打）；主动推进亦落地——GotoNearestHostile 跨图版取邻图最近 → StartPath 结构判据桥接过缝 → Bridge 落地收编袭击 lord（防无 lord 被 JobGiver_ExitMap 离场“过缝秒退”）） |
| 24 | 敌方机械族 | AI 战斗移动（追击/走位） | 预加载边界带非传送点格（距 void 3-15 格） | 无（AIFightEnemy 等不带） | 玩家不能通过跨图逃离追击，除非敌人和旧地图一起被清理 | 有 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事 | 已实现（2026-08 阶段5：TryRegisterCombatStepTransfer——NPC 战斗体+战斗 job（AttackMelee/AttackStatic/战斗 lord duty 下 Goto）+追击目标在对端图才即席 Pursue 许可；MarkPursuers 意图三判据——Attack 系 job / mindState.enemyTarget / 推进期 Goto 的 targetA=刚跨图者（还能跨缝射则原地打）；主动推进亦落地——GotoNearestHostile 跨图版取邻图最近 → StartPath 结构判据桥接过缝 → Bridge 落地收编袭击 lord（防无 lord 被 JobGiver_ExitMap 离场“过缝秒退”）） |
| 25 | 敌方机械族 | AI 逃跑（flee，无 flag） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（普通 Flee 不设） | 原版 AI 因恐慌逃跑时不会跑出地图；按 NPC 原则不该跨图 | 无 | 无事 | 无事 | 否 | 无事 | 已实现（登记制：无许可不传 = 无事） |
| 26 | 敌方机械族 | AI 逃跑（flee，无 flag） | 预加载边界带非传送点格（距 void 3-15 格） | 无（普通 Flee 不设） | 原版 AI 因恐慌逃跑时不会跑出地图；按 NPC 原则不该跨图 | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事 | 已实现（登记制：无许可不传 = 无事） |
| 27 | 敌方机械族 | 撤离 duty（袭击撤退/盟友·访客·商队·旅行者离场/囚犯越狱） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 有（JobGiver_ExitMap / JobGiver_PrisonerEscape） | 敌人需要能进入对端已加载地图继续跑，直到其到达没有加载对端地图的撤离点（视为跑出玩家视野） | 无 | **传送** + 对端续发撤离 job（继续跑，直到无加载对端的出口→原生撤离） | 原生撤离（视为跑出视野） | 否 | 出口格→撤离（NPC 只能加入现有队） | 已实现（Evacuation 许可：对端已加载→传送+续发撤离 job，VisitedTiles 防回弹；未生成→原生撤离） |
| 28 | 敌方机械族 | 撤离 duty（袭击撤退/盟友·访客·商队·旅行者离场/囚犯越狱） | 预加载边界带非传送点格（距 void 3-15 格） | 有（JobGiver_ExitMap / JobGiver_PrisonerEscape） | 敌人需要能进入对端已加载地图继续跑，直到其到达没有加载对端地图的撤离点（视为跑出玩家视野） | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 出口格→撤离（NPC 只能加入现有队） | 已实现（Evacuation 许可：对端已加载→传送+续发撤离 job，VisitedTiles 防回弹；未生成→原生撤离） |
| 29 | 盟友增援 | AI 闲逛/工作移动 | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（GotoWander/工作 job 均不带） | 同敌人（NPC） | 无 | 无事 | 无事 | 否 | 无事（可停在出口格但不离图） | 已实现（登记制：无许可不传 = 无事） |
| 30 | 盟友增援 | AI 闲逛/工作移动 | 预加载边界带非传送点格（距 void 3-15 格） | 无（GotoWander/工作 job 均不带） | 同敌人（NPC） | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事（可停在出口格但不离图） | 已实现（登记制：无许可不传 = 无事） |
| 31 | 盟友增援 | AI 战斗移动（追击/走位） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（AIFightEnemy 等不带） | 玩家不能通过跨图逃离追击，除非敌人和旧地图一起被清理 | 有 | **传送** | 无事 | 否 | 无事 | 已实现（2026-08 阶段5：TryRegisterCombatStepTransfer——NPC 战斗体+战斗 job（AttackMelee/AttackStatic/战斗 lord duty 下 Goto）+追击目标在对端图才即席 Pursue 许可；MarkPursuers 意图三判据——Attack 系 job / mindState.enemyTarget / 推进期 Goto 的 targetA=刚跨图者（还能跨缝射则原地打）；主动推进亦落地——GotoNearestHostile 跨图版取邻图最近 → StartPath 结构判据桥接过缝 → Bridge 落地收编袭击 lord（防无 lord 被 JobGiver_ExitMap 离场“过缝秒退”）） |
| 32 | 盟友增援 | AI 战斗移动（追击/走位） | 预加载边界带非传送点格（距 void 3-15 格） | 无（AIFightEnemy 等不带） | 玩家不能通过跨图逃离追击，除非敌人和旧地图一起被清理 | 有 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事 | 已实现（2026-08 阶段5：TryRegisterCombatStepTransfer——NPC 战斗体+战斗 job（AttackMelee/AttackStatic/战斗 lord duty 下 Goto）+追击目标在对端图才即席 Pursue 许可；MarkPursuers 意图三判据——Attack 系 job / mindState.enemyTarget / 推进期 Goto 的 targetA=刚跨图者（还能跨缝射则原地打）；主动推进亦落地——GotoNearestHostile 跨图版取邻图最近 → StartPath 结构判据桥接过缝 → Bridge 落地收编袭击 lord（防无 lord 被 JobGiver_ExitMap 离场“过缝秒退”）） |
| 33 | 盟友增援 | AI 逃跑（flee，无 flag） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（普通 Flee 不设） | 原版 AI 因恐慌逃跑时不会跑出地图；按 NPC 原则不该跨图 | 无 | 无事 | 无事 | 否 | 无事 | 已实现（登记制：无许可不传 = 无事） |
| 34 | 盟友增援 | AI 逃跑（flee，无 flag） | 预加载边界带非传送点格（距 void 3-15 格） | 无（普通 Flee 不设） | 原版 AI 因恐慌逃跑时不会跑出地图；按 NPC 原则不该跨图 | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事 | 已实现（登记制：无许可不传 = 无事） |
| 35 | 盟友增援 | 撤离 duty（袭击撤退/盟友·访客·商队·旅行者离场/囚犯越狱） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 有（JobGiver_ExitMap / JobGiver_PrisonerEscape） | 敌人需要能进入对端已加载地图继续跑，直到其到达没有加载对端地图的撤离点（视为跑出玩家视野） | 无 | **传送** + 对端续发撤离 job（继续跑，直到无加载对端的出口→原生撤离） | 原生撤离（视为跑出视野） | 否 | 出口格→撤离（NPC 只能加入现有队） | 已实现（Evacuation 许可：对端已加载→传送+续发撤离 job，VisitedTiles 防回弹；未生成→原生撤离） |
| 36 | 盟友增援 | 撤离 duty（袭击撤退/盟友·访客·商队·旅行者离场/囚犯越狱） | 预加载边界带非传送点格（距 void 3-15 格） | 有（JobGiver_ExitMap / JobGiver_PrisonerEscape） | 敌人需要能进入对端已加载地图继续跑，直到其到达没有加载对端地图的撤离点（视为跑出玩家视野） | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 出口格→撤离（NPC 只能加入现有队） | 已实现（Evacuation 许可：对端已加载→传送+续发撤离 job，VisitedTiles 防回弹；未生成→原生撤离） |
| 37 | 访客/旅行者/商队 | AI 闲逛/工作移动 | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（GotoWander/工作 job 均不带） | 同敌人（NPC） | 无 | 无事 | 无事 | 否 | 无事（可停在出口格但不离图） | 已实现（登记制：无许可不传 = 无事） |
| 38 | 访客/旅行者/商队 | AI 闲逛/工作移动 | 预加载边界带非传送点格（距 void 3-15 格） | 无（GotoWander/工作 job 均不带） | 同敌人（NPC） | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事（可停在出口格但不离图） | 已实现（登记制：无许可不传 = 无事） |
| 39 | 访客/旅行者/商队 | AI 逃跑（flee，无 flag） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（普通 Flee 不设） | 原版 AI 因恐慌逃跑时不会跑出地图；按 NPC 原则不该跨图 | 无 | 无事 | 无事 | 否 | 无事 | 已实现（登记制：无许可不传 = 无事） |
| 40 | 访客/旅行者/商队 | AI 逃跑（flee，无 flag） | 预加载边界带非传送点格（距 void 3-15 格） | 无（普通 Flee 不设） | 原版 AI 因恐慌逃跑时不会跑出地图；按 NPC 原则不该跨图 | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事 | 已实现（登记制：无许可不传 = 无事） |
| 41 | 访客/旅行者/商队 | 撤离 duty（袭击撤退/盟友·访客·商队·旅行者离场/囚犯越狱） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 有（JobGiver_ExitMap / JobGiver_PrisonerEscape） | 敌人需要能进入对端已加载地图继续跑，直到其到达没有加载对端地图的撤离点（视为跑出玩家视野） | 无 | **传送** + 对端续发撤离 job（继续跑，直到无加载对端的出口→原生撤离） | 原生撤离（视为跑出视野） | 否 | 出口格→撤离（NPC 只能加入现有队） | 已实现（Evacuation 许可：对端已加载→传送+续发撤离 job，VisitedTiles 防回弹；未生成→原生撤离） |
| 42 | 访客/旅行者/商队 | 撤离 duty（袭击撤退/盟友·访客·商队·旅行者离场/囚犯越狱） | 预加载边界带非传送点格（距 void 3-15 格） | 有（JobGiver_ExitMap / JobGiver_PrisonerEscape） | 敌人需要能进入对端已加载地图继续跑，直到其到达没有加载对端地图的撤离点（视为跑出玩家视野） | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 出口格→撤离（NPC 只能加入现有队） | 已实现（Evacuation 许可：对端已加载→传送+续发撤离 job，VisitedTiles 防回弹；未生成→原生撤离） |
| 43 | 囚犯 | AI 闲逛/工作移动 | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（GotoWander/工作 job 均不带） | 同敌人（NPC） | 无 | 无事 | 无事 | 否 | 无事（可停在出口格但不离图） | 已实现（登记制：无许可不传 = 无事） |
| 44 | 囚犯 | AI 闲逛/工作移动 | 预加载边界带非传送点格（距 void 3-15 格） | 无（GotoWander/工作 job 均不带） | 同敌人（NPC） | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事（可停在出口格但不离图） | 已实现（登记制：无许可不传 = 无事） |
| 45 | 囚犯 | AI 逃跑（flee，无 flag） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（普通 Flee 不设） | 原版 AI 因恐慌逃跑时不会跑出地图；按 NPC 原则不该跨图 | 无 | 无事 | 无事 | 否 | 无事 | 已实现（登记制：无许可不传 = 无事） |
| 46 | 囚犯 | AI 逃跑（flee，无 flag） | 预加载边界带非传送点格（距 void 3-15 格） | 无（普通 Flee 不设） | 原版 AI 因恐慌逃跑时不会跑出地图；按 NPC 原则不该跨图 | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事 | 已实现（登记制：无许可不传 = 无事） |
| 47 | 囚犯 | 撤离 duty（袭击撤退/盟友·访客·商队·旅行者离场/囚犯越狱） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 有（JobGiver_ExitMap / JobGiver_PrisonerEscape） | 敌人需要能进入对端已加载地图继续跑，直到其到达没有加载对端地图的撤离点（视为跑出玩家视野） | 无 | **传送** + 对端续发撤离 job（继续跑，直到无加载对端的出口→原生撤离） | 原生撤离（视为跑出视野） | 否 | 出口格→撤离（NPC 只能加入现有队） | 已实现（Evacuation 许可：对端已加载→传送+续发撤离 job，VisitedTiles 防回弹；未生成→原生撤离） |
| 48 | 囚犯 | 撤离 duty（袭击撤退/盟友·访客·商队·旅行者离场/囚犯越狱） | 预加载边界带非传送点格（距 void 3-15 格） | 有（JobGiver_ExitMap / JobGiver_PrisonerEscape） | 敌人需要能进入对端已加载地图继续跑，直到其到达没有加载对端地图的撤离点（视为跑出玩家视野） | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 出口格→撤离（NPC 只能加入现有队） | 已实现（Evacuation 许可：对端已加载→传送+续发撤离 job，VisitedTiles 防回弹；未生成→原生撤离） |
| 49 | 野生动物 | AI 闲逛/工作移动 | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（GotoWander/工作 job 均不带） | 同敌人（NPC） | 无 | 无事 | 无事 | 否 | 无事（可停在出口格但不离图） | 已实现（登记制：无许可不传 = 无事） |
| 50 | 野生动物 | AI 闲逛/工作移动 | 预加载边界带非传送点格（距 void 3-15 格） | 无（GotoWander/工作 job 均不带） | 同敌人（NPC） | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事（可停在出口格但不离图） | 已实现（登记制：无许可不传 = 无事） |
| 51 | 野生动物 | AI 逃跑（flee，无 flag） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（普通 Flee 不设） | 原版 AI 因恐慌逃跑时不会跑出地图；按 NPC 原则不该跨图 | 无 | 无事 | 无事 | 否 | 无事 | 已实现（登记制：无许可不传 = 无事） |
| 52 | 野生动物 | AI 逃跑（flee，无 flag） | 预加载边界带非传送点格（距 void 3-15 格） | 无（普通 Flee 不设） | 原版 AI 因恐慌逃跑时不会跑出地图；按 NPC 原则不该跨图 | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事 | 已实现（登记制：无许可不传 = 无事） |
| 53 | 野生动物 | 野生动物恐慌逃跑（原生 50% Flee+flag） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 有（Pawn_MindState 原生） | 逃跑的动物需要能进入对端已加载地图继续跑，以此允许玩家跨图追猎，直到其到达没有加载对端地图的撤离点（视为跑出玩家视野）。未掷中 flag 的 flee 不跨图（原则同上） | 无 | **传送** + 对端续发撤离 job（继续跑，直到无加载对端的出口→原生撤离） | 原生撤离（视为跑出视野） | 否 | 出口格→撤离（ExitMap 回世界） | 已实现（Evacuation 许可，同撤离 duty：对端已加载→传送继续跑） |
| 54 | 野生动物 | 野生动物恐慌逃跑（原生 50% Flee+flag） | 预加载边界带非传送点格（距 void 3-15 格） | 有（Pawn_MindState 原生） | 逃跑的动物需要能进入对端已加载地图继续跑，以此允许玩家跨图追猎，直到其到达没有加载对端地图的撤离点（视为跑出玩家视野）。未掷中 flag 的 flee 不跨图（原则同上） | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 出口格→撤离（ExitMap 回世界） | 已实现（Evacuation 许可，同撤离 duty：对端已加载→传送继续跑） |
| 55 | 驯养动物 | 跨图 goto（mod 注入桥接） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无 | 玩家要求被征召动物去新地图 | 有 | **传送** + 续程 Goto | 不涉及（对端必须先生成才能 goto） | 不涉及（对端必须先生成才能 goto） | 不存在 | 已实现（点击重放 + 公共函数层：IsCrossMapOrderable 覆盖殖民者/殖民地机械族/玩家阵营驯养动物；Bridge 许可 TransitTag；选点 = 双侧代价场联合最优） |
| 56 | 驯养动物 | AI 闲逛/工作移动 | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（GotoWander/工作 job 均不带） | 同殖民者（不该瞎逛到另一地图） | 无 | 无事 | 无事 | 否 | 无事（可停在出口格但不离图） | 已实现（登记制：无许可不传 = 无事） |
| 57 | 驯养动物 | AI 闲逛/工作移动 | 预加载边界带非传送点格（距 void 3-15 格） | 无（GotoWander/工作 job 均不带） | 同殖民者（不该瞎逛到另一地图） | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事（可停在出口格但不离图） | 已实现（登记制：无许可不传 = 无事） |
| 58 | 驯养动物 | AI 逃跑（flee，无 flag） | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（普通 Flee 不设） | 原版 AI 因恐慌逃跑时不会跑出地图；按 NPC 原则不该跨图 | 无 | 无事 | 无事 | 否 | 无事 | 已实现（登记制：无许可不传 = 无事） |
| 59 | 驯养动物 | AI 逃跑（flee，无 flag） | 预加载边界带非传送点格（距 void 3-15 格） | 无（普通 Flee 不设） | 原版 AI 因恐慌逃跑时不会跑出地图；按 NPC 原则不该跨图 | 无 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事 | 已实现（登记制：无许可不传 = 无事） |
| 60 | 驯养动物 | 驯养动物跟随主人 | 传送圈格（离散边圈∪带外圈，spot 实铺位置） | 无（Follow/FollowClose 不带） | 跟随主人时应该能在主人跨过接缝前往新地图时跟随主人；但不该独立触发地图生成 | 有 | **传送** | 无事 | 否 | 无事 | 已实现（Follow 许可：跟随目标刚跨图→传送；其余闲逛/工作无事） |
| 61 | 驯养动物 | 驯养动物跟随主人 | 预加载边界带非传送点格（距 void 3-15 格） | 无（Follow/FollowClose 不带） | 跟随主人时应该能在主人跨过接缝前往新地图时跟随主人；但不该独立触发地图生成 | 有 | —（目标非传送点，传送点不激活） | —（目标非传送点，传送点不激活） | 否 | 无事 | 已实现（Follow 许可：跟随目标刚跨图→传送；其余闲逛/工作无事） |

<!-- GEN:行为表 END -->

---

## 三、传送点激活判据（实现判据，定稿）

`TryTriggerTransfer` 对"pawn 踩到传送点格"按序判定：

1. **flag（撤离意图）且属玩家征召 goto（殖民者）** → 放行原生 ExitMap/组队——玩家显式要求脱离地图，不跨图。殖民地机械族保留原版 `!IsColonyMech` 例外（无 flag，站住）。
2. **flag 且属 NPC 撤离场景**（撤离 duty：袭击撤退/盟友·访客·商队·旅行者离场、囚犯越狱、野性恐慌逃跑）：
   - 对端已加载（`hasArrival`）→ **传送** + 对端续发撤离 job（继续跑，出口选择远离来向接缝，直到到达没有加载对端的撤离点 → 原生撤离，视为跑出视野）。
   - 对端未生成 → 放行原生撤离。
3. **跨图登记**（玩家跨图 goto：殖民者/殖民地机械族/被征召驯养动物）→ **传送** + 续程 Goto。
4. **敌方主体战斗移动**（追击/走位——Pursue 许可：追击扫描 `MarkPursuers` / 战斗踩点 `TryRegisterCombatStepTransfer`，见第五节）→ **传送**（跨图追击，防玩家靠跨图甩掉追兵）。
5. **驯养动物跟随主人**（Follow/FollowClose job）→ **传送**（无续程）。
6. 其余（所有人形/动物的闲逛、工作、无 flag 逃跑）→ **无事**。

原则：传送资格 = flag∧NPC撤离 ∨ 跨图登记 ∨ 敌方战斗 ∨ 驯养跟随；撤离资格 = 原生 flag（玩家征召/对端未生成）。目标格非传送点的移动不因路径经过传送点而激活（闲逛/工作/逃跑的目标恒为图内格）。

预加载原则：**只能由玩家操作触发**（征召 goto 预加载边界带非传送点格）；传送点格本身不触发（撤离意图不需要对端）。

## 四、与现状的差异 / 实现待办（历史差分记录）

> **2026-08 状态**：下列待办①-⑦ 已全部实现（映射与实现锚点见第五节"实现落地记录"）；⑧ 保持已排除结论。本节保留作"定稿时与当时现状的差分"历史记录。

1. **flag job 分流**：现状 `TryTriggerTransfer` 对 flag job 一律放行原生撤离（`SeamlessMapTransferTrigger.cs:87`）。需改为：玩家征召 goto（殖民者）→ 放行；NPC 撤离/囚犯越狱/野性恐慌 → 对端已加载则**传送+续发撤离**，未生成才放行。
2. **撤离续程的方向性**：对端续发的撤离 job 出口选择需远离来向接缝（否则到达锁解除后会在接缝带来回横跳/传回原地图）。
3. **传送资格收紧**：现状无 flag 即传（人形闲逛/工作/逃跑全传）。收窄为：跨图登记 ∨ 敌方战斗 ∨ 驯养跟随。
4. **敌方战斗移动传送**（新增 patch）：识别"敌方主体 + 战斗 job"并允许传送。判据候选：敌方 pawn + job 非 GotoWander/非工作类（如 AIFightEnemy 的 Goto）——实现时验证覆盖面。
5. **跨图 goto 扩展**：桥接注入（`Patches_FloatMenuMakerMap`）现状只对殖民者；需扩展到殖民地机械族、被征召驯养动物。
6. **桥接传送许可标记**：`SeamlessCrossMapOrders.TryBridgeJob` 下发的 Goto（`SeamlessCrossMapOrders.cs:80`）与普通 AI Goto 无区分，收紧后被拦。桥接时给 pawn 登记 transit 标记（可伴随 `SeamlessCrossMapPendingDestinations`，传送消费时清除）。
7. **预加载排除传送点格**：`SeamlessBorderPreloader.CheckPawnGoto` 的边界带判定排除传送点格——征召 goto 传送点 = 撤离意图，不应触发对端生成。
8. ~~预加载触发的 playerForced 条件~~ **已排除**：`TryTakeOrderedJob` 统一设 `job.playerForced = true`（`Pawn_JobTracker.cs:897`），征召 goto 现有分支已覆盖，无需改动。

---

## 五、实现落地记录（阶段5，2026-08）

架构（用户拍板）：**判定收拢——传送资格不在踩点时判定，状态变更事件处登记传送许可（Grant）并绑定传送点；踩点热路径只查登记表，无许可不传**。"收紧"（待办③）成为结构性结果。登记点三处：

| 状态变更事件 | 登记 | 代码锚点 |
|---|---|---|
| NPC 撤离 job 下发（`exitMapOnArrival && !playerForced`） | Evacuation 许可（首跳不绑点：首个踩到的传送点，已加载→传送续链 / 未生成→原生撤离） | `Patches_Job.cs` → `SeamlessTransferGrants.RegisterEvacuation` |
| 玩家跨图 goto 桥接 | Bridge 许可（绑定 exitSpot + 最终目的地） | `SeamlessCrossMapOrders.TryBridgeJob` |
| 传送完成（`NotifyPawnTransferred`） | 撤离链续程（出口远离来向）+ 追击者扫描（意图：AttackMelee/AttackStatic job、非 playerForced 推进 Goto 的 targetA、或 mindState.enemyTarget = 刚跨图者；NPC 战斗体）+ 跟随者扫描（Follow/FollowClose 目标=刚跨图者）+ Bridge 落地 NPC 战斗体收编（袭击 lord / 游荡宽限） | `SeamlessTransferGrants.NotifyPawnTransferred` |

待办①-⑦ → 实现锚点：

1. flag 分流 → `SeamlessMapTransferTrigger.HandleEvacuation`（`playerForced` 区分在 StartJob 登记侧：玩家征召 goto 不登记，踩点走原生）。
2. 撤离续程方向性 → `SeamlessTransferGrants.TryFindEvacuationExit`（三级候选：未访问+对端未加载 > 未访问 > 无过滤 ForceExit；VisitedTiles 跨 hop 防回弹）。续程 job **不带** `exitMapOnArrival`（落地格常是出口格，flag 的 pre-tick 会在落地瞬间原生离场）。
3. 传送资格收紧 → 登记制结构性结果（`TryTriggerTransfer` 无许可即返回）。
4. 敌方战斗移动传送 → 追击扫描（`MarkPursuers`）：意图由"目标刚从此点跨图"事件保证，意图三判据（AttackMelee/AttackStatic job、非 playerForced 推进 Goto 的 targetA、mindState.enemyTarget——2026-08 补推进期 Goto：GotoNearestHostile 推进中原版不设 enemyTarget，只认 Attack 系会漏掉赶路中的推进者）；cast-position 走位 Goto（cell 目标）仍无判据不激活。主动跨图推进另由 GotoNearestHostile 跨图版 + StartPath 结构判据桥接承担（2026-08 落地；旧 NpcApproachTag dutyTag 自标识被 ThinkNode_Duty 覆写不可用，教训见 `AGENTS.md` 跨图交互架构 L3-⑤）。
5. 跨图 goto 扩展 → `SeamlessBoundaryRules.IsCrossMapOrderable`（殖民者/殖民地机械族/玩家阵营驯养动物），`InjectCrossMapGotoOption` 显式资格判定。
6. 桥接传送许可标记 → Bridge Grant + `job.dutyTag = SeamlessTransferGrants.TransitTag`（自标识驱动 job，StartJob 钩子不清除其许可；非 TransitTag job 启动=许可失效）。
7. 预加载排除传送点格 → `SeamlessBorderPreloader.CheckPawnGoto` + `SeamlessEdgeCells.IsSeamEdgeCell`（thingGrid O(1)）。

配套变更：**删除到达锁**（arrivalLocks——登记制下无许可不传 + VisitedTiles 防回弹，锁"站在任一传送点保持"语义会卡死沿相邻边带内行走的撤离者）；删除 `SeamlessCrossMapPendingDestinations`（折入 Bridge Grant）；Pursue/Follow 落地 NPC 游荡兜底（600 ticks 无接战/无 lord → 转撤离链跑出世界）；静态清扫挂锚点地图 `MapComponentTick`。

> **2026-08 后续演进**：跨图菜单入口由首版 `InjectCrossMapGotoOption`（GetOptions 整方法接管 + 注入跨图 GoHere）改为**点击重放 + 公共函数层**（跨图选项由原生 provider 在真邻图产出；`IsCrossMapOrderable` 资格判定保留在公共函数层）；静态清扫已从"锚点地图 MapComponentTick"迁至 `SeamlessDormancyGovernor.GameComponentTick`（软休眠后锚点图可休眠、其 tick 不再可靠）。

2026-08 追击意图补全 + 推进桥接复活（根因存档见 `跨图命令适配审计.md` 第四节）：MarkPursuers 补推进期 Goto 意图（非 playerForced Goto 的 targetA=跨图者——GotoNearestHostile 推进中原版不设 enemyTarget）；跨图推进 job 的 StartPath 桥接识别改**结构判据**（非 playerForced 的 Goto 且 targetA Thing 在邻图——旧 NpcApproachTag dutyTag 自标识被 `ThinkNode_Duty.TryIssueJobPackage` 无条件覆写为 duty.tag(null)，从未生效，**job.dutyTag 不能作为 think-tree job 的自标识通道**；TransitTag 幸存只因走 StartJob 直发不经 think tree）；Bridge 落地 NPC 战斗体收编（TryAttachAssaultLord / 游荡宽限，防无 lord 被 JobGiver_ExitMap 离场"过缝秒退"）。

已知边界（接受）：cast-position 走位 Goto（cell 目标）不激活追击传送（推进期 Goto(目标 thing) 已入意图判据，2026-08）；狂猎动物追击不登记（表无此行）；跨图菜单多选拖框沿用单 pawn 判定；叛乱奴隶（玩家阵营，表无此行）战斗追击经 Bridge 跨图后由 lord 续接策略重建/并入叛乱 lord 继续叛乱（2026-08，`TryContinueLordOnArrival`），逃亡展叛奴经撤离链跨图不续接（跑出世界目标已由链达成）。

### 五.6 袭击外缘生成（2026-09-05 落地，`SeamlessRaidOuterSpawn` + `Patches_RaidOuterSpawn`）

步行类袭击的**进场位置**规范（进场后的行为不变——仍走原版袭击 AI / 本表既有的追击与撤离行）：

| 事件 | 行为 | 代码锚点 |
|---|---|---|
| raid 族步行袭击（EdgeWalkIn 系 arrival mode） | 落点候选池按"外侧接缝"过滤：对侧无图或休眠的边（同图约束，ambient 窗口 = 本次 TryExecuteWorker）；敌对且策略 lord 为纯 AssaultColony（非工兵/破墙/围攻/集结）时优先**生成在一跳邻图 B 的外侧边**并在 B 上重建袭击 lord，行军跨缝入 A 由既有推进链（行 4/第⑥位 fallback）承担 | `Patch_IncidentWorker_Raid_TryExecuteWorker_RaidOuterSpawn` → `Patch_CellFinder_TryFindRandomEdgeCellWith`（4 参）→ `Patch_RaidStrategyWorker_MakeLords_RaidOuterSpawn` |
| 发狂动物（`IncidentWorker_AggressiveAnimals`）/ 食尸鬼（`IncidentWorker_GhoulAttack`） | 仅同图方向约束（外侧接缝）；不跨图——动物非 NPC 战斗体、近战索敌不跨图，无法行军 | 两个 worker 的 TryExecuteWorker Prefix/Postfix |
| 邪教徒围攻（`IncidentWorker_HateChanters`） | **不适用（v1 排除，用户定夺）**：强制专用 arrival mode + 自带"边缘分批布点 + 吟唱"lord，且跨图化难度剧增 | 无（不经 IncidentWorker_Raid 链） |

判定口径：**不活跃 = 对侧无图或休眠**（降频算活跃——可见即被看见）；候选缝随机选择、逐个验证落地（同图 = 朝向格非空即可，validator 原版把关；跨图 = 锚点可站且能走到 B 朝 A 侧缝，防密封口袋）。任何失败回落原版行为（原版落点 = 本图接缝圈随机格）。总开关 `raidOuterSpawnEnabled`（默认开）。

已知边界（接受）：跨缝入 A 的袭击者由 `TryAttachAssaultLord` 收编（不绑架/不偷窃，与追击链一致）；袭击超时计时从 B 上建 lord 起算（含行军时间）；B 上有玩家资产时袭击者先与本地交战（涌现，用户接受）。
