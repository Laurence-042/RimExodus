# RimWorld 完整地图生成步骤（含 RimExodus 注入点）

本文档记录 RimWorld 地图生成的完整 genStep 执行顺序，并标注 RimExodus 在哪里插入了什么。
目的是让后续开发快速恢复"原版做了什么、我们在哪一步介入"的上下文。

> **排序规则**（`MapGenerator.cs:MapGeneratorPostInit`）：`orderby def.order, def.index`。
> genStep 按 `GenStepDef.order` 升序执行，order 相同时按 `Def.index`（加载顺序）决胜。
> **MapGeneratorDef 里 XML `<li>` 列表顺序不影响执行顺序**——只决定包含哪些 GenStepDef。

## 注入目标

分帧增量路径在排序后执行步骤时，也必须应用原版 GenStepDef.preventsGenSteps 过滤；这与 biome/mutator 的 preventGenSteps 不同，Odyssey 的互斥地图步骤会实际依赖它。

RimExodus 通过 `1.6/Patches/MapGeneration.xml` 的 `PatchOperationAdd`，把四个自定义 GenStepDef
追加到**全部表面层 `MapGeneratorDef`** 的 `genSteps` 列表（2026-08 通配化，原为点名
`Base_Player`/`Base_Faction`/`Encounter` 三个；通配覆盖 EscapeShip/Archonexus 周期/AncientStockpile/
InsectLair 等显式设置名单外 mapGenerator 的 def，以及 mod 自定义 MapGeneratorDef——Patch 在全部
mod Defs 加载后跑，后加载者同样被注入）。核心命中：

- **`Base_Player`** — 玩家家园（锚点 A）+ 邻接地块地图（B/C）复用此链。
- **`Base_Faction`** — 派系基地。
- **`Encounter`** — 遭遇/任务地点（绝大多数 Site def 不设 mapGenerator，`MapParent.MapGeneratorDef`
  回落默认此链）。

三者都继承自 `MapCommonBase`（抽象父类），共用大部分 genStep。

**通配排除谓词（勿删）**：① xpath 带 `[defName]` 存在性——`MapCommonBase` 是无 defName 的 Abstract
基类但有 genSteps 节点，通配命中它会使注入经 XML 继承（Patch 先于继承解析跑）传播给全部子类，
Base_Player/Encounter 等会**双份注入**（389/392 重复跑）；② 空间层家族显式排除
（Space/Asteroid/AsteroidBasic/OrbitalItemStash/OrbitalRelay/Mechhive/SettlementPlatform——后三者同
`ParentName="SpaceMapGenerator"`）：genStep 内部 `GetMapWorldTile` 守卫对空间层/口袋图返回 -1 早退，
注入本无害，但语义上空间图不属于表面无缝网格。无 `<genSteps>` 子节点的 def（如 SpacePocket 纯继承）
不被 xpath 命中、天然跳过。

## 完整执行顺序表（Base_Player 链）

下表按 order 排列。**RE** = RimExodus 自定义 GenStepDef；**RE-HP** = RimExodus Harmony patch
（Postfix/Prefix，在该 order 的原版 genStep 内部运行）。标注"仅玩家"的步骤只出现在 Base_Player。

| order | defName | 来源 | 说明 / RimExodus 介入 |
|------|---------|------|----------------------|
| 10 | ElevationFertility | Core | 高程/肥力噪声。 |
| 20 | MutatorPostElevationFertility | Core | |
| **100** | **RimExodus_CenterCorridor** | **RE** | 中心走廊（2026-08，端点算法 v2 = 2026-08-30 用户定稿）：邻接生成图在 `MapGenerator.Caves` 浮点网格上挖"B 中心 ↔ 各已生成邻居方向接缝"的星形走廊，RocksFromGrid(200) 在 caves>0 格不 spawn 岩石（原版洞窟同一条无后效通道，零 Destroy/零屋顶支撑后效；深水等非岩地形不算阻挡；穿山段保留岩顶 = 原版山内隧道语义）。端点算法：对每个已生成邻居 N——N 侧接缝带（朝 B 的边、格心距边线段 ≤6）无自然岩格按 **region 分集合**（可达性等价类；边上无岩=1 集合、3 条洞穴到边=3 集合）→ 每集合随机代表 → 可达目标者随机取一（触发图 A 的目标 = goto 触发位置 `NeighborGenerationSourceTriggerCell`[QueuePreload 钻孔，缺失回落 A 中心]；其余 C = C 中心）→ 无通过者不为该 N 挖洞（A 侧死胡同不白挖）→ B 侧端点 = 代表 + offset 镜像（镜像格 N 侧保证无岩，392 照抄区不回填堵口）。首版"B 侧排序探测 60 上限 + 盲挖回落"已被实测否决（探测窗口只盖中点 ±15 格：中心连通洞穴被误判没通道 / 死胡同反被盲挖命中），勿回退。形态 = 直线骨架 + Perlin 蜿蜒（端点正弦包络）/宽度浮动/短分支。只挖 B 侧（单向原则）。**走廊格免疫 392 混合**（足迹运行时组件并入 BuildRoadGuard 道路保护集——N 侧 void 外条带快照是清 void 前原生山体，照抄区会把岩 spawn 回 B 带上压掉走廊末端；精确足迹含低 elevation 格，无 ±3 缓冲）。**必须在 200 之前（勿后移）**。无已生成邻居（初始图）端点集空早退。不进 MapPreview 预览（预览不消费 caves）。**深山洞室**：走廊格把 caves 计数推过 N×1000 巢数阈值时"欠"一个放不进窄隧道的 Hive（守卫点找不到 → 红字）——全部走廊深山采样点中最深处（距端点 >10 勿跨照抄区）挖一个直径 ~8 洞厅作 Hive 宿主位，欠账最多 1 巢、一个洞厅闭环。 |
| 200 | RocksFromGrid | Core | 原版行为（spawn 岩石 + 设岩顶）。**RimExodus 不再 patch 此步**（2026-08 删除 `Patch_GenStepRocksFromGrid`——曾在此预清将来-void 格的岩石/屋顶，但 void 格含接缝带外条带，提前清理会破坏 389 才备份的原生岩体/屋顶快照，导致新图照抄区岩壁整齐切断；清理全部归 389 的 ApplyPolygonTerrain，在三层快照备份之后依次清 roof → rock → terrain）。 |
| 210 | Terrain | Core | 基础地形（据 elevation/fertility 铺 terrainDef）。 |
| 220 | MutatorPostTerrain | Core | 原版 Coast/River 等 TileMutator 在此铺水（海洋/河流/三角洲）。**RE-HP**：`Patch_TileMutatorWorker_River_GetMapEdgeNodes` Prefix 在此 genStep 内部运行——河端点替换为接缝边中点锚点，与对端地图对齐。 |
| 225 | BiomeVariants | GL（运行时注入） | Geological Landforms 的群系变体层（GL 的 GenerateContentsIntoMap Prefix 运行时构造 GenStepDef 注入，不在任何 XML 链；landform 的高程/地形改写在 20/220 的 TileMutatorWorker_Landform 钩子里，非独立 genStep）。RimExodus 增量分帧路径不经该方法 → 由 `SeamlessLandformsCompat` 在 Start 组装 genSteps 时复刻注入（含各 landform CustomGenSteps，默认 order≥230；GL 预览线程过滤 ≥230）。GL 全链兼容机制与 CheckWorldObject 白名单见 `AGENTS.md` GL 兼容条（2026-08）。 |
| **230** | **RimExodus_CoastalEdgeFill** | **RE** | 海岸补铺：Coast mutator(220) 只按世界图距离铺水，不看局部六边形。本步补铺六边形边缘的水/沙/深水带。须在 Plants(900) 之前——让植物生成跳过新淹没格（fertility=0）。 |
| 240 | RemoveTinyIslands | Core | |
| **389** | **RimExodus_SeamlessTile** | **RE** | **关键步骤**（2026-08 从 1400 提前；2026-08-30 从 391 再提前到 Roads 前——道路 A* 视界内六边形外不可走无绕行通道，河端点免延伸到方形边）：① 备份完整矩形 topGrid 到 baseTerrainSnapshot（void 裁切前，基础地形写入步骤 210/220/230 已完成；Roads(390) 在其后但被 TerrainGrid void 守卫拦住写不进 void 格，外条带快照不受影响）；② 按六边形铺 void（六边形外格写 `RimExodus_Void` + 清实体 roof → rock → terrain）；③ **pathGrid 全量刷新**（`pathing.RecalculateAllPerceivedPathCosts`——直写 topGrid 不触发重算，不刷则后续步骤的 Standable/Walkable 读旧缓存；**MapPreview 预览线程跳过**——预览图组件被裁剪，刷新会 NRE，见 `SeamlessMapPreviewCompat`）；④ 撤离 void 格上的 Pawn（纯防御，正常时序下此点无 pawn）。 |
| 390 | Roads | Core | **RE-HP**：`Patch_GenStep_Roads_FindRoadExitCell` Prefix 在此 genStep 内部运行——强制道路出口格选在目标方向的六边形接缝锚点上（多边形几何算，不依赖传送点——传送点要到整条 genStep 链之后才铺设）。两端锚点通过 offset 对齐。 |
| **392** | **RimExodus_SeamOverride** | **RE** | 接缝覆写（2026-08 从 1410 提前）+ 混合后 pathGrid 刷新 + 捕获接缝条带快照（`SeamStripData.CaptureAndStore`：B∪T 最终实况 + 外条带原生快照）。混合：B 三层字面照抄对端、T 卷积权重覆盖，详见 AGENTS.md。末尾**预设 PlayerStartSpot**（已裁切实况判定 + 未设守卫）——选址(850) validator 的 `district.TouchesMapEdge` 在裁切图上恒 false、必走 fallback 红字，预设（原版"上游已设跳过"守卫）让揭雾根=六边形中心附近无顶格。**MapPreview 预览线程在混合（ApplyOneWay，预览要显示接缝效果）之后 early return**：条带快照/pathGrid 刷新/StartSpot 预设全跳过（预览图 parent null 白干 / NRE 点 / 预览线程写 static 竞态）。**2026-08 追加：快照捕获后调 `SeamlessVoidRockLink.PlaceAfterSeamOverride`**——在"切比雪夫 1（8 邻含对角）内有本图岩石 + 延续判据（最近边邻居已生成 → 其条带快照镜像格 a = c − offset 的 3×3 邻域有岩；未生成 → own baseBuildingSnapshot 该格 3×3 邻域有岩）"的 void 格上铺隐形 `RimExodus_VoidRockLink`（LinkGrid Rock flag，边界岩 atlas 选连续片；单向原则无回铺；**必须挂 392 末勿移 389**——紧贴 void 的接缝带岩石多数是 392 照抄 SyncRockBuildingTo 才 spawn 的，389 时点条件全灭；随预览早退路径一并跳过）。 |
| 400 | Settlement（Base_Faction）/ 各 site 结构（Outpost/BanditCamp/ItemStash/Turrets/AncientComplex 等） | Core | **会写 topGrid**：走 BaseGen `SymbolResolver_FloorFill` → `SetTerrain` 铺地板/挖地基。选址 patch 已把锚点/矩形拦在六边形边外（margin 20/10），void 格上的写入另被 `Patches_TerrainGrid` 守卫拦截；残余缺口（贴附矩形/降落平台）观察项。 |
| 500 | MutatorCriticalStructures | Core | **部分 mutator 会写 topGrid**：Harbor 铺 Bridge/PackedDirt（`TileMutatorWorker_Harbor.cs:248`），ArcheanTrees 铺 SoilRich。 |
| 500–700 | MutatorNonCriticalStructures | Core | |
| 740 | AncientRuins_Scarlands/Special/FrozenRuins | Odyssey | 生物群落相关。 |
| 750 | ScatterRuinsSimple | Core | 只 spawn 建筑 Thing，不写 topGrid。 |
| 750 | ScatterShrines（仅玩家） | Core | 只 spawn Thing。 |
| 750 | AncientUtilityBuilding | Ideology | |
| 850 | FindPlayerStartSpot | Core | |
| 875 | ScenParts | Core | 只 spawn Thing，对邻接地块无害（GameInitData==null 守卫）。 |
| 890 | Pollution | Biotech | 写 pollutionGrid，不写 topGrid。 |
| 900 | Plants | Core | 只 spawn 植物 Thing（读 fertility），**不写 topGrid**。 |
| 950 | SteamGeysers / GeothermalVents / Craters* / ScarlandsJunkPrefabs | Core/Odyssey | 只 spawn Thing。 |
| 960 | RockChunks + 古代杂物步骤（约 11 个） | Core/Ideology/Biotech/Odyssey | 只 spawn Thing。大量并列，`preventsGenSteps` 过滤。 |
| 1100 | PoluxTrees（Biotech）/ HarbingerTrees（Anomaly） | DLC | |
| 1120 | CaveHives（仅玩家） | Core | 只 spawn Thing。 |
| 1150 | Snow | Core | 写 snowGrid + tempGrid（SetTempTerrain ThinIce），**不写 topGrid**——snapshot 不含 tempGrid，不受影响。 |
| 1200 | Animals | Core | 只 spawn Pawn，**不写 topGrid**。在 void(389)/混合(392) 之后于最终地形上选址——Standable 门（pathGrid 已刷新）天然跳过 void 格，不落 void（2026-08 修正的动物站 void bug 即旧序 1400 下的产物）。 |
| **1490** | **RimExodus_EnterSpots** | **RE** | 铺传送点（2026-08 从 onComplete 提前，勿回退）：必须在 Fog(1500) 之前——Fog 的 `UnfogMapFromEdge` fallback 三级候选都要过 `CanReachMapEdge`/`TryFindRandomEdgeCellWith`，这些 patch 以"传送点已铺"（`HasSeamEdge` = listerThings 实况）为门；点在 onComplete 才铺时 Fog 走原版方形边缘语义，裁切图上恒不可达 → 零揭雾、整图全雾（2026-08 偶发全雾成因之一）。铺设幂等；`RefreshEnterSpotArrivals` 仍留 onComplete（依赖邻居表 offset）。 |
| 1500 | Fog | Core | 揭雾：**被 `Patch_GenStep_Fog_SeamOrigin`（Patches_GenStepFog.cs）分径接管**——初始地图（开局/扎营/定居/gravship 等原生入口）原版行为（PlayerStartSpot 中心洪水 / invalid 时 UnfogMapFromEdge 三级候选走接缝语义 patch，1490 铺点后生成期已生效）；**邻接生成图**（增量 IsGenerating ∨ Settlement 预加载标志）只从生成方向接缝洪水揭雾（`NeighborGenerationSourceTile` 取向、异常容错回退活跃边；**2026-08 多根化：对源边全部 Standable 传送点逐个发根**——山体延伸到接缝带把源边室外区分割成多段"走廊"时单最优根只揭一段、其余段留雾，全 spots 发根各段各揭（同连通域后续洪水因 PassCheck 要求 fogged 立即空转去重）；源边零可站 spot（spots 全被岩石挡住）也降级全部活跃边再收集，防整图留雾；原版口径继承：洪水只被实心 edifice 挡、水/关着的门挡不住、4 邻遍历）+ 全图 void 直接揭雾（跨缝视野通透）。首版自建 1505 genStep 已删（用户定夺"不自搓第二套清除"）。 |
| 1600 | MutatorFinal | Core | **少数 mutator 的 GeneratePostFog 会写 topGrid**：AncientUplink 铺 AncientTile、InsectMegahive 铺 InsectSludge/rock。Coast/River 不写（River 只建 lookup texture）。这些罕见任务地块上 392 覆写会被 1600 覆盖——已知小瑕疵。 |
| 1600 | AnimaTrees（Royalty）/ AncientExostriderRemains（Biotech）/ VoidMonolith（Anomaly） | DLC | "PostFog"步骤，仅起始玩家地图。 |
| 1650 | QuarryDebris 子定义 + AncientTuneller | Odyssey | |
| 1700 | GravshipMarker | Odyssey | |

## RimExodus 自定义 GenStepDef 汇总

定义于 `1.6/Defs/MapGeneration/SeamlessTileGenerator.xml`：

| order | defName | class | 作用 |
|------|---------|-------|------|
| 100 | RimExodus_CenterCorridor | `GenStep_CenterCorridor` | 中心走廊：写 Caves 网格防岩石 spawn（B 中心↔各已生成邻居端点星形网，仅排岩不排水；端点 = 邻接侧 region 分集合随机代表镜像）。 |
| 230 | RimExodus_CoastalEdgeFill | `GenStep_CoastalEdgeFill` | 海岸补铺（深水/浅水/沙滩带）。 |
| 389 | RimExodus_SeamlessTile | `GenStep_SeamlessTile` | 备份 snapshot + 铺 void + pathGrid 刷新。 |
| （1500 行为） | （Patch_GenStep_Fog_SeamOrigin） | `Patches_GenStepFog.cs` | 揭雾分径：初始地图原版中心洪水；邻接生成只从生成方向接缝洪水（室外连通域）+ void 全揭。Harmony Prefix，非 genStep。 |
| 392 | RimExodus_SeamOverride | `GenStep_SeamOverride` | 接缝卷积混合 + pathGrid 刷新 + 条带快照捕获 + 预设 PlayerStartSpot。 |
| 1490 | RimExodus_EnterSpots | `GenStep_EnterSpots` | 铺传送点（Fog 前接缝语义 patch 生效 + 幂等补铺）。 |

230/389/392 带 `MapPreview.GenStepProperties includeInPreviews=true`（MayRequire=`m00nl1ght.MapPreview`）；
100/1490 不进预览（预览只关心地形：铺 thing 无意义 / 预览不消费 Caves 网格）。

**void 区预览残留岩石（2026-08 修复）**：MapPreview 的 `PreviewTextureGenStep` 画预览纹理不读实体——用 `MapGenerator.Elevation >= 0.7` 代理判据直接画石色；389 清岩不写 elevation → 曾有岩的 void 格在预览里仍显岩石。修复 = 389 铺 void 后**预览线程限定**把 voidCells 的 `MapGenerator.Elevation` 置 0（预览是该阈值的唯一消费者，真实渲染不读它，主生成链零改动）。

## RimExodus Harmony genStep patch 汇总

| patch 文件 | 目标方法 | 类型 | 生效 order | 作用 |
|-----------|---------|------|-----------|------|
| ~~`Patches_GenStepRocksFromGrid.cs`~~ | ~~`GenStep_RocksFromGrid.Generate`~~ | ~~Postfix~~ | ~~200 内部~~ | **已删除（2026-08）**：预清破坏 389 快照备份的原生岩体/屋顶数据（照抄区岩壁切割）。清理归 389。 |
| `Patches_TileMutatorRiver.cs` | `TileMutatorWorker_River.GetMapEdgeNodes`（protected） | Prefix | 220 内部（经 MutatorPostTerrain） | 河端点从"随机直线图外交点"替换为**接缝边中点本身**（2026-08-30 定稿，旧"延伸到方形边"方案已弃——走廊水进 overlap 空间被对端照抄、方向发散时接缝带内剪刀差；void 已提前到 389[Roads 前]，道路 A* 无绕行通道，延伸失去必要性）。原版深度场端点无收窄，缝两侧满宽对接。边匹配走共用的 `FindEdgeByWorldHeading` + river-link 过滤（GetRiverDef != null，2026-08 修复全池匹配锚错边致接缝断裂）。 |
| `Patches_TileMutatorRiver.cs` | `TileMutatorWorker_River.GetDisplacedPoint`（protected virtual） | Postfix | 220 内部 | **端点垂直接近段**（2026-08-30 定稿，替代旧压弯窗口——bell 在端点=0 使河道过中点方向=中点连线，不垂直于边）：端点 ≤12 格内强制河道沿边法线（等边模型下中心→边中点连线天然垂直于边），smoothstep 过渡到原版弯曲。两侧接近段是同一条空间法线 → 缝上共线直线对接（河链弯折无折角）；垂直穿越消灭六边形内侧旱楔子（路 A* 无贴岸绕行通道）。 |
| `Patches_GenStepRoads.cs` | `GenStep_Roads.FindRoadExitCell`（private） | Prefix | 390 内部 | 道路出口格强制对齐到接缝锚点。边匹配走共用的 `FindEdgeByWorldHeading` + road-link 过滤（只遍历 GetRoadDef != null 邻居——原版 CalculateNeededRoads 的角度偏置+抖动会污染 angle，两条路夹角 60° 时偏置达 60° 错到隔壁边；私有拷贝 `FindRoadLinkEdgeByHeading` 已删，2026-08 与河流公共抽取）。可达性检查用原版两级放宽（NoPassClosedDoors → PassAllDestroyableThings）。 |
| `Patches_GenStepRoads.cs` | `GenStep_Roads.Generate` | Postfix | 390 内部 | 把 static `GenStep_Roads.paths`（A* 路径节点）快照到 `SeamlessRoadPaths` MapComponent——分帧增量生成下 390→392 之间 static 可能被其他地图清空。SeamOverride 用它识别本地图道路格（±3 格缓冲保护，防窄路被卷积抹掉）。 |
| `Patches_GenStepRoads.cs` | `GenStep_Roads.ApplyDistanceField`（private） | Postfix | 390 内部 | 接缝锚点 ≤6 格且 fromRoad≤1.5 的格强制补铺主路面材质——原版双重随机抽签在土路中线留 ~11-14% 断格（接缝 3 格断概率 ~36%）。水格/桥格（terrain.bridge）/已铺路格跳过；FlagstoneSandstone 映射 rockDef。 |
| `Patches_BuildingPlacement.cs` | `GenStep_Scatterer.CanScatterAt`（protected virtual） | Prefix | 400-970 内部（全部 Scatterer 子类） | 建筑锚点六边形外或距边 <20 格（最大建筑半宽+1）直接拒绝——原版选址只按方形边界收缩（BoundsRect(12) 等），不感知六边形，建筑跨边被 void 切半。 |
| `Patches_BuildingPlacement.cs` | `MapGenUtility.GetClearRects` | Postfix | 400-500 内部（清晰矩形路：Outpost/AncientComplex/Gravcore/SurveySite/Harbor 等） | 过滤四角+边中点距六边形边 <10 格或六边形外的矩形（Burst 内核不可 patch，此托管入口是唯一可行点）。 |
| `Patches_BuildingPlacement.cs` | `CellFinder.RandomNotEdgeCell` | Postfix | 850 兜底 + 运行时 | 采样六边形外 → Invalid（FindPlayerStartSpot tightness 降级兜底 + CompDeepScanner/incident，此时六边形外是 void 语义正确）。 |

## 关键时序约束

1. **snapshot 备份在 389（2026-08 反转旧"必须在 1400"结论；2026-08-30 从 391 再提前到 Roads 前）**：备份须在全部**基础地形**写入步骤（Terrain 210 / Coast 220 / CoastalEdgeFill 230）之后——这是 389 下限的硬约束；Roads(390) 虽在其后，但道路写不进 void 格（TerrainGrid 守卫）、外条带快照只覆盖 void 侧格，不受影响。order 400+ 的地板写入（Settlement BaseGen `SymbolResolver_FloorFill` / Harbor 桥 / ArcheanTrees）已被选址 patch（ScatterEdgeMargin=20 / ClearRectMargin=10）拦在接缝带之外，快照的游戏逻辑消费面仅剩 void 侧外条带（`SeamStripData.CaptureAndStore`），B∪T 读活数据非快照；残余缺口（贴附矩形/降落平台/Harbor 桥溢出）处行为有变化，记观察项（见下节）。Plants(900)/Animals(1200)/Snow(1150) 不写 topGrid。
2. **void 铺设在 389 的原因（勿回退到 1400）**：让 Settlement(400) 及之后全部步骤在最终地形上工作——Plants 的 fertility 门（FertilityGrid 实时算，void=0）、Animals(1200)/威胁步骤(1600) 的 Standable 门天然跳过 void 格。**前提**：铺 void 直写 topGrid 后必须即调 `map.pathing.RecalculateAllPerceivedPathCosts()`——Walkable/Standable 读 PathGrid 缓存数组而非 terrainGrid（GenGrid.Walkable → pathGrid.WalkableFast），不刷新则动物照落 void（2026-08 实测历史 bug：旧序 1400 直写后等 FinalizeInit 才重算，`EvacuatePawnsOnCells` 的 FindNearestWalkable 读旧缓存 + 径向首候选即自身格 → 撤离原地空转）。
3. **SeamOverride 必须在 void(389) 之后、Settlement(400) 与 Fog(1500) 之前**：读两端数据 + 写覆写后的地形；混合同样直写 topGrid，结束后同样刷新 pathGrid（否则带格混出水后 Standable 仍判可走，pawn/植物落水上）。Fog 基于最终地形揭雾。
4. **道路 patch 在 390 靠多边形几何算锚点**：传送点 1490 才铺设（原为整条 genStep 链之后的 onComplete），390 时不能查 `SeamlessEdgeCells`，仍用纯多边形几何。
5. **传送点必须在 Fog(1500) 之前铺（EnterSpots=1490，勿回退到 onComplete）**：Fog 的 `UnfogMapFromEdge` fallback 与选址兜底依赖 `CanReachMapEdge`/`TryFindRandomEdgeCellWith` 的接缝语义 patch，patch 以 `HasSeamEdge`（listerThings 传送点实况）为门——点在 onComplete 才铺时这些判定在生成期走原版方形边缘语义，裁切图上恒不可达 → 零揭雾整图全雾（2026-08 偶发全雾成因之一）。铺在 1490 而非更早（392）：400-1490 期间保持与旧序一致（patch 未生效）零回归，且 Plants/Animals 生成时传送圈格上无 thing 干扰。
6. **PlayerStartSpot 是进程级 static，跨图生成必须重置**：`IncrementalMapGenerator.Start` 已补 `PlayerStartSpot = Invalid; rootsToUnfog.Clear()`（对齐原版 `MapGenerator.GenerateMap` 开头，`ClearWorkingData` 本身不动这两个字段）——不重置则上一张图的坐标残留，850 的"已设跳过"守卫跳过本图选址、Fog 用他图坐标揭雾（落 MakeFog 建筑时零揭雾，2026-08 偶发全雾成因之二）。392 末的预设（已裁切 + 未设守卫）另消除选址 fallback 的每图红字。
7. **MutatorFinal(1600) 对地形的影响**：多数 mutator 不写 topGrid，但 AncientUplink/InsectMegahive 的 `GeneratePostFog` 会 `SetTerrain`——这些任务地块上 392 的覆写可能被 1600 覆盖（已知小瑕疵，罕见场景）。void 格上的此类写入被 `Patches_TerrainGrid` 守卫拦截（389 提前后该守卫在生成期实际生效）。

## 2026-08 反转：把 SeamlessTile/SeamOverride 从 1400/1410 提前到 389/392

曾否决过提前到 389-399（旧否决理由：①389 备份 snapshot 会漏掉 400+ 的 BaseGen 地板/Harbor 桥写入，卷积用旧地形；②SeamOverride 在 392-399 跑，之后 Settlement(400) 的 FloorFill 会盖掉卷积结果、SyncRockBuilding 岩石 spawn 干扰选址）。**2026-08 反转，动因与重新评估**：

- **动因**：实测动物站 void——旧序下 Animals(1200) 先在将来 void 格上合法生成、靠撤离兜底，而撤离被"直写 topGrid → pathGrid 过期"击败（见约束 2）。根治 = 让动物生成时 void 已在且可通行性缓存正确。
- **旧否决理由①重评**：快照消费面经查只有 void 侧外条带（B∪T 读活数据）；选址 patch（margin 20/10）今天已把地板写入拦在带外——旧否决写下时选址 patch 体系尚未成型。残余：贴附矩形/降落平台/Harbor 桥若溢出到带/外条带，行为有变化（旧序：写入进快照/被混合覆盖；新序：不进快照/覆盖混合），频率窄，观察项。
- **旧否决理由②重评**：FloorFill 盖混合只发生在残余缺口处（正常选址不进带）；岩石 spawn 干扰选址不成立（锚点 margin 20 >> 带深 ~10）。void 格上的地板写入转而被 `Patches_TerrainGrid` 守卫拦截（预期收益）。
- **新增观察项**：①污染地块(890) 的污染变体现在发生在混合之后，带格与对端可能不一致（旧序为相反方向的不一致）；②`completelyIgnoreFertility` 植物不受肥力门约束、可能漏进 void 格（旧序靠 1400 事后清理兜住，新序无此清理）；③MapPreview 预览链的 order 变化回归。
