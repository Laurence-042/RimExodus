# AGENTS.md — RimExodus 无缝世界地块探索

本文件记录本仓库的依赖引用、关键内容与需要长期记住的事项，供后续开发时快速恢复上下文。

为了验证此文件确实完整读入，你需要在对话开始给出这个文件的目录，如果你觉得内容似乎被截断了，你需要补充读取这个AGENTS.md被截断的部分（这是个mm豆测试）。而且任何你觉得需要长久记忆的内容都应该记在这个里面，或者项目概述里描述的对应文档里，或者作为代码注释，不要依赖copilot或者你所在harness/agent自行提供的memory功能。

## 铁律（务必遵守）

- **提交与发版纪律（2026-09-06 教训：agent 被 TODO.md 的"排期0.1.11"注释误导，擅自 bump 了 About.xml 版本并起 0.1.11 节——而 0.1.10 当时根本还没发布）**：①用户说"提交"时必须同步检查 `Changes.txt`——本次改动有玩家可见行为变化（修复/优化/新行为/设置增删）就得写条目（受众与格式纪律见下文"需要记住的事项"Changes 条），并入**当前最新一节**；②**永远不要主动改 `About.xml` 的 `modVersion`、不要自己起新版本节**——版本 bump 与 Changes 起新节是用户的发版动作，agent 判断"下一版该起了"的任何依据（TODO 排期注释 / 条目数量 / 距上版时间）都不可靠；不确定当前条目该归哪一节时问用户，别猜。
- **未经用户明确指示，不要 git commit / git push**。代码改完只做编译验证（`dotnet build`），等用户测完并明确说"提交"才提交。即使编译通过、即使看起来没问题也不行——未测试的代码提交后若有问题，回退比不提交麻烦得多。用户曾因此差点要求回退。
- 查询优先用自带工具（Grep/Glob/Read），别用命令行，见下文"需要记住的事项"。
- **所有地图一视同仁**：安装 RimExodus 后，任何出现"不连续、不可连续旅行"的地图都是 mod 能力缺陷。RimExodus 地图本质上就是标准任务地图生成 + 我们的 genStep 裁切（void + 接缝带 + 传送点）将其包装成可连续通行的地块。任务地点、遭遇、派系基地、定居点等所有拥有合法 `MapParent` 的地图都走 RimExodus 完整链。代码中 `GetMapWorldTile(map) < 0` 的守卫是**纯防御性 fallback**（防 `map.info.parent == null` 或 `MapParent_SeamlessTile.worldTile` 未初始化等异常态），**不是"RimExodus 地图 vs 普通地图"的类型区分**——对所有合法地图它都不会触发。patch 新功能时不要加"非 RimExodus 地图放行原版"的分支，那与 mod 目的相违背。**唯一例外（2026-08 收口，勿删）**：`GetMapWorldTile` 对**口袋图（`PocketMapParent`，含 VMF 载具内部图——其硬编码 `Tile=0` 会冒充真实地块污染邻居解析/天气域/传送落点）与空间层图（`map.Tile.LayerDef != Surface`——Odyssey 轨道 tileId 与表面恒撞号，被收进表面天气域会读错群系甚至反客为主当宿主把表面域天气锁死 Clear）返回 -1**——这不是"放行原版"分支，而是把这两类图排除出表面无缝语义（与 SetupNativeParentMap/IsNativeFamily 的表面层守卫同族）；收口在这一个点（全部按 tileId 的查找——天气域/TileGraph/BorderLookup/governor 源——都经它，一处生效全链路受益）。

## 项目概述

RimWorld Mod：实现"无缝世界地块探索"系统，使相邻世界地块的局部地图在视觉与操作上连续连接，Pawn 可直接从一张地图走入相邻地图，无需组成远行队。

文档索引（逐条列表见 `README.md`「项目文档索引」节，**开工前先读 README**——它还包含「源码目录结构」节，是各源码文件用途的单一事实源，本文件不重复该清单）：

- `README.md` — **对外正式文档**：mod 简介、核心特性、Mod 兼容性结论（已适配/共存/预期不兼容）、已知限制、设置项表、**源码目录结构（Source/ 九个子目录的职责与关键文件）**。兼容性/已知限制/设置项/文件用途的对外结论以此文件为单一事实源（AGENTS.md 只记内幕机制与教训）。
- 各 `doc/` 文档的权威归属速查（详细描述见 README 索引）：接缝带几何 → `doc/接缝带定义.md`；地图滚动生命周期 → `doc/地图滚动休眠.md`；跨图命令矩阵 → `doc/跨图命令适配审计.md`；genStep 顺序与注入点 → `doc/地图生成步骤.md`；阶段叙事 → `doc/第X阶段-*.md`（其中 `doc/无缝世界地块探索.md` 为主计划：核心交互场景是最终目的，实现过程中避免临时 patch）。

## 依赖引用目录

> **重要**：本仓库已包含全部所需源码与配置，**无需去游戏安装目录（`SteamLibrary/steamapps/common/RimWorld/`）查找**。游戏的源码、物品/Def 配置、以及 VMF/VF 框架源码都在 `references/` 下，直接在此目录内检索即可。

- `references/RimWorldDecompiled/` — RimWorld 反编译源码（含原生 `MapParent`、`PocketMapParent` 等）。
- `references/VehicleMapFramework/` — VMF（Vehicle Map Framework），跨地图框架（调研参考，非依赖）。
- `references/Vehicle-Framework/` — VF（Vehicle Framework），车辆基础框架（调研参考，非依赖）。
- `references/PerspectiveShift` + `references/PerspectiveShift.decompile` — Perspective Shift（ferny.PerspectiveShift，第一人称自由移动）原始 mod 与扁平化反编译（2026-08 适配调研 + 兼容层依据，见"邻居预加载与异步加载"节 PS compat 条）。

## 需要记住的事项

- **原版生成与存档恢复约束（2026-09 PR #3 审阅收口）**：增量生成必须执行 `GenStepDef.preventsGenSteps` 互斥过滤（语义 = 声明者保留、被声明的步骤移除，与 biome/mutator 的 `preventGenSteps` 不同）；快照 Def 表新格式索引 0 保留为空，读取同时兼容旧零基数据；分帧生成的成功收尾由 `FinishGeneration` 统一释放防重入锁/方向/触发格，所有准备、genStep 或收尾异常统一走 `CleanupFailedGeneration`（同时清半成品 Map/邻接反链，以及已注册进世界的半成品 parent），勿在 onComplete 子步骤分散清理。
- **接缝参考源（2026-09-08 条带快照删除定稿，勿复引 `SeamStripData`）**：生成新图 C 时只枚举世界相邻且**实际存在于 `Find.Maps` 的表面 Map**；每格 a=c−offset 在邻图 B∪T(depth≤`SeamStripInnerDepth`)优先读当前 terrain/自然岩 edifice/roof，当前 terrain 为 null/void 时回落该图 389 裁切前的**完整基础三层快照**，void 外条带也读基础快照。读取必须返回 `(has,value)`，`has=true,value=null` 是“明确无岩/无顶”，绝不能当缺数据；顶点最多两参考继续走权重合成，dOut 直接查邻图 `SeamBandInfo`，`dSq/(dSq+dOut)`、3×3、dither、道路/走廊/河保护不变。地块图基础快照不持久化；原版 `MapGenerator.GenerateMap` Prefix 与 `IncrementalMapGenerator.Start` 都必须在占用生成 static **之前**调用 `EnsureNeighborSnapshots`，为目标周围全部已加载参考图逐张补缺失 `<389` 快照（完整链先做 `preventsGenSteps` 互斥再截断）；MapPreview 当前线程、口袋图、空间层跳过，单图失败 Warning 且仅缺失 void 侧采样跳过。封存图无 Map，不参考；恢复先自然生成+与届时活邻图混合，再重放 `ZoneMapRecord`。旧档 `seamStrip` XML 自然忽略、下次保存消失，无迁移/版本 bump。
- **殖民者栏派生缓存约束（2026-09 PR #3 审阅收口）**：`CheckRecacheEntries` Postfix 过滤 entry 并重编 group 后，必须同步重建 `cachedDrawLocs` / `cachedReorderableGroups` / `cachedScale`，否则原版绘制以旧长度索引新 entry 可越界或错位；四个私有字段的反射绑定缓存在 patch 类静态字段，勿每次 recache 重复 `AccessTools.Field`。
- **RiverIsland 接缝弯曲修正（2026-09 PR #3 审阅收口）**：`TileMutatorWorker_RiverIsland` 自行 override `GetDisplacedPoint`，Harmony 对基类同名方法的 patch 不会覆盖该 override；须单独 Postfix 并复用普通河流的缝边弯曲归零实现。
- **多 Pawn 关联传送恢复（2026-09 Giddy-Up 2 分离修复）**：外部 mod 可能用多个独立 spawned Pawn 表示一个移动主体，不能只传实际寻路者。通用扩展点 = `Source/Transfer/SeamlessTransferAssociations.cs`：主 Pawn despawn 前捕获 session，主 Pawn 落地后让关联 Pawn **复用 `TryTransferPawnInternal` 全套状态保留链**（`captureAssociations=false` 防递归），再由 provider 恢复外部关系；勿在核心硬编码具体 mod 字段。首个 provider `SeamlessGiddyUpCompat` 全软反射（packageId `MemeGoddess.GiddyUp`）：以 Giddy-Up `isMounted` cache O(1) 门控，取 rider 的 `ExtendedPawnData.Mount` 随迁；落地必须先 `EndCurrentJob(InterruptForced)` 结束缓存源图的旧 `JobDriver_Mounted`，再调用其 `GoMount(Instant)` 重建双向数据/cache/job——直接 GoMount 会因旧 CurJobDef 已是 Mounted 提前返回，勿省略。未装 mod 不注册，provider 空表只付一次零迭代。
- **文档同步范围（勿只更新 AGENTS.md/TODO.md，2026-08 教训：GL 兼容第二轮漏主文档被用户指出返工）**：功能改动落地后，除 `AGENTS.md` 与 `doc/TODO.md` 外，还必须同步 `doc/` 下的相关文档——主计划文档 `doc/无缝世界地块探索.md`（「预期限制」对应条目处续注实际结果 + 「当前阶段结果」补汇总条目），以及该改动的**专题权威文档**（按"项目概述"文档索引归属：genStep 顺序/注入点变化 → `doc/地图生成步骤.md`；生命周期/休眠 → `doc/地图滚动休眠.md`；接缝带几何 → `doc/接缝带定义.md`；阶段叙事 → 对应第 X 阶段文档，等等）。**对外可见的结论变化（Mod 兼容性增删/已知限制变化/设置项增删）还须同步 `README.md`** 的对应小节——README 是这类对外事实的单一事实源。**Changes.txt 受众纪律（2026-09 确立，用户要求）**：版本条目（`Changes.txt`，About.xml modVersion bump 时起新节）面向**不了解项目代码结构的玩家**——只写"玩起来有什么变化"（症状 → 修复/新行为，直白措辞），中英双语逐条对照；禁止出现内部类名/机制行话（如 seamBandCache/MapRemoved/"接缝带几何缓存"——对玩家就说"已删除地图的内部数据"这类日常用语）。**用户可见文本（设置 UI/消息/对话）一律走 Keyed 翻译键**（`1.6/Languages/{English,ChineseSimplified}/Keyed/RimExodus.xml`，"Key".Translate()）——新增/修改键必须两个语言文件同步改（2026-08 设置窗口重构时确立：原生 TabDrawer 分 tab（地图生成/滚动休眠/跨图战斗/高级诊断）+ 滚动兜底 + 全条目 tooltip，中文翻译随附）。**术语纪律（2026-09-01 确立，玩家可见中文勿自造词——"据点"翻译误导教训）**：官方简中 **Settlement=派系基地**（Core WorldObjectDef；"定居点"**不是** Settlement 通用译名——那是 Odyssey 太空层 [SpaceSettlement=轨道定居点 / DestroyedSpaceSettlement=被摧毁的定居点] 与超凡枢纽周期定居点的用词）；Site 无统一显示名（世界图显示 SitePart 名：伏击/物品藏匿点/匪徒营地/哨所/沉睡的机械体等；Odyssey 站点类用"站点"）；Camp=营地；CaravansBattlefield 类挂在 AttackedNonPlayerCaravan def=远行队（遇袭）；DestroyedSettlement=派系基地（已摧毁）。查证处 = `Data/<DLC>/Languages/ChineseSimplified (简体中文)/DefInjected/WorldObjectDef/`。内部文档行话"据点"=Settlement（历史用词，读时按此映射即可），新写玩家可见文案（Keyed/README）一律用官方词。
- **查询优先用自带工具，别用命令行**：正常情况下使用 Grep/Glob/Read 等内置查询工具做检索与定位，不要用 Bash 跑 `Select-String`/`grep`/`findstr` 等命令。命令行转义（尤其 Windows + Git Bash + PowerShell 的引号/路径混用）容易出错，还会消耗用户的检视精力去判断命令是否安全。**例外**：Grep 工具在本仓库偶尔对明确存在的内容返回空结果（不报错，静默失败），此时可改用 PowerShell `Select-String -Path <绝对路径> -Pattern <正则>`（绝对路径用正斜杠）作为后备，这是已知的可靠替代。
- **分模块诊断日志（2026-09，取代单一 verboseLogging 总开关）**：`RimExodusLog` + `RimExodusLogModule`（`Source/Core/RimExodusLog.cs`）九模块（Generation/Transfer/CaravanExit/Weather/Dormancy/Combat/Compat/Settlement/Core）与 `RimExodusSettings.log*` 九字段一一对应，设置"高级"tab 逐项开关 + 总控（默认全关）。门控写法 = `RimExodusLog.Enabled(RimExodusLogModule.X)`（2026-09 已全量迁移自旧 `RimExodusMod.Settings?.verboseLogging ?? false`，**日志正文与前缀原样保留**——[caravan-exit]/[diag] 既有标签归 CaravanExit 模块、混排文件按行分配）；新埋点用 `RimExodusLog.Message(X, "...")` 输出 `[RimExodus:X]` 前缀。**纪律（勿回退）**：①全部 Warn/Error 不受开关控制（信号非 spam）；②常开清单不收编——Dormancy SLEEP/WAKE/DELETE、Throttle ON/OFF（文档定过的心跳）、GL 注册自检（刻意的成功路径可判别）、Uninstall restore 一次性输出、Dev 调试动作输出（显式请求的反馈，门控会让开发者漏看自己刚点的动作结果）、TileManager heal/sync 一次性、ShadowCaravan Scrubbed（诊断器：报告玩家看到即坐实）；③收编后默认静默的原无条件 spam：TraderDialog 14+1、ShadowCaravan created/dismantled、CheckPawnGoto 5（每次右键移动必刷）、Camp 生成 1。旧 verboseLogging 字段保留仅旧档兼容（无 UI、无消费点）。**天气域诊断埋点**（Rebuild 成员/激活/来源、Join 接入同步、Broadcast 转发目标、Gate 翻转、Activate 交接、Resume 校准、Save-Load activeTiles）全走 Weather 模块——排查"邻图天气不一致"类报告只开它。
- **切比雪夫/邻格遍历统一实现（用户纪律 2026-08，勿再手搓 dx/dz 循环或自建方向表）**：8 邻环（含对角不含中心）一律 `GenAdj.AdjacentCells`（原版偏移数组）、含中心 9 格窗一律 `GenAdj.AdjacentCellsAndInside`、切比雪夫距离标量用 `SeamlessGridMath.ChebyshevDistance`——消费方地图与历史教训见 `Source/Core/SeamlessGridMath.cs` 类注释（接缝带膨胀/权重深度/卷积采样/void 岩石判邻曾各自手搓致口径漂移：正交 4 邻与切比雪夫 8 邻混用过）。半径 >1 的方形窗口（道路保护 RoadGuardRadius=3）无原版数组，保留显式双层循环但注释须指回 SeamlessGridMath 口径。
- **源码目录组织（2026-08 重组，勿再平铺新文件到 Source/ 根）**：`Source/` 按功能域分 9 个子目录（Core/Generation/Lifecycle/Transfer/Interaction/Combat/EdgeBehavior/Rendering/Compat），**Harmony patch 与所属功能域同目录**、全部同 namespace `RimExodus`、csproj 隐式 glob 无需登记。各目录职责与逐文件用途 = `README.md`「源码目录结构」节（单一事实源）。新文件按域放置；文中引用源码路径时用新路径。
- **非序列化缓存的 built 旗标勿持久化（2026-08 读档断链教训）**：`SeamlessBorderLookup` 曾把 `built` 存进存档而 `borderCells` 刻意不序列化——读档恢复 built==true + 字典 null，`MapComponentTick` 的 `!built` 补建分支永不进 → `TryGetPreloadTarget` 恒 false → **读档后邻图预加载/生成整链断裂**（次生：`IsInNoBuildBand` 同因失效=禁建带读档后放行建造）。修 = ExposeData 全空、读档由首 tick 补建。纪律：运行时可重建状态（字典缓存+配套旗标）要么整体序列化要么整体不序列化，只存旗标不存数据 = 堵死自愈路径。另注：整条邻图生成链的序列化面已全面审计（邻居表 Scribe_References/worldTile/传送点 targetWorldTile 均正确序列化；基础快照按载体策略处理；preloader 队列/几何缓存/休眠集合/增量生成 static 均设计上可丢弃），BorderLookup 是唯一断点。**同族第二案（2026-08-27 修复勿回退）：传送点 `hasArrival/cachedArrivalCell` 不序列化且全部刷新点都是事件驱动（邻居登记/生成 onComplete/SetupNativeParentMap[仅 MapGenerated]/Sleep/Wake/口袋自愈），读档后一个都不触发 → 全图 spot hasArrival=false → 跨图选点候选集空、双向全拒（症状 = "no reachable seamless enter spot" 反复刷、点击重放正常）——修 = `SeamlessTileManager.MapComponentTick` 首 tick 一次性自愈（`enterSpotArrivalsStale` 旗标默认 true，刷新后置 false；新图生成后多刷一次幂等无害；休眠图首 tick 不跑但读档即睡经 Sleep 双端刷新、唤醒经 Wake 刷新闭环）。教训收口：非序列化运行时缓存的刷新点若全是"事件驱动"，读档后没有任何事件会补发——必须有首 tick 自愈路径（与 built 旗标案同纪律的镜像面）。**
- **几何进程缓存生命周期（2026-09 泄漏收口，勿回退为"永不失效"）**：`SeamlessPolygonGeometry.seamBandCache/polygonCache`（static，键 (worldTile, mapSize)）的生命周期 = **图删除释放 + 换档全清**——释放收口 `SeamlessTileManager.MapRemoved`，换档全清在 governor `LoadedGame`/`StartedNewGame`。每条 `SeamBandInfo` 把全图格装进多个集合/深度字典（250 图推算约 4 MB/tile），不释放会随探索无界累积。**2026-09-08 内存/存档收口**：已删除 `SeamStripData`，随之消失的历史成本为每图约 4.2 MB lookup + 1.3 MB 列表，以及存档解压后每图 2.2–4.1 MB 的 `seamStrip` XML；剩余基础三数组约 1.5 MB/图，地块图仅驻内存且按需重建，原生图的 `baseSnapshotData` 约 1.43 MB/图继续为卸载恢复持久化。真正大头仍是软休眠原版 Map 本体（每图约 50–150 MB）。
- **编译**：仓库根目录运行 `just build`（默认任务也是 `build`），底层命令为 `dotnet build Source/RimExodus.csproj -c Debug`，输出 `1.6/Assemblies/RimExodus.dll`。
- **离线 PatchAll 验证器（2026-08 新增，写新 Harmony patch 后必跑）**：`C:\Users\Laure\.zcode\tmp\rimptest\`（dotnet net48 控制台，引用游戏 Managed + workshop 1.6 0Harmony + 本 mod DLL，逐类执行 PatchClassProcessor 报告全部绑定/transpiler IL 失败，不用重启游戏试错）。**能力边界（2026-08 实测确立，勿当放行依据）**：它跑在 CLR 上，只能拦"绑定/锚点/签名"类错误；**Mono DMD 特有的非法 IL 它拦不住**（Stance_Warmup.InitEffects transpiler 案：验证器 BOUND OK、游戏内 InvalidProgramException = mod 加载失败）——反方向的伪迹也存在（DrawAimPie ref-Prefix 在验证器 ECall 失败、游戏内正常应用）。**权威确认 = 游戏启动日志的 mod 实例化行 + `GetPatchedMethods` 绑定报告**（总数常显、明细 verbose）。**基线**：12 个 patch 在 CLR 下必然伪迹失败（GetClearRects/Selector_SelectInternal/TerrainGrid_SetTerrain/4×Dormancy·IncrementalMapGen 的 MapPreTick·MapUpdate/MapGenTiming Pathing/Reachability_CanReach（跨图战斗期加入）/GenDraw.DrawAimPie（2026-08 瞄准 pie 修正，ref 改参式仍 ECall——与 Selector_SelectInternal 同款纯静态方法伪迹，待游戏内绑定报告确认）/GenStep_Fog_SeamOrigin（2026-08 揭雾分径，TypeLoadException 同款伪迹——待游戏内绑定报告确认）/Patch_MapInterface_SeamOutline（2026-08 接缝中心线勾勒，TypeLoadException 同款伪迹——待游戏内绑定报告确认）——错误模式 `TypeLoadException: 接口中的非抽象、非 .cctor 方法` / `ECall` / `KeyValuePair Deconstruct 找不到` / `Ambiguous match（Static 双重载须显式参数类型，见 Patch_FleckMaker_Static* 写法）`；2026-09 天气域形态 B 重构删除 WeatherCluster SkyManagerUpdate 守卫（本属伪迹 FAILED），13→12），**已确认全部在游戏内活跃**（Pathing 计时日志/休眠/天气域/跨图选中均有实测功能证据；Deconstruct 引用在原方法体 Pathing.cs:98 非 patch 侧），非死代码。），**已确认全部在游戏内活跃**（Pathing 计时日志/休眠/天气域/跨图选中均有实测功能证据；Deconstruct 引用在原方法体 Pathing.cs:98 非 patch 侧），非死代码。**BOUND OK 基线 78**（2026-08：71 + PlaySettings 全局控制条 toggle 与 ExitMapGridUpdate 绘制门控两 patch；2026-08-25 + SaveGame 生成期拦截与存档对话框拦截两 patch；同轮的 RegionListerNullGuard 诊断守卫已随根因修复删除；2026-08-26 影子远行队最终形态三 patch：CaravanUtility.GetCaravan 还原 / Caravan.GetGizmos 清空 / ThingOwnerUtility.GetRootMap 持有链 Map 还原——同日曾加的 Caravan.TickInterval 抑制与三个世界图隐藏/选中 patch 随"影子不进 Find.WorldObjects"方案 B 定稿整体删除，净 +3。均 BOUND OK、非 CLR 伪迹；2026-08-27 + Caravan.AddPawn 影子成员释放 patch（真组队冲突修复，__0 位置参数绑定），79。）已捕获的真问题：`ReachabilityUtility.CanReach` 实为 **7 参**（4 必选 + canBashDoors/canBashFences/mode 可选，只声明 4 参 = Undefined target method）；`Reachability.CanReach` 第 4 参名为 **traverseParams**（Harmony 按名绑定，拼错 = 启动失败）。**RimExodusMod 启动时输出 `GetPatchedMethods` 绑定报告**（总数常显、明细 verbose）——新 patch 上线后用它与预期清单比对即可权威确认绑定。**手动绑定（AccessTools.Method + harmony.Patch，不走 [HarmonyPatch] 特性）不在离线验证器覆盖内**（2026-08 实测教训）：按名绑定的 patch 参数拼错（5 参 `TryFindRandomEdgeCellWith` 的方向参数是 **dir**，写成 rot）= 构造器抛异常 = **mod 实例化失败、只剩 PatchAll 半套 patch 且其后手动绑定全丢**（症状：游戏内"mod 加载失败"+行为全面异常、verbose 零输出）；诊断类手动绑定必须 try/catch 包裹（诊断绝不杀死 mod）。
- **PatchAll 当前实测基线（2026-09-08）**：`BOUND OK 105 / FAILED 14`；14 项均是已知 CLR 验证器伪迹（历史 12 项 + 两个 Sustainer ECall）。本轮新增 `Patch_MapGenerator_GenerateMap_PrepareSeamReferences`、`Patch_Projectile_UpdateRateTicks`、`Patch_GenUI_TargetsAtMouse_CrossMapCombat`、`Patch_Targeter_OrderVerbForceTarget_CrossMapFeedback`、`Patch_Verb_DrawHighlight_CrossMapTarget`、`Patch_SoundStarter_PlayOneShot_SeamlessView` 已 BOUND OK；后续比对以本行为当前基线。
- 主设计文档的"当前阶段计划"定义推进顺序：VMF 调研 → 最小技术原型 → 六边形裁切 → 连续地形 → 跨地图寻路与射击（5 个阶段）。阶段 1-4 已完成，阶段 5 全部落地（边界行为/传送许可制 + 跨图索敌与射击缝交接机制，2026-08；游戏内回归进行中）。

## 当前架构核心事实（地块对等论）

以下是长期有效的架构事实，细节见各阶段文档。

### 基础地图对等架构（非口袋地图）
- **所有地块都是基础地图**：`MapParent_SeamlessTile : MapParent`（原生基类，非 `PocketMapParent`）。`mapParent.Tile = 真实 PlanetTile`，`map.TileInfo` 自动读 `Find.WorldGrid[Tile]`（含真实 biome/hillness/mutators/rivers），原生 Coast/River/Delta 等 TileMutator 自然生效。
- **无家园特殊论（用户定夺 2026-08）**：家园图不特殊。天气按**群系连通域共享**（`SeamlessWeatherClusterManager`，GameComponent 全局注册机制）——同群系（PrimaryBiome）且世界图邻接连通的图同属一个天气域，跨群系边界切断传递（雨林→温带→另一片不连通雨林 = 三个域）；**2026-09 起为"决策集中 + 执行各图"模型（形态 B，机制与勿回退要点见"天气域与休眠的交互"条）**——每图保留原生 sky/weather 三 manager 实例，共享的是决策与事件（换天决策集中在激活图 + TransitionTo 广播全域执行），非旧"字段替换共享实例"；域结构不序列化（每域仅存激活 tile id），读档 FinalizeInit 重算；图移除后 RebindAll 重算激活——gravship 起飞销毁家园图无善后需求。家园图仅剩两处工程性差异（均归一管理）：数据存储载体（原生 parent 图的邻居表与基础快照存 `SeamlessTileManager` 组件，**读写一律经 `SeamlessMapData`**）与生命周期特权（不休眠不删除，**判定唯一出处 `SeamlessMapGovernance.IsProtectedHome`**，消费点 = governor 保活分支）。
- 无 `sourceMap`/`IsPocketMap`/`pocketMaps` 语义。所有地块对等，通过直接邻居表维护关系。
- 历史的 PocketMap 路线（`PocketMapParent`/`sourceMap` 宿主机制）已废弃，VMF 调研结论中相关描述仅作历史参考。

### 直接邻居表（对称性的根基）
- `MapParent_SeamlessTile.neighbors`：`List<NeighborLink>`，每条含 `{worldTile(int), neighbor(MapParent), offset(IntVec3)}`。`worldTile` 是主键，`offset` 隐式编码方向（无 direction/edgeAngle 字段）。
- **偏移契约**：`offset = 邻居本地坐标 → 本地块坐标`的平移。即 `neighborLocal + offset = myLocal`。所有消费者（渲染/归属/边界带/传送点）严格遵守。
- 原生 parent 图（家园/原生家族 Settlement 等）的邻居表存于 `SeamlessTileManager.neighbors`（MapComponent）；地块地图存于 `MapParent_SeamlessTile.neighbors`。载体分支唯一出处 = `SeamlessMapData`（读/写/清理都经它），`SeamlessTileGraph`（静态）提供邻接查询入口（`GetAllNeighbors`/`TryGetNeighborLinkByWorldTile`/`AreNeighbors`，含休眠过滤）。**`IsAnchorMap` 已删除（2026-08，勿复引）**——"锚点"是口袋地图时代遗产名，其特权判定散落 8 处且口径漂移（家园"睡而不删"不对称的根因）；管辖/家园特权/可删判定唯一出处 = `SeamlessMapGovernance`（`IsGoverned`/`IsProtectedHome`/`CanRollingDelete`/`IsNativeFamily`）。
- `SeamlessNeighborRegistry`（静态）：邻居表登记工具（`RegisterNeighborBidirectional`/`SetNeighborOnMap`/`ComputeNeighborOffset`/`CleanupNeighborLinks`——已泛化到任意 MapParent）。

### 地图生命周期（软休眠滚动模型，已实现 2026-08；权威文档 = `doc/地图滚动休眠.md`）
- **软休眠（用户定夺 2026-08，勿回退为"卸 Map"方案）**：休眠图的 **Map 对象保留在 `Find.Maps` 不卸载**，照常随档全量序列化（存档零额外机制，建筑/物品完好）。休眠只意味三件事：不 tick（`Patches_Dormancy` 跳过 MapPreTick/MapPostTick/MapUpdate + `Find.TickManager.RemoveAllFromMap` 摘全局 Thing tick——原版 tick 表按 TickerType 全局分桶不按地图分组，只跳 Map 方法停不掉 Thing）；不作为邻接地图显示（`SeamlessTileGraph` 邻接/已加载口径过滤休眠：渲染零邻居清色帧、传送点 hasArrival 失效、跨图菜单关闭、撤离链 tier0 视对端"未加载"——NPC 不唤醒跟丢/原生离场，用户定夺防级联唤醒）；无地图访问入口（殖民者栏的地图分组框——原版 CheckRecacheEntries 每图一组、无 pawn 也画空分组框可点击切图，**必须 `Patch_ColonistBar_CheckRecacheEntries` Postfix 过滤**（重编 group 须用 Entry 构造器重建：reorderAction 闭包捕获构造时 group）；Sleep/Wake 收尾 `MarkColonistsDirty`；休眠图殖民者尸体 entry 一并隐藏，接受；世界地图"查看地图"gizmo 保留为显式唤醒入口。**可选空组过滤（2026-08 增，默认关 = 殖民者栏原生行为）**：PlaySettings 全局控制条 toggle `Patch_PlaySettings_ColonistBarToggle`（`Patches_PlaySettings.cs`，与接缝带开关同族；门控口 `HideEmptyColonistBarGroups.Enabled`）控制 `hideEmptyColonistBarGroups`——开启时同 patch 追加移除"非玩家家且无玩家 pawn"的活跃图整组（口径 = AllIncidentTargets 过滤②；玩家家含空家保留 = 原版"空家仍显示"语义；休眠过滤不受开关影响、常开）。toggle 前后比较字段值并 `MarkColonistsDirty` 立即生效（ToggleableIcon 走 ref 无回调）；图标缺失回落复用接缝带贴图，无 HasSeamEdge 门）。**唤醒 = 一切进图路径汇聚的 `Game.CurrentMap` setter Prefix 同步 Wake**（重注册 spawnedThings 的 tick，轻量无生成）；边界预加载带（CheckPawnGoto 休眠分支）与 governor 距离回落兜底。**休眠状态不序列化**：读档后全活跃（tick 注册由 FinalizeLoading 重 spawn 恢复），governor 首轮重新收敛。
- **距离策略（`SeamlessDormancyGovernor`，GameComponent 周期（默认 600 ticks、设置 `dormancySweepIntervalTicks` 可调，UI 秒 1-60、clamp ≥60，每轮现读拖动即时生效，2026-08 增）+ 事件触发即时）**：源 = 所有玩家阵营 pawn 所在图 **∪ 玩家阵营远行队所在 tile**（2026-08 修复"全员远行 → 源空 → BFS 距离表全空 → 全部管辖图按无穷远误删"的结构性炸弹——旧代码里 B 图靠 CurrentMap 恰好停在其上才幸存；用户原则"距离对全部 pawn 成立"，远行队里的 pawn 也在世界网格上占 tile；**空源守卫**：无任何玩家 pawn 的异常态本轮不睡不删），世界网格 BFS 拓扑距离、每轮现算无维护状态（源每刻在动，预算好的距离表写完即过期——用户提议的事件维护图结构已被此方案替代：维护状态会脏，实时查询不会）。**pawn 变动即时触发（2026-08；2026-08-29 收拢进统一底座）**：跨缝传送完成（`AfterTransfer`）/ 远行队组队离图（`MapParent.Notify_CaravanFormed` Postfix）/ 远行队进图（`Patch_CaravanEnterMap_Enter`）三类事件调 `SeamlessPawnLocationTracker.NotifyChanged()`（原 `RequestSweepSoon` 已删——影子远行队与地图休眠本质都是"pawn 在哪"的派生计算，共享同一触发系统与更新机制、只在派生逻辑处差异实现，见下 Settlement 条第六件；只置脏旗标，下一 GameComponentTick 位置刷新段执行——**勿在事件回调直接睡/删图/拆影子**，Sleep 摘全局 tick 表落在 thing tick 遍历中途不安全）；600 ticks 周期兜底（远行队逐 tile 移动靠周期采样）。保活三条件：CurrentMap 无条件 / 有玩家 pawn（仅玩家 pawn 保护，相邻图因玩家踏入自动距离 0，天然防"跨图躲追击"）/ **`IsProtectedHome`（= 原版 `Map.IsPlayerHome`，2026-08 用户定夺"不休眠不删除"——开局家园/定居/gravship 降落/引力引擎营地；特权判定全项目唯一行为消费点在此，删除分支无独立豁免）**。距离 ≥sleepHops 休眠、≥deleteHops 且 `CanRollingDelete`（受管辖且非家园）→ 删除（**距离策略定稿 2026-08-29**：sleepHops 下限收回 2（曾放宽到 1 后被用户否决——邻图预加载生成时玩家就在旁边，距离 1 恒达，1 会让刚生成的图立刻休眠甚至删除；UI 滑条与运行时 clamp 同步 2-8）；deleteHops 允许与 sleepHops 相等（= 休眠当轮即删），运行时 clamp ≥ sleepHops——语义"先满足休眠条件才可能被删除"，到达删除距离当轮直接删除无需先休眠一段时间，tooltip 含 2/3 示例；脚下安全仍由三条保活承担）。**管辖范围 = `SeamlessMapGovernance.IsGoverned`（地块图 ∪ 原生家族，见下条）**。设置：`dormancyEnabled`/`dormancySleepHops`(下限1，默认2)/`dormancyDeleteHops`(≥sleepHops，默认3)/`dormancySweepIntervalTicks`(默认600，UI 秒 1-60)（唤醒距离 = sleepHops 回落，无独立设置）。
- **活跃威胁保活 = 保活第四条（2026-09 用户定夺"图上有活跃敌人即忽略降速"；判定唯一出处 `SeamlessMapGovernance.HasActiveThreat`，消费 = governor 保活分支 + 追踪轮询）**：谓词 = 原版 `GenHostility.AnyHostileActiveThreatToPlayer(map)` 一行（走 attackTargetsCache 事件增量缓存非全图 pawn 遍历；官方先例 = 原版撤离门 CaravanExitMapUtility/CompShuttle；内置倒地/逃跑/休眠/雾中排除 + **虫激怒窄判**[LordJob_DefendAndExpandHive 且 duty 非 AssaultColony 不算——与原版威胁/撤退判定同语义，用户定夺勿复犯宽判]）+ 蹒跚怪补充 `mapPawns.SpawnedShamblers` 存活（动物版猎群 faction==null 走 GenHostility 的 shambler 专用分支判非敌对、进不了缓存桶；无 Anomaly 恒空天然安全）。**零排除分支（用户定夺 2026-09）**：驻军、炮塔一律不排除——语义始终是"活跃敌人"，一致性优先；已知后果 = 敌对据点/Site 图因常驻驻军恒保活（活跃圈内不降频、告警常驻），记录为定夺行为而非缺陷。机制形态 = **发现-清零双频段轮询（自愈式，勿改全事件触发；复查间隔亦勿与休眠扫描合并——2026-09 agent 曾误合并被用户撤销：复查平时零执行、有敌人时较高频跑以保证敌人行为正常回落，休眠扫描平时就要一直低频跑，节奏本质不同；设置 UI 两个滑条相邻摆放但机制独立）**：发现 = Sweep 每轮对非休眠未追踪图跑谓词（≤600t 延迟；已追踪图跳过谓词省成本；顺带自愈"袭击 incident 选中降频图、敌人冻在 0% 生成点"的半 bug）；清零 = GameComponentTick 独立轮询段（`threatKeepalivePollIntervalTicks` 默认 120 可配、名单空零成本）复查 `null/Disposed/IsDormant/!HasActiveThreat` → 注销（死亡/离图皆覆盖——轮询查实况而非事件）。**追踪中 = 保活第四条**（Sweep 262 行条件 + 原因链第四档）→ continue 豁免降频/休眠/删除全部下游（玩家跑远图不删，敌人清零后下轮 Sweep 正常睡/删；0% 图回落重新摘表走既有幂等路径）。**休眠图刻意不注册不唤醒**（休眠冻结防级联语义保留——袭击打不进休眠图[AllIncidentTargets 已剔除]，唯一残留 = 玩家撤离留追兵后图休眠，冻着属预期；玩家手动睡追踪中的图 → 轮询的 IsDormant 分支注销追踪让位手动锁，手动锁守卫挡保活 Wake）。追踪名单 = governor 实例字段 `HashSet<Map>`（随 Game 重建天然换档干净、不序列化）；告警 = `Alert_ThreatKeepalive`（AlertPriority.Medium = 枚举最低档"需要治疗"族；原版 AllLeafSubclasses 自动发现零注册；**刻意 AlertReport.Active 不做点击跳转**——跳图意味着切 CurrentMap 太激进；数据源 = governor 快照，每 24 帧重算零遍历）。设置 `threatKeepalivePollIntervalTicks`（默认 120、UI 秒 1-30、clamp ≥60）。心跳日志（Threat keepalive ON/ended）无条件——生命周期事件非 spam。无总开关（正确性行为，有需要后补）。
- **分级休眠中间档（2026-08，勿回退；权威文档 = `doc/地图滚动休眠.md` 2.13）**：三档 = 活跃（全速）→ **降频（`Source/Lifecycle/SeamlessTickThrottle.cs`）** → 休眠。降频接管"活跃圈内（< sleepHops）但无玩家 pawn"的空图（此前"醒着空转"的图），设置 `dormancyThrottlePercent`（0-100，默认 50；100 = 原生 = 关闭；0 = 永不放行 = "可见的凝固"，等价休眠但保留显示与 tick 注册——此时 Throttle 顺手结束该图 sustainer 防幽灵音）。**实现要点（勿改形态）**：①执行层跳 tick、注册表不动（区别于 Sleep 的 RemoveAllFromMap 全摘）——`Patch_Thing_DoTick_Throttle`（`Patches_TickThrottle.cs`）挂 `Thing.DoTick`（三桶 Normal/Rare/Long 唯一共同出口，单点覆盖，热路径集合空早退；**Rare/Long 桶按调用序号 `t/bucketInterval` 取模门控**——2026-08-27 两轮修正定稿：第一版对 TicksGame 取模与桶哈希 gcd 混叠（部分 thing 整个冻死），第二版整体豁免又被实测否决[0%+fastZone 0 下 calls 不降：野外图数万 Rare 桶植物 ~15k calls/window，违背"0%=全凝固"]；序号严格递增无混叠）；②MapPreTick/MapPostTick 与 thing tick **同相位谓词**放行（`(TicksGame + map.uniqueID) % N`，uniqueID stagger 防多图同拍尖峰；Pre/Post/ThingTick 不同拍会致 haulables/powerNet 与 thing 状态错位）；③`MapUpdate`（每帧）**照常放行**——tick 相位谓词不适用于每帧方法（会渲染抖动），降频图仍作为邻图可见；④**接缝快速区** `throttleSeamFastRadius`（默认 15，0 = 关）：距接缝边 ≤ 半径格内 thing 全速（`ComputeVoidBand` 同源几何，Throttle 时构建、Sweep 幂等重入时按半径变更重建）——跨缝战斗不吃降频；图级系统（powerNet/天气/野生生成）**不分区**（已知限制）；⑤Pawn 一并降频（用户定夺 2026-08——1.6 原版视野外降频 `GenTicks.GetCameraUpdateRate`=15 + `TickInterval(delta)` 是官方同族先例，但那只节流 TickInterval 部分、Tick() 主体照跑，整图降频仍需本层门控且与之无冲突[外层粒度 ≥ 内层，被跳 tick 不累计 delta]；语义 = 远处世界变慢而非省着跑：TicksGame 差值型逻辑速率不变、累加型慢 N 倍）。**即时解除**（不等下轮 Sweep，防玩家落图后 ~600 ticks 走走停停）：CurrentMap 切图 Prefix / 跨缝 AfterTransfer / 远行队进图 / `TryWakeByWorldTile` 降频分支；Sleep/Wake/Forget 联动清（三档互斥）；Sweep 的保活/`!enabled`/100% 分支全 Unthrottle。**⑥pawn 移动速率补偿（2026-08-27 实测"降频图动物变慢"修复，勿删）**：移动是纯累加器（`Pawn_PathFollower.nextCellCostLeft -= CostToPayThisTick()`，无 TicksGame 差值补偿——原版视野外降频不慢移动纯因它从不跳 Tick()，PatherTick 在每 tick 跑的 Pawn.Tick() 里）——修 = `Patch_PatherFollower_CostToPayThisTick_Throttle` 放行 tick 缴 N 倍成本（`MoveCostMultiplier`：快速区/非降频图恒 1 倍防 N 倍速），平均速率回满速；残余 = 背景渲染动画阶梯化。**⑦tick 剖析器（2026-08-27，`Source/Core/SeamlessTickProfiler.cs`，设置 `tickProfilingEnabled` 默认关 + `tickProfileIntervalTicks` 可调 60-3000 默认 600）**：周期逐图分桶（things 放行+skipped/pre/post/update[每帧]）+ 每 tick 墙钟（governor GameComponentTick 心跳——**勿 patch BurstCompile 的 DoSingleTick**，ECall 伪迹+Stance_Warmup 前科已实测踩过一次后撤回）。离线验证器 BOUND OK 实测 82（含本功能 DoTick/CostToPayThisTick 两 patch；FAILED 13 伪迹基线不变，游戏内绑定报告待复核；2026-08-31 河流 v3 +2 至 **84**：RiverTerrainAt/RiverBankTerrainAt void 跳过两 patch，GetDisplacedPoint 蜿蜒归零 Postfix 顶替旧 perp blend——净 +2；2026-09 离场链诊断 +2 至 **86**：CheckArrived 集结等待明细 + Pawn.ExitMap 终点确认两 patch，Patches_CaravanExitDiagnostics；2026-09-01 POI 手动休眠/删除 gizmo +1 至 **87**：MapParent.GetGizmos Postfix，SeamlessManualDormancyGizmos.cs；2026-09-02 天气域形态 B 重构 −3+4 至 **89**：删 Patches_WeatherCluster 幂等守卫/宿主跟随/ForcedWeather 并集三 patch，+ Patches_WeatherDomain 四 patch（WeatherDeciderTick 决策门控[Priority.First] / TransitionTo 广播 / DisableRainFor 转发[均 Priority.Last] / RegisterCondition 激活 Postfix）均 BOUND OK；SkyManagerUpdate 守卫本属伪迹 FAILED 基线，伪迹 13→12；2026-09-02 影子 DeSpawn 释放 +1 至 **90**：`Pawn.DeSpawn` Prefix 先释放影子成员（穿梭机登机消失案，`SeamlessShadowCaravan.cs`），BOUND OK；2026-09-05 袭击外缘生成 +4 至 **94**：IncidentWorker_Raid / IncidentWorker_AggressiveAnimals / IncidentWorker_GhoulAttack 三 worker 的 TryExecuteWorker + RaidStrategyWorker.MakeLords（`Patches_RaidOuterSpawn.cs`，见"跨图索敌与射击"节袭击外缘生成条），均 BOUND OK；2026-09-06 +2 至 **96**：`Sustainer.SustainerUpdate` 降频桥接 + `Sustainer.Maintain` Ended 静默（`Source/Lifecycle/Patches_SustainerMaintenance.cs`——**降频跳 tick 断 PerTick sustainer 维护契约**（SustainerUpdate 断供 1 tick 即超时 End），AncientVent_Ambient 死亡后 CompAncientVent 持死引用每 tick 红字 "Tried to maintain ended sustainer"（实测邻图降频 50% 排放中持续刷屏）；桥接 = 降频非 0% 图每帧推 lastMaintainTick（与"只有 0% 才 EndAllInMap 防幽灵音"的既有意图一致），Ended 静默 = 休眠/0% 死亡的 sustainer 被 comp 死引用 Maintain 时跳过红字（多图架构预期态，后果仅静默到 comp 侧重开）；两 patch 验证器 ECall 伪迹 FAILED[14=12+2]，游戏内绑定报告确认为准；**2026-09-06 勘误**："+2 至 96" 误把两个实入 FAILED 的 Sustainer patch 计进 OK——实测全源码 [HarmonyPatch] 类共 109、OK 94 + FAILED 14；同日地标选址修复 +1（`Patch_MapGenUtility_TryGetStructureRect`，BOUND OK）→ **OK 95 / FAILED 14（伪迹清单不变）**；2026-09-06 tick 剖析器 unaccounted 拆桶 +2 至 **OK 97 / FAILED 14**（`Patch_World_WorldTick_Profiler` / `Patch_GameComponentUtility_GameComponentTick_Profiler`，Patches_TickThrottle.cs，均 BOUND OK）；同日撤离首跳修复 +1 至 **OK 98 / FAILED 14**（`Patch_JobDriver_Goto_TryExitMap_EvacuationDefer`，挂 private `JobDriver_Goto.TryExitMap`，验证器实测 BOUND OK））。**⑧热路径收口 + 0% 凝固摘 thing tick 表（2026-09-06 玩家实测"6 邻图全 0% 降频仍比无邻图 +1~1.5ms/tick"定位修复，勿回退）**：实测判据 = 6 邻图全 0%（things 全 skipped、pre/post 全 0）时 unaccounted 从 ~1.9s 涨到 ~2.8-3.4s/606t——**0% 凝固图 thing 的 DoTick 前缀链空转**：DoTick 的 Harmony Prefix+Postfix 调度 + ShouldThingTick 判定对每个被跳过的 thing 照跑（6 图 ~620 次调用/tick × ~1.5µs；skipped 调用不计入 things 桶，全落 unaccounted）。修复两件：①**热路径收口**——设置（percent/fastRadius/N）由 governor `GameComponentTick` 每 tick 刷新一次进 static 缓存（`RefreshSettingsCache`，滑条即时性损失 ≤1 tick），`ShouldThingTick`/`MoveCostMultiplier` 从"每 thing 每 tick 两次设置读+Clamp + throttledMaps.Contains+fastRegions.TryGetValue 双查询"收敛为"单次 TryGetValue + static int 读"（两集合合并为单 `Dictionary<Map, MapThrottleState>`）；②**0% 摘表**——`Throttle` 进入 NeverTick 时把**快速区外**的 thing 逐个 `DeRegisterAllTickabilityFor` 摘出全局 tick 表（对"判定结果恒为跳过"的物体是纯等价变换、连空转判定都省掉；**快速区内 thing 刻意保留注册与判定**——0% 仍全速，跨缝战斗保护勿删，2026-09 首版全摘曾误伤快速区、实测 fastRadius 0 未暴露勿复犯；显示/邻接口径不动，仍走活跃图路径与 MapUpdate 每帧放行）。**摘/注册簿记三铁律（TickList.RegisterThing 无去重，双注册 = 活物双 tick）**：摘表来源唯一标记 `tickSuspendedMaps`；恢复注册唯一实现 `RestoreThingTicks`（**先 RemoveAllFromMap 整图归零再全注册 = 幂等**——0% 摘表只摘了区外、区内 thing 一直注册着，直接补注册会双注册；Wake 的重注册段收口于此，含影子陈旧条目的 `t.Map == map` 过滤兜底）；**Sleep 调 Unthrottle 必须传 `restoreTicks:false`**（图即将整表全摘，恢复无意义且会与 Wake 的重注册双注册）+ Unthrottle 恢复前查 `IsDormant`（防御路径：图已休眠则注册归 Wake 独责）。Throttle 幂等重入条件从"仅 radius 未变"扩为"radius 与摘表态均未变"（0%↔50% 设置变更经 Sweep 重入时先恢复注册再按新相位重建）。配套 ⑦ 剖析器升级：unaccounted 拆桶（world / gameComponents / other 三段——第三方 GameComponent 遍历全部地图的放大从此可归因）+ 状态标签 `throttled-frozen`（**该图快速区外 thing 的 calls 归零是预期——DoTick 不再被调用；fastRadius > 0 时残余 calls 全部来自快速区**，勿误读为门控失效）。残余待重测：当前图各桶普涨 15-40%（things 159→285ms 同 calls）疑 GC/CPU 缓存效应（7 图常驻），待摘表落地后重测归因。**手动绑定 patch 不计入此基线**（验证器只扫 [HarmonyPatch] 特性）：除既有 CellFinder×2/GenStep_Roads×3/TryFindRandomEdgeCellWith 族外，2026-09-03 + GL 河流适配三 patch（`Source/Compat/SeamlessGLRiverCompat.cs`：GetOrCreateTileLinkData Postfix + GeneratePostTerrain Prefix/Postfix，GL 在场才绑；2026-09-04 v9 Path 树钉位 +1：PathTracer.Trace Prefix（object path 参数绑定），`Source/Compat/GLRiverWarp.cs`）；2026-09-05 + VEF ObjectSpawns 格级过滤 1 patch（`Source/Compat/SeamlessVEFCompat.cs`：格级 CanSpawnAt Postfix（__0/__1 位置绑定），VEF 在场才绑——见"邻居预加载与异步加载"节 VEF 条）——游戏内权威确认 = 启动日志 "GL river compat: bound" 三行、"VEF compat: bound ObjectSpawns cell filter" 一行 + GetPatchedMethods 总数。
- **营地接入生成链（2026-08，勿回退，`Patches_CampTileMap`）**：原版 `Camp.ShouldRemoveMapNow` 地图无人即解散（`MapParent.TickInterval`→`CheckRemoveMapNow` 每 tick 检查，删 Map+WorldObject+原地留 AbandonedCamp）——"1 跳邻图上的营地被删"的真凶（与 governor 无关，日志无 Dormancy DELETE 行），根因是"设立营地"走原版 `GetOrGenerateMap` 建了原版 Camp parent。修复 = 整方法 Prefix 接管 `GetOrGenerateMapUtility.GetOrGenerateMap`（仅 `def == WorldObjectDefOf.Camp`——已查证 Camp 唯一创建路径就是玩家设立营地，任务/NPC 无创建者）：换 `RimExodus_SeamlessTileMap` parent，**worldTile 必须生成前设置（勿改）**——晚设则 389/392/1490 全被 GetMapWorldTile≥0 守卫跳过；尺寸用 `initialMapSize`（弃 Camp 的 overrideMapSize=200）；同步 `MapGenerator.GenerateMap`（Base_Player 链；SetupCamp 的 LongEventHandler 里同步生成本就是原版行为，GenerateMap 自带 static 重置）+ onComplete 子集集成（天气 BindMap / 传送点幂等补铺 / AutoConnectWorldNeighbors）。营地从此归滚动策略（≥2 眠 ≥3 删），不再自动解散、不留 AbandonedCamp、失去 Camp def 的 TimedDetectionRaids/Map_TempIncident（与地块图对等）；"立即重组远行队"的 `CheckRemoveMapNow` 变 no-op（基类默认 false）。防御放行（原版行为）：tile 已有图/已有 parent / `mapBeingGenerated != null`（防增量生成交错）/ def 取不到。
- **原生家族滚动接管（2026-08，勿回退）**：原版"全员离开即删图"族 = `ShouldRemoveMapNow` 的表面层覆写类 `Settlement`/`Site`（含埋伏 AmbushEdge/AmbushCenter、遗迹 AncientComplex 等一切 site part）/`Camp`（def 缺失回退）/`CaravansBattlefield`/`DestroyedSettlement`（第 6 个 `SpaceMapParent` 属空间层被 `IsNativeFamily` 的表面层守卫排除）。这些图**生成时就已被裁切**（4 个 genStep 由 `1.6/Patches/MapGeneration.xml` **全表面层 MapGeneratorDef 通配注入**（2026-08，原点名 Base_Player/Base_Faction/Encounter 三个；通配覆盖 EscapeShip/Archonexus 周期/AncientStockpile/InsectLair 等 + mod 自定义 def；**排除谓词勿删**：`[defName]` 存在性防命中 Abstract 基类 MapCommonBase 经 XML 继承双份注入，空间层家族 Space/Asteroid/AsteroidBasic/OrbitalItemStash/OrbitalRelay/Mechhive/SettlementPlatform 显式排除），Settlement 走 Base_Faction、Site 默认 Encounter，`GetMapWorldTile` 对原生 parent 回落 `map.Tile`）。接管两层：①`Patches_NativeMapFamily` 在 `MapParent.CheckRemoveMapNow`（public 非虚单一收口，覆盖全部子类覆写与"立即重组远行队"/`MoveColonyUtility` 直调；主动 `WorldObject.Destroy` 不经此路径不受影响）Prefix 拦截原生家族被动删除（`dormancyEnabled=false` 放行原版）；②governor 扩权管辖（≥2 眠 ≥3 删）。**删除语义 = 延迟执行原版偏好（用户定夺）**：`RemoveNativeFamilyMap` 删图前先跑未被 patch 的 `ShouldRemoveMapNow` 取 alsoRemoveWorldObject——Settlement 删图留对象（世界图据点仍在、再访重生成驻军 = 原版重访语义）、CaravansBattlefield/DestroyedSettlement 连对象删、Site 按 parts 语义（ConditionCauser/RaidSource 存活保留）；原版判定 false（建筑/pawn 阻挡）→ 本轮不删保持休眠下轮再评估（"只改删除时机、不改谁决定"）。**原生图接线钩子** = `SetupNativeParentMap`（MapGenerated 后延迟 1 tick，家园与家族统一）：天气 BindMap + 传送点幂等补铺 + **AutoConnectWorldNeighbors（2026-08 补的关键缺口：原生路径生成的图此前无人接线、无法跨缝互走）** + 刷新缓存 + **Settlement 贸易商补指定（2026-08，见下条）**；**worldTile 取值必须经 `GetMapWorldTile`、口袋图双点早退（MapGenerated + SetupNativeParentMap，2026-08 实测教训勿裸读 map.Tile）**：空间层图会把轨道 tileId 打进表面 WorldGrid（潜伏 bug 修复）；VMF 载具内部图（MapParent_Vehicle : PocketMapParent）的 genStep 链被 MapGeneration.xml 通配注入、SeamlessTileManager 组件随之挂上，而 VMF 的 `SetTile()` 会把内部图 `Parent.Tile` 同步成载具所在图 tile（如家园）——裸读会把"车的内部图"当该 tile 的原生家族图接线：车内铺传送点 + AutoConnect 把内部图登记为各邻图直接邻居并**覆盖对真家园的链接**（游戏内表现 = 邻图上不再显示家园、跨缝目标指向车内、读档后预加载去重误跳过邻接生成）。修 = 双点早退 + `MapComponentTick` 的 `HealPocketNeighborLinks` 旧档自愈（口袋链接重解析该 tile 真实图改指 / 无则删链 + 刷新传送点缓存，日志标志 `Healed neighbor link`）。原"开档级联预加载全部邻居"（preloadAllNeighborsOnStart）已于 2026-08 删除（调试功能被证明无用徒增维护负担；邻居生成本就事件驱动边界预加载）。
- **Settlement 无缝接入与据点贸易商（2026-08 已实现待游戏内回归，用户定夺勿回退）**：走近自动生成范围 = **一切表面层原生 parent 占位（全 POI 泛化，2026-08 用户定夺三层架构：普通地图[分帧增量] / POI 地图[原生单帧 GetOrGenerateMap，Settlement/Site/远古机械师建筑群/埋伏/战场等一切 MapParent 占位] / Settlement[POI + 额外处理]——勿回退为"仅 Settlement"，勿加 per-def/per-part patch：mod 可能 hook 原版地图生成管线上获取额外信息，走我们的管线会导致兼容问题；非表面层占位保持 skip 日志）**。玩家自己派系定居点同分支（Base_Player、不指定贸易商）。四件套：
  ①**生成** = `GenerateTileMap` 占位分支对原生 parent 调原生 `GetOrGenerateMapUtility.GetOrGenerateMap(tile, mapSize, null)`（复用既有 WO 不换 def——据点建筑/驻军/site parts/重访重生成全原版语义；**mapSize 必须用调用方的源图尺寸**——接缝六边形几何按 mapSize 计算；入口 mapBeingGenerated 守卫 + 分支内 IsAnyGenerating 兜底防与分帧增量生成交错；表面层守卫与 SetupNativeParentMap 同族），生成后 `EnsureNeighborRegistered`（源图↔POI 图；`is Settlement` 时再贸易商指定），其余接线由 SetupNativeParentMap 幂等补齐；`TryPreloadNeighbor` 忙时（增量生成进行中）**QueuePreload 重排队下一 tick 重试**（替代"警告+丢弃"，防玩家走近下令被吞；普通 tile 不变）。**方向否决（2026-08，勿重提）**："patch 原版 MapGenerator 让任意地图生成获得分帧能力"不可行——原版契约 = 同步调用同步返回完整 Map（`SettlementUtility.AttackNow` 等全部调用方立即解引用返回值），全局拦截必崩在调用方；opt-in 拦截（仅自家调用点）覆盖不了其他 mod/原版入口的生成，收益塌缩为 Settlement 迁分帧 + 少量代码去重，却给原生地图引入半生成可见/交错等不可控性。单方法 head-loop-tail 跨帧拆分仅有 transpiler / 异常展开（GenerateMap finally 的 ClearWorkingData 会销毁网格 statics）/ 提前跑 tail（空图 FinalizeInit 是毒药）三条路，均被项目纪律或清理语义否决。Settlement 维持原生同步生成（单帧冻结 = 原版行为的无加载屏版本；进度提示走"入队时点亮"方案——见 MapGenerationProgressUI 条）。
  ②**贸易商指定** = `SeamlessSettlementTrader.EnsureTraderAssigned`：门槛 `TraderKind != null`（baseTraderKinds 空的派系自然无）且非 permanentEnemy 且非 HostileTo（口径 = 原版 CaravanArrivalAction_Trade）；候选 = 据点图上该派系 spawned pawn 过滤用户 7 条件（Humanlike / `!WorkTagIsDisabled(WorkTags.Social)` + Talking CapableOf / !Downed + 意识、移动 ≥0.3 / `pawn.trader?.traderKind == null` / 非囚犯非奴隶 / 从任一传送圈格可达其位置（PassDoors+Danger.Some，可在室内）），**MarketValue 最高者优先逐个探测可达，首个可达者当选**；**仅生成时一次性（用户定夺）——死亡/倒地/被俘/敌对化不补选**，图删除重生成才重选；登记载体 = `SeamlessSettlementTraderComp`（MapComponent 随图序列化，读档不丢）；**不写 `pawn.trader`/`wantsToTradeWithColony` 原版字段**（用户否决行商化路线——防原生 provider 双入口与 wantsToTrade 的 AI 副作用）。
  ③**对话入口** = `FloatMenuOptionProvider_SettlementTalk`（继承即被 `FloatMenuMakerMap.Init` 的 AllSubclassesNonAbstract 扫描自动注册，零 patch）右键登记贸易商（同图或跨缝——点击重放天然支持；CanReach 走跨图真实答案）→ `RimExodus_TalkWithSettlementTrader` job（新 JobDef XML + `JobDriver` 克隆 TradeWithPawn 结构：Reserve→Goto Touch→开窗；跨缝由 StartPath 桥接通用包装）→ `SettlementTraderDialog.Open`（"远行者，你想要做什么？"）——原生 DiaOption 面板 `Dialog_NodeTreeWithFactionInfo`（= `ChoiceLetter.OpenLetter` 打开信件的同一渲染链、creepjoiner 同款；**用户定夺 2026-08 纯对话框不进信件堆栈、勿回退为自绘 Window**——自绘对 UI mod/字体缩放不兼容）。**选项来源（2026-08 影子远行队重构，用户定夺"据点图上的玩家 pawn 就是远行队——远行队能在据点上做啥操作，他们就能做啥操作（攻击等另生成地图的除外——**可贸易与可攻击可并存，双管线都必须显式过滤攻击**，2026-08-26 实测教训见下）"）**：①**影子远行队采集 `settlement.GetCaravanGizmos(shadow)` + `settlement.GetFloatMenuOptions(shadow)` 双管线**（`Source/Lifecycle/SeamlessShadowCaravan.cs`；2026-08 首版只采 gizmo 管线，用户实测金鸢尾兰缺售后查明其挂载点是 `Settlement.GetFloatMenuOptions` 的 Harmony Postfix（追加 `CaravanArrivalAction` 选项）——**mod 远行队交互两条管线都要采**）——原版远行队站在据点上的全部交互（交易 = `CaravanVisitUtility.TradeCommand`、谈判者 = 图上最优谈判者；送礼/满足贸易请求/mod 交互）。float 管线过滤"攻击"、"进入地图"与"访问"（**`StartsWith` 前缀匹配而非精确 `==`，2026-08-26 实测教训：原版 Dev 选项带 "(Dev: instantly)" 后缀、禁用项原因内联进 Label——精确匹配漏网进面板，点击走 AttackNow 连环 NRE**；键 = `"AttackSettlement".Translate(settlement.Label)` / `"EnterMap".Translate(...)` / `"VisitSettlement".Translate(...)`——访问与进入同理：pawn 已在图上，对影子执行 Arrived 会二次入场/触发进入信件，2026-08-26 用户定夺友方据点"访问"一律不进面板；排除另生成地图的交互）+ 双管线标签去重（交易两管线各出一条）。**贸易回退条件 = "采集命令中无交易命令"（`defaultLabel == "CommandTrade".Translate()`）而非旧"零命令"（2026-08-26 水仙花实测：`SettlementVisitedNow` 要求 `settlement.Visitable`，OA 居住点 false → 原版 TradeCommand 不产生，但 gizmo 仍采到"外交交互"等其它命令 → 旧条件不触发 → 面板完全没有交易项）；原版交易命令存在时优先原版**。**gizmo 管线同样过滤攻击（2026-08-26 实测教训勿删）：`Settlement.GetCaravanGizmos` 的攻击命令 label 键 = `"CommandAttackSettlement".Translate()`（与 float 管线的 AttackSettlement 不同键）——旧假设"可贸易据点 Attackable 恒 false 天然不出现"被 OA 水仙花居住点（可贸易且可攻击并存）实测否决；玩家从面板点攻击 = `SettlementUtility.AttackNow(shadow) → CaravanEnterMapUtility.Enter(shadow)` 对图上 spawned 成员二次 Spawn（"already spawned" 红字）+ Enter 的远行队侧簿记破坏 pawn 归属 → 后续战斗 TakeDamage/map-null NRE 连环。第二道兜底 = `Patch_CaravanEnterMap_Enter` Prefix 对影子整方法跳过（任何把影子塞进 Enter 的路径都拦，含未知 mod）。****arrival action 的执行 = pather.StartPath(目标=当前 tile) → AtDestinationPosition 同步 PatherArrived → Arrived()**（Caravan_PathFollower.cs:168-171）。**同 tile 真实玩家远行队不冲突不跳过（2026-08 用户定夺，原生支持同 tile 多远行队——首版守卫被实测否决）**。影子远行队 **v2 = 常驻实例**（2026-08 用户方案，替代 v1"同帧借用"——金鸢尾兰"外交交互"实测暴露 v1 结构性缺陷：其 Command_Action 只开 FloatMenu，真正逻辑 `Notify_CaravanArrived → FindBestDiplomat` 在玩家点浮菜单的**后续帧**闭包里跑，v1 借用窗口已归还 → 空列表 → "无外交官"；常驻模型让 mod 无论何时[即时/延迟回调/跨帧闭包]读 caravan 都查得到成员）：`Source/Lifecycle/SeamlessShadowCaravan.cs` 维护 tile→Caravan 注册表——每个"有玩家自由殖民者的据点图"（原生 Settlement parent + 表面层）一个常驻影子，成员 = `FreeColonistsSpawned` 按引用注入，governor `GameComponentTick` 每 60 ticks 同步名单（进/离图/死亡自动跟随；读档后首轮自动重建——**影子不序列化**，存档前 SaveGame Prefix 调 `EnsureReleasedForSave` 全量拆除，否则成员被 `Caravan.ExposeData` 的 Scribe_Deep 双存成远行队成员）。三条硬约束（反编译确证勿违反）：①**不能用 `Caravan.AddPawn`**（内部 `DeSpawnOrDeselect` 脱 spawn）也**不能用 `Caravan.pawns.TryAdd`**（`ThingOwner.TryAdd` 检查 holdingOwner，图上 pawn 的 owner 是 Map 容器，直接拒绝[实测红字 "already in another container"]）——正解 = 反射 `ThingOwner<Pawn>.innerList` 直写内部列表[字段在泛型类上，反射基类恒 null——实测教训] + 把 pawn.holdingOwner 改指影子容器（`ThingOwner.Contains(Thing)` 的判定就是 `item.holdingOwner == this`（ThingOwner.cs:619），`Caravan.IsOwner` 的 pawns.Contains 全走它——不转移则影子成员恒 "not owner"[谈判者/外交官全灭的实测根因]；pawn 实体与地图注册全程不动）；②**影子不进 `Find.WorldObjects`（2026-08-26 方案 B，见下"世界图隐藏"条）**——零 tick/零序列化/世界图与殖民者栏天然不可见，原 TickInterval 抑制 patch 已删除；③**`CaravanUtility.GetCaravan`（扩展方法，定义类上 patch）Postfix 对影子返回 null**——holdingOwner 指影子后原版 ~40 处 GetCaravan 消费面（心情/娱乐/殖民者栏/Dialog_Trade/Patches_SettlementTrade 守卫…）会误判"在远行队"，返回 null 还原地图语义；`Caravan.IsOwner` 走 pawns.Contains（holdingOwner 比对）不经 GetCaravan，FindBestDiplomat 不受影响。配套：`Caravan.GetGizmos` Postfix 对影子清空（防御性兜底）；`Storyteller.AllIncidentTargets` Postfix 剔除影子（Patches_Dormancy，冗余安全网）；`Patches_SettlementTrade` 的 GetCaravan 守卫因 ③ 自动正确（影子成员恒走地图分支）。**第四 patch（2026-08-26，勿删）：`ThingOwnerUtility.GetRootMap` Prefix——影子成员 inventory 物品的 MapHeld 还原**：holdingOwner 指影子后其持有链（inventory→pawn→Caravan→世界）不含 Map → `Thing.AmbientTemperature`（`SpawnedOrAnyParentSpawned → GetTemperatureForCell(PositionHeld, MapHeld)`）每 TickRare 刷 "Got temperature for null map"（奴役场景实测暴露，实为**一切带腐烂食物的影子成员即触发**，与奴役无因果）；修 = 持有链遇影子 Caravan 返回其据点图（shadowMaps 伴生注册表，Refresh 写/Dismantle 清），掉落/腐烂/心情等一切 MapHeld 消费面一并还原；影子不在链上（绝大多数调用）首步放行原方法。配套加固：成员移除/拆除时归还 holdingOwner，pawn 仍 Spawned 则直指 `p.Map.spawnedThings`（快照不可信——加入与归还之间容器可能已变），未 spawn 才还快照。**第五 patch（2026-08-27，勿删）：`Caravan.AddPawn` Prefix 先释放影子成员（`ReleaseIfShadowMember`，__0 位置参数绑定）**——玩家在据点图上真组队（`LordJob_FormAndSendCaravan.SendCaravan → ExitMapAndCreateCaravan → MakeCaravan → AddPawn`）时，成员 holdingOwner 还指着影子容器（60 ticks 名单同步赶不上组队瞬间），AddPawn 的 DeSpawnOrDeselect 只清地图容器 → `pawns.TryAdd` "already in another container" 拒绝、pawn 进不了新远行队（实测红字 "Tried to add Rowan..."+"Couldn't add pawn"）；Prefix 释放后 pawn 归还地图容器、AddPawn 走完整原版路径，影子本体由下轮 Refresh 按"图上无玩家 pawn"自然拆除。**第六件（2026-08-29 持有链审计根治 + 统一 pawn 所在地追踪底座收拢，用户定夺）：注入期 DeSpawn 的 spawnedThings 陈旧条目三下游（两名玩家报告"pawn 同时在两个地点"的定案根因，零 Error 日志、读档自愈故难复现）**：holdingOwner 指影子期间 pawn 在据点图上任何 DeSpawn（跨缝走出/原版 SplitOff 抱起/死亡）中 `map.spawnedThings.Remove` 因 `ThingOwner.Contains` 指针判定（`item.holdingOwner == this`）**静默失败**，innerList 残留正常 API 删不掉的陈旧条目——下游三害：①休眠 Wake 按 innerList 重注册 tick（`TickList.RegisterThing` 无去重，Sleep 的 RemoveAllFromMap 按 Map 过滤摘不掉）→ 活人双 tick（需求/衰老 2 倍速）；②删图时 MapDeiniter 对后续图逐条 `DecrementMapIndex` 补偿 Find.Maps 索引位移，陈旧条目给活人多减一次 → pawn.Map 指向错图（"被计数在另一个 tile"：点殖民者栏头像相机跳错图、下令失灵；玩家自救"重组远行队走到该 tile 进图"= 完整 ExitMap→Enter 登记重写，与之精确吻合）；③被删图 `NotifyEverythingWhichUsesMapReference` 持有链遍历命中陈旧条目 → 活人被 `Notify_MyMapRemoved` 置 Discarded(-3)（鬼影/雕像）。**修复 = 影子远行队与地图休眠收拢到统一 pawn 所在地追踪底座 `SeamlessPawnLocationTracker`（用户定夺："两者本质都是对 pawn 所在地变动的追踪，共享同一个触发系统、同一个底层更新机制，只在真正差异处分别实现"）**：①触发系统 = 事件源全集（跨缝传送/组队完成/远行队进图 → `NotifyChanged()`；删图 Forget → `NotifyMapRemoving(map)`）只认底座入口；②更新机制 = governor GameComponentTick 内固定顺序"位置刷新段"——影子 `Maintain(dirty)` 在前（事件即时名单同步 / 60t 轮询额外清扫注入副作用），休眠 Sweep 在后（事件即时或自持间隔——睡/删决策永远看到影子同步后的名单与清扫后的列表）；③差异实现 = 影子侧清扫（判据 `t==null || (t.Map != map && t.holdingOwner != map.spawnedThings)`，60t 轮询清残余源[SplitOff 抱起/死亡，不 patch 无法事前拦截] + `OnMapRemoving` 删前清扫[一切删图路径经 Forget，使 DecrementMapIndex/持有链遍历永跑干净列表]；清除时 Message 兼野外诊断器——报告玩家看到 `Scrubbed N stale spawnedThings ...` 即坐实）+ Wake 重注册按 `t.Map == map` 过滤兜底；休眠侧 BFS 距离逻辑不变。传送流程对影子零感知（V1 的 DeSpawn 前内联释放已删，纯事件驱动）。**勿复犯**：不可把活成员移出 innerList（"对称注入/释放"被否决——DecrementMapIndex 索引补偿遍历的就是各图 innerList，移走 = 漏补偿反向损坏）；不可 patch MapDeiniter；事件回调只置旗标（Sleep/拆影子不在 thing tick 中途跑）。残余观察项（接受）：原版直删图（gravship PostRemove 等不经 Forget 的路径）落在 60t 清扫窗内的理论暴露。**第七 patch（2026-09-02 穿梭机登机消失案，勿删）：`Pawn.DeSpawn` Prefix 先释放影子成员（`ReleaseIfShadowMember(__instance)`）——影子成员一切"离图进容器"路径的统一闸门，第五 patch(AddPawn) 的同族推广**：原版不变量 = pawn DeSpawn 前 holdingOwner 必须是本图 spawnedThings 或 null（原版 `Thing.SplitOff`/`Pawn.Kill` 的"DeSpawn 后 `holdingOwner?.Remove(this)`"先例）；影子注入期 DeSpawn 的 `map.spawnedThings.Remove` 静默失败后 holdingOwner 悬指影子 → 后续任何容器 TryAdd 全被 "already in another container" 拒绝——`JobDriver_EnterTransporter`（穿梭机登机：toil 先 `DeSpawnOrDeselect()` 再 `GetDirectlyHeldThings().TryAdd(pawn)`，**TryAdd 返回值被原版忽略** = pawn 离图又进不了舱，实测"登机即消失"、存读档即永久丢失——玩家报告 Liara 上 PassengerShuttle 即此案）、`JobDriver_EnterCryptosleepCasket`（TryAcceptThing 同款）、`JobDriver_EnterPortal`（DeSpawn→Spawn 新图，Spawn 侧 TryAdd 同要求持有者为空）。Prefix 时 pawn 仍 Spawned → 释放归还本图容器，DeSpawn 本体 Remove 正常成功并清空 holdingOwner，后续一切容器转移恢复原版；跨缝走出/SplitOff 抱起/死亡路径的注入期 DeSpawn 陈旧条目（第六件三下游）由此**事前不发生**，60t 清扫降级纯兜底（保留勿删）。与第五 patch 幂等互备（组队链 AddPawn 内部的 DeSpawnOrDeselect 亦经此闸，第五 patch 保留为防御性双保险）。纯容器簿记、thing tick 中途安全（不拆影子——拆除由下轮 Refresh 在 GameComponentTick 评估）。配套孤儿容器兜底 `ReleaseIfOrphanShadowHolder`（现役匹配失败且 holdingOwner 指向"不在世界对象表、非现役影子"的 Caravan 容器 → 释放归还 + Warning 探测器；真远行队在册不碰）与 SyncMembership 注入/移除日志（Settlement 模块，名单变化时各一行）。**复现案定案（2026-09-02，勿再查）**："交易后再登穿梭机复现" = 测试误用 release 旧二进制（游戏启用的是 Workshop 版而非本地开发版），新 DLL 下基本场景与交易场景均正常——**测试前必须确认游戏实际启用的 mod 实例/加载的 DLL 是刚编译的版本**（启动日志 GetPatchedMethods 绑定总数比对，与 PS compat rev 标记纪律同族，这次轮到作者本人踩）。**世界图隐藏与不可选中（2026-08-26 方案 B 定稿：影子不 `Find.WorldObjects.Add`，勿回退为 Add+逐面 patch）**：原"Add 进世界表 + patch 隐藏"被实测否决——`Find.WorldObjects.Caravans` 的枚举面远不止绘制/选中：殖民者栏组框（`ColonistBar.CheckRecacheEntries` 遍历 Caravans 把 `PawnsListForReading` 整组登栏 → 影子成员被**第二次**显示成独立殖民者组合）、框选（`SelectInsideDragBox` 经殖民者栏条目 `CaravanMembersCaravansInScreenRect` 选中影子，与鼠标点击是两条路）、闲站告警、**远行队合并（同 tile 真实远行队可被合并进影子——逻辑风险）**……逐面打地鼠不可收敛。方案 B = 影子创建后**不 Add 进世界对象表**（Spawned 恒 false）：不 tick（原 `Caravan.TickInterval` 抑制 patch 删除）、不序列化、上述枚举面全部天然不可见（上轮的 3 个隐藏/选中 patch 一并删除）。**交互链不依赖 Spawned（反编译确证）**：成员注入/IsOwner/FindBestDiplomat 走 holdingOwner；面板选项执行 = `pather.StartPath(同 tile) → AtDestinationPosition → PatherArrived → Arrived()` 全链无 Spawned 检查（`Moving` 对未 spawn 恒 false 恰好使 AtDestinationPosition 立即判"已到达"）。**代价（接受）**：`SettlementVisitedNow` 开头 `!caravan.Spawned → null` 恒成立 → 原版 TradeCommand 永不产生（由 TraderDialog"无交易命令则回退自建交易项"覆盖）；未知 mod 若检查 `caravan.Spawned` 可能悄悄少给选项（金鸢尾兰 FindBestDiplomat 路径已确认不查，观察项）。配套 `Storyteller.AllIncidentTargets` 剔除影子的分支成为冗余安全网（影子不在事件目标枚举里），保留无害。同 tile 真实玩家远行队不冲突不跳过（原生多远行队合法）。影子失败（图上无玩家 pawn / parent 非 Settlement）→ 回退旧面板（自建交易项，`TradeAcceptanceReport` 口径 = 原版 CaravanArrivalAction_Trade.CanTradeWith，开 `Dialog_Trade(negotiator, settlement)`——Settlement 本体即 ITrader，库存/掉落由 ④ 的图上 patch 接住）零回归。观察项：世界图 tile 据点+影子双图标、远行队 Alert（Alert_CaravanIdle 等）可能对影子产生条目；mod 序列化影子引用（信件 lookTargets）读档后变 null；金鸢尾兰"外交争锋"对影子执行 CreateFixedCaravan（转驻留多日 FixedCaravan）不拦截（用户定夺 2026-08：无法通用判定选项是否会转 fixed，出问题再单独适配）。②**据点 gizmo 集合** = `settlement.GetGizmos()` 枚举转 DiaOption（mod 经 WorldObjectComp 加的据点交互天然出现；过滤"查看地图"（label 匹配，ShowMapCommand 是 private 不能比 icon）与"组建远行队"教学项（icon == `Settlement.FormCaravanCommand`））；离开（面板无 X/Esc，退出只能点选项——原版 node tree 语义）。已知边界（观察项）：mod gizmo 点击后延迟读 `caravan.Spawned/Tile`（确认回调里再查 SettlementVisitedNow）会拿到 false——原版无此模式（TradeRequestComp.Fulfill 纯库存操作安全）。
  ④**地图内交易 3 patch**（`Patches_SettlementTrade`，统一守卫 **`playerNegotiator.GetCaravan() != null` → 放行原版 = 远行队交易零回归**）：`Settlement_TraderTracker.ColonyThingsWillingToBuy` Prefix（原版体开头解引用远行队，地图谈判者必 NRE——改为枚举谈判者所在图全部玩家阵营 pawn 的 `inventory.innerContainer`，随身白银即支付来源）；`GiveSoldThingToPlayer` Prefix（物品 `GenPlace.TryPlaceThing` 掉落**贸易商身边**（注册表取，缺失回落谈判者）；买入 pawn 落回地图，`PreTraded(PlayerBuys)` 已做派系转换）；`TradeDeal.InSellablePosition`（private）Prefix（随身物非 Spawned 被原版判不可卖——`t.ParentHolder is Pawn_InventoryTracker && tracker.pawn.Faction == player` 放行；**out 参数在 Prefix 以 ref 声明，return false 前必须赋值**）。`GiveSoldThingToTrader` 不 patch：物品分支不碰远行队（只有卖 pawn 分支解引用 caravan），卖入 stock 走原版 stock.TryAdd——**v1 只支持随身物品交易，随行囚犯/奴隶出售为后续扩展**。
  ⑤**败亡误结算与善后（2026-08 游戏内两轮实测定稿，`Patches_NativeMapFamily`，勿删勿加码）**：原版 `IsDefeated` 判据 = "图上无对玩家的**活跃威胁**"——隐含假设"据点图只在进攻期间存在"。**中立**据点图（RimExodus 预加载生成）驻军非敌对 → 判据恒真 → 生成后、或玩家跨缝走进的**当 tick**被误走进攻胜利结算（DestroyedSettlement 顶替 + Settlement WO 销毁 + 该派系敌人 +20 好感 + 可能 faction.defeated + CaravanAssaultSuccessful 传说从空殖民者集随机取人 NRE；败亡还会偷换 `map.info.parent` 给废墟 WO → 贸易商查找 `map.Parent as Settlement` 落空 → 对话选项整体消失只剩原版拘捕——第二轮实测"解雾后仍不可对话"的真凶；`Settlement.IncidentTargetTags` 亦被误真打 Map_PlayerHome 标签招事件）。**关键事实**：败亡结算先偷换 parent 再 Destroy——`MapParent.Map` 按 info.parent 实时查找（`Game.FindMap`），PostRemove 的"持图连删"不触发，图存活易主为废墟 WO（其删除走 CheckRemoveMapNow 被原生家族拦截归滚动休眠）——"瞬间摧毁"= WO 销毁 + 邻图反向链指向死 WO（渲染断链/跨缝断/spot 缓存陈旧）。**定稿修复 = `CheckDefeated` 单点 Prefix：派系非敌对（含 null/玩家派系）→ 跳过；敌对 → 原版照走**（原版进攻入口本就要求敌对）。**配套传说记录空参数守卫（2026-08，`Patch_TaleRecorder_RecordTale_NullArgGuard`）**：敌对据点被**无人进场**击溃（陷阱/跨缝 turret——玩家 pawn 从未踏上据点图）时原版末行 `RecordTale(CaravanAssaultSuccessful, FreeColonists.RandomElement())` 从空集取人得 null → `TaleData_Pawn.GenerateFrom` NRE 红字（TaleFactory 自行 catch 不崩但每 tick 刷）；修复 = Prefix 挂 `TaleRecorder.RecordTale` 本体，参数含 null 即跳过——原版语义里 null 参数的唯一去向就是"Failed to create tale"红字 + 什么都不记录，跳过对一切正常调用（含第三方）零行为变化。**刻意不用 Transpiler 重定向 CheckDefeated 内调用点（首版方案已替换，用户纪律"能 Prefix/Postfix 就不 Transpiler"）**。**历史教训勿回退**：两版守卫均被"玩家进场瞬间"穿透——①CheckDefeated 的"无玩家在场"单条件；②挂 `IsDefeated`（public static）的"无玩家在场"（且越权改 public static 语义影响第三方调用方）。同 patch 保留 **Postfix 重接**：真败亡（敌对据点被打下）后以 `factionBase.Destroyed` 为门，扫全部图邻居表把指向死 WO 的 link 就地改指废墟 parent（同 tile 同 offset）+ 两端刷新传送点缓存（否则渲染断链、且 governor 删废墟图时 CleanupNeighborLinks 匹配不到死 WO 反链清不掉）。配套两件：`Settlement.PostMapGenerate` Postfix 以 `GeneratingNativeSeamlessly` 生成标志（预加载链 try/finally 维护）跳过 TimedDetectionRaids 报复倒计时（def 自带组件对任何据点图生成都启动 4 游戏日倒计时——中立图会 1 天后收无端威胁信、4 天后被刷敌对袭击）；`Storyteller.AllIncidentTargets` Postfix 扩展（Patches_Dormancy）剔除"无玩家阵营 pawn 在场的非玩家家活跃图"（废墟图无条件带 Map_PlayerHome 标签——原版给拾荒玩家设计；玩家家含空家保留 = 原版"空家可被袭"语义）。敌对据点预加载生成本就安全（驻军是活跃威胁）。**配套三件：非敌对驻军 lord 不进"进攻殖民者"分支（2026-08，`Patch_LordJob_DefendBase_NoFriendlyAssault` + `Patch_LordJob_SitePawns_NoFriendlyAssault`，Postfix 挂两个 LordJob.CreateGraph）**——`LordJob_DefendBase` 的 DefendBase→AssaultColony 转换触发器**全部无敌对门控**（含 `Trigger_TicksPassed(25000)` 确定性必发 + `Trigger_ChanceOnTickInterval(2500,0.03)` 随机），触发时发"来自X的Y正在攻击你的殖民者"（ThreatBig 消息，faction.def.messageDefendersAttacking）——原版此 lord 只在进攻期间短暂存在掩盖了无门控，友方据点图常驻 tick 后图龄超时误发威胁消息（友好无实际攻击目标只弹消息）；修复 = 派系非敌对（含 null/玩家）时移除**不含** `Trigger_BecamePlayerEnemy` 的进攻型转换（target 为 LordToil_AssaultColony/LordToil_HuntDownColonists）——保留敌对化升级路径零语义损失，敌对派系图零改动；faction 私有字段经 `AccessTools.FieldRefAccess` 读。原版全部删图路径共 5 条（DeinitAndRemoveMap 全调用方）：CheckRemoveMapNow 族（已拦截）/ `MapParent.PostRemove`（任何 `WorldObject.Destroy` 对持图 parent 连带删图——任务结算/gravship Abandon 等**主动销毁按设计不拦**，死链善后靠"走近自愈"= BorderPreloader→EnsureNeighborRegistered 重登记，观察项）/ 迁都结局 / 口袋图 / Dev 工具。
  ⑥**贸易商问号标记与接缝方向揭雾（2026-08，用户定夺"参考原生 Creepjoiner 机制、勿自绘图标"+"从生成方向算无fog范围"）**：`Pawn.ShouldShowQuestionMark`（public virtual，仅 PawnUIOverlay 一处消费）的 fallback 分支 `return CanTradeNow` 就是原版访客商人问号的来源（Creepjoiner 标记走同函数的 Anomaly 分支）——`Patches_SettlementTrade` 的 Postfix 对注册贸易商返回 true，整条原生绘制链（QuestionMark 脉冲材质/altitude/雾裁剪）零成本复用；败亡后 TryGetSettlement 落空问号自动消失。揭雾 = **`Patch_GenStep_Fog_SeamOrigin`**（Prefix 挂 `GenStep_Fog.Generate` 本体，2026-08 用户定夺"不自搓第二套清除、不与中心揭雾并存——新生成地图的 fog 应该且只应该从接缝处 unfog"，首版自建 1505 genStep 已删勿回退）：**分径**——初始地图（游戏开始/扎营/定居/gravship 降落等原生入口，判定 = 非 `IncrementalMapGenerator.IsGenerating(map)` 且非 `GeneratingNativeSeamlessly`）放行原版体（PlayerStartSpot 中心 FloodUnfog）；**邻接生成图**（预加载链）接管：全雾（`Refog(WholeMap)`，SetAllFogged 是 internal）→ 全图 void 格直接揭雾（void 无内容非探索对象，雾留 void 唯一效果是跨缝看邻图被 fog mesh 挡视线）→ 从**生成方向**共享边（`SeamlessTileManager.NeighborGenerationSourceTile`，GenerateTileMap 两路径设置/onComplete+finally 复位；异常悬挂容错 = 校验非本图世界邻居时回退全部活跃边）的可站立接缝格发起 `FloodFillerFog.FloodUnfog`——洪水揭穿整个室外连通域、停在围墙房间（室内留雾待探索）；rootsToUnfog 镜像原版处理（防御）。**多根化（2026-08，勿回退为单最优根）**：对源边**全部 Standable 传送点逐个发根**——每个室外连通域需要至少一个洪水根，山体延伸到接缝带把源边室外区分割成多段"走廊"时单最优根只揭一段、其余段即使从接缝可直接走入也留雾；同连通域的后续洪水因 PassCheck 要求 fogged 立即空转天然去重、开销可忽略。源边收集到 0 个可站 spot（spots 全被岩石/地形挡住——铺设刻意无通行性过滤）或 source 校验失败时，降级**全部活跃边**（休眠过滤口径）再收集一轮发根，防整图中间全留雾。原版 FloodUnfog 口径继承不改：洪水只被实心 edifice（`MakeFog` = Fillage Full）阻挡，**深水/关着的门挡不住洪水、且为 4 邻遍历**（只留对角缝的墙缝也挡）——被水围住的谷地会被揭雾、对角连通的谷地会留雾，与 pawn 可达性不一致属原版语义，未偏离。配套：**分帧生成进度提示 `MapGenerationProgressUI`**（GameComponentOnGUI 左上角"Generating map... (步数/步骤)"，读 `IncrementalMapGenerator.GenerationProgressDescription`，2026-08 用户需求；2026-08-31 扩展覆盖同步重操作——快照精简重生成/原版同步生成逃生通道/POI 原生生成都是单帧冻结、冻结帧内 OnGUI 无从重绘，故标签在 `SeamlessTilePreloader.QueuePreload` 入队时点亮（保证重操作前至少一帧带标签画面渲染出来，冻结期间屏幕保持的正是它），三个同步操作 try/finally 经 BeginSyncOp/EndSyncOp 换具体文案（"重建地形快照"/"原版管线生成"）；显示优先级 = ActiveSyncOpLabel > 分帧步数 > 入队占位；熄灭判据 = 队列空 && 无分帧生成 && 无同步操作[GameComponentTick]，EndSyncOp 必须放 finally 顶层勿进局部 try/catch[悬挂 = 标签永驻]；**入队帧门（SeamlessTilePreloader.minConsumeFrame，2026-08-31 实测教训勿删）**：入队与消费可能落在同一渲染帧（同 tick 的 MapPostTick 消费 / 高速档一帧多 tick / 点击在 OnGUI 事件段入队且该帧 Repaint 已过），标签没渲染过就冻结 = 玩家仍看到无标签画面直接卡住——ConsumeQueued 在上次入队帧 +2 前不消费[+2 覆盖"OnGUI 事件段入队、首绘在下一帧"的最晚情形]，代价 ≤2 帧延迟）。Creepjoiner 调研结论（复用边界）：问号 overlay（ShouldShowQuestionMark/OverlayDrawer）与"浮窗→Goto Touch→开面板"交互骨架是通用基础设施可复用；`Pawn_CreepJoinerTracker`/QuestPart/LordJob 三件是深度耦合 kindDef+quest 信号的专属状态机，抄模式不抄实例；creepjoiner 的"对话"本体 = `ChoiceLetter_AcceptCreepJoiner`（信件系统 DiaOption 面板，标准信件 UI 非自绘）——"远行者"面板已转同款基础设施（`SettlementTraderDialog` → `Dialog_NodeTreeWithFactionInfo` 纯对话框，用户定夺 2026-08，勿回退自绘 Window）。
- **删除 = `RemoveRollingMap` 统一入口**（governor 与 Dev Force Delete 共用）：地块图 → `RemoveTileMap`（销毁 Map + WorldObject = "从未出现过"，下次进入走生成链重建）；原生家族 → `RemoveNativeFamilyMap`（延迟原版偏好，见上条）。两者开头都 `SeamlessDormancyManager.Forget` 防集合持已 Dispose Map 引用，结尾 `RebindAll` 天气域重算。图量 ≤ 以玩家为球心的 deleteHops 跳球（默认 ≤37 图），`Game.AddMap` 127 上限（sbyte）的实际压力由此解除。
- **2026-09-08 封存参考语义覆盖说明**：下条前哨保留机制中的居住区记录、恢复顺序与删除入口仍有效；其中“封存 WorldObject 的条带可供邻图参考”是已废弃历史细节。封存状态只保留 `ZoneMapRecord`，因为没有 Map，既不参与接缝混合也不提供 void 岩参考；恢复时先重生成自然地图、与届时实际加载邻图混合，再重放居住区。
- **前哨保留：封存/重放（2026-09 落地，权威文档 = `doc/地图滚动休眠.md` 2.15；判定语义唯一出处 = `Source/Lifecycle/SeamlessMapModificationTracker.cs`，与删除策略解耦——用户定夺"语义拎出来"）**：有居住区（Home area 字面、**零膨胀**——用户定夺"用且仅用居住区"，画居住区即画保留区；采矿不扩 home，未铺地板隧道重放复塞属预期）的**地块图**（v1 范围，原生家族不纳入）到删除距离时不删而封存：`ZoneMapRecord.Capture`（四层稀疏——建筑[含自然岩/遗迹墙，带 hp/stuff/faction loadID/quality]、屋顶、地板[**仅 Floor/基架不录土壤**——"自然层相信重生成"]、物品；**comp 级状态明确不保真**；零 pawn——Deinit 善后与删除一致进世界池，无重复实体问题）→ 挂 `MapParent_SeamlessTile.preserveRecord` → `DeinitAndRemoveMap` 拆图但 **WO 刻意保留**（与 RemoveTileMap 三差异勿"顺手对齐"：不销毁 WO/不 CleanupNeighborLinks/不 ReleaseTileMesh——邻居链跨档有效 + 封存期间邻图生成经 `TryGetNeighborSeamStrip` 的 WO 回落读条带 + 世界图第四态）。恢复 = 生成守卫识别"无图有记录"复用 WO 走生成链（**tileOrigin 不重算**——恢复触发方向可能与原生成方向不同）+ `GenStep_ZoneRestore`（**order 1100**，用户定夺"生成建筑的 mutator/landmark genStep 之后、pawn 与动物生成之前"——身前覆盖全部常规建筑源[Settlement 400/MutatorCriticalStructures 500/ScatterGroupPrefabs 950/ScatterGroup 960，含 BaseGen 从锚点铺开绕过选址根原语的 landmark 建筑群]、身后避开 Animals 1200[置换时无 pawn 免推移]；MapGeneration.xml 注入全生成路径自动覆盖）单段权威置换：**清障判据 = `def.destroyable` 勿改**（可拆全清 Vanish + **存活复查升格保留**[mod patch 拒拆 = 事实不可拆]、不可拆保留并让位——玩家可接受营地被新内容破坏、不可接受吞其他 mod 关键建筑；传送点 destroyable=false 天然保留侧）+ 物品推移不销毁、植物可清 + 地板清除回落本次生成自己的 389 baseSnapshot + **居住区恢复**（zone = 原 home area 逐格 Home[c]=true；不恢复 = 下次离开 Evaluate=None 被直删的二次丢失 bug——2026-09 首轮回归实测缺口）+ 末尾 pathGrid 重算铁律；Fog patch（1500）末尾**区域整区补揭**（原已探索态恢复；恢复的墙先于 Fog 参与接缝洪水、密封房间留雾 = 原版一致）；`SeamlessTileManager.FinishZoneRestore` 消费记录置 null（两生成路径共用）。**删除流程单一入口（2026-09 首轮回归定夺"整个删除流程根本只有一个入口，其内部处理封存"——霰弹教训：gizmo 删除曾绕过封存直删达阈前哨；**二轮归一**：首版把 BelowThreshold 弹窗策略留在 sweep 分支 = 显式删除路径不弹的残留，实测撞破后已折进入口）** = `RemoveRollingMap(parent, preservePromptAnswered=false)` 内部三分流：tile parent + 活图 + Evaluate==Auto → governor.`ArchiveTileMap`[internal]；BelowThreshold 且提示开且非弹窗答复执行 → `QueuePreservePrompt`[internal，同 tile 去重返回"已延后"] **本次不删**（任何删除路径——距离/gizmo/Dev——统一触发；`preservePromptAnswered=true` 仅供弹窗"不保留"答复执行位防死循环）；其余 → RemoveTileMap/RemoveNativeFamilyMap + DELETE 日志（入口统一输出，调用方勿再记）。governor sweep/gizmo/Dev Force Delete/Dev 批量清场/弹窗"不保留"全部经此，调用方零前置判定；gizmo 对达阈图换 `RimExodus_ArchiveTileMapConfirm` 封存语义确认文案（彻底放弃两步走：封存后世界图"丢弃已封存"[Destroy 前先 ReleaseTileMesh]）；卸载恢复预清 preserveRecord 后改经入口（mod 即将卸载记录无意义）。**勿回退路线（否决案，勿重提）**：整图序列化 blob（跨引用重绑定/pawn 重复/loadID 管理三座手术 + 存档膨胀 + 冻结照片无法支持离开期演化）、增量序列化（无原版机制 = 自研序列化器）、age 判别器增量重放（玩家改未占领遗迹 / 本 mod 生成链调整挪遗迹时双份堆叠或漏恢复）、**首版 order 395 + 源头禁布三 patch + +120t 延迟冲突清理的 patch 思路**（实测漏：landmark 锚点在区域外、34-38 宽 rect 压进区域，且 MutatorCriticalStructures 本就 500 > 395——已随 order 后置整体拆除勿加回）。残差观察项：AncientTuneller(1650)/MutatorFinal(1600) 地形覆写在 1100 之后，实测污染前哨区域再按特例处理。**淘汰不设硬上限**（用户定夺：封存记录 KB 级零内存代价，上限是管理偏好非性能保护）：保留数量 N（0=不限）+ 权重 `X×homeCells + Y×新近度序号`（1=最近封存），超限淘汰**权重最低**者（默认 X=0/Y=-1 = 淘汰最旧）；`archiveOrdinal` 由 governor 持久化分配（`nextArchiveOrdinal`）。小于阈值（默认 20，0=关闭）弹窗：三按钮（保留=封存 Enter 同效 / 不保留=删除 / 不再询问→**二次确认**讲清收益风险）+ Esc=稍后出队下轮重弹 + 答复时重验资格 + 队列一次一张（任意 Dialog_MessageBox 在场避让，forcePause 期间队列静止）。图标第四态 `StateArchived`（青绿）+ 中心白点（数据源 `tracker.PreservedTiles`，Sweep 末全量重写——达阈活图 ∪ 已封存 WO）。**重放确定性定案（2026-09-04 用户实测定位 + 三轮调研收敛，用户指示暂不处理）**：实测判据 = "同进程内多次读档→删图→重生成**完全一致**，重启进程后同一档**必然不同**"——据此定位主因：**`Map.NextGenSeed = System.HashCode.Combine(tile哈希, generatedId++, 世界种子)`（Map.cs:493）解析到 BCL `System.HashCode`，其静态种子 `s_seed = new Random().Next()` 每进程随机、进程内只读恒定**（防哈希碰撞官方设计，跨进程不稳定；反编译树无自有 HashCode 类）。分类定案：**BaseGen 建筑布局层（settlement/landmark 全部建筑摆放/地板/门）= 进程耦合**——跨进程必然漂移、进程内跨读档完全可复现（新图 generatedId 恒从 0 起）；LayoutWorker 房间内容种子 `Combine(sketch.id, j)`（:303/311，sketch.id 从未赋值恒 0）同因进程耦合（settlement 链不走 LayoutWorker，AncientComplex 系走）。**genStep 外层流（选址锚点/尺寸/派系抽取/尸体散布/老化/建筑损坏）= 跨进程真固定**——种子 `Gen.HashCombineInt`（Verse 自实现 murmur，Gen.cs:189）不含进程熵。次级漂移源（叠加）：NextGenSeed/步内 Rand 消费次数随内容与 mod 环境变化的级联、世界状态输入（派系候选池随存亡敌对）、LayoutWorker `?? Rand.Int` 非生成期 fallback。**设计立场 = 正确性不依赖确定性**：1100 权威置换吸收一切 gen 期漂移。将来加固方向（勿现在做）= prefix `Map.NextGenSeed`/`LayoutWorker` 的 HashCode.Combine 换 `Gen.HashCombineInt`（一刀切根除进程耦合）或 per-tile 持久化派系。心跳日志 ARCHIVE/RESTORE/ARCHIVE EVICT 常开（一次性状态迁移事件非周期状态，不刷屏）；ZoneRestore 统计行走 Generation 模块（verbose 门控）。M3 演化结算（按 capturedAtTick 推进作物成熟/风化/新居住者，plants schema 扩展）未实施、记录结构已留位。
- **玩家手动休眠/删除 gizmo + 世界图三态图标（2026-08，勿回退）**：`MapParent_SeamlessTile.GetGizmos` 覆写——活跃非家园受管辖图 →"休眠此图"（`Sleep(manual:true)`；本图是 CurrentMap 时 Disable）；休眠图 →"删除此图"（`CreateConfirmation` 确认后走 `RemoveRollingMap`，与 Dev Force Delete 同路径）；家园/分帧生成中无 gizmo。**玩家 pawn 风险确认（2026-09，勿删）**：手动休眠/删除的“有人”判定必须复用 `SeamlessMapGovernance.HasPlayerPawn`（全部玩家阵营 spawned pawn，含殖民者/奴隶/动物）；休眠有人时新增一次确认，删除保留原删除/封存确认并在其答复后按最新状态复查、有人则追加第二次破坏性确认；确认回调执行前复核 parent/map 仍存活且绑定一致。只约束玩家 gizmo，不扩散到自动/Dev 路径。**原生家族扩展（2026-09）**：gizmo 构造唯一出处收口 `SeamlessManualDormancyGizmos.ForParent`（`Source/Lifecycle/SeamlessManualDormancyGizmos.cs`），双入口——地块图（含营地，CampTileMap 已换 parent）仍走覆写（行为不变）+ 原生家族 POI（Settlement[含玩家自家/Archotech]/Site[一切 SitePart 图]/CaravansBattlefield/DestroyedSettlement = IsNativeFamily 全集）走 `Patch_MapParent_GetGizmos_ManualDormancy`（`MapParent.GetGizmos` Postfix——**迭代器方法必须惰性包一层转发原序列再追加**，base 体执行时机不变；派发面：覆写类 Settlement/Site/Camp/DestroyedSettlement 恰调一次 base（base 调用是非虚直调，patch 的方法体仍执行）、CaravansBattlefield 不覆写直达基类——每对象恰一次；Postfix 排除 MapParent_SeamlessTile 防地块图重复）。门同套（IsGoverned/IsProtectedHome[玩家自家定居点无 gizmo，原版"放弃定居点"路径仍可用]/IsGenerating）。两点分叉：①**删除预检** `parent.ShouldRemoveMapNow(out _)` false → Disable 给原因（用户定夺 2026-09"按原版规则拦下"——殖民者/运输舱/重力锚在场不删，与滚动删除同语义、与地块图无条件删**有意不对称**防误删丢殖民者）；②删除 Desc/Confirm 走 POI 新键（RimExodus_DeletePoiMap*，据点删图留对象再访重生成驻军语义）。配套：gizmo = `Command_ManualDormancy`（Command_Action 空标记子类），据点贸易商对话（SettlementTalk 枚举 settlement.GetGizmos 转 DiaOption）按**类型**过滤防生命周期操作进"远行者"面板（不靠 label/icon 匹配）。三态图标不扩展到原生家族（原生 parent 自有据点图标）。**手动休眠锁** = `SeamlessDormancyManager.manualDormantMaps`（运行时真值）+ **持久化面 `SeamlessDormancyGovernor.manualDormantTiles`（世界 tile id 集合，随档序列化——2026-08 用户要求；休眠状态本身不序列化，读档后图全活跃，governor 首轮 Sweep 对锁内 tile 的图以 manual:true 重新入睡续期锁（**主路径 = `GameComponent.LoadedGame()` 读档即睡（2026-08 方案 3，用户定夺防"横跳窗口被玩家误读为休眠漏洞"）——该回调在 maps.FinalizeLoading 之后、首 tick 之前，锁内图从第一 tick 起即休眠、无活跃窗口；Sweep 内另有幂等安全网分支（覆盖 d < sleepHops 活跃圈等异常路径——Sleep 分支够不着、唤醒守卫只挡已休眠图），2026-08 实测缺口**；Manager 的 Sleep(manual)/Wake/Forget 桥接同步增删该集合）**：governor Sweep 的保活/距离回落分支对锁定的休眠图跳过唤醒（`IsManuallyDormant` 守卫，CurrentMap 除外——**两分支都必须有**：回落分支曾漏加，实测玩家走到手动睡的图旁 1 跳被 `BFS dist=1 < sleepHops` 唤醒，2026-08 修复勿复犯），只有玩家主动进图（CurrentMap setter patch）与 pawn 被命令接近其接缝（BorderPreloader `TryWakeByWorldTile`）两类入口能唤醒（用户定夺）；任意来源 Wake 都解除锁（含持久化集合）；**删除分支不受锁影响**（Forget 时同步清持久化 tile）。三态状态填充（2026-08 二次重设计，勿回退为固定形状 PNG 图标或平铺染色）= def `useDynamicDrawer=true` + `MapParent_SeamlessTile.Draw()` 覆写：按本 tile 真实多边形顶点（`WorldGrid.GetTileVertices`，六/五边形各异）重建单格 mesh（中心顶点+环形三角扇），**颜色与 alpha 写进顶点色**做径向渐变——中心 `CenterAlpha`(0.65) 近实仍透地形、边缘 alpha 0 自然让位给原版 MouseTile/SelectedTile 描边（无需内收缩）；活跃有人橙 / 活跃无人蓝 / 休眠灰（蓝橙 CVD 安全对）。材质全状态共享 `new Material(WorldMaterials.VertexColorTransparent)`（原版 RaycastableGrid 先例）；mesh 按 PlanetTile 进程缓存、几何一次构建，状态切换只重写顶点色（bakedState 守卫），`RemoveTileMap` 处 `ReleaseTileMesh` 释放。教训勿复犯：①SolidColorMaterials+WorldOverlayTransparent 无贴图不设 renderQueue = 完全不显示；②贴图染色平铺（0.45 均匀 alpha）休眠灰对比度不足看不见（日志确认已 DRAW，纯视觉问题）——动态层对象由 `GenWorldUI.DynamicallyDrawnObjectDirectClickRadius`（0.35×tileSize）自动可点击，Print 保持空。**FormCaravanComp**：`RimExodus_SeamlessTileMap` def 加 `WorldObjectCompProperties_FormCaravan`——原版"重组远行队"按钮的挂载条件（WorldGizmoUtility.TryGetCaravanGizmo 查 tile 上 MapParent 的该 comp，缺失则只剩 Send caravan to tile）；非家园图自动走 Reform 分支（Dialog_FormCaravan reform:true，自带威胁门控），出口由 Patches_RCellFinder 重定向到传送点。
- **生成守卫（勿删，会断点）**：`GenerateTileMap` 入口用 `worldObjects.MapParentAt` 查 parent——查到则 Wake + 补登记并返回，**绝不 MakeWorldObject 新建**（`TryGetMapByWorldTile` 被休眠口径过滤后查不到休眠图，不拦会造同 tile 双 parent）。**类型通用化（2026-08，勿回退为 `as MapParent_SeamlessTile`）**：守卫与 `SeamlessDormancyManager.TryWakeByWorldTile` 都认**任意 MapParent**——玩家家园图的原生 parent 不是 SeamlessTile，旧转型令两道守卫对它同时失明（边界带命中 → 唤醒查询 false → 入队生成 → 生成守卫穿透），家园休眠时预加载家园 tile 会走完整生成链造出**重复家园图**（实测："多人跨图命令"的乱坐标 job 是触发源）。现语义四分支：任意 parent 有活 Map（含休眠图）→ Wake + 补登记不生成；`MapParent_SeamlessTile` 无 Map（历史 RemoveTileMap 残留孤儿）→ 销毁后重建（原防御）；**其余原生 parent 无 Map（Settlement/Site 等一切表面层 POI 占位）→ 原生单帧生成（2026-08 全 POI 泛化，见上条）**；非表面层占位 → 跳过。
- **休眠图接缝参考**：软休眠 Map 仍在 `Find.Maps`，可读取当前实况/基础快照；封存或完全卸载图没有 Map，不参与参考，也没有 WorldObject 数据回落。
- **天气域 = "决策集中 + 执行各图" + 注册制激活（2026-09 形态 B + 2026-09-02 第三案定稿，勿回退为字段替换共享实例或任何"重算时推断激活"；权威文档 = `doc/地图滚动休眠.md` 2.5）**：域 = 同群系连通区域中**有图**的成员集合（口袋图/空间层图/未报到图 = 不在域缓存 = 全部 patch 放行原版，fail-open）。每图保留**自己的** WeatherManager/WeatherDecider/SkyManager 原生实例照常 tick——物理天气效果（雨湿地/积雪/毒性）、闪电事件（eventMakers 各图独立掷骰）、环境音（VolumeOfAmbientSound 的 map==CurrentMap 门）、CurSkyGlow（太阳能/植物光照按本图算）全部 per-map 原生正确。共享的是**决策与事件**：①**决策门控**（`Patch_WeatherDecider_WeatherDeciderTick_DomainDecisionGate`，**Priority.First**）：只有激活图的 decider tick 运行，被动成员整方法短路——**连第三方 prefix 一并吞是预期行为（用户定夺 2026-09）**：被动图没有决策过程，挂着决策 tick 做统计的第三方 mod 若照跑会看到"每 tick N 次决策事件"而真实只有 1 次；观察天气变化的正确钩子 TransitionTo/curWeather 经广播对每图原生成立。**激活 = 注册制（用户模型 v3）**：激活控制器是一份持久记录（`activeTiles` 集合，每域一条，随档保存），**不是重算派生值**。每张地图生成完成时**报到**（BindMap——家园/地块/营地/原生据点全经过它，生成串行保证有序）：区域已有记录 → 新图被动 + 从激活图同步天气；区域无记录 → 登记区域、新图自己当选（**区域第一张图天然当选，无任何选择规则**——历史三案教训：①新图最小编号抢已有域激活且零交接；②新域首成时新图把随机 StartInitialWeather 复制给老图[10534 晴天冲掉 103177 暴雨]；③三源解析/首成特判补丁链——皆由"推断激活"产生，注册制下结构性不可能，勿复活推断）。记录只在**三种事件**变更，其余一律不动：①报到；②**激活图停摆**（休眠[Sleep 钩子]/降频 0% 凝固[Throttle 钩子] = 等价休眠 → 还活着成员最小编号接任，交接 duration/禁雨窗口；全停摆不动，首个唤醒者经 `OnMapResumed` 接任；删除 → 重算时清失效记录 + 无源兜底[最小编号 + 全新随机窗口]）；③**强制天气注册**（RegisterCondition Postfix 识别 `ForcedWeather() != null` → 条件持有图接任[交接]，接任后不回切——条件结束 getter 自然回 null）。读档重建 = 记录编号与重算区域做字面 in 判断。②**换天广播**（`Patch_WeatherManager_TransitionTo_DomainBroadcast`，**Priority.Last**）：任一成员 TransitionTo → 同 tick 对域内全部成员（含休眠图，纯字段写无害）各 TransitionTo 一次；放最后使第三方取消型 prefix（return false）先于我们执行——Harmony 跳过其后全部 prefix（含我们的），取消时广播不发生，全域一致（"执行方法让"与"决策方法吞"的**优先级不对称原则**）。`DisableRainFor` 同款转发（Priority.Last + 重入标志）。**重入标志是正确性单点**（转发的成员调用再进 prefix，漏防 = 递归广播），try/finally 必备。③**强制天气接任**（`Patch_GameConditionManager_RegisterCondition_ActivateForcedWeather` Postfix，全 try/catch）见事件③——激活图的 ForcedWeather getter 原生读自己图条件，Anomaly 的 GameCondition_ForceWeather 族（UnnaturalDarkness/DeathPall/GrayPall/BloodRain）无需任何 getter patch。**持久化**：`activeTiles`（存档键 weatherDomainActiveTiles 沿用）= 记录本体，运行时由三种事件写入；各图 manager 状态由原版 per-map 序列化自然保存（广播保证同步），旧"N 份相同快照选一"重绑语义废除。**状态同步点**：报到（新图 ← 激活图，唯一方向）/ 唤醒与解除降频（`OnMapResumed`：激活图有效 → 校准 age 漂移；激活图失效[被删/仍在睡/仍凝固] → 唤醒图接任[保留自身状态，事件②补充检测点]）/ 接任（交接或保自身）。**已删除勿复活**：Patches_WeatherCluster 三 patch（幂等守卫/宿主跟随/ForcedWeather 并集）与一切重算解析激活（三源/live-prev/最小编号推断/首成特判）——2026-08 旧模型三类架构性缺陷（物理效果/闪电/环境音只落钉死图、CurSkyGlow 共享单值、创图删除后效果落幽灵图）随形态 B 根治，三案激活 bug 随注册制根治。**接受项**：激活图休眠且全域休眠 = 决策冻结（唤醒即接任恢复）；自然轮换滤波（大火/温度/preventRain）读激活图数据（激活随强制事件走，成员并集留作可选）。
- **incident 过滤**：`Storyteller.AllIncidentTargets` Postfix 剔除休眠图（防袭击打到冻结图）；`RandomPlayerHomeMap` 等消费面大的 getter 不 patch（误伤风险，观察项）。
- 历史修正：全局静态清扫（Selection/Grants/CrossMapOrders）已从"仅家园图 MapComponentTick"迁至 `SeamlessDormancyGovernor.GameComponentTick`（家园无玩家 pawn 时可休眠，图组件 tick 不再可靠）。
- 历史的“卸 Map 留完整接缝数据”路线已删除；封存只保留 `ZoneMapRecord`。地块图读档后的基础快照缺口由正式生成前的按需精简重建解决。

### 六边形裁切与 void（阶段3；接缝带定义 2026-08 重构，权威定义 = `doc/接缝带定义.md`）
- **2026-09-08 接缝数据语义覆盖说明**：本节历史段落中“捕获条带快照/对侧条带参考”的表述已失效。`BuildSeamBand` 仍是几何唯一实现；参考数据改为实际加载邻图的当前实况与完整基础快照，权威规则见本文件“接缝参考源”及 `doc/接缝带定义.md`。
- 圈层（从核心区向外）：**核心区 → 带内圈 → 离散边圈 → 带外圈 → void**，三圈合称接缝带 B。
- **术语铁律**：不用"六边形外/内"指格集合（离散边格横跨连续边，内外归属歧义）；方向词用"接缝带内（核心侧）/接缝带外（void 侧）"；"格中心在多边形内/外"仅作标量判定。
- 连续边 = 浮点多边形顶点线段（**等边模型（2026-08，勿回退为外接圆半径恒 0.5S）**：目标边长 = 0.5S，半径 = 0.5S / 相邻顶点方向平均弦长——正六边形精确回落 0.5S 零回归、正五边形 ≈0.4253S 边长与六边形相等；世界测地网格各边球面等长，旧半径恒定模型五边形边长 +17.6% 且五-六共享边两图投影不等长、offset 中点对齐后向边端发散 ~11 格 @S=250 超 3 圈带吸收能力；旧档已生成的五边形图建议删除重建，N=5/6 通用）。离散边 D = 格方块与连续边线段相交（slab 法，含角点接触，过四格公共角时 2×2 格都入 D）的格。B = Cheb(D,1)（约 3 圈厚，切比雪夫膨胀填对角缝隙无洞）。**传送圈 = D ∪ 带外圈**（外侧 2 圈铺传送点，带内圈无传送点）。**void = 接缝带外**（格中心在多边形外且 ∉B）——带外圈为实地形（不再是 void），这是传送落点 ±1 格偏差不进 void 的关键（pawn 由内向外必先踩离散边圈 spot，落点偏移落对侧带内/带外圈均实地形）。防孤岛等价：凸多边形下角在内的格必属 {中心在内}∪D，取代旧"中心或 4 角任一在内"角检测。
- `BuildSeamBand`（`SeamlessPolygonGeometry`，进程缓存键 (worldTile, mapSize)）是带几何唯一实现：void 铺设 / 传送圈铺设 / SeamOverride 混合范围与邻居参考判定 / 条带快照捕获共用。**清理全部归 389 的 ApplyPolygonTerrain，依次清 roof → rock（实体）→ terrain**（用户定夺 2026-08；三层快照（terrain/building/roof）先备份再清理，时序天然安全）。**order 200 patch 已删除（勿回退）**：曾提前清 void 格岩体/屋顶——void 格含外条带，提前清理使 389 才备份的原生数据丢失（岩壁整齐切断）。接缝带 B 上的岩石保留（自然地形，混合时按对端/地形增删；挡个别传送点是旧版一致的既有行为）。**照抄/卷积参考语义（2026-08 两轮调查定稿，勿再动）**：参考 = **对端生成时 snapshot 的渲染映射格值**（B∪T 段=混合后定格实况 / 外条带段=389 清 void 前原生快照——T·1 复刻对端 void 之下的自然山体延续 = 设计语义），一切参考只认 a = c − offset（地图偏移带旋转，切比雪夫圈对应 ≠ 渲染映射对应）。两轮被否决的方案勿复犯：①GatedRockRef 连续性门控（先生成假设+后生成兜底，用户否决"对可维护性的蔑视"）；②外条带岩体/屋顶快照改存"清除后实际(null)"数据归一——实测误杀对端带内有延续岩的真实山体（T·1 该出岩处不出）。"一侧无石一侧整条"原症状未复现于该路径，待复现再用结构化探针查。配套探针 `DescribeCellReport`（2026-08 重构，替代旧 DescribeCellMixing）：本侧/对侧两栏 = 位置（以接缝带中圈=离散边圈为基准的相对圈数，两侧各自用自己带几何）+ 生成时 snapshot + 当前实际 + 本侧混合追踪（权重/两侧各层 3×3 卷积/混合结果与 [层:X→Y] 判定）+ 对侧条带快照参考值；self 卷积优先采 389 快照（读档后不可用回落当前并注明）。
- **389 批量注销 O(n²) 已修复（2026-08，"暂存清空"法，勿回退为逐 thing 直接 Destroy）**：逐 thing `Destroy(Vanish)` 的注销簿记（`spawnedThings.Remove` 头/尾扫描 + `listerThings.Remove` per-def 与每 ThingRequestGroup 组列表线性删）单件成本随列表总长二次增长（实测 52µs/thing @ spawnedTotal 5.4k；"生成快删除慢" = Add O(1) vs 删线性查找搬移）。修复 = 销毁前把热点容器**活列表**（`ThingOwner<Thing>.InnerListForReading` / `ListerThings.ThingsOfDef` / `ThingsInGroup`，live 引用零反射）复制暂存并 Clear → 销毁循环里 DeSpawn 的 Remove 全命中"空表不含"O(1) 早退（其余副作用原样执行）→ finally 幸存者按原顺序装回（过滤 removed + Destroyed——非 destroyable 走 DeSpawn 不算 Destroyed，必须靠 removed 过滤；`holdingOwner` 在 Destroy 前手工清空对齐旧时序）。**事故教训勿再犯：①组枚举必须跳过 `ThingRequestGroup.Undefined`（`AllGroups` 含全部枚举值、`ThingsInGroup` 对其抛 "Invalid ThingRequest"）；②暂存/清空/销毁必须全部在 try 内、finally 恢复已装填部分——暂存段放 try 外时异常令注册表以清空态泄漏（后续 genStep 在损坏注册表上跑：BaseGen RemoveAt 越界、Rand 栈不配平；异常中断 ApplyPolygonTerrain 后半段 → void 未铺 + BorderLookup cells=0，"void 丢失/边界失效"双症状同源）**。已回归（荧光森林四图 9-12µs/thing、无红字）；verbose 计数保留（things/spawnedTotal/µs-per-thing）。
- void 铺设：`SeamlessTerrainFill.BackupSnapshotAndApplyVoid`（备份 snapshot + 按 `IsVoidCell` 铺 void + **pathGrid 全量刷新**）。void 地形 `RimExodus_Void`：`passability=Impassable`、`dontRender=true`、`fertility=0`。
- **直写 topGrid 后必须即调 `map.pathing.RecalculateAllPerceivedPathCosts()`（2026-08 教训，勿删）**：`Walkable/Standable` 读 PathGrid 缓存数组（`GenGrid.Walkable → pathGrid.WalkableFast`）而非 terrainGrid，直写 topGrid 不触发重算、FinalizeInit 之前的一切消费者拿旧缓存。历史 bug：旧序（void 在 1400）下 Animals(1200) 先在将来 void 格上合法生成，撤离兜底的 `FindNearestWalkable` 又因 pathGrid 过期 + 径向首候选即自身格而原地空转 → 动物站 void。**2026-08 反转 + 2026-08-30 再提前（勿回退到 1400/1410/391）**：void 提前到 389（Roads 390 前）、混合仍在 392（Roads 后——B∪T 最终实况快照须含道路供后生成邻图照抄），让 Roads(390)+ 全部后续步骤在最终地形上工作（道路 A* 视界内六边形外不可走 → 无绕行通道，河端点因此免延伸到方形边，见"道路与河流接缝对齐"节；Plants 肥力门 / Animals+威胁步骤 Standable 门天然跳过 void 格）；快照消费面经查仅剩 void 侧外条带，Roads 写不进 void 格（TerrainGrid 守卫）、400+ 地板写入被选址 patch 拦在带外（残余缺口观察项见 `doc/地图生成步骤.md` 反转节）。`Patches_TerrainGrid` 的 void 守卫（当前是 void 拦新 SetTerrain）随提前从生成期空操作变为生效——Roads/400+ 写不进 void 格。
- `ComputeVoidBand`（读 terrainGrid void 实况的多轮膨胀）现仅剩 `SeamlessBorderLookup` 预加载带(15)/禁建带(3) 消费，自动适应新 void 形状。

### 传送机制（阶段4b；接缝带定义 2026-08 重构）
- `CompSeamlessTileEnterSpot`：`targetWorldTile`（标记对端）+ `cachedArrivalCell`/`hasArrival`（缓存对端坐标，不序列化）。废弃了旧的互绑 `CounterpartSpot`。
- `SeamlessEnterSpotPlacer`（静态）：`PlaceEnterSpotsAllNeighbors`（铺**传送圈** = 离散边圈 ∪ 带外圈，BuildSeamBand 纯几何。**无任何可通行性过滤（用户定夺 2026-08，勿回退为 cell.Standable 或地形 passability 判定）**——spot 是纯逻辑连接设施：能不能走由地形运行时决定，不能走 pawn 自然绕路（与地图中央的深水/岩石挡路同构），地形变化（挖岩石/铺桥/水位）后 spot 已在、即时可用；带内圈无传送点）+ `RefreshEnterSpotArrivals`（邻居关系建立后按 offset 算缓存）。
- **铺点时机 = GenStep 1490（Fog(1500) 之前，2026-08 从 onComplete 提前，勿回退）**：`GenStep_EnterSpots` 在生成链内铺本图全部邻居方向的点（幂等）；onComplete 只剩 originMap 防御性补铺 + `RefreshEnterSpotArrivals`（依赖邻居表 offset，必须在登记后）。提前的硬理由：Fog 的 `UnfogMapFromEdge` fallback 三级候选都要过 `CanReachMapEdge`/`TryFindRandomEdgeCellWith`，这些 patch 以 `HasSeamEdge`（listerThings 传送点实况）为门——点在 onComplete 才铺时 Fog 在生成期走原版方形边缘语义，裁切图（方形边缘全 void）上 `District.TouchesMapEdge` 恒 false → 三级候选结构性全败 → 零揭雾、**整图全雾**（2026-08 偶发全雾 bug 成因之一）。铺在 1490 而非 392：400-1490 期间保持 patch 未生效（与旧序一致）零回归，Plants/Animals 生成时传送圈格上无 thing。道路 patch(390) 仍不能用 `SeamlessEdgeCells`（1490 > 390，纯几何算锚点不变）。
- **offset = 连续边中点对齐**：`offset = round(midSource − midNew)`，两端各用自己多边形上共享边的连续边中点，渲染平移后两端连续边中点精确重合（浮点级），无收缩。**旧镜像假设公式（round(2·(midA−centerA) − SeamOverlap·unit)，只用源端几何）有系统性 ±1 格渲染错位，勿回退**——同一条世界共享边在两图投影的内切距不等（顶点方向角间隔差 1° ≈ 内切距差 ~1 格；2026-08 实测聚焦 C 时接缝带内偏一格，赤道正北侧同样复现）。取整残差 ≤1 格/分量由 3 圈接缝带吸收。公式天然对称（round(−x)=−round(x)），任一端算等值。旧常量 `SeamOverlap` 曾改名 `RoadAnchorInset`（=2），现值 **0**（道路锚点贴 void 边界无内偏，见"道路与河流接缝对齐"节；Source/Core/SeamlessTileManager.cs:23-27）。
- ~~防反弹：pawn 级锁~~ **已删除（阶段5 改传送许可登记制，无许可不传 + 撤离链 VisitedTiles 防回弹；锁的"站在任一传送点保持"语义会卡死沿相邻边带内行走的撤离者）**。事件驱动传送检测保留但**必须用 Prefix + `pather.nextCell`（即将进入的格），不能用 Postfix**——历史教训（勿回退）：进入终点格时原方法体内部同步跑 `PatherArrived → job 完成 → think tree 发新 job（Wait_Combat 等）→ StartJob 钩子清掉传送许可`，Postfix 永远晚于这条链，终点格传送永不触发（实测 Bridge issued 后紧跟 "Grant cleared by Wait_Combat"）；Prefix 还抢先于撤离 job 到达 toil 的原生 TryExitMap（对端已加载时撤离者应传送而非原生离场）。传送后 Prefix 以 **Map 前后变化**判定跳过原方法体（教训勿回退：不能用 `pawn.Spawned` 判定——传送后 pawn 在新图上仍 Spawned，旧图 path/nextCell 状态跑方法体必然错乱，实测每次传送当 tick NRE 于 `TryEnterNextPathCell`）。`TryTransferPawn` **不校验 pawn 与 spot 的格距**（教训勿回退：原版 pather 的 `SetupMoveIntoNextCell` 节点去重双消费 + 路径重建可合法产生"nextCell 距 pawn ≥2 格"的调用，曼哈顿 ≤1 校验会误拒；不变量由调用方保证——触发器在 nextCell 上找到该 spot 即"正在进入"），坐标映射只依赖 spot。

### 邻居预加载与异步加载（阶段4a）
- **生成入口统一化（2026-08-27，用户定夺勿回退）**：邻接地图生成的**唯一对外入口 = `SeamlessTilePreloader.QueuePreload`**（队列 → `ConsumeQueued`（每 tick）→ `TryPreloadNeighbor` → **私有** `GenerateTileMap`）。忙态自动重排队下一 tick、多请求串行消化——玩家连续命令 pawn 触发六邻生成与 Dev "Generate All Seamless Neighbors" 走同一条链同语义。**勿新增 `GenerateTileMap` 同步直调用方**（已私有化，编译器兜底）：分帧增量生成全程持有 `mapBeingGenerated`，同步循环直调 = 第一个启动后其余全撞入口忙守卫被拒（曾致 Dev action "只生成了一个图"，病灶 `GenerateForNeighbor` 已删）。POI 原生分支（`GetOrGenerateMap` 接管）与营地 patch 不在此列——它们本就刻意走原生同步管线。
- 事件驱动：Hook `Pawn_JobTracker.StartJob`，仅 `playerForced==true` 的 Goto 触发边界检测（避免动物级联加载）。
- `SeamlessBorderLookup`（MapComponent）：边界带速查表 `Dictionary<IntVec3,int>`，O(1) 查询。
- `SeamlessBorderPreloader`/`SeamlessTilePreloader`：异步队列，下一 tick 消费。
- **分帧增量生成**（`IncrementalMapGenerator`）：主线程每帧跑 1+ genStep，不暂停 tick、无进度画面。generating map 被 `Patches_IncrementalMapGen` patch 跳过 MapPreTick/MapPostTick/MapUpdate。纯 Thread/拆分式异步/LongEventHandler 均不可行（Rand/MapGenerator static 非 ThreadStatic + AddMap/genSteps/FinalizeInit 时序依赖）。**分帧开关（2026-08，设置 `incrementalGenerationEnabled` 默认开，勿回退）**：关闭时 `GenerateTileMap` 普通 tile 分支改走**原版 `MapGenerator.GenerateMap` 方法本体**同步单帧生成（与 POI 分支同族，营地 patch 同款先例）——任何 patch 原版生成管线的第三方 mod（GL 等）原生生效，这是特殊 mod 环境玩家的逃生通道；**必须是调原方法，勿回退为内联复刻 genStep 链**（用户定夺）。`SeamlessLandformsCompat` shim 只挂分帧链，同步路径零介入不双份；`GeneratingNativeSeamlessly`/`NeighborGenerationSourceTile` 同步路径同样 try/finally 设置（Fog 接缝揭雾分径）；生成后内联执行 onComplete 全部接线（worldAdd/BindMap/邻居登记/传送点/AutoConnect），返回 null 走调用方既有收尾（ClearGeneratingTile 幂等双清）。**重步骤分帧批次 = 64 格/批（2026-08 修正；2026-08 起可由设置 `generationBatchSize` 调节，默认 64、UI 16-512、运行时 clamp——调大 = 生成更快但每帧更卡，仅分帧开启时有实义）**：CheckSpawnWildPlantAt 实测 ~85µs/格，旧 batchSize=2000 ≈170ms/帧超 8ms 预算 20 倍 = 巨型 tick（"分帧"名存实亡）；64 ≈5.4ms/批预算真正生效，代价 wall 拉长（CPU 总量不变，平滑优先）；进度日志阈值改累进（64 不整除 10000，取模判定永不触发）。void 格在肥力检查前 3 次网格读即早退，无需 void 跳过。
- **GenerateMap postfix 通用复放（2026-09，`Source/Generation/GenerateMapPostfixReplay.cs`，勿回退为逐 mod 硬编码调用其内部方法）**：增量分帧路径内联复刻 genStep 组装、从不调用 `MapGenerator.GenerateMap` 方法本体——挂在该方法上的第三方 Harmony **Postfix** 在地块图上静默不跑（首发案例 = VEF 的 ObjectSpawns 地图物体刷出：VVE 载具残骸/Dark Ages 巢穴/Mythic Ages 等的生成机制，2026-09 玩家报告"残骸全在边缘 void 够不着 + 地块图完全不刷"定位）。修复 = `FinishGeneration` 成功后（**finally 之后**调用，对齐原生"Harmony postfix 在原方法 try/finally 清完 static 后执行"的时点；异常路径不复放——原生语义原方法抛异常 postfix 不跑）经 `Harmony.GetPatchInfo` **每次现读**该方法的全部 postfix（会话中途新打的 patch 也会被拾取）逐个手工调用。**纪律**：①**只复放 postfix**——prefix 的语义是"原方法执行前改参/短路"，脱离原方法单独跑是错的（GL 的 GenerateContentsIntoMap Prefix 由其专属 compat 复刻，不在本处）；transpiler 是 IL 级改写、对未被调用的方法无从复放（既有盲区，维持）；finalizer 无异常可交；②参数编组复刻 Harmony 注入子集：`__result`→生成好的 Map / 按名或 `__N`→Start 时 stash 的原方法实参快照（`generateMapArgs` 字段，形参序对齐 GenerateMap，isPocketMap/stepDebugger 恒 false）/ `___xxx`→反射读 MapGenerator 静态字段 / `__state`→default（其配对 prefix 在增量路径同样没跑过，default 是最接近语义）/ `__runOriginal`=true / `__exception`=null；无法编组的参数（未知名且无默认值）→ 跳过该 patch + Warning（签名漂移可观测）；③逐 patch try/catch——一个坏 patch 不拖累其它，异常 Warning 点名 owner；`BlockedOwners` 常量（初始空）为外科手术式禁用预留，勿为此加设置项；④**只在增量路径触发**——同步逃生/POI 原生/营地/初始图路径调原方法本体、patch 原生生效，复放会双跑；⑤快照重生成器只复刻逻辑不经 IncrementalMapGenerator → 临时图零复放、快照零污染；⑥复放在 onComplete 之后 → VEF 的 no-thing 判据能看到已铺传送点，物体不会压点。**Harmony API 细节（编译时踩过）**：`Harmony.GetPatchInfo` 返回 `HarmonyLib.Patches`——大写属性 `Postfixes`/`Prefixes`/`Transpilers`（`ReadOnlyCollection<Patch>`，用 Count 不是 Length）；`Patch.PatchMethod`（属性）取 MethodInfo，`Patch.owner`（字段）取 Harmony 实例 id（自排除 = "RimExodus.SeamlessWorld"）。
- **VEF ObjectSpawns 位置过滤（2026-09，`Source/Compat/SeamlessVEFCompat.cs`，手动绑定，待游戏内回归）**：VEF 的 `DoMapSpawns` 按 `map.AllCells.InRandomOrder()` 逐格过格级 `CanSpawnAt(IntVec3, Map, ObjectSpawnsDef)`，判据只有"格上无非 Filth thing + 地形过滤（allowedTerrains/disallowedTerrainTags/allowOnWater）"——**无 standability 检查、不知道六边形裁切**。RimExodus 图方形边缘 void 环（约 40-45% 格数、389 清空无 thing、RimExodus_Void 无 Water tag）是全图最大"合格空格池"（植被/岩石群系里核心区几乎全被 thing 占据）→ 物体系统性落 void：不可达不可修；"过缝消失" = 该世界区域从邻图视角属于邻图自己的方形、本图 void 区的东西不再被渲染。修 = Postfix 挂 VEF 格级 CanSpawnAt（`VEF.Maps.VanillaExpandedFramework_MapGenerator_GenerateMap_Patch` 类；**两个 CanSpawnAt 重载（Map 级/格级）必须带类型数组消歧**；位置参数绑定 `__0`(IntVec3)/`__1`(Map)），对 `GetMapWorldTile ≥ 0` 的图把 **void ∪ 接缝带 B 三圈**判不可刷（用户定夺 2026-09"与禁建带同语义"——传送圈干净、VF 载具落点矩形不受挤压）；per-map bool[] 屏蔽表惰性缓存（`ConditionalWeakTable`，`BuildSeamBand` 纯几何与 389 同口径，生成完成后恒定）；fail-open + 一次性 Warning。**单点收口覆盖全部触发路径**（初始家园/POI 原生/同步逃生/营地 = VEF 自有 Postfix 原生生效 + 增量分帧 = 复放器跑其 Postfix，两者内部都调被本 patch 过滤的 CanSpawnAt）。VEF packageId = `OskarPotocki.VanillaFactionsExpanded.Core`（历史遗留名，就是 VEF 本体）；启动确认行 = "VEF compat: bound ObjectSpawns cell filter"。
- **生成收尾段计时（2026-08 诊断插桩，verbose 门控）**：genStep 链后的 FinishGeneration 单帧长尾可观测——`FinishGeneration` 输出各阶段耗时（scenario/FinalizeInit/mapComponents/parentPostMapGenerate/postMapInitialized/onComplete）+ 全程摘要（wall/ticks/genSteps/genStepCpu，wall−genStepCpu = 分帧预算摊薄）；`FinalizeInit` 内部三大重活由 `Patches_MapGenTiming`（含 `SectionTimer` 分段计时工具类）patch 拆解：`Pathing.RecalculateAllPerceivedPathCosts`（389/392 genStep 期两次 + FinalizeInit 第三次）、`RegionAndRoomUpdater.RebuildAllRegionsAndRooms`、`MapDrawer.RegenerateEverythingNow`（增量路径下 `ExecuteWhenFinished` 无进行中 LongEvent 时立即同步执行（LongEventHandler.cs:298-305），即 mesh 全量重建也在 FinishGeneration 帧内）；onComplete 回调链子步计时在 `SeamlessTileManager.GenerateTileMap` 的 lambda（worldAdd/weatherBind/registerNeighbor/placeOriginSpots/refresh×2/autoConnect）；`SeamlessBorderLookup.BuildBorderLookup` 日志含耗时（生成完成后下一 tick 的残余长尾）；Plants 等分帧步（`RunSubStepChunk`）在完成时补一条同格式 GenStep 行并计入 genStepCpu（CPU 不含帧间等待，`SubStepState.cpuMs` 跨调用累计）。门控 = verboseLogging + IsAnyGenerating（MapPreview 预览线程不经过增量生成器，天然无跨线程日志），非生成期零负担。注意 `a ?? false && c` 的 ?? 优先级低于 &&（解析为 `a ?? (false && c)`，verbose 开启后右侧条件被短路绕过——Plants 进度日志节流失效即此 bug，2026-08 修正为显式括号，新代码勿复犯）。
- **分帧生成期间拒绝存档（2026-08-25 修复"生成中途存档 → 读档雪崩"，`Patches_IncrementalMapGen` 的 SaveGame Prefix + 存档对话框 Prefix，勿删）**：半成品图会随 `Game.ExposeData`（序列化 Find.Maps 全列表）整体存进档，但 `RimExodus_SeamlessTileMap` parent 在 onComplete（生成完成）才 `WorldObjects.Add` → 存档时即警告 "WorldObject_... referenced but not deep-saved"，读档后该图 parent==null → `Map.TileInfo → WorldGrid[invalid tile]` 越界，VacuumComponent/温度/植物 tick/FinalizeLoading 全线异常（新进程里增量生成 static 为空，无人恢复也无人丢弃）。**为什么不修读档侧而拦存档侧**：genStep 间共享数据（Elevation/Fertility 等 GenGenFloatGrid）挂 `MapGenerator` 进程级 static `data` 字典、刻意不序列化——"读档恢复生成"结构性不可行；parent 提前登记也救不了（fog 未揭/plants 半铺/FinalizeInit 未跑）。**手动存档必须拦在对话框层**（`Dialog_SaveFileList_Save.DoFileInteraction` Prefix——该方法在调 SaveGame 后无条件弹 "SavedAs" 假成功消息并 Close，只拦 SaveGame 深处 = "显示存档成功、实际未存"；方法为 protected override，HarmonyPatch 须字符串名+显式参数类型绑定到 `Dialog_SaveFileList_Save` 类型，绑基类 `Dialog_SaveFileList` 会 Undefined target method）；自动存档不经对话框，由 `GameDataSaveLoader.SaveGame` Prefix 兜底（拒绝后下一周期自然重试，生成 wall ~10s ≪ 存档间隔，无饿死）。**用户可见提示用 ThreatBig 信件**（左上角消息条不显眼被用户否决；文案带玩笑"请勿在地图加载过程中存档来测试本mod可靠性，这个mod的作者已经深刻理解到它的不可靠了"；Keyed `RimExodus.SaveBlockedDuringGeneration(Label)` 中英同步）。
- **Start 必须重置 `MapGenerator.PlayerStartSpot = Invalid` + `rootsToUnfog.Clear()`（2026-08 教训，勿删）**：这俩是进程级 static 且 `ClearWorkingData` 不动它们（原版 `GenerateMap` 开头显式重置，MapGenerator.cs:83-85）。不重置则上一张图的坐标残留：`GenStep_FindPlayerStartSpot(850)` 的"已设跳过"守卫跳过本图选址，`GenStep_Fog(1500)` 用**他图坐标**揭雾——坐标落 MakeFog 建筑且 5 格内无无顶可站格时 `StandableCellNear` 返回 Invalid → 零揭雾 → 整图全雾（2026-08 偶发全雾成因之二）；`rootsToUnfog` 残留则本图按他图坐标乱揭。
- **392 末预设 PlayerStartSpot（2026-08，勿删）**：选址 validator（CellFinderLoose.TryFindCentralCell）含 `district.TouchesMapEdge`——**直读 District 属性、不经 CanReachMapEdge patch**，裁切图上恒 false → 选址必然 1000 次采样全拒走 fallback（每图一条红字 "Found no good central spot" + 随机格）。392 末（pathGrid 刷新后，StandableCellNear 读缓存）在"已裁切实况判定（四角格是 void）+ 未设守卫"下预设六边形中心附近无顶可站格——原版"上游已设则跳过选址"守卫（GenStep_Labyrinth 设 Zero 同款先例）消红字、揭雾根确定。锚点图同链受益：出生点从 fallback 随机格变为中心格（用户定夺 2026-08）。揭雾全链：392 预设（主）→ 850 fallback 随机格（预设失败兜底）→ UnfogMapFromEdge（invalid 时，1490 铺点后接缝语义可成功）。
- **交错互斥（2026-08 git 考古 + 用户确认：结案为方向否决，勿重提）**：增量分帧与原生 `MapGenerator.GenerateMap` 可交错插入（无双向互斥）——原生开头会 reset `playerStartSpotInt`/清 `data` 字典/RockNoises，进行中增量图的后续 genStep 读 data NRE 被 `RunOneGenStep` catch 吞掉（地图质量 bug，症状不止 fog；上述重置+预设+铺点已把 fog 症状兜住）。**反向互斥本质不可实现**：原生 GenerateMap 是同步契约（`SettlementUtility.AttackNow` 等调用方立即解引用返回值，无法对任意调用方 defer/reject）——与 Settlement 分帧化否决同理。现有防线 = 自家调用点单向守卫（营地 `mapBeingGenerated` 早退 / Settlement 分支 IsAnyGenerating 兜底 + QueuePreload 重试）+ Start 重置 fog static 独立开局。
- **static current 悬挂（2026-08 已修，勿回退）**：`CleanupFailedGeneration` 开头统一清 `current`（此前只有 TickGeneration catch 额外清——Start 内层 catch 路径在 current 已赋值后失败不清 → IsAnyGenerating 永真、后续生成全拒 + 进度 UI 常驻）；`MapComponentTick` 驱动前自愈校验（current 目标图不在 `Find.Maps` = 换档/删图残留 → 丢弃，替代读档钩子，全窗口覆盖）。
- **Geological Landforms 兼容（2026-08，`SeamlessLandformsCompat`，已游戏内回归通过）**：GL 1.6 主 hook 挂 `MapGenerator.GenerateContentsIntoMap` 的 Harmony Prefix（`Landform.Prepare` 建进程级静态上下文 + 运行时注入 GenStep_BiomeVariants(order 225)/各 landform CustomGenSteps + BiomeGrid 初始化）——增量路径**内联复刻 genStep 组装、不调用该方法本体** → 方法级 patch 全失效 → GL worker（`TileMutatorWorker_Landform.GeneratePostElevationFertility`）首行守卫"GeneratingLandforms 含本 landform"不成立恒早退 → **地块图上 landform 静默缺失**（Settlement 原生同步路径正常，症状只在地块图）。修复 = 软反射复刻 Prefix/Postfix 语义：`Start` 组装 genSteps 前 `TryPrepare` + `InitBiomeGrid` + `GetExtraGenSteps` 注入（`Landform.Prepare` 种子 = 世界⊕tile 确定性派生、不消费全局 Rand——增量 RNG 流零扰动）；`RunOneGenStep` 每步前 `EnsureContextAlive`（**存活判据必须用 `GeneratingTile != null`，不能用 AnyGenerating**——无 landform 的 tile 早退时 GeneratingLandforms 恒 null，用它会误判"丢失"每帧重 Prepare；真丢失 = GL 自己的 MapPreview 后台线程/交错原生生成 CleanUp 了进程级静态 → 重 Prepare 幂等自愈，首行自带 CleanUp）；`FinishGeneration` 开头（对齐 GL Postfix 时点）与 `CleanupFailedGeneration` 里 `Cleanup`。全 try/catch 软绑定（GL 缺失 available=false 短路零开销零红字；签名变更降级出 Warning 不崩）。刻意不做 `LandformData.CommitDirectly`（选择本身确定性，Commit 只服务 MapPreview 指示/跨尺寸重生成锁定，反射面大收益小）。游戏内验证标志 = verbose 日志 `GL compat: Landform.Prepare done (landforms=N)` + landform 地形出现；不装 GL 生成零变化。**第二轮修复（2026-08，两个残留症状同一根因，基线对照坐实 + 游戏内回归通过，勿回退）**：症状 = ①建营地进 GL 地块 → 营地图 landform **永久**失效；②邻接进入后地块存在期间 MapPreview 预览丢 GL、图滚动销毁后恢复。根因 = GL `NodeUIWorldTileReq.CheckWorldObject`（namespace **只有 `GeologicalLandforms.GraphEditor`——文件路径 GraphEditor/Nodes/UI/ 的目录段不在 namespace 里**，反射写全名时按路径拼 = TypeByName 恒 null = 注册静默失败无红字、症状与未修复一模一样——2026-08 首版实测教训，修正版已加"按简单名扫 GL 程序集兜底 + 注册后读回自检日志"）对 tile 上"非玩家派系、defName 不在 IgnoredWorldObjects 白名单（原版仅 Camp/AbandonedCamp）、非 Settlement"的 MapParent 把该 tile 全部 landform commonness 归零——无派系的 `RimExodus_SeamlessTileMap` 命中：营地链生成**前**就 `WorldObjects.Add(parent)` → 营地图 landform 全缺，且 GL Prefix 的 `LandformData.CommitDirectly` 把空列表烧进世界数据 → 该 tile 永久失效（`WorldTileInfo.Get` 此后走存储分支，`Landforms==null` 直接产出 null）；地块图 parent 常驻（onComplete Add）期间 MP 预览线程与运行时 `Tile.Mutators` 查询同被过滤（图销毁 parent 移除 + InvalidateCache 自愈——"进入前正常/存在期间失效/销毁后恢复"）。增量路径原本正常纯因 parent 推迟到 onComplete 才 Add。基线对照证据（2026-08 实测）：同 tile 预览（parent 不在场）GL 正常 vs 营地生成 `(F)` 程序化判定 Landforms:None 且 Topology 正常产出 → 差异变量唯一 = parent 在场。修复三件：①`SeamlessLandformsCompat.RegisterIgnoredWorldObject()`（RimExodusMod 构造器调，反射往 `private static readonly List<string> IgnoredWorldObjects` 加 defName——readonly 只锁引用、内容可变；mod 加载阶段早于一切 GL 判定与预览线程，List 非线程安全但注册后全程只读；成功标志日志 `GL compat: registered RimExodus_SeamlessTileMap in IgnoredWorldObjects (verified...)`）；②EnsureContextAlive 归属判据从"`GeneratingTile != null`"收紧为"**引用 == TryPrepare 记录的 preparedContext**"（GL 中该静态仅 Prepare 赋值/CleanUp 置 null，引用相同 ⟺ 未被替换；旧判据下 MP 预览线程 Prepare 装上**别的 tile** 的上下文时主线程 genStep 会消费别人的 landform 数据）；③增量启动避让在飞预览：`SeamlessMapPreviewCompat.IsPreviewInFlight`（仅全局 IsGeneratingPreview——**勿用于跳过 genStep 内部逻辑**，线程精确判定仍用双条件属性 IsGeneratingPreviewOnCurrentThread）并入 `TryPreloadNeighbor`/`GenerateTileMap` 忙判据（预览线程占用 mapBeingGenerated 等进程级 static，增量 Start 同时启动会互相覆盖/互清——MP 对原生 GenerateMap 有主线程 WaitUntilIdle 互斥但护不到自建增量流程，入口对等避让）；忙态分支统一 QueuePreload 重排队（普通 tile 弃"警告+丢弃"，预加载意图保留、重试有界）。**旧档已毒化 tile 不自动清理（用户定夺 2026-08）**：毒化特征 = LandformData HasData 且 Landforms==null，与"程序化判定合法无 landform"不可区分，需 GL 工具手动 Reset。回归标志 = 启动日志注册 verified 行 + 营地图/存续期预览均见 landform。
- **MapPreview 预览兼容（2026-08，勿删守卫）**：230/389/392 带 `includeInPreviews=true` 进 MapPreview 白名单（其默认 filter = 原版地形步骤名单 ∪ 显式 opt-in；1490 未标不进预览，但 genStep 内仍加了双保险守卫）。预览在**后台线程**跑，且预览图的 Map 组件被 MapPreview 的 FillComponents transpiler 裁剪（Reachability 不在 `IncludedMapComponentsFull` 白名单 → null）→ 389/392 的 `pathing.RecalculateAllPerceivedPathCosts` 会在 `NotifyCellDirtied` 首行 `map.reachability.ClearCache()` NRE（每张预览两条红字，被原版单步 catch 吞掉，预览功能无损）。守卫 = `SeamlessMapPreviewCompat.IsGeneratingPreviewOnCurrentThread`（软反射 `MapPreviewAPI.IsGeneratingPreview` && `MapPreviewGenerator.IsGeneratingOnCurrentThread`（ThreadLocal 线程精确），未装 MapPreview 恒 false；双条件防"后台预览进行期间主线程真实生成被误跳"——MapPreview 自身 CheckSkipComponent 同款组合，GeologicalLandforms 示例同款 API）。389 刷新跳过（预览不消费 pathGrid，语义正确）；392 在 ApplyOneWay（混合——预览要显示接缝效果，保留）之后 early return——条带快照（预览图 parent null，捕获白干）、pathGrid 刷新（NRE 点）、StartSpot 预设（预览线程写进程级 static 有竞态）全跳过。另：预览线程打 verbose 日志会与开发者日志窗口（LudeonTK 非线程安全）竞态偶发 "Collection was modified"——开发期现象，verbose 关即消，接受。**void 区预览残留岩石（2026-08 修复，勿删）**：MapPreview 画预览纹理不读实体——`PreviewTextureGenStep` 用 `MapGenerator.Elevation >= 0.7` 代理判据直接画石色；389 清岩不写 elevation → 曾有岩的 void 格在预览里仍显岩石。修复 = `ApplyPolygonTerrain` 铺 void 后**预览线程限定**把 voidCells 的 `MapGenerator.Elevation` 置 0（`MapGenFloatGrid` 索引器收 IntVec3 非 index）——预览是该阈值的唯一消费者，真实渲染不读它，主生成链零改动。
- **2026-09-08 MapPreview 补充**：正式生成前的缺失快照重建必须在 MapPreview 当前线程跳过；392 不再捕获任何条带数据。SeamOverride 的临时采样集合为 `[ThreadStatic]`，避免后台预览与主线程正式生成互踩。

- **PerspectiveShift 兼容（2026-08，`SeamlessPerspectiveShiftCompat`，待游戏内回归）**：PS（ferny.PerspectiveShift，第一人称 WASD 自由移动）的移动**完全绕过 job/pather**（Avatar.ProcessMovement 每帧直写 pawn.Position + Notify_Teleported + pather.nextCell + StopDead）→ 我方两大移动触发器（StartJob 的 playerForced Goto 预加载 / TryEnterNextPathCell 的 nextCell 踩点分派）对 avatar 失明。修复 = 手动绑定 `Avatar.ProcessMovement` Postfix（软检测 `ferny.perspectiveshift` 未装短路；绑定/签名漂移/Postfix 体全程 try/catch——兼容绝不杀死 mod；离线验证器不覆盖手动绑定，须盯启动日志 `PS compat: bound` 行）：avatar 换格时①进边界带（接缝带/传送点格排除，与 CheckPawnGoto 同口径）→ QueuePreload/TryWake；②踩传送点且对端活跃邻居 → 即席 Bridge Grant（**NextJob/FinalDest 均空 = ContinueBridgeMove 天然 no-op，勿给它填 Goto 续程——avatar 由玩家 WASD 驱动，下发 job 会与 PS 物理移动冲突**）→ TryTriggerTransfer 既有分派链；对端未生成不即席生成（预加载兜底）。**传送成功后显式切 `CurrentMap` 到落地图（2026-08 用户实测"跨图丢控制"修复）**——TryAutoFocusOnArrival 只覆盖"首个殖民者进新地块"（autoFocused 每地块一次 + 仅 MapParent_SeamlessTile），avatar 二次进同图/进原生家族图都不切 → 视角留旧图 = 输入作用于别的图；PS 相机锁 avatar 跟随 pawn 对象，CurrentMap 一切下帧自动跟上（setter 同时触发休眠唤醒链；**缩放必须跨切图手工保留——切前记 RootSize、切后 SetRootPosAndSize 恢复（TryAutoFocusOnArrival 同款：Notify_SwitchedMap 会用落地图 rememberedCameraPos 恢复位置+缩放，而 PS 每帧只写 rootPos 不写 RootSize，不补则缩放跳变，2026-08 用户实测修复）**）。**防镜像回传两层（第二层 2026-09 rev4 定稿勿删）**：第一层 = 换图帧只重置基线不处理（落地帧豁免）。第二层 = **方向意图门**（用户方案）：传送落点本身就在对端接缝带上，落地带内的后续踩点若仍按"踩点即意图"处理会把 pawn 弹回同一世界位置的镜像格，恒定按住方向时形成沿缝振荡（实测：家园图↔邻图连环弹射、Throttle 刷屏、影子 scrub 副产物）。门 = 命令方向与邻居链 `offsetDir`（**本图中心→对侧中心**，契约 neighborLocal+offset=myLocal，实测传送对数值定案）同向的点积 ≥ `MinCrossSeamDot`（**噪声地板 0.05**，用户定夺——曾有 0.5"意图角"版实测把 dot=+0.24 的真实斜向跨越拦三十多格，阈值并非防振荡所需）才传送；数据不可用（反射漂移/邻居链缺失）fail-open 放行（最坏 = 旧行为振荡，绝不卡死过缝）。**防振荡的结构保证 = 镜像对称**：对侧 offsetDir 恒为本侧反向量 → 同一恒定方向的点积两侧互为相反数 → 恒定方向数学上不可能双侧过门；真掉头立即放行；沿缝平行（dot≈0）不触发。开销零多边形：步行反射 PS `Avatar.moveInput`（public 字段；其 ProcessMovement 直接把它加到 physicsPosition 上 = 实际位移方向）/ 驾驶用 postfix 现成 `inputDir` 参数，一次邻居链字典查询 + 一次点积，仅踩 spot 的那一格。**勿复犯三记**：①门控符号曾随"offset=对侧→本图"的方向误记写成 dot≤−阈值（真过缝被拦/背离反而传），常显诊断插桩 + 玩家实测数据终判后翻正——几何符号错误只有实测定案；②空间窗方案（v1 传送圈+目标==源图 / v2 全接缝带锁）两版整体回退——v1 被顶点三图循环与 ±1 带内圈落点穿透，v2 释放分支不可达（spot⊆Band，惰性判定永远在带内跑 → 落地后永久无法跨缝的死锁）且漏改载具调用点连编译都没过；③**加载验证纪律：改完必须 build，测试前核对启动日志 `PS compat: bound …(rev N: …)` 标记**——2026-09 三轮实测全跑在不含受试代码的旧二进制上白烧（触发文案未改时新旧版本日志相同，不构成版本证据）。传送后把 PS 的 `physicsPosition`（私有 Vector3?）反射置 null，防其 desync 检测刷警告。**PS 右键下令零改动天然兼容**（Selector.HandleMapClicks 注入 avatar 后走原版 FloatMenu + TryTakeOrderedJob，job 一律 playerForced=true，我方 StartJob/StartPath/PawnGotoAction 钩子全生效——但其注入与点击重放的叠加时序是观察项，同 VMF FloatMenuContext transpiler 锚点互吃口径）。已知边界：第一人称鼠标瞄准解析限当前图（跨缝瞄准未适配）；PS 的相机只改主相机 transform/size，与邻图背景 CommandBuffer 无结构性冲突，锁 avatar 跟随 pawn 对象跨图自然切换。**PS×VF 驾驶共存（2026-08，`SeamlessPerspectiveShiftCompat` + `SeamlessVehiclesCompat` 协作，待游戏内回归；PS 反编译调研结论勿重查）**：PS 有原生 VF 集成——avatar 进舱当驾驶员后 `Avatar.UpdatePhysics` 走车内分支先 return（**ProcessMovement 不再跑，我方步行 Postfix 静默**），WASD 由 `ModCompatibility.ProcessVehicleMovement` 每帧反射驱动 `vehiclePather.StartPath`（无 job → playerForced 预加载链也失明）；且 avatar 在舱内 unspawned，其 `pawn.Map`（holder 链解析）随车变——**车传送后若不切 CurrentMap，UpdatePhysics 首行 "avatar.Map != Find.CurrentMap" 冻结驾驶输入且永不自愈，相机（TryGetSpawnedContainer 需 thing.Map == CurrentMap）同步卡死**。三件补丁（前两件在 PS compat、双装才绑）：①Postfix 挂 PS 的 `ProcessVehicleMovement`（public static bool(Pawn, Vector3)，玩家按住方向键期间每帧）对**载具**做换格检测——进边界带 → 预加载/唤醒（TryPreloadAt 主体换车）；踩传送点且对端活跃 → 即席 Bridge Grant（**NextJob/FinalDest 刻意空——车由玩家 WASD 持续驾驶，不对车下发续程 Goto**）+ 立即 TryTriggerTransfer + `NotifyVehicleTransferred`（即席传送不经 VF pather tick，VF 侧 Prefix 的传送后清理不会跑，须手动补 Notify_Teleported + RequestGrids）；②`AfterTransfer` 统一调 `FocusIfAvatarAboard`（avatar 本人或 avatar 所乘单位[holder 链判定，VF 舱内/被扛抬通用]传送 → 切 CurrentMap + 缩放保留，`FocusArrivalMapWithZoom` 公共方法与步行路径共用、CurrentMap 相等幂等早退——**顺带覆盖了此前无人聚焦的"avatar 跨图右键下令桥接传送"**；avatar 在舱内时 physicsPosition 由 PS 每帧自清，桥接路径由此钩子兜底清）；③驾驶输入冻结判定链（UpdatePhysics 首行）与相机跟随链（State.OnGUI 的 TryGetSpawnedContainer）都以 CurrentMap 为门，②切图后全部自愈，无需单独处理。

- **Vehicle Framework 兼容（2026-08 v2 机制重写，`SeamlessVehiclesCompat`，待游戏内回归；调研结论勿重查）**：VF（SmashPhil.VehicleFramework）的 **VehiclePawn : Pawn**——jobs/inventory/drafter/选中/注册进 mapPawns 全原版语义 → **休眠 governor 的玩家图源收集天然覆盖载具**（"载具视为 pawn"零成本）；下令链走原版 `JobDefOf.Goto` + `TryTakeOrderedJob`（playerForced 成立）→ **StartJob 边界预加载钩子天然命中**；**乘客在 inventory.innerContainer 内非 spawned，随 ThingOwner 链天然跨缝存活**。但移动被 VF 整体接管：VF 用 Prefix 把 `Pawn_PathFollower.StartPath` 重定向到 `vehicle.vehiclePather.StartPath`，逐格推进在 `VehiclePathFollower.TryEnterNextPathCell`（private，自有 PatherTick 驱动）→ 我方挂 Pawn_PathFollower 上的两大触发器（踩点分派 / StartPath 跨图桥接包装）对载具失明，且 VF 的 Prefix 返回 false 会短路同方法上我方前缀。
  **跨图传送机制（v2 唯一权威叙述 = 复刻 VF 官方进图管线，全部车辆特殊逻辑收敛在 TryTransferPawn 车辆分支三步）**：①`EnsureGridsReady`——对图 `pathGrid.Enabled && !Suspended` 就绪判定，未就绪则 `RequestGridsFor(def, Urgency.Urgent)` **主线程同步生成**路径网格+区域网格（VF 官方对非玩家派系载具与 Skyfaller 的同款做法；玩家载具平时 Deferred 只为防卡顿，低频传送事件同步生成可接受；已就绪零成本跳过；**2026-08 实测教训：就绪判定链的类型勿按名查找——`VehiclePathData` 是嵌套类（`VehiclePathingSystem.VehiclePathData`），点分隔全名 `TypeByName` 恒 null → 判定链静默缺失 → "grids still not ready after Urgent" 无限拒传（Urgent 生成本身同步成功，坏的是复查反射面）。修法 = 类型取自 `get_Item` 索引器返回值（Suspended/VehiclePathGrid 声明在非嵌套基类 `Vehicles.PathData` 上，对继承 public 属性天然可见）；判定链任一缺失绑定期必须告警——与 GL compat"按名绑定失败须可观测"同族纪律**）；②`TryResolveVehicleArrivalCell`——落点 = spot 镜像格 → 整车矩形+他车占用判定（VF 官方判据 `MapHelper.NonStandableOrVehicleBlocked` = CellRectStandable ∪ VehicleInhabitingCells）→ 不过则径向搜索（上限 2×车长）→ 找不到 = 拒绝传送并明确记录（对端接缝无车辆可站地块，真实地形限制）。**落点判据换 Drivable 口径 + 四向放宽（2026-08-25 第四/五轮"能去回不来"根因修复，勿回退为 NonStandableOrVehicleBlocked 固定朝向）**：`IsBlockedAt` 跨图复刻 VF `Drivable`（Ext_Vehicles.cs:756——`VehiclePositionManager` 占用 + `VehiclePathGrid.WalkableFast`，**不扫 thingGrid**）——矩形枚举零反射复刻 `VehicleDef.VehicleRect`（= `GenAdj.OccupiedRect` + West→East/South→North 归一化），他车占用扫 `mapPawns.AllPawnsSpawned` 的 `OccupiedRect().Overlaps`；径向搜索每候选格对 Rot4 四向取或（当前朝向优先、命中朝向写回 ref rotation 供 GenSpawn.Spawn 落地）。两处旧判据的坑：①VF 原生 `NonStandableOrVehicleBlocked`/`CellRectStandable` 逐格扫 thingGrid，`passability != Standable` 全拒——原版植物是 PassThroughOnly，**野外接缝环上一棵树就毙掉整个候选**（树在 costGrid 只是通行代价，`Drivable` 口径下"树卡车底"合法——回程四连拒的真根因之一，去程成功的落点恰好无树）；②固定 vehicle.Rotation 单一朝向对 16 格长边 × 3 格宽接缝带结构性放不下。**勿改成直接调 `TryFindNearestStandableCell`/`TryFindVehicleStandableNear`：它内部全走 `vehicle.Map`（源图）与 CanReachVehicle（从当前位置），跨图落点判定必须显式传目标 map**。pathGrid 反射缺失回落旧 NonStandableOrVehicleBlocked 保底。拒绝时 verbose 诊断 `arrival diagnostics`（`VehiclePathGrid.Walkable` 采样最近 8 候选格，区分"网格不可走=地形"vs"grid-ok 仍拒=他车/矩形越界"）；③原 DeSpawn/Spawn + `ResetVehiclePather`（Notify_Teleported 清旧图路径状态）。传送后网格已就绪 → **续程（ContinueBridgeMove）与 pawn 完全同路，无任何等待/重试机制**。
  **兼容层四个手动绑定**（软检测 mod id；签名漂移降级不崩；离线验证器不覆盖，盯启动日志 `VF compat: bound N vehicle patches`）：A `VehiclePathFollower.TryEnterNextPathCell` Prefix（镜像踩点触发器，`vehicle`/`nextCell` 都是 public 字段；**含近距兜底**——VF 的 A* 对多格载具把 Goto 终点按整车矩形解析成 spot 邻近格，活动 Bridge 许可 + BoundSpot 距载具切比雪夫 ≤2 时直接以 BoundSpot 触发传送，坐标映射只依赖 spot、"不校验格距"既有契约；Map 前后变化跳过原方法体，同原版教训不能用 Spawned 判定）；B `VehiclePathFollower.StartPath` Prefix（镜像跨图桥接包装，仅 playerForced——NPC 载具跨图战斗不在范围）；C `FloatMenuOptionProvider_OrderVehicle.PawnGotoAction` Prefix（载具右键 GoHere 桥接——**终点用登记格 ct.cell（邻图框架）而非 gotoLoc**：VF 在宿主图上解析的同数字坐标格对邻图无意义）；D `VehicleCanGoto` Postfix（重放会话跨图点击灰显判定——pawn 强制放行；**载具改 `CanBridgeTo` 实测**（内部 CanReachLocal = VF 口径，与执行侧选点同判据），不可达保持"无法通过此处"诚实状态）。
  **配套接线**：`TryBridgeJob` 对 pawn.Position == exitSpot.Position 直接 TryTriggerTransfer（C1=0 即时完成、零 pather 步进 → 触发器永不跑的退化情形）；`TryFindBestBridgeSpot` 载具分支 `TryPickVehicleBridgeSpot`（按 total 代价序逐个过 CanReachLocal 验证，首个真可达者当选——pawn 权重代价场不感知 VehicleDef 通行差异，pawn 分支零变化）；`CheckPawnGoto` 已加载/已唤醒分支对邻图 Deferred 预热网格 + 口袋图（无 SeamlessBorderLookup）静默早退；`SeamlessBoundaryRules.IsCrossMapOrderable` 加载具分支（**VehiclePawn 非 IsColonist/Animal，不加会被一切跨图下令链拒绝**）；`CanBridgeTo` 可达性经 `CanReachLocal` 走 VF 口径（原版 CanReach+TraverseParms.For(pawn) 对载具的悬浮/轮式/涉水判定不成立）。
  **近 spot 选格放宽（2026-08-25"无畏舰始终无法到达/走本图同数字坐标"双症状修复，勿回退为 spot 格直达判定）**：VF 的 `FloatMenuOptionProvider_OrderVehicle.GetSingleOption` 拿重放后的**邻图框架 ClickedCell** 在**载具自己图上**跑 `TryFindNearestStandableCell`/`VehicleCanGoto`——投影数字常落本图 void 带或远离点击处（实测 gotoLoc 与点击差 ~160 格），这是 VF 原生灰显与本图同数字坐标 Goto 的共同来源。两件修复：①`CanReachVehicle` 要求**终点格上整车矩形可立**，接缝带 ~3 格宽装不下大型多格载具（巡沙车小能过、无畏舰恒 false）——载具分支的 spot 验证（执行侧 `TryPickVehicleBridgeSpot` + 菜单探测 `TryFindNearestReachableBridgeSpot`）统一走 `TryResolveVehicleGotoCell`：spot 直达失败则环形外扩找首个 VF 可达格当 Goto 目标（半径 = `VehicleNearRadius`（⌈整车最长边/2⌉、最小 2，2026-08 第二轮从固定 ≤2 放宽——大车车头探进传送圈即可触发，选点/触发/落点三处必须同口径；VehicleDef 尺寸成员是**小写 size 字段**，反射取 "Size" 恒 null → 落点半径退化 6 的实测教训）；`TryBridgeJob` 的 Goto 发 gotoCell、`grant.BoundSpot` 仍记 spot 格——防 VF A* 对不可立终点 "ran out of cells"→PatherFailed 红字循环）；传送被拒（对端无整车可站格等确定性拒绝）时 HandleBoundTransfer 撤销许可，止住"站 spot 每步重试"的刷屏循环；pawn 分支零变化。
  **整车矩形判据统一（2026-08 第三轮"能去回不来"根因修复，勿回退为 region 可达选终点）**：VF 的 A*（Burst V2 拿 hitbox / legacy 逐步 VehicleRect）对每步整车矩形校验，且终点有 `DrivableRectOnCell(AnyRotation)` 独立门槛——终点矩形放不下时 A* 截断路径 → follower 吞完节点 `AtDestinationPosition` 恒 false → "ran out of path nodes" PatherFailed；而 `CanReachVehicle`（region BFS）是**单格语义、对矩形一无所知**（"reachable ≠ 整车开得过去"）。我们的选点/续程曾只查 region 可达 = 违反该假设。修复 = 全链换用 VF 原生"就近合法终点" `PathingHelper.TryFindNearestStandableCell`（径向搜 Standable + DrivableRectOnCell(AnyRotation) + 无他车 + CanReachVehicle；VF 菜单 GoHere 的 gotoLoc 即来自它），包装为 `TryFindVehicleStandableNear`（半径 = `VehicleNearRadius` 统一口径 R = max(min(边)×2, ⌈最长边/2⌉+1)，选点搜索/触发兜底/菜单三处同径）；反射在位而搜索失败时**不得**用 region 可达格顶替（否则回到截断循环），仅反射缺失回落旧环扫（`VehicleStandableReflectionAvailable` 门）。配套 patch E（第 5 个手动绑定，启动日志 `bound 5 vehicle patches`）：Prefix 挂 `TryFindNearestStandableCell`——重放窗口（SeamlessReplayContext.Active）内载具在本图时改答"指向目标图的桥接 goto 格"（`TryFindVehicleMenuGoto`，与执行侧同判据；重入守卫 `_inVehicleStandableSearch` 防递归）。**回程灰显根因**：VF GetSingleOption 拿重放邻图框架坐标在本图跑该函数，投影区域对大型载具敌意 → 恒 false 灰显且 VehicleCanGotoPostfix 无从介入——去程能过纯靠投影区域恰好开阔的地形运气（方向不对称）；patch E 使亮暗/gotoLoc 语义与原生下令一致。`ContinueBridgeMove` 载具终点同步换 `TryFindVehicleStandableNear`（pawn 的 Walkable 单格判据对载具终点同样会被截断）。②`PawnGotoActionPrefix` 登记 TryGet 失败时不再直接放行原生：先 `TryResolveMapPosition(clickCell)`（VF 传入的 clickCell 是重放邻图框架格，数字落渲染邻图区域即恢复出目标图/格）兜底桥接，解析不出（真本图命令）才放行；全部 decline 分支 verbose 记录原因（旧版静默 decline = 症状长期无法定位的直接教训），本图菜单清除登记（Patches_ClickReplay Finalizer）也 verbose 记录。
  **历史教训（v1 五轮补丁为何全错，勿重蹈）**：v1 在"对图网格未生成"状态下判定/传送/下令——未生成时 `Drivable` 假阳性（未初始化 costGrid 默认全 0=可走）、`CanReachVehicle` 恒 false（无区域 → 搜索空队列 → "Tried to queue null region"）、`TryFindNearestStandableCell` **自指失效**（其内部走 CanReachVehicle：从坏格出发非自身格全被否决、自身格过不了 Standable → 恒 false）——于是续程推迟队列/看护踢发/落点校正/网格自愈锤逐个失效循环。**根因只有一个：先同步就绪化网格（Urgent），再做一切判定。** 已知边界（接受）：NPC 敌对载具不跨图追击；跨图朝向（endRot）不保留；对端接缝无车辆可站地块时如实拒绝（真实地形限制）；多格载具矩形与接缝带几何的相互作用观察项；VF OrientationController 与点击重放的叠加时序观察项。
  - **VMF GetOptions transpiler 锚点冲突（2026-08 游戏内日志确诊已修，勿回退优先级/链式回落）**：装 VF 必然连带 VMF（硬依赖），VMF 对 `FloatMenuMakerMap.GetOptions` 的 transpiler 用 `CodeMatcher.MatchStartForward(Calls(InBounds(Vector3))) → .Set(...)`——**锚点找不到时 MatchStartForward 静默失败、matcher 位置无效，.Set 才抛 "Cannot set values at invalid position" = VMF 整类 patch 应用失败 + 启动红字**（其车图跨图菜单失效）。我们同方法的点击重放 transpiler 是替换式（换 InBounds 调用 operand），先应用（Mod 构造器期）会把锚点毁掉。修复 = 我们的 transpiler `[HarmonyPriority(Priority.Low)]`（VMF 的 Normal 先在原始 IL 上成功应用——transpiler 每次重打包都从原始方法体重跑全链，无顺序不稳定）+ **链式接管**：Vector3 调用点无论已被 VMF 换成它的 `InBounds`（签名同 (Vector3,Map)→bool）还是原版，都换成 `ReplayInBoundsClick`——VMF 在场时**先走其判定**（保留车图语义：CurrentMap 是车图时它以 TryGetVehicleMap 取代原生判定），无缝解析作为其 false 后的扩展，反射调用异常按 false 落回无缝解析（降级不炸）；VMF 缺席 = 纯原生+无缝（与旧行为等价）。IntVec3 调用点 VMF 只 Insert 不替换（原调用仍在），我们的替换不影响其插入体。回归标志 = 双装 VF 时启动无 "[VehicleMapFramework] Error while apply patching" 红字。**残余观察项（接受）**：VMF 的 FloatMenuContext ctor Prefix 与我方同 ctor Prefix 共存（同 Normal 优先级、我方先应用先跑）——我方把点击重写到邻图后，VMF 前缀仍会对重写后坐标跑 TryGetVehicleMap(Find.CurrentMap)（找带内部图的载具矩形）——坐标数值恰好落在当前图上某辆 VMF 载具的占位矩形内时会被二次改写进车图、破坏重放（需"带内部图载具停在缝区 + 跨缝点击其数值重叠位"才触发，极边缘）。

### 接缝覆写卷积混合（阶段4 连续地形；3 圈接缝带重构 2026-08，权威定义 = `doc/接缝带定义.md`）
- 各地块正常用原版噪声独立生成，只在接缝带 B ∪ 过渡带 T 做 terrainDef 混合（**T 数据驱动**：由实际加载邻图的 `SeamBandInfo` 映射与深度决定；历史教训勿回退：曾用固定窄过渡带覆盖区中部截断，2026-08 用户纠正）。**混合范围 = C 自己的带格显式枚举**（旧“从 A 全图 snapshot 枚举候选”已废弃：它会让 A 方形角落绕六边形顶点投影进 C 侧向楔形区；现为只枚举 C 带格，每格反向查最多两张实际邻图）。
- **参考源 = 所有实际加载邻图的当前实况/完整基础快照**：B∪T(depth≤6) 当前地形非 void 时三层都读当前图，当前地形为 null/void 或映射格落外条带时三层同源读 389 基础快照；`has=true,value=null` 表示明确无岩/无顶。地块图快照仅驻内存并按需重建，原生图快照可紧凑序列化用于卸载恢复；无 Map 的封存/删除图不参与参考。
- **混合规则（用户定夺 2026-08，规则轴 = 本端圈层，对端只提供数据不参与规则判定，勿回退为"按对端圈层分规则"）**：**参考格 a = c − offset（镜像格 = 同一空间位置——offset 契约 + 中点对齐保证；**勿再加任何参考偏移**：2026-08 曾按用户提议内移对端边法向 1 格（"接缝带和接缝带内一格一致"），游戏内实测整缝系统性偏 1 格——本侧带抄"对端同一位置再往里 1 格"的值，图案向本侧平移、"本该重叠的格子向外偏一格"，已回退。两图接缝带在缝两侧空间重叠 3 圈（本侧带外圈 ≡ 对侧带内圈同一空间带），无缝的正确条件就是本侧带 = 对端同一空间位置的值 = 镜像格）**。**B_C（三圈）→ 三层字面照抄对端参考格**（strip 有数据即抄地形/岩体/屋顶——岩体用对端 def 跨缝岩色连续，屋顶照抄对端原生岩顶 Thick/Thin 防裸顶岩壁，地形未变时岩体/屋顶同步仍执行，无任何地形例外；错位时 a 落在对端哪个圈层无所谓，照抄天然免疫错位——历史版本按 B_A/T_A/外条带/核心区四条判定需错位补偿补丁，已废弃）；**T·1（过渡带 depth=1，2026-08 用户逻辑并入照抄区）→ 同款字面照抄**：本图 T depth=1 与对侧 OuterStrip depth=1 是同一条空间带（距缝切比雪夫 2）——先生成侧在该 void 带按 own snapshot 放的 void rock，要求后生成侧在镜像位置直接复刻（含岩体），否则先生成侧假设落空、其视角的接缝（带外圈外沿 ↔ 透 void 看到的对端 mesh）断裂；对端该位置读的是其外条带原生快照 = 先生成侧 own snapshot 的镜像数据，两侧同源自洽；**void_C → 不在枚举范围**（直接用自己的 void）；**T_C depth≥2（过渡带，全深数据驱动）→ 卷积权重覆盖**（**源地图轴权重 w = dSq/(dSq+dOut)**：dOut = a 距源接缝带切比雪夫深度（贴缝≈0），dSq = a 到源方形边切比雪夫距离——接近源六边形权重高、到源方形边渐近 0，混合范围截止边界（源方形边）恰是权重归零处自然闭合；×乘性 dither 打散等值线。历史教训勿回退：曾按"本端距接缝带固定深度衰减 + 外条带限深 7 格"，数据边界处权重残值戛然而止，源方形边在 C 上投影成一条直线）。self+参考 3×3 分布 → 众数（地形走卷积混合）；**岩体/屋顶跟随主导参考（w 最大 cellRef）的 def**——与照抄区语义统一（离散层跟随参考；历史教训勿回退：地形驱动 spawn 会在参考无岩体处生成本端岩体，2026-08 用户实测不一致）。顶点楔形区（B_C 多邻居命中）卷积合成（各 w=1）。offset 现算与邻居表登记同公式恒等（genStep 期邻居表未登记——登记在 onComplete 晚于 genStep 链；运行时消费走邻居表）。
- **平行三层参考（terrain/building/roof）**：三层在同一映射格使用同一来源选择。岩体判据固定为 `def.building.isNaturalRock`（含矿石，勿回退 `naturalTerrain != null`）；照抄区用邻图岩石 def，过渡带岩体/屋顶随主导参考。`SyncRockBuildingTo` 保持无岩→spawn、有岩→Vanish、def 不同→Vanish+spawn 三分支。当前实况为空必须能清掉本端岩/顶，这是本轮修复沙地石带陈旧复制的关键。
- **多邻居仲裁**（顶点楔形区）：各参考按 w 加权合成，Σw ≥ 1 时 self 权重 0——替代旧"串行后写者胜"。卷积：self 用 C 当前 topGrid 3×3（越界 clamp），neighbor 用快照稀疏字典 3×3（缺格跳过，水跳过）；N 路加权 → 众数（平局 defName 稳定决胜）。
- **保护判据：特征级例外（道路/走廊/河流），无地形类型例外（2026-08-30 河流并入，用户定夺；旧"只有道路"定夺由河端点改缝中点方案取代）**：SeamOverride 只做三件事——按卷积权重覆盖、随机化边缘（dither）、特征保护。①本格 `IsRoad`/`bridge` → 跳过；②`SeamlessRoadPaths`（`GenStep_Roads.Generate` Postfix 快照 static paths 到 MapComponent，防分帧增量生成下被其他地图清空）±3 格切比雪夫缓冲兜底 Gravel 等无 Road tag 路面；③中心走廊精确足迹（CorridorCellsComponent）；④**河段走廊**（riverGraph 段 ± width/2×1.15+1.5，2026-08-30——河端点取缝中点后本图 void 侧快照在 overlap 空间是干的，不保护则照抄区把缝边的河抄成旱地断在离缝 3 圈处）。**水体/沼泽等一切非河特征地形照常参与混合与直接拷贝**——河的保护是河段几何级（只圈 riverGraph 覆盖的格）非水体类型级，海岸连续性走 CoastalEdgeFill+照抄不受影响；历史的水体例外（本格 IsWater 跳过、卷积跳水）是旧 offset ±2 格系统误差的补丁，中点对齐后不成立（还曾因 `HasTag("Water")` 前缀匹配误伤 Marsh 造成接缝断裂）。判据用生成期权威数据而非局部模式识别。不做 A 侧结构继承。
- Perlin dither 只作用衰减区（乘性 `w×(1+n·amp)`，端点 0/1 不动）——完全一致区保持字面一致。旧 `seamOverrideWeightCap` 设置已删（w=1 直接拷贝 + 衰减公式自带上限，无消费者）。
- GenStep 顺序：`CenterCorridor(100)` → `CoastalEdgeFill(230)` → `SeamlessTile(389, 完整基础快照+铺void+pathGrid刷新)` → `SeamOverride(392, 实时/基础参考混合+void rock link+pathGrid刷新+预设PlayerStartSpot)` → `EnterSpots(1490, 铺传送点)` → `Fog(1500)`。392 不再捕获任何派生条带；同步/分帧两条生成路径在开始前统一为全部已加载参考邻图补缺失基础快照。
- **中心走廊（2026-08，`GenStep_CenterCorridor`(100)/`SeamlessCenterCorridor`，勿回退为 building Destroy 方案）**：邻接生成图 B 保证"B 中心 ↔ 各已生成邻居方向接缝"不被岩石阻挡（用户定义：深水等非岩地形不算阻挡）。机制 = 原版洞窟的无后效通道——写 `MapGenerator.Caves` 浮点网格，`RocksFromGrid(200)` 对 caves>0 的格不 spawn 岩石，零 Destroy/零屋顶支撑后效；**必须在 200 之前**（岩石 spawn 后只剩带后效的销毁路线，用户否决）。形态 = 直线骨架 + 自研 Perlin 蜿蜒（垂直偏移、端点正弦包络归零）/宽度浮动（2-5）/短分支（参数量级对齐 `MapGenCavesUtility`；`SetCaveAround` 是 private、`Dig` 会在离岩群时终止无法跨非岩区，径向写入是仿写非复用）。**端点算法 v2（2026-08-30 用户定稿，勿回退首版"B 侧排序探测+盲挖回落"——探测窗口只盖源边中点 ±15 格，洞口偏移即误判，实测两症状：中心连通洞穴没通道 / 死胡同洞穴反被盲挖命中）**：对 B 的每个已生成邻居 N（含休眠，region 随档序列化可查）——①N 侧接缝带三圈中属于"朝 B 的边"（格中心到边线段距离 ≤6，Fog 揭雾同口径）且无自然岩 edifice 的格；②按 region 分集合（可达性等价类天然现成，无 region 的深水/岩出局；边上无岩=1 集合、3 条洞穴到边=3 集合）；③每集合随机代表，可达目标者随机取一——触发图 A 的目标 = goto 触发位置（`NeighborGenerationSourceTriggerCell/MapId`，与 SourceTile 同点 try/finally 设置，QueuePreload 钻孔自 CheckPawnGoto/PS compat 传入；缺失回落 A 中心），其余邻居 C 的目标 = C 中心；④无通过者不为该 N 挖洞（A 侧死胡同时挖了白挖）；⑤B 侧端点 = rN + offset（契约 N.local+offset=B.local；**offset 一律以当前图为 origin 计算**——与 CollectNeighborRefs 同约定 `ComputeNeighborOffset(worldTile, neighborTile, map)`，勿反方向算再心算翻符号，2026-08-30 实测翻错致端点必然越界、全部邻居被跳过"no valid endpoints"）——镜像格 N 侧保证无岩，392 照抄区不会回填岩堵口。多邻居 = 星形走廊网汇于 B 中心。只挖 B 侧（单向原则，不改任何已生成邻居）。**走廊格免疫 392 混合（2026-08-30 用户实测指出，勿删）**：N 侧 void 外条带快照是清 void 前的原生连绵山体，照抄区会把岩体 spawn 回 B 的接缝带压掉走廊末端——修 = `SeamlessCenterCorridor.CorridorCellsComponent`（运行时足迹集，WriteCaveDisc 记录整条走廊宽度含低 elevation 格——照抄区 spawn 岩无视本端 elevation）并入 `BuildRoadGuard` 保护集（与道路保护同机制，混合主循环 continue 在照抄/卷积分流之前，两路全免疫；精确足迹无 ±3 缓冲——那是 Bezier/无 tag 路面的兜底近似，走廊无此问题）。穿山段保留岩顶（原版山内隧道语义）。初始图（无已生成邻居）端点集空早退。不进 MapPreview 预览（预览纹理 elevation≥0.7 代理画石、不消费 caves——预览里走廊处显岩属已知近似，与 void 预览残留岩石同族）。**深山洞室（2026-08-30，Hive 宿主位）**：巢数 = round(全图 caves 格数/1000)——走廊格把计数推过阈值时游戏"欠"一个 Hive，但窄隧道放不下守卫点（`CompSpawnerPawn.CreateNewLord` 5 格内找不到可站可达格 → "Found no place for pawns to defend Hive" 红字）。修 = 全部走廊的深山采样点（elevation 最大者，须 >0.7 且距端点切比雪夫 >10，勿跨接缝带照抄区——392 SyncRockBuildingTo 会按对端快照填回岩）挖一个直径 ~8 的洞厅（沿走廊方向三盘噪声轮廓）——走廊难超 1k 格、欠账最多 1 巢，一个洞厅闭环（用户定夺"对等地补能放 Hive 的 cave"，勿回退为候选排除/patch 方案）。残缺观察项：山体太薄时洞室与走廊格都不过 CaveHives 的 10 格腐蚀过滤 → Hive 不被尝试放置（静默无报错）；"原版洞窟+走廊"混合计数且深山全在原版洞窟侧的极端组合仍可能欠账（概率极低）。
- 历史（勿回退参考）：旧 dSq/(dSq+dHex) 逐格局部归一权重（wCap=0.9）连同候选枚举方案一起废弃；更早的"全图 void 最大深度"/"候选集 maxDepth"全局归一被角落格污染的教训仍有效——新架构不再有全局统计量。连续 Perlin 全局对齐方案已废弃（elevation 被组合器包裹无法叶子层对齐），代码在 `continuous-perlin` 分支。

### 道路与河流接缝对齐（阶段4 连续地形）
- **道路**（`Patches_GenStepRoads.cs`，order 390 内部）：①Prefix `FindRoadExitCell`（private）——道路出口格强制对齐到接缝锚点（**统一工具 `ComputeSeamCellForEdge` + `SeamCrossingPoint`（Road salt，2026-08-31）**：确定性穿越点沿真·边外法线（`EdgeOutwardNormal`）的最外非 void 格 = 新 void 边界内侧第一格（带外圈），与带宽无关；道路 `RoadAnchorInset=0` 贴边——跨缝两侧路相接。历史：旧"边中点固定内偏 2 格"是旧抽象（void 边界=连续边）写死范围，接缝带定义变更后路出口距地图边缘 3-4 格跨缝断路，2026-08 修正；旧"中点−中心"法线近似在五边形/不规则投影下有偏移，2026-08-31 换真边法线）。边匹配用 `FindSeamEdge(Road)`：**只遍历有 road link 的邻居**（GetRoadDef != null）做世界 heading 匹配——原版 `CalculateNeededRoads` 对多条路的 angle 加向量平均偏置+随机抖动（让方形边缘出口散开；两条路夹角 60° 时偏置可达 60°，推过邻居间隔错到隔壁边），被污染的 angle 不能信任，过滤到 road 邻居后偏置再大也命中正确 link。可达性检查用原版同款两级放宽（NoPassClosedDoors → PassAllDestroyableThings，**不能用 NoPassClosedDoorsOrWater**——比原版严，锚点被河挡住时误放行原版导致出口落回方形边）。②Postfix `ApplyDistanceField`——接缝锚点 ≤6 格且 fromRoad≤1.5 的格强制补铺主路面（原版双重随机抽签在 DirtPath/DirtRoad 中线留 ~11-14% 断格：接缝最后 3 格断概率 ~36%；StoneRoad 唯一实心 mult=0）。跳过条件含 `terrain.bridge`（已铺桥格——桥在 foundationGrid，TerrainAt 遮蔽返回 Bridge，tags 只有 Floor 无 Water/Road tag，没有此判据会把桥格 SetTerrain 成路面：topGrid 从水变土、桥塌后露出河里的路）；place 为 FlagstoneSandstone 时映射 rockDef（对齐原版区域岩色）。③Postfix `Generate`——快照 static `paths`（A* 路径节点）到 `SeamlessRoadPaths` MapComponent（防分帧增量生成下被其他地图清空），供 SeamOverride 道路保护用。
- **河流 v3（2026-08-31 用户定夺，勿回退 v1"方形边延伸"/v2"锚点+垂直接近段+外延"）**（`Patches_TileMutatorRiver.cs`）：
  - **跨地块结构抽象（用户定夺，道路后续同构）**：河流、道路之类跨地块结构**生成期不进 void 区、SeamOverride 不可侵犯**；地面类型/岩壁仍可混合调整。端点/穿越点全部由世界数据确定性决定，不入存档（读档重生成不变）。
  - Prefix `GetMapEdgeNodes`（protected 元组返回，RimExodusMod 手动绑定+启动确认日志）：**中线 = 两个穿越点的连线，延伸到方形 rect（±25 余量）边界**（端点=连线与 rect 交点）——不掰曲线（直线中线无法两端独立指定位置+方向；v2 垂直接近段已删——深度场 t∈[0,1] 截断面 ⟂ 弦的斜切楔口靠"截断面推出方形边外"根治，v2 曾实测 45° 强斜弦在缝处留楔）。**穿越点三级来源**（优先级递减，全部确定性）：①邻图记录（`SeamlessRiverCrossings` MapComponent，键=对端 tile id；crossing = 邻记录 + ComputeNeighborOffset 纯几何 offset；读邻图直接遍历 Find.Maps——数据查询绕过休眠过滤）；②邻图 riverGraph 直线映射后与共享边求交（对侧 fallback vanilla 时跟随其实际穿越位置；无交回落③）；③对称哈希 `SeamCrossingPoint`（对侧未生成；两侧同值闭环：先侧哈希、后侧取记录，两侧独立生成亦一致）。成功路径把两端 crossing 写入本图组件（幂等，Confluence 多次调用兼容）。边匹配 `FindSeamEdge(River)` 不变；fallback 分支无条件 FALLBACK 日志。
  - **双穿越点零中心 Logistic**（`Patch_TileMutatorWorker_River_GetDisplacedPoint` Postfix；2026-08-31 修"穿越点对齐但曲线没贴着走"，2026-09 平滑定稿）：端点延到方形边后穿越点在弦参 t≈0.1-0.2，原版 Perlin 蜿蜒 bell 在该处已有显著偏移，两侧噪声种子各自随机（Rand.Int）→ 缝两侧水带各偏各的。历次错误方案均废弃：①25 格 smootherstep 到窗口末端精确恢复 1，仍有有限支撑边界；②"穿越点外侧恒归轴、内侧恢复"在内外形态间再造突变；③对最近穿越点绝对距离取 `tanh²` 把负半轴折回正半轴，丢失跨轴连续走向，且 min 在两穿越点中部另造导数折角。定稿 = **两个穿越点共同构造、奇对称零中心 sigmoid**：令沿弦物理坐标为 s、穿越点为 a&lt;b，`q=(s-a)(b-s)/(b-a)`（区间内正、区间外负、两根为 0、全域光滑），中线 = 弦点 + 蜿蜒位移×W，`W=2·Logistic(q/25)-1`。河心在穿越点精确归轴；内侧沿正半轴恢复原版偏移，外侧沿负半轴反向延伸偏移，跨轴曲线同向连续；无有限窗口、无分段/min/abs 折点。弦上下文（端点+穿越点弦参 tA/tB）由 Prefix 登记 static `riverChords`（Confluence 每 node 一条）；Postfix 端点匹配容忍 IsFlowingAToB 交换，反向时**只映射 tA/tB 为 1−t，当前采样 t 不翻转**，始终保持 RiverNode.start→end 坐标系。RiverIsland override 继续复用同一实现；GL 河流走独立 Path warp，不受本规则影响。
  - **void 跳过 + 修改格记录**（`Patch_TileMutatorWorker_River_TerrainVoidSkip`/`_BankVoidSkip`，特性绑定 private `RiverTerrainAt`/`RiverBankTerrainAt`）：cell 属将来 void（IsVoidCell，与 389 同口径）→ 返回 null 不铺水/岸——**外条带快照（389 清 void 前数据）不再携带河水/岸，根治 v2"大外延经快照互盖"**；两方法返回非 null 时把 cell 记入 `SeamlessRiverCells`（实际修改格，含被 edifice 挡住未实际铺的岸格——略宽无害）。
  - SeamOverride：`UnionRiverCorridor`（riverGraph 走廊近似）已删——v3 中线延到方形边后近似失真；`BuildRoadGuard` 并入 `SeamlessRiverCells.cells`（实际修改格，精确）。
  - 原生河 = 过随机中心(Rand 0.3-0.7×Size)的直线 + Perlin 弯曲，无原生对齐保证。Confluence 流入/流出支流各取元组一端连汇合点，天然兼容。
  - **GL 1.7 河流地貌盲区 → 已适配（2026-09 **v9 = Path 树钉位**，主线在 `Source/Compat/GLRiverWarp.cs`（patch PathTracer.Trace Prefix）+ `SeamlessGLRiverCompat.cs` 三 patch，勿回退 v1"offset 注入"/v2-v7"末端地形场补丁"/v8"Named 函数场 warp"路线；README 已撤"禁用河流地貌"指引）**：GL 1.7 "River Update" 用自有地貌系统（`TileMutatorWorker_Landform` + `OutputWaterFlow` 图函数）**替换**原版河流 tile mutator——`GetMapEdgeNodes` 不会被调用、原版河流 patch 对 GL 图静默不生效（原版图照常）。**架构事实（五轮实测调查定案）**：GL 河系统是"公共路径 → 多层派生"——主图 `Path: Trace` 从 **Path 树**（`TerrainGraph.Flow.Path` 的 Segment 列表，树/图结构；每段起点 = 父帧.pos + `Segment.RelPosition`[public 可写，Root 段即 map 域进场锚]、出场方向由 `TraceParams.Target`[struct 内 `Vector2d?`，map 域] 驱动 A*）trace 出河，逐帧烘焙进 MainGrid/DistanceGrid/SideGrid/ValueGrid/OffsetGrid；grid 经 Named IO 注入 RiverTerrain layer——biome（Soil 带）/岸（Riverbank 带）/水/海拔（河谷）/洞穴全部层从这批 Named Input 取值（跨 canvas 只有 Named IO 一条路）。**v9 机制**：patch `PathTracer.Trace(Path, int)` **Prefix**（手动绑定、object path 参数、全反射遍历、零 emit 零编译期引用）——①Root 段钉位：进场锚（RelPosition）在某条 river-link 边带内（深度 ≤40、lat ±12 容许；河源图中央 Root 天然出局）→ `RelPosition = SeamCrossingPoint(River)` 对称哈希穿越点（两侧一致、混合管线同点）；②Target 段钉位：段的 TraceParams.Target 在 river-link 边带内（深度 ≤60——覆盖 Confluence 支流 margin 0.2≈50 格端点，拉到缝上 = v7 e5 SKIP 案正解）→ Target = 边哈希 target（TraceParams 是 struct，装箱读-改-写回）。两端各钉同一对哈希 target → 两侧缝上对齐；中间河形由 GL 自己的 A* 连续寻路——**无离散场、无切割、无混合平均**；全部派生 grid/全部消费层自动一致，旧河道根本不生成。**天然幂等**（重钉增量恒 0；collision retry 在 Trace 内部不重进 Prefix；残余 = 复用树被 collision handler 调整后再钉会撤销调整，无害接受）。边带缓存单槽 key=(tile,size)。**门控**：`MapPreviewAPI.IsGeneratingPreview` 跳过（预览原样）；无 river-link 边带命中不钉（世界数据权威）；全程 try/catch 安全失败 = 不钉（勿杀 Trace）；反射解析失败降级 Warning（WorldTileInfo.TileId 是 **internal readonly 字段非属性**——2026-09-04 v8 实测 GetProperty 恒 null 教训）。**Patch B 保留**：GeneratePostTerrain Prefix（ThreadStatic topGrid 快照）+ Postfix 只剩 void 还原 + `SeamlessRiverCells` 登记（Eval 即新位置实况）+ guard 日志。**Patch A 保留为自定义地貌辅助**（GetOrCreateTileLinkData Postfix 注入 offset）。观察项：进场角度（RelAngle）不校正（原版 v3 同族接受）；Root 钉位平移整树而 cost 场（海拔）不平移 → 河形与 GL 原生有偏差（A* 自己绕障，特性级）；riverFlowMap 不重算（纯视觉）；MapPreview 预览原样。诊断 = Compat 模块（"GL river warp pin: tile=… roots=N targets=N root(锚)->e{i}t(目标)…" + guard 汇总；绑定确认常开行 "GL river compat: bound" 三行）。适配只对新生成的图生效。**v1-v8 历史教训（全档详见 SeamlessGLRiverCompat.cs / GLRiverWarp.cs 类注释，勿回退）**：v1 offset 注入（官方图零消费 Offset 输出、outflow 终点种子随机）；v2-v7 末端地形场补丁（行扫描/防串/岸拷贝/清除——三层分离四失效点）；v8 Named 函数场 warp（emit 平移采样代理 + BFS eff 格网场——离散查表在位移跳变处沿轴向切割河["水平/竖直切割"实测]、同河两端 target 不同时中段加权平均拧碎——**离散场缺陷结构性修不掉，函数层包装整条路线否决**；其编译期引用会令 GL 缺席玩家 GetTypes 抛 ReflectionTypeLoadException 打红字的调研结论仍有效，防复用）。**v9.1 出场端残余的两轮失败尝试（2026-09-04 实测定案，勿再试）**：①StabilityPoint 稳定点（target 处 Range 15）——stab 只在被 Cost/Swerve/Extent 参数函数消费时生效，官方河图未连、零效果；②RelAngle 初始方向对齐（名义帧推演 + 硬扭指向新 target）——单链树无收益（d 无改善 + 河斜穿缝变窄 span 12→5），Confluence 分叉树直接崩（名义推演在 Split/RelShift/蜿蜒上误差大 → 期望方向算偏 → a=-39 硬扭触发初始角钳制冲突 + collision retry 整树重排 → e4/e5 双 NO-CROSS"河断了"）；两轮均已回退（git reset 到 f294801）。出场端残余 = ±6~10（marginHead 过冲 + A* 接受半径 + 初始方向蜿蜒，map5 支流端 +10 最大）——原版河 v3 同族接受，观察项。**Segment.Id 是字段非属性**（AccessTools.Property 返 null → NRE，与 WorldTileInfo.TileId 同族教训第二次）。
  - **混合期河流保护（2026-08-30，用户定夺；推翻旧"无地形例外"定夺，但保护是河段几何级非水体类型级——历史水体例外误伤 Marsh/海岸的教训不复发）**：河端点停在中点后，本图 void 侧外条带快照在 overlap 空间是干的，若不保护，本图 392 照抄区会把自己生成到缝边的河抄成旱地、断在离缝 3 圈处。修 = `map.waterInfo.riverGraph`（MapComponent，零持久化）每条河段中线 ≤ width/2×1.15+1.5 格的点集并入 `BuildRoadGuard`（与道路/中心走廊同机制，混合主循环 continue 在照抄/卷积分流之前）。海岸/沼泽照常参与混合不受影响。
- 共享几何（2026-08-31 河/路完全统一，四件套都在 `SeamlessPolygonGeometry`）：`FindSeamEdge(worldTile, angle, SeamLink)`（**统一边匹配入口**：SeamLink.River/Road 内部映射 linkFilter[GetRiverDef/GetRoadDef != null]，收编旧 `FindEdgeByWorldHeading` 调用方自传 filter 的写法；河上下游 = angle/angle+180° 两次调用，路单次。**勿改回本地边中点角度近似**：六边形边方向离散 60° + 投影扭曲，会把"正南流向"锚到 SE/SW 边——用户实测南北河被画成东北-西南、下方地图连不上）+ `SeamCrossingPoint(worldTile, edgeIdx, mapSize, SeamLink)`（**确定性穿越点**：摆脱恒边中点限制——对称哈希派生 t=0.5±0.25，salt 区分河/路（同一条边上路和河错开）；**哈希必须用固定 FNV-1a，勿换 System.HashCode**——后者进程随机种子，跨会话/读档后再生成会得出不同穿越点（2026-08-31 用户实测,哈希是跨存档契约，算法常数永不改）；**两侧一致性靠世界 3D 顶点身份**：边的两个 3D 顶点（GetTileVertices 与 2D 投影逐索引对应）用对称谓词（四舍五入坐标字典序 `SymLess`）选参考端点，决定本侧 t 折算为 t 或 1−t——不依赖绕序/投影方向假设；若实测两侧 crossing 镜像错位改折算方向一处即可）+ `EdgeOutwardNormal(verts, edgeIdx)`（**真·边法线** = (v1−v0) 垂直向量取外侧[顶点质心判定]，替代"中点−地图中心"射线近似——后者在五边形/不规则投影下与真实边法线有偏差，是道路锚点偏移来源之一）+ `ComputeSeamCellForEdge(…, crossingOverride)`（**统一锚点工具**泛化：穿越点[默认中点]沿真边法线步进到最外非 void 格再内偏 offsetCells；基于 BuildSeamBand/IsVoidCell 唯一口径，与带宽无关）。
- **道路末端垂直化（2026-08-31，`Patch_GenStep_Roads_RefinePath`，手动绑定 Postfix 挂 private `RefinePath`）**：refined 折线是 Bezier/距离场的输入，其末端方向 = A* 进近角（从中心斜到达锚点 → 接缝折角）。修 = 折线端点等于接缝锚点格时，末端 8 格替换为"锚点沿边内法线的直线段"接回原折线——与河流垂直接近段同构，两侧共线对接。失败模式 = 端点非锚点/直线格越界或落 void → 该端保持原样（无破坏面）。启动绑定确认日志 `GenStep_Roads.RefinePath manual bind`（手动绑定不在离线验证器覆盖内）。
- `RefineEndcap` 的 ≤5 格端点检查**不会**移动六边形内侧锚点（它只是"是否执行 endcap 重路由"的门槛，锚点距方形边 ~19 格 → 恒跳过）——已排除的嫌疑，勿再查。

### 建筑选址避开六边形边（阶段4b；2026-09 GetClearRects 改裁剪语义 + 补 TryGetStructureRect ④级 fallback 收口）
- **问题**：原版建筑选址全按方形边界收缩（如 `GenStep_Settlement.CanScatterAt` 的 `BoundsRect(12)`），不感知六边形（边中点距方形边 ~17 格）→ 建筑锚点跨六边形边 → order 389 铺 void 时被切半。
- **四个 patch**（`Patches_BuildingPlacement.cs`）：①Prefix `GenStep_Scatterer.CanScatterAt`（protected virtual，字符串声明特性；子类 base 调用命中）——锚点六边形外或距边 <20 格（=最大建筑半宽 19+1，Settlement 38×38）拒绝，上层 1000 次重试消化。②Postfix `MapGenUtility.GetClearRects`——**半平面裁剪（2026-09，勿回退为整块移除）**：返回矩形收缩进六边形（`ClearRectMargin=10`）、仍满足调用方 minWidth/minHeight 才保留（清晰矩形路：Outpost/AncientComplex/LargeRuins/Gravcore/SurveySite/Harbor 等；Burst 内核不可 patch，此托管入口是唯一可行点）。**整块移除的教训（2026-09 用户日志定案）**：vanilla maximal 矩形天然延向方形边缘，小图（UI 最小 200，内切半径 86.6）上几乎全被删 → LargeRuins 族（CrashedMechanoidPlatform/OrbitalWreck → `GetLargestClearRects`）三级降级（海拔 0.7→1.0→免水）全拿空列表 → "did not find any valid rects to generate ruins" 警告 + 内容整块缺失——vanilla 本有 `TryFixInvalidRects` 逐格收缩降级（void 无 affordance → IsCellValid false → 收缩），上游删光矩形即失效；裁剪保证"保留矩形必在六边形内且距边 ≥10"语义不变、候选数量恢复（净拒绝压力下降）。③Postfix `CellFinder.RandomNotEdgeCell`——采样六边形外 → Invalid（FindPlayerStartSpot tightness 降级兜底 + 运行时 CompDeepScanner/incident）。④Postfix `MapGenUtility.TryGetStructureRect`——**结构矩形零拒绝改写（2026-09，Icarus 报告"小图大地标缺一角"根因修复）**：该方法（AncientStructure 族地标精炼厂 46~56/仓库最大 61~81 含围栏 + Harbor/Stockpile worker 的主体选址）四级重试中**④级 fallback 直接 `CellFinder.TryFindRandomCell` 全图随机采样**（MapGenUtility.cs:794）——绕过 GetClearRects 即绕过①②③拦截；其 Validator 只查方形边界收缩 10 格 / **水占比 ≤25% 即放行**（:808，void 非水全过）/ UsedRects / 距中心 0.75×S，完全不查通行性/affordance/空位。小图+大地标+山地图（elevation 清晰矩形碎小）→ ①②③全败 → ④矩形压进 void 角落 → sketch 以 `forceTerrainAffordance` 落地时 void 的 SetTerrain 被 `Patches_TerrainGrid` 守卫拒 → `CanPlaceBlueprintAt` 失败 → **单件静默跳过**（Sketch.Spawn 失败回调传 null）= 缺角；**复现难** = 三条件叠加+种子依赖（默认 250 图①恒成功）。修法 = 原方法原样跑，Postfix 仅在返回矩形未过 8 点安全检查（四角+四边中点、距边 ≥10）时改写：沿矩形中心→地图中心逐格滑动取首个安全位（局部性），滑到中心仍不安全（病态巨构/超小图）则中心锚定（vanilla 终端 fallback `map.Center.RectAbout` 同款语义，S≥200 恒容纳 81 见方仓库）；①②③级矩形来自裁剪后安全清晰矩形内接不进改写分支。**勿改成 extraValidator 拒绝注入（2026-09 用户否决）**——那会在小图上把缺角换成 "Failed to find location structure" 红字激增，与②旧版整块移除同类饥饿；本轮两修复净拒绝压力为零/为负。Generation 模块 verbose 记录每次改写（"Structure rect re-anchored for hexagon safety"）。
- 几何判定用 `BuildPolygonVertices` 纯几何（不读地形实况、与 void 铺设时机无关——2026-08 起 void(389) 虽先于选址铺好，几何判定与其等价），勿用 `HasSeamEdge`（传送点 1490 才铺，400-970 的选址期恒 false）。裁剪/安全判定共用 helper = `BuildingPlacementGeometry`（同文件：8 点采样判定 + 半平面裁剪——内法线由"含多边形中心一侧"定向、矩形各轴按 n 分量符号取下界侧、违反量 ceil 内移，收缩是子集操作不会重新违反已满足的半平面故单遍收敛）。
- 残余缺口（接受）：`GetOutpostRect` 贴附矩形、`GenerateLandingPadNearby`——锚点已安全后溢出概率低，观察（2026-08 void 提前后这些缺口处地板可盖住混合带结果，旧序相反，见 `doc/地图生成步骤.md` 反转节观察项）。

### void 渲染与邻居背景（`SeamlessTileRenderer`）
- **表面层守卫（2026-08 修复"太空背景变黑"，勿删）**：`MapComponentDraw` 入口在 CurrentMap/DrawingMap 守卫之后还有一道 `GetMapWorldTile(map) < 0` 早退——太空/轨道图（`renderWorld` 模式）的 `DrawingMap` 恒 true 上文守卫拦不住，而其背景（星球+星空）由世界相机（depth 0/1，星空 skybox+星球层）先画好、主地图相机只清深度叠加；MapComponent 逐图自动实例化使太空图上也挂了本 renderer，零邻居分支的全屏 `ClearRenderTarget` 会把星球背景整屏擦成黑。口袋图（VMF 载具内部图）同理排除。切图 buffer 释放无需额外处理（旧图 renderer 的 MapComponentUpdate 分支释放，太空图 renderer 不再 Ensure）。
- void 地形 `dontRender=true` 实际画在 `MatBases.ShadowMask`（半透明不写不透明色），主相机只清深度——void 带的背景完全依赖 `SeamlessTileRenderer` 的 CommandBuffer 每帧 `ClearRenderTarget(true,true)` 全屏清色 + 邻居地形 mesh（`BeforeForwardOpaque`）。
- **邻居收集层 = Terrain / ThingsGeneral / LightingOverlay / FogOfWar 四层（2026-08 补光照遮罩，勿回退为三层）**：原版 1.6 地图亮度 = 纯 albedo 地形/物体 mesh × `SectionLayer_LightingOverlay` 顶点色（glow 颜色 + 岩顶黑暗 alpha，per-section 菱形渐变）× 共享材质 `MatBases.LightOverlay` 的昼夜色（每帧被当前图 SkyManager 染色）——不收集 LightingOverlay 则邻图背景无昼夜变暗、无灯光 glow、无岩顶黑暗（永远满亮度）。该层 mesh 顶点同为地图本地绝对坐标（y = AltitudeLayer.LightingOverlay ≈13.17，低于 FogOfWar），offset 矩阵直接平移；glow/roof 脏标记（Roofs|GroundGlow）由收集循环内的 Section.TryUpdate 视区重建消化（邻图灯光/屋顶变化即时反映，见下条）；可见性经 `layer.Visible` 自动尊重 `DebugViewSettings.drawLightingOverlay`。SunShadows/Gas 等其余层仍不收集（shadow/grid 依赖）。**邻图动态绘制物（pawn/弹丸/RealtimeOnly thing 三类，雾过滤勿删）**：`DrawNeighborPawns`/`DrawNeighborProjectiles`/`DrawNeighborRealtimeThings` 的立即模式 `DrawNowAt` 绕过原版 `DynamicDrawManager`——原版雾中 pawn 不可见**不是靠 fog mesh 遮挡，而是 DynamicDrawManager 根本不画雾里的动态绘制物**（DynamicDrawManager.cs:69 `!seeThroughFog && fogGrid.IsSet` 即跳过）；静态 thing 走 mesh 与 fog mesh 同管线天然被盖，动态物必须镜像该判定（`!def.seeThroughFog && fogGrid.IsFogged(Position)` 则 continue），否则邻图未探索区域的动物/弹丸凭空可见。**RealtimeOnly 补画（2026-09 AncientHeatVent 邻图背景不可见定案修复，待游戏内回归）**：Odyssey ancient vent 族实例探针（Dev "Probe: Neighbor Things Mesh" + "Force Regen Neighbor Sections"，`DebugActions_SeamlessTile.cs`）定案——`AncientHeatVent` 显式 `drawerType=RealtimeOnly`（Buildings_Natural.xml:269）→ 唯一绘制路径是聚焦图 DynamicDrawManager（`Map.MapUpdate` 的 CurrentMap 门），静态 mesh 被 `SectionLayer_Things.Regenerate` 的 RealtimeOnly 过滤结构性排除 → 四收集层 + 旧两类手画全不覆盖、跨缝整只缺失、Force Regen 无效（结构性）、切回仍不可见（mesh 里没有）。修 = `DrawNeighborRealtimeThings`：遍历邻图 `dynamicDrawManager.DrawThings`（公开只读属性，零反射），**只挑 `drawerType == RealtimeOnly`**（MapMeshAndRealTime 本体已在 mesh，再画双画）、显式排除 `Pawn`/`Projectile`（各有专门通道），DrawNowAt + 视区 + 雾过滤与 pawn 通道同族；已知边界 = thing 自定义 DrawAt 读 `Find.CurrentMap` 全局态的罕见 mod 写法会画错位（与 pawn 通道同级风险面，try/catch 防崩）。同族缺口挂账：①`AncientSmokeVent`/`AncientToxVent` 是 MapMeshOnly（本体在 mesh 内应显示——其缺失报告实为与 HeatVent 混观或另有原因，待复测）；②其标志性烟柱是 `CompFleckEmitterLongTerm` 的 fleck（`FleckMaker.GetDataStatic`+`CreateFleck` 直调，不经 `FleckMaker.Static`，既有跨图 fleck 重定向 patch 接不住）——fleck 属邻图结构性不画的动态粒子（`FleckManagerDraw` 仅聚焦图），聚焦才喷烟，是否补画待定夺。探针教训：四边形命中判定按"中心 ±3.6 格内顶点数"统计会把周边 filth/岩石顶点误计（假阳性），结论以 cond[drawerType] 行为准。
- **邻图脏 section 视区重建（2026-09 性能修复，勿回退为"任意脏 → RegenerateAllLayers + 清零"的全图全层扫描）**：玩家报告"暴雨/暴雪 + 有邻接地图时非常卡"根因——原版 `SteadyEnvironmentEffects`（MapPostTick 每 tick 采样 `Area×0.0006` 格 ≈38 格 @250 图）在雨雪期间**持续标脏 mesh**：降雪 `SnowGrid.AddDepth → CheckVisualOrPathChange` 同时标 Snow|Things 双位（regenAdjacentCells:true 波及 ≤4 个 section）、融雪标 Snow、雨冲污物 `ThinFilth→Destroy` 标 Things；降频邻图照常积脏（雪积累是模拟语义、降频只减半速率），而**邻图脏标记的唯一消费者是我们渲染器**（原版 `MapMeshDrawerUpdate_First`/`DrawSection` 都被 CurrentMap 门住）——旧 `EnsureSectionsGenerated` 每帧每邻图**全图 225 section 扫描**、任意非零 dirtyFlags 一律 `RegenerateAllLayers()`（全层重建，含最贵的 LightingOverlay 逐格 glow 采样）+ 手工清零，**无视区/无层过滤/无上限** → 雨雪期间每帧数 ms~数十 ms 的重建风暴。单图不卡（原版当前图路径自带视区+命中层双收敛）、休眠图不渲染不触发 = 精确吻合"有邻图才卡"；"暴雨也卡"成立条件 = 图上有积雪（雨先融雪）或污物（雨冲刷），温带雨雪季常态。修复 = 收集循环内对**视区内**（`neighborView.Overlaps(section.Bounds)` 通过后）非零 dirtyFlags 的 section 调**原版 `Section.TryUpdate(neighborView)`**——方法体无全局态依赖（视区是参数、`MapDrawLayer.Dirty` 纯自动属性零副作用），旧注释"邻居 section 不在当前地图 ViewRect 内不能依赖 TryUpdate"不成立（参数化视区方法，传平移后的 neighborView 即可）。语义三要点：①视区外脏 section 的 dirtyFlags **保留**（stale mesh 本就不被收集[同一视区判定]、点击拾取走 thingGrid/thing collider 不依赖 section mesh；相机平移后新进视区当帧重建；邻图转正为 CurrentMap 时由原版路径闭环——layer.Dirty 保留正是 TryUpdate 留给 DrawSection 兜底的原生语义）；②Snow 位命中我们不收集的雪层也**照常重建、勿剥位省性能**——dirtyFlags 清零后无人再标，邻图转正后雪层 mesh 永久 stale（正确性不可换性能）；③Dev "Force Regen Neighbor Sections"（DebugActions_SeamlessTile）保留独立全量重烘实现作为手动锤。顺带（同轮）：MapEdgeClipDrawer patch 每帧 6-10 个 List 分配改静态缓冲复用（渲染热路径 GC 底噪；`GetNeighborFootprints` 内部两层小 List 分配量级可忽略未动）。**调研排除项（核实过非嫌疑，勿重查）**：降频×天气交互（只减半积脏速率）、天气域四 patch（决策门控 O(1) 净节省、TransitionTo 广播仅换天瞬间一次、DisableRainFor 原版唯一调用方是闪暴 condition）、雨雪绘制/音频（原版 CurrentMap 门：CameraDriver 只画 CurrentMap 天气、VolumeOfAmbientSound 对非当前图返 0 不 spawn sustainer）、FleckMaker/MoteMaker patch（同图 3-4 次比较早退且原版雨滴非 fleck 是 overlay 材质）、TickProfiler（默认关零成本）。残余接受项：多图 SteadyEnvironmentEffects 固有成本（38 格/tick × N 图，架构性）、降频图每帧 MapUpdate 放行（既有设计勿改形态）、邻图背景缺雨雪覆盖层（既有视觉边界，与本性能问题无关）。
- **光照天色分层（2026-08，勿回退为"邻图 overlay 用原材质"）**：LightOverlay shader 的**天色染色走材质 color alpha、glow/roof 走顶点色，两通道独立**（游戏内实验验证：材质 color=(1,1,1,0) 时灯晕在/岩顶黑在/户外昼夜变暗消失；原版先例 `SkyManager.cs:49` disableSkyLighting 群设置同款开关；注意顶点 (0,0,0,0) 的语义 = 按材质色染天色**而非透传**——`MapDrawLayer_ExteriorLightingOverlay` 专门用它给图外区域染昼夜色）。直接给邻图 overlay 用原材质会在 void 透明圈双重染天色（当前图 overlay 的 mesh 覆盖全图方形含 void 格 + 邻图 overlay 各染一次 = sky² 暗带）。分层：邻图 overlay 材质换 `(1,1,1,0)` 克隆（`NeighborGlowOnlyMaterial`，纯 glow/roof 数据层，全图照画）；天色由当前图 overlay（自己方形内，含 void 透明圈）+ 全零顶点 quad（`CollectSkyTintRects`，邻图方形 − 当前图方形的 L 形 ≤4 矩形，配共享材质）各管一块——任意像素天色恰好一层。重叠区两图 glow 叠加 = 跨缝照明（物理合理，接受）；void 格无 roof，岩顶不会双份。fog 层重叠双叠未处理（症状弱，观察项）。跨天气域邻居的天色跟当前图走（共享材质限制，近似，观察项）。
- **零邻居分支也必须清色**（曾经的历史 bug）：`cachedNeighbors.Count == 0` 时不能提前 return——否则清色被短路，void 带保留上一帧像素成红色拖影（新档首图生成/孤岛地块/Remove All Tile Maps/读档邻居未再生成四种场景）。零邻居时执行"清色-only"帧（不画邻居 mesh），同时 `commandBuffer.Clear()` 防邻居全部卸载后旧 buffer 逐帧重放已 Dispose 地图的 DrawMesh。
- **2026-09-08 void 岩参考覆盖说明**：下条 link 的挂点、范围和 3×3 判据继续有效；数据源已改为“邻图存在时读邻图当前格，当前格为 null/void 时回落邻图基础快照；邻图不存在或已封存时读本图基础快照作自然延续假设”。不再读取条带数据，邻图后来开采会影响后生成地图的 link 判定。
- **void 边界岩隐形 link 延续体（2026-08 视觉优化，`SeamlessVoidRockLink` + `RimExodus_VoidRockLink` def，勿删）**：自然岩 atlas 选片由 `Graphic_Linked.LinkedDrawMatFrom` 查本图 4 邻 `LinkGrid.LinkFlagsAt` 决定——void 格岩石被 389 清掉 → flag None → 边界岩画断崖侧面，即使对侧邻图对应格有岩石（应连续）。修复 = **genStep 392 末（混合之后、快照捕获之后）**（`GenStep_SeamOverride.Generate` 调 `PlaceAfterSeamOverride`——**必须 392 末勿移 389**：紧贴 void 的那圈接缝带岩石多数是 392 混合 B 照抄 `SyncRockBuildingTo` 才 spawn 的（原生无岩），389 时点"4 邻含岩石"条件全灭一格都铺不出，2026-08 实测；own snapshot 非序列化但生成期仍在内存可读，双分支判据不受挂点影响）在 void 格上 spawn `RimExodus_VoidRockLink`（Ethereal + drawerType=None 不渲染、graphicData.linkFlags=Rock——`LinkGrid.Notify_LinkerCreatedOrDestroyed` 读的是 def.graphicData.linkFlags 原始字段不触发图形初始化；Spawn 自动以 regenAdjacentCells:true 脏化邻格 Things mesh，无需手工脏标记；预览线程早退）。铺设规则（用户逻辑 2026-08 四条闭环，类注释为权威展开）：**范围 = 邻接接缝带的 void（OuterStripDepth==1 圈，距缝切比雪夫 2），无本地岩石邻接过滤**；**判据 = 参考源镜像格 3×3（切比雪夫 1，GenAdj.AdjacentCellsAndInside）窗口有岩**——归属邻居已生成 → 对端条带快照 building 层镜像格 a = c − offset（不内移）；未生成 → **own baseBuildingSnapshot** 镜像位 3×3（本侧被截断的 rock 只能按本侧 snapshot 假设延续）；**复刻闭环**：own-snapshot 假设由 SeamOverride 照抄区（B ∪ {T·1}）承担——对端在 T·1 字面照抄本侧原生岩体，先生成侧假设成真（单向原则，勿回退为回铺——首版 propagate 方案已被否决）。已知限制（用户接受）：分支假设与后来实际生成的对端不一致、或对侧岩石后被开采 → 该处仍显示连续（视觉滞后，无功能影响），不追对端动态变化。

### 边缘行为统一（阶段4b）
- **exit grid 拓宽到 Band 全宽 + 接缝中心线勾勒（2026-08 视觉优化两轮，现状勿回退）**：`Patches_ExitMapGrid` Prefix 重建把标记集从"传送圈传送点格"拓宽为**接缝带三圈并集 Band ∪ 传送点格兜底**（`IsExitCell` 覆盖带内圈——带撤离 flag 的 pawn 踩带内圈即原生离场，用户定夺：接缝两侧的撤离带在 Band 全宽上镜像连续，切图不跳变；出口查找类逻辑不经 ExitMapGrid 不受影响）。**渲染保持原版单色浅绿**（原版 `CellBoolDrawer` 画满 Band）——首版三色分圈方案（带内蓝绿/离散边绿/带外黄 + `GetCellBool` 谓词收紧 + 两个自建 drawer）因颜色过浅缺乏区分度被用户实测否决回退，勿再做分色。**接缝位置改由中心线段勾勒**：`Patch_MapInterface_SeamOutline`（Postfix 挂 `MapInterface.MapInterfaceUpdate`，自带 CurrentMap/DrawingMap/截图模式门控）每帧沿 `BuildPolygonVertices` 的多边形边（连续边 = 接缝带的几何中心线）`GenDraw.DrawLineBetween` 画 6 条绿色线段（与原版 `GenDraw.DrawMapBoundaryLines` 同款基础设施/同款 MetaOverlays 高度，Graphics.DrawMesh 延迟提交零自绘 mesh）；两侧线段即中点对齐的视觉基准，对不齐 = 地块投影角度细微偏差（正常，不是 bug）。**2026-09-06 注**：Band 全宽标记不再吃掉撤离首跳——持 Evacuation 许可、目标为传送点的 pawn 由 `Patch_JobDriver_Goto_TryExitMap_EvacuationDefer` 推迟 pre-tick 离场到 spot（见"跨图边界行为"节 NPC 离场链全景），拓宽保留为无许可/重定向失败情形的安全网。
- 根原语 patch：`Patches_CellFinder`（TryFindRandomEdgeCellWith **两个重载**——4 参随机重载 + 5 参 Rot4 方向重载（`Patch_CellFinder_TryFindRandomEdgeCellWithRot4`，手动绑定，候选池 = 该方向半平面的接缝格，中心线含等号使对角边格被两方向共享，近似原版"方形整条边"宽覆盖语义；dir 无效退化为 4 参不滤）、RandomEdgeCell）、`Patches_Reachability`（CanReachMapEdge）、`Patches_RCellFinder`（自构方形边的三个出口方法）。覆盖袭击入口/远行队出口/撤退/行人穿越。**5 参重载勿再回退（2026-08 两轮教训）**：组队**路线规划** `Notify_ChoseRoute → BestExitTileToGoTo → AvailableExitTilesAt`（每相邻世界 tile 按 45° 扇区算 primary/secondary 方向各调一次 5 参）与 Send 兜底 `TryFindExitSpot`（严格档）都走该重载；原版候选格钉死方形边（RandomEdgeCell(dir) + 穷举 GetEdgeCells(dir)），裁切图上六方向全败 → 0 tiles → startingTile 无效 → Send 时 `CheckForErrors` 弹 "MessageNoValidExitTile"（"你的远行队无法离开此区域。确保有任一方向可抵达"——与 TryFindClosestEdgeCellTo 无关，CheckForErrors 失败即 return 走不到 Send 主门）。首版落地后曾据"未倾斜图方向枚举正常（4/6 tiles）"回退为纯诊断——**那只是六边形角点恰好轴对齐（顶点朝向来自世界球面，θ≡0 mod 30° 才触方形边）时原版靠接触格侥幸通过**，倾斜图方形边整圈 void 必然全灭（2026-08 实测复现后重新落地）。validator 原样调用（含 `!x.Fogged`——未侦察方向出口不可用，揭雾分径保证来向侧可用，用户定夺）。诊断残留：`Patches_CaravanExitDiagnostics`（AvailableExitTilesAt 计数，verbose）+ 各出口 patch 失败日志（DescribeEnterSpots 统计）+ 5 参 Prefix 自带的 seam pool exhausted 统计。观察项：`RCellFinder.TryFindTravelDestFrom` 把方形边条件内嵌 validator（4 参链），靠两级距离兜底（LengthHorizontalSquared>10000/2500）在接缝池下仍可用，未单独 patch；`TryFindExitSpot` 宽松档自枚举方形边，仅在 TryFindClosestEdgeCellTo 失败（有 Standable 传送点即成功）时触达。
- **区域触边判定接缝化（2026-08，`Patches_RegionMaker.cs`，勿删）**：`Region.touchesMapEdge` 的唯一计算点 = `RegionMaker.AddCell`（原版仅方形边格置位），经 `District.AddRegion` 计数流到全部消费者（District/Room.TouchesMapEdge）。Postfix 置位判据两路（任一）：区域含传送圈格（IsSeamEdgeCell）或 **4 邻格含 void 地形**（最外圈实地形——void=389 已铺而传送点 1490 才铺，只有 void 邻接能让**生成期**消费者也拿到正确触边）= "地图边缘"对区域系统整体变为六边形边，天然复用原版缓存；Region/District 不序列化（读档全量重建）无旧档陈旧旗标。修复面：远行队打包点 `TryFindRandomSpotJustOutsideColony` FinalValidator（district.TouchesMapEdge——2026-08 "未发现有效的打包点"根因，倾斜图上无任何区域触方形边 → 恒 false）、袭击/野生动物入口 `TryFindRandomPawnEntryCell` validator（同款条件——**修复前倾斜图上 WildAnimalSpawner 静默死亡（永不刷野生动物）、袭击 EdgeWalkIn 选点失败**）、`CellFinderLoose.TryFindCentralCell`（FindPlayerStartSpot——392 预设仍是主链权威，本 patch 只让它自然通过）、GenStep_Animals 的 `RandomAnimalSpawnCell_MapGen`（修复前倾斜图 validator 结构性全败 → fallback `RandomCell` 零校验 + `RandomClosewalkCellNear` 找不到返回 center 本身 → **初始野生动物偶现生成在 void 上**——方形角部距六边形 ≈69 格 > 搜索半径 45，随机格落进角部 void 区即复现，落进六边形内无症状，偶现来源）。语义辐射（接受）：动物栏舍触及接缝圈/void 边 = 视为开放、外圈房间不自动建顶、外圈动物尸体按时消失；无 void 与传送点的图两判据恒 false = 原版零改动。**已知噪音（定性，不修）**：飞鸟（tryStartFlying 族）飞行落点不查可走性（原版行为，原版水面同理），落在 void 上被原版 StartPath 传送自愈 + `Pawn_FlightTracker.Notify_JobStarted` 单次 NRE（CanEverFly 内联段，鸟下一 tick 自动恢复；2026-08 两轮实测均见）——RegionMaker 修复使倾斜图野生动物刷新复活后此噪音会被更多鸟放大，非缺陷。
- `SeamlessEdgeCells`：接缝带格统一入口（复用传送点格集合 = 传送圈的 Standable 格）。
- **沉浸模式开关 `seamExitBandEnabled`（2026-08，默认开，勿回退门控点）**：PlaySettings 全局控制条 toggle（`Patch_PlaySettings_SeamBandToggle` Postfix `DoPlaySettingsGlobalControls`，仅地图视图 + 有接缝的图显示；状态存 ModSettings）。关闭 = 沉浸模式（录视频/截图，用户定夺"隐藏后误触撤离比看不见边线严重"）：①绿带与中心划线隐藏（`Patch_ExitMapGrid_HideInImmersiveMode` Prefix 跳过 `ExitMapGridUpdate` 绘制 + SeamOutline early-return）——**只藏视觉不废功能**（IsExitCell 照常，NPC 离场链依赖它，勿改为关 MapUsesExitGrid——那会连逃窜动物/离场访客的出口一起废掉）；②玩家征召 job 的 `exitMapOnArrival` 在 StartJob Prefix 清除（只清 playerForced，NPC 撤离 grant 链不门控——门控会造成追兵消失/动物滞留）；③组队出口重定向 `TryFindClosestEdgeCellTo` 放行原版（裁切图方形边全 void → 组队报错，属预期，此模式不为正常游玩设计）。跨缝步行/跨图下令/传送零影响。
- **CheckPawnGoto 撤离意图排除 = 整个接缝带 Band（2026-08 修正，勿回退为仅 spot 格）**：exit grid 标记集是 Band 三圈 ∪ 传送点，排除集必须同口径——否则征召 goto 带内圈非传送点格会白预加载一张永远不会被进入的对侧图（pawn 到站原生离场成队）。机械族原版例外（IsColonyMech 不设 flag、站原地）保持不动（用户定夺：机械族远行队走大地图组队模式，防其自行离场成独立远行队）。
- `TryFindClosestEdgeCellTo`（`Patches_RCellFinder`）出口可达性口径（2026-08 补）：原版本体 = 从 root 区域 BFS（PassDoors）到可达边缘区域——保证出口可达；patch 同口径过滤（`FindNearestEnterSpot` 新增 `requireReachableFromRoot`，`TraverseParms.For(TraverseMode.PassDoors)` 从 root CanReach）。出口格是 pawn 要实际走到并 ExitMap 的终点，无可达过滤会选到隔河/隔山传送点 → 集结卡路径 + 打包点搜索（CanReach(exitSpot→打包点)）连带失败。

### 跨图交互架构（2026-08 二次重构：点击重放 + 公共函数层，VMF 同构——用户定夺，勿回退为"GetOptions 整方法接管 + 到处硬 patch"）
目标行为：点击本图区域 = 完全原生（所有 patch 同图早退零介入）；点击本图渲染的邻图区域 = 拦截点击在**真邻图上重放**拿原生选项与命令，公共函数（可达性/选位/下令/距离）跨图化。六层：
- **L0 命中判定**：`SeamlessMapUtility.TryResolveMapPosition`（多边形归属，`TryGetOwnerNeighbor`）——渲染与拾取共用邻居表 offset；休眠邻居不渲染也不解析。
- **L1 点击重放（`Patches_ClickReplay`）**：①`FloatMenuContext` ctor **Prefix(ref clickPosition, ref map)**——点击解析到活跃邻图 → 坐标改写邻图局部 + map 换真邻图 + 开重放槽（`SeamlessReplayContext`，生命周期 = 整个 GetOptions：ctor Prefix 开 / GetOptions Finalizer 关）；ctor **Finalizer** 为"站在本图的选中 pawn"登记命令目标（`SeamlessCommandTargets` CWT 侧表，VMF TargetMapUtility 同构；**本图菜单则清除残留登记**——生命周期 = 每次 GetOptions 重置，防陈旧登记误导后续本图命令）；ctor **Transpiler** 把体内 `GenUI.ThingsUnderMouse` → `SeamlessGenUI.ThingsUnderMouseReplay` 垫片（重放激活 → 忠实镜像 `SeamlessGenUI.ThingsUnderMouse`：显式 Map + 鼠标等价点参数、逐语句复刻原版含两个私有比较器；否则转调原版，本图零变化）。②`GetOptions` **Transpiler** 两处 `GenGrid.InBounds` 调用点重定向（原始点击位 → "本图界内或可解析邻图"；ClickedCell → 重放目标图界内）。③`ShouldGenerateFloatMenuForPawn` Prefix 放宽"必须同图"（活跃邻图 pawn 允许，其余判定复刻原版）。④`FloatMenuMap.StillValid` **Transpiler**：`get_PositionHeld` → `PositionHeldForRevalidate`（邻图目标转宿主系，开菜单重验证一致）。**transpiler 纪律**：全部锚点 = 调用指令的方法句柄（`Calls()` 匹配 call/callvirt），替换时 **opcode 必须归一为 Call**（实例 getter 的 callvirt + 静态替换目标 = 非法 IL，2026-08 实测 MissingMethodException）。
- **L2 原生评估（零 patch）**：52 provider 原生在真邻图上产出——移动 GoHere/攻击（含狩猎/多选/auto-take）/开采/砍伐/搬运/驯服全原生选项与禁用态。
- **L3 公共函数层（`Patches_CrossMapCommon` + `Patches_ReachabilityCrossMap`）**：①**CanReach 真实化**——下令语境（重放帧内 / pawn 的 CommandTarget 指向目标图）给真实答案 `CanBridgeTo`；NPC/自动评估保持安静 false（跨图执行能力仅玩家下令链享有，防 NPC 拿"可达"却走不过去；防递归：CanBridgeTo 内部走 pawn 所在图同图原生查询）。②`RCellFinder.BestOrderedGotoDestNear` **虚拟传送括号**（`SeamlessVirtualTeleporter`：FieldRef 直写 `Thing.mapIndexOrState` + `positionInt`（Position 属性的**后备字段**），Prefix 传到邻图跑原版选位 / Postfix 恢复 + 登记队形格；**勿回退为 `thing.Position = ...` 属性赋值（2026-08 修复 NRE 根因）**——Position setter 带完整网格簿记（region lister/thingGrid 注销+重注册），先切 mapIndex 再走 setter 会在错误地图上注销/注册，两张图各泄漏陈旧条目，pawn 后续 DeSpawn 清不掉，残留为"未生成无 holder"后任何 ClosestThingReachable BFS 扫到即 `ThingFromRegionListerReachable` NRE；直写 positionInt 才真正做到评估窗口零写入。曾有 null 守卫 patch 作诊断兜底，用户定夺删除——patch 越多越不稳定，根因已修勿再加回）——root 框架歧义启发式消解："邻图界内且距命令格 ≤5" = 已邻图框架（DraftedMove 单选 action 的 curLoc），否则 −offset 转换（控制器插值根点，StartInteraction 已归一宿主系）。③`PawnGotoAction` Prefix——命令登记指向邻图 → 以 gotoLoc（邻图框架队形格）桥接，**队形跨缝逐 pawn 保留**（每 pawn 各自终点）；跳过原生消灭误读 IsExitCell 置 exitMapOnArrival。④`MultiPawnGotoController`：`StartInteraction` Postfix 锚点 +offset 归一回宿主系（与 ProcessInputEvents 的 end 框架一致——根治混框架插值乱坐标）；Draw/OnGUI Prefix 复刻（邻图格 +offset 绘制、迷雾按归属图、**材质惰性反射——勿在 Prepare/静态构造期反射，会过早触发 MultiPawnGotoController 的 StaticConstructorOnStartup 链（ShaderDatabase/MaterialPool 未就绪）**）；**原生挂起-释放流程零拦截**（多选右键 → 原生 HandleMultiselectGoto → 控制器 → 释放 → 原生 IssueGotoJobs → 被 ③ 逐 pawn 桥接）。⑤`Pawn_PathFollower.StartPath` Prefix——playerForced job 的路径目标在邻图（Thing 目标帧无歧义 / Cell 目标匹配命令登记格）→ `TryBridgeJob(nextJob: curJob)` 跨图包装，传送后 `ContinueBridgeMove` 直接续跑原 job——**一切点击下令 job 通用**（近战/开采/砍伐/搬运/驯服…）；job 不克隆（点击下令 job 无累积状态，MakeNewToils 从头跑）；桥接失败放行原生（本图坐标失败 = 不可达表现）。**NPC 战斗 job 扩展（2026-08 原版两层模型，勿回退为"仅 playerForced"）**：`job.def == AttackMelee/AttackStatic`（战斗 job 目标跨图——近战追击直桥）或结构判据「非 playerForced 的 Goto 且 targetA Thing 在邻图」（GotoNearestHostile 跨图推进的自下发 Goto，见"索敌"节）同样桥接；NPC 战斗 job 不查 `IsCrossMapOrderable`（敌对 NPC 非可下令主体但战斗推进合法）；其余 NPC job（撤离/追击）走既有 Grant 链。**dutyTag 自标识教训（2026-08 实证，勿复用）**：think tree 产出的 job 在 `ThinkNode_Duty.TryIssueJobPackage`（反编译 ThinkNode_Duty.cs:24-29）被无条件覆写 `job.dutyTag = duty.tag`（AssaultColony 等 duty 无 tag → null）——旧判据 `dutyTag == NpcApproachTag` 在 StartJob 前就失效，**推进 job 从未被桥接过**（症状：`Cross-map approach` 日志后无任何 `Bridge issued`/`Cross-map move rejected`，敌人在本图坐标上走向目标本地格的镜像数字格——方向都不对）。TransitTag 幸存只因其 Goto 走 `StartJob` 直发不经 think tree。**job.dutyTag 不能作为 think-tree job 的自标识通道**。
- **L4 执行（保留）**：TryBridgeJob / TryFindBestBridgeSpot（双侧代价场联合最优，单跳——用户定夺不做多跳）/ SeamlessPathCostField / TransferGrants（Grant.NextJob 扩展）/ TransferTrigger / 传送链。
- **L5 战斗簇（保留 + 门限放宽）**：见"跨图索敌与射击"节。
- **命令适配边界（2026-08 审计定稿，权威矩阵 = `doc/跨图命令适配审计.md`）**：点击下令的 job 分三类——直接目标型（~35 provider，选项判定读目标本身，重放+公共层天然可用，含 Reserve/FailOn/重启幂等等执行期时序证明）；designation 前置型（9 组：Mine/CutPlant/Harvest/Deconstruct/Open/Flick/Hunt/Tame/Slaughter/Sow + Haul/DoBill/Build——选项判定读 `pawn.Map` 的标记/列表全局状态，跨图选项不出现，**挖矿族暂不适配系用户定夺**，修复方向三档见文档）；split-target 型（6 个 provider 的 targetB 搜在本图，修复点在 targetB 搜索侧）。玩家下令路径恰好两条互斥入口（PawnGotoAction 桥接 / StartPath 包装），NPC 链独立成链，无冗余实现。

- **跨图选点 = 双侧代价场联合最优（2026-08，跨图 A* 仅单跳——用户定夺，`doc/第二阶段-最小技术原型.md` 的"不做多跳地图图"约束继续有效）**：`SeamlessCrossMapOrders.TryFindBestBridgeSpot` 对候选 spot 取 C1（本图 pawn→spot 代价场）+ C2（对图 dest→落点代价场）总和最小者（平手取 C1 小；无有限 total 回退最小 C1，保持旧"走到最近可达点后停下"降级）——单跳总代价在过缝点处可分解，等价两图缝零代价边后的全局最优过缝点。代价场 = `SeamlessPathCostField`（Dijkstra；口径对齐原版 PathFinderJob 基础层：PathGrid 缓存成本 + pawn 移动 tick 步进 + 对角切角，**刻意不复刻逐请求层**——关门/挡路 pawn/水/fence 只影响选点近似精度，段内执行仍原版 A*）；落点不可走的 spot 自然得 ∞ 被避开（旧版要到传送瞬间才发现）。`CanBridgeTo` 菜单热路径无目标格，仍用 `TryFindNearestReachableBridgeSpot` 轻量最近可达探测，不跑场。
- `SeamlessSelectionTracker`：跨切图的选中保持集（解决切图 ClearSelection 丢选中；Register 移至点击重放 ctor Finalizer）。
- `SeamlessCameraFocus`：首个殖民者跨图自动聚焦 + 无感相机（切图后用 SetRootPosAndSize 同时恢复位置+缩放）。

### 跨图边界行为（阶段5，传送许可登记制，权威规格 = `doc/边界行为表.md`）
- **NPC 离场链全景（2026-09 调研定案，"caravans stuck on the border"用户报告排查基础）**：访客（LordJob_VisitColony）/商队（LordJob_TradeWithColony，trader duty `ExitMapBestAndDefendSelf`、carriers `Follow(trader)`、牲畜 `Escort`）/旅行者/袭击者撤退**共用同一条撤离链**——`JobGiver_ExitMap*` → `RCellFinder.TryFindBestExitSpot/TryFindRandomExitSpot`（我方 patch → 最近 Standable+可达传送点，找不到放行原版 = 裁切图方形边结构性全败 → null job → 站桩）→ `Goto + exitMapOnArrival` → StartJob 登记 Evacuation（**不分 lord 类型**）→ 踩点分流（对端已加载→传送续链 / 未加载→pre-tick IsExitCell→`Pawn.ExitMap`）。原版事实：**NPC（非玩家阵营）`Pawn.ExitMap` 不重建 caravan**（`FindCaravanToJoinFor` 对非玩家阵营恒 null → despawn + PassToWorld 变 world pawn 由 GC 清）——"商队走出门散在世界里"即原版语义，跨图传送不欠任何 caravan 重建债。**实测主路径 = 带内圈原生离场（2026-09-01 用户 verbose 日志定案，勿误判为传送链失效 bug；2026-09-06 起已被首跳修复取代，见本条末）**：exit grid 拓宽到 Band 全宽（含带内圈）使 `JobDriver_Goto` pre-tick `IsExitCell` 在撤离者刚进带内圈时即原生 despawn——从图内出发的撤离者结构性到不了传送圈（Band 外两圈）spot 格，"传送续链"分支仅对**已在传送圈站位的 pawn**（战斗追击踩点 Pursue、游荡 StrayNpcs 转化）触达。用户实测"对侧堵死商队仍正常撤离"正是此机制（商队从不过缝）。**成因未定论（用户定夺 2026-09-01：证据不足暂不改行为，只保留诊断日志）**：Steam 报告若在当前版本这条路径不会卡（带内圈兜底），嫌疑 = 玩家远行队组队 CheckArrived 等待 / Band 拓宽前旧版（当时 exit cell 只在传送圈，spot 被堵即卡，拓宽可能已顺带修复）。**卡点候选与 verbose 插桩（2026-09，纯日志零行为改动，标签统一 `[caravan-exit]`）**：A 出口找不到（passthrough 日志原有）/ B 传送拒绝后 SelfDriven 续程无离场兜底（flag 驱动有 pre-tick 兜底——`Evacuation transfer rejected` 日志区分两者）/ C 落点隔离（TryFindEvacuationExit 三级全灭——`Evacuation chain stuck` + `Evacuation exit search failed` 候选统计）/ D 玩家组队 `CheckArrived` 等待（每 100t 要求全员距 exitSpot 10 格内且可达——`Caravan gathering waiting` 记录未满足成员，600t/lord 节流）/ E HandleBoundTransfer 对端卸载作废（TransitGoto 走到头站住，非战斗体不进 StrayNpcs 宽限）。终点确认 = `Pawn.ExitMap` Prefix 诊断（一切原生离场 despawn 单点，用 Prefix 非 Postfix——despawn 后 Map/lord 已清空）。验证器基线 86。**首跳带内圈提前离场修复（2026-09-06，用户报告"敌人撤退时消失在两边都有地图的接缝上"；撤离链全体成员适用——用户定夺）**：`Patch_JobDriver_Goto_TryExitMap_EvacuationDefer`（`Source/Transfer/Patches_EvacuationExitDefer.cs`，挂 `JobDriver_Goto.TryExitMap`（private，pre-tick 与到站检查唯一汇聚点））Prefix 对【Evacuation 许可 + job 目标是传送点格 + 人未站在传送点上】的 pawn 推迟原生离场——首跳撤离者（袭击者撤退/访客/商队/越狱囚犯/恐慌动物）从此真正走到 spot 分派：对端已加载（含降频）→ 传送续链（双图缝可见跨缝，主计划理想场景"敌人过缝后玩家仍能看到其继续逃跑"对活跃对端成立）；未加载/休眠 → 在缝线上原生离场（Q4 不唤醒不变，仅离场点从带内圈挪到缝线）。安全网勿删：无许可/非 Evacuation（玩家 pawn 不登记、机械族无 flag）放行；已站在 spot 上放行（tier0 交接与"传送被拒后 flag job 到站兜底"依赖）；目标非传送点（重定向失败的原版边格兜底）放行；6000t 许可超时兜底。已知小边界（接受）：撤离 job 下发时人恰好已站在 spot 上 → 原地缝上离场不做即席分派；`JobDriver_ExitMapFlying`（飞行离场）不经 JobDriver_Goto 维持原版（观察项）。与威胁保活正向协同：撤退敌人=活跃威胁，其所在图保活全速。
- **判定收拢原则**：传送资格不在踩点时判定——状态变更事件处登记许可（Grant）并绑定传送点，踩点热路径（`SeamlessMapTransferTrigger.TryTriggerTransfer`）只做一次字典查询 + 匹配分派。**无许可不传**（闲逛/工作/无 flag 逃跑一律无事是结构性结果）。
- `SeamlessTransferGrants`（静态登记表 `Dictionary<Pawn,Grant>`）：Kind = Bridge（玩家跨图 goto）/ Evacuation（NPC 撤离链）/ Pursue（跨图追击）/ Follow（跟随跨图）。`TransitTag`（"RimExodus.Transit"）写在我们下发 Goto 的 `job.dutyTag` 上（干净可序列化字段，无引擎消费者）——StartJob 钩子据此识别"许可驱动 job"，不当 job 替换清除许可。
- 三个登记点：① `Patches_Job` StartJob Prefix——`exitMapOnArrival && !playerForced`（撤离 duty/囚犯越狱/野性恐慌/释放访客）→ Evacuation 许可；任何非 TransitTag job 启动 → 旧许可清除（清理先于登记，天然处理 job 重发/队列复用）。playerForced 精确区分玩家征召 goto（原版 5 个 flag 赋值点中只有 DraftedMove 走 TryTakeOrderedJob 设 true）→ 玩家撤离不登记，踩传送点走原生撤离/组队。② `SeamlessCrossMapOrders.TryBridgeJob`——Bridge 许可（绑定 exitSpot + 携带最终目的地，吸收已删除的 SeamlessCrossMapPendingDestinations）+ TransitTag Goto。③ `NotifyPawnTransferred`（传送完成事件）——撤离链续程 + 追击者/跟随者扫描（目标/主人刚跨图的瞬间事件标记，跨图意图由事件本身保证，无需踩点时判意图）。
- **撤离链方向性（防回弹/横跳）**：`TryFindEvacuationExit` 三级候选——未访问边且对端未加载（到达即原生离场，链终止最快）> 未访问边（链继续）> 无过滤（只剩已访问边，ForceExit 踩点直接 `pawn.ExitMap`）。VisitedTiles 跨 hop 传递（含来向 tile）。**续程 job 不带 exitMapOnArrival**（传送落点本身常是出口格，flag job 的 pre-tick IsExitCell 检查会在落地瞬间原生离场，破坏"继续跑"）；Evacuation 首跳许可不绑具体点（首个踩到的传送点：对端已加载→传送续链，未生成→交还原版 JobDriver 原生撤离）。
- 追击扫描：出发地图上正在追击跨图者的 NPC 战斗体（`SeamlessBoundaryRules.IsNpcCombatant`：人形/机械族非玩家阵营，覆盖敌人+盟友）→ 借同一传送点传送追击。**意图判据（2026-08 扩展，勿回退为仅 AttackMelee）**：①AttackMelee/AttackStatic job 目标=跨图者（交战瞬间）；②非 playerForced Goto 的 targetA=跨图者（**GotoNearestHostile 推进期——原版此阶段不设 mindState.enemyTarget**，2026-08 补：只认 Attack 系/③会漏掉正在赶路的推进者，"殖民者返回后无人追击"的帮凶）；③`mindState.enemyTarget`=跨图者（AIFightEnemy 走位 Goto 期间持续有效——其下发者已写 enemyTarget）。**远程统一门槛**：`TryGetAttackVerb`+`CanHitTarget`（走我们的跨图射击线）能打 → 留原地继续跨缝射，打不着才传送。狂猎动物不属战斗体（行为表无此行）。
- **战斗踩点资格（2026-08 阶段5，行为表行 15/23/31 落地）**：`SeamlessMapTransferTrigger.TryRegisterCombatStepTransfer`——无许可 pawn 踩传送点时，NPC 战斗体 + 战斗 job（AttackMelee/AttackStatic/战斗 lord duty（AssaultColony/AssaultThing/Defend/DefendBase/PrisonerAssaultColony）下的 Goto）+ **追击目标在对端图**（job.targetA 或 mindState.enemyTarget 的 Map）→ 即席 Pursue 许可传送；对端未生成不登记（不触发生成）。"目标在对端"意图判据防止沿接缝带走位的袭击者被误甩过缝。
- 跟随扫描：出发地图上 `Follow/FollowClose` 目标=刚跨图者 → 跟随传送（驯养动物 + NPC 随从统一覆盖，防商队过缝解体）。
- **追击 lord 接管（2026-08 修复"追击者传送后消失"，勿删）**：`TryTransferPawn` 剥离原 lord 后，落地敌对追击者是"无 lord 敌对 pawn"——原版 think tree 的 `JobGiver_ExitMap` 分支会在下一 think tick 直接给它离场 job（堆栈实测），离场点被 Patches_RCellFinder 重定向到脚下传送点格 → 原生离图 = 玩家视角"传送后消失"。修复 = `NotifyPawnTransferred` Pursue 分支对敌对 pawn 立即并入/新建 `LordJob_AssaultColony`（canKidnap/canSteal 关、canTimeoutOrFlee 开；600 ticks 内同图同阵营复用同一 lord 编队）——原版袭击 AI 完整接管追杀，目标再跨图时 MarkPursuers 的 job 判据（AttackMelee/AttackStatic 来自 lord duty）仍成立，追击链跨多图延续。盟友追击兵不建（敌对性不成立），走游荡宽限。**Bridge 落地同款收编（2026-08 补，勿删）**：跨图推进（GotoNearestHostile 桥接）落地的 NPC 战斗体与 Pursue 落地同构——`TryTransferPawn` 同样剥离原 lord，不收编则续跑 job 过期后 JobGiver_ExitMap 直接离场（"过缝后秒退"）；`NotifyPawnTransferred` Bridge 分支对 `IsNpcCombatant` 先 `TryAttachAssaultLord`、失败登记游荡宽限，再 `ContinueBridgeMove`。玩家方桥接者（殖民者/机械族/殖民地动物）不属 NPC 战斗体不受影响；狂猎动物靠 mental state 驱动，天然不进此路径。
- **lord 跨图续接（2026-08 修复"叛乱奴隶跨图追击后不再叛乱"，勿删）**：行为状态跨图分层——**mental state**（发狂行凶/精神崩溃/狂猎）是 pawn-local，传送链从不清（DeSpawn/Spawn 均不触碰 mentalStateHandler，Pawn.DeSpawn 只清 mindState.droppedWeapon），think tree 在任何图上自动按其重发 job，零逻辑天然可用（**勿加清理**）；**lord 是唯一 map-anchored 行为载体**（per-map LordManager + 构造参数/toil 焦点全是本图坐标与实体），原版对"pawn 换图"的答案就是离开 lord（Pawn.ExitMap → Notify_PawnLost）——位置锚定类（DefendBase/Staging/Party/Visit 等）跨图即失是与原版一致的正确语义，不续；仅"意图与地图无关"的 lord 忠实续接，当前唯一项 = 奴隶叛乱（`SlaveRebellionUtility.IsRebelling` ≡ lord 是 LordJob_SlaveRebellion，敌对性/SlaveIsSecure/殖民者自动反击全挂此判定——叛乱奴隶（玩家阵营）经 StartPath 战斗包装 Bridge 跨图后剥离 lord = 叛乱定义上终结，落地变回温顺奴隶即本 bug）。实现（底层收口，用户定夺勿回退为逐症状 patch）：①`TryTransferPawn` 剥离点原子捕获 `grant.PrevLordJob = prevLord?.LordJob`（一切 lord 类型、一切 pawn 统一备份；捕获与剥离同点，任何新传送入口不可能"剥了没备份"；pawn 至多一个 lord=pawn.lord 单引用，多叛奴各自捕获同一引用落地合流；方法因此改 internal——Grant 是 internal 类型）；②续接策略唯一出处 = `SeamlessTransferGrants.TryContinueLordOnArrival`（NotifyPawnTransferred Bridge 分支顶部调用，未续接走既有 AssaultColony 收编/游荡分流，续接后仍 ContinueBridgeMove 先续跑包装 job、过期由 lord duty 接管）：SlaveRebellion → 优先并入落地图存活叛乱 lord（多叛奴先后跨图合流一场、追回原图并回原叛乱；AddPawn→AddPawnInternal 自带 attackTargetsCache.UpdateTarget，无需手动刷新），否则 `LordMaker.MakeNewLord` 重建（groupUpLoc/exitPoint=落地点、sapperThingID=-1、passive 保留原展别——IsAggressiveRebellion 是 public 属性无反射；激进展计时器新图重起，26k-38k ticks 后 ExitMapFighting 撤出 = 原版超时放弃语义）；未来需忠实续接的新类型（如 PrisonBreak）在此加分支，不动下游。逃亡展叛奴经撤离链跨图不续接（lord 目标=跑出世界已由链达成，重建会双驱动）——已知边界，见边界行为表"已知边界"。
- **游荡兜底**：Pursue/Follow 落地 NPC（传送时 lord 已被 Notify_PawnLost 剥离、无 duty）若无目标会永久滞留——600 ticks 宽限内重新接战/有 lord 则解除，超时转入撤离链跑出世界（排除来向 tile）。敌对追击者已由追击 lord 接管先行解除此路径。
- 预加载排除传送点格（`SeamlessBorderPreloader.CheckPawnGoto` + `SeamlessEdgeCells.IsSeamEdgeCell` thingGrid O(1)）：征召 goto 传送点格 = 撤离意图，不触发对端生成。
- 跨图指令主体（`SeamlessBoundaryRules.IsCrossMapOrderable`）：殖民者 / 殖民地机械族（`IsColonyMech`，保留原版 `!IsColonyMech` 不自行离图例外）/ 玩家阵营驯养动物。
- 静态清扫（Grants 失效/超时（6000 ticks 兜底，正常生命周期由 StartJob 替换清理）、游荡宽限检查、`SeamlessSelectionTracker.PurgeInvalid`）**已迁至 `SeamlessDormancyGovernor.GameComponentTick`**（2026-08 软休眠：原挂"仅家园图 tick"，家园无玩家 pawn 时可休眠不再可靠；原 `SeamlessCrossMapOrders.PurgeInvalid`（pendingMenuTargets 泄漏清扫）随 pending 机制移除而删除）。

### 跨图索敌与射击（阶段5，2026-08 已实现待游戏内验证；缝交接机制——用户定夺，勿回退）

- **VGE 世界炮击兼容（2026-09-08，待游戏内回归）**：Vanilla Gravship Expanded 的 `Projectile_Artillery` 在世界炮击时先飞向发射图方形真边界，抵边才生成 `WorldObject_ArtilleryProjectile`；RimExodus 普通弹丸缝交接位于更靠内的六边形边，若先把它 respawn 到已加载邻图，原定方形边界目的地随 offset 平移后成为邻图内部格，VGE 的 `ImpactSomething` 又在 `targetTile.Valid` 时刻意 no-op，弹丸遂永久卡死。收口 = `SeamlessVGECompat.IsWorldArtilleryLeavingMap` 软反射识别【类型继承 VGE `Projectile_Artillery` + `targetTile.Valid` + targetTile != projectile.Tile】，在 `Patch_Projectile_TickInterval` 缝迁移前早退，交还 VGE 原生出图链。**勿扩大排除**：抵达目标图由 `ArtilleryUtility` 新生成的落地弹丸其 `targetTile` 默认 Invalid，必须继续正常飞行/命中；普通 VGE 本图射击与 RimExodus 跨缝直射也不得排除。拓扑症状“D 原可炮击，生成 A-D 间邻图 E 后 D 失效”正由 E 提前接管射线解释。
- **统一坐标（宿主=射手图）**：`unified = targetLocal + offset`（邻居表契约原式）。LOS/射程/弹道全在本图坐标计算；**准入门限 = 多边形可路由（2026-08 放宽，用户定夺"看得见就打得着"）：目标统一格不再要求本图方形内——逐格阻挡按归属图路由（越界格查归属图网格），仅角部双 void 越界格（无归属者且越本图方形）按透明处理（void 无物可挡）；弹丸落点 clamp 仅兜底"野 miss 散布推出两图"（合法深目标不收，缝交接平移回目标图本地坐标系）**。逐格归属统一口径 = `SeamlessTileRegistry.TryGetOwnerNeighbor`（LOS 分段与弹丸交接共用；false = 本图或角部双 void）。休眠邻居取不到战斗连接（`SeamlessCombatCoords.TryGetCombatLink`）——休眠 = 不可射也不回击。
- **分段 LOS（`SeamlessCrossMapSight`）**：Bresenham 骨架逐字段复刻 `GenSight.LineOfSight`（含半格偏移拐角规则、终点格不测试），逐格 `CanBeSeenOverFast` 按归属图分发。射击线（`TryFindShootLine`）= 统一射程（跨图无邻射豁免）+ 目标可射格（`CalcShootableCellsOf` 传目标坐标系射手格）× 射手侧候选（pawn lean 格/炮塔占格）；产出统一坐标 ShootLine（消费方：气体扫描/弹丸落点/瞄准与爆炸预览绘制——void 投射区视觉正确）。门限：仅 `Verb_LaunchProjectile` 且非近战非 flyOverhead（迫击炮类不跨缝——落点结算会错图）。
- **弹丸缝交接（勿回退为 VMF 式 transpiler 统一坐标拦截）**：VMF"弹丸全程飞基底图"前提（载具图与基底图物理重叠）在我们并排架构下不成立。①`Projectile.Launch` Postfix——跨图发射时 destination 从目标图本地平移到统一坐标（+offset；落点坐标系三分支用"到目标两种坐标系距离"判别：命中/强制 miss/掩体 miss=目标本地，野 miss（作用于我们的统一 ShootLine）=已统一；offset 幅度 ≈0.87×mapSize ≫ miss 散布，判别无歧义），clamp 进宿主方形，重算 ticksToImpact/lifetime；同时用 `ConditionalWeakTable` 标记本次跨图弹丸，`UpdateRateTicks` getter 对其从发射到销毁固定返回 1——原版非 CurrentMap 弹丸会降到 15 tick，虽每帧手画但 `DrawPos` 不更新，跨缝后呈阶梯跳动。仅实际跨图直射付费，同图弹丸保持原版。②`Projectile.TickInterval` **Prefix**（必须 Prefix：原版弹速下跨缝飞行常仅 1-3 tick，Postfix 来不及）——步起/步终格归属异图（凸性：两端皆本图 ⟺ 无越界）→ origin/destination 同 offset 平移（模长不变→飞行进度精确保持）→ DeSpawn+Spawn 邻图 → 放行原方法体在邻图原生跑完该步（拦截余段+结算全覆盖，含一步冲过缝的高速弹）。交接后拦截/命中/爆炸 100% 原生（弹着点在邻图=原生邻图爆炸，AoE 不跨缝——功能边界）。已知边界：单步越缝弹丸的本图侧子段拦截跳过（void 无物可拦）。
- **武器 gizmo 邻图点选与反馈（2026-09-08）**：原版 `GenUI.TargetsAtMouse` 只查 `Find.CurrentMap`，所以画在接缝对侧的真实 Thing 会被读成当前图 void 格。`Patch_GenUI_TargetsAtMouse_CrossMapCombat` 仅对直射 `Verb_LaunchProjectile` 生效：按 `TryResolveMapPosition` 解析活跃邻图，保留格内小数偏移并复用 `SeamlessGenUI.ThingsUnderMouse` 原版忠实镜像，返回携带真实 `Thing.Map` 的目标，后续 Validate/CanHit/OrderForceTarget 复用既有跨图射击链。原版 gizmo 的 `Verb.OrderForceTarget` 不生成 FeedbackShoot（仅右键攻击 provider 显式生成），故 `Patch_Targeter_OrderVerbForceTarget_CrossMapFeedback` 在 Targeter 已验证并确认下令的公共出口捕获一次目标，再在统一投影位置生成一次反馈；多选不重复、无效目标进不了该出口。hover 红白目标圈的独立缺口在 `Verb.DrawHighlight`：原版直接读邻图 Thing 本地坐标；Prefix 仅把本次绘制参数替换为射手图统一 cell，真实 Targeter/下令目标不变，并复用完整原版绘制。`LocalTargetInfo(cell)` 不携带 Map，故邻图空地点射刻意不伪造，当前只承诺 Thing 目标。近战/flyOverhead 与非武器 Targeter 全放行原版。
- **复合视图统一投影与邻图枪械/声音（2026-09-08）**：`SeamlessViewProjection` 是“当前图原坐标 / 活跃直接邻图 +offset / 其余拒绝”及相机视野判定的唯一公共入口，刻意不依赖 `crossMapCombatEnabled`（视觉/声音属于无缝视图，不属于战斗开关）。Fleck/Mote 重定向、邻图 pawn/projectile/RealtimeOnly 绘制与武器修正复用它。邻图 Pawn 已用投影 drawLoc 画本体，但旧 `DrawEquipmentAiming` 修正仍绕 `pawn.DrawPos` 本地支点旋转，武器被甩到错误位置；现改为绕投影后的 Pawn 支点旋转，角度仍按射手图统一弹道向量计算，保留原版材质/后坐力/自定义绘制。`SoundStarter.PlayOneShot` 公共 Prefix 仅把**当前视野内、活跃邻图、带空间位置**的 one-shot Maker 重建到 CurrentMap 投影格，完整保留 volume/pitch/test/Maintenance/DefinedParameters；镜头外、同图、Sustainer、环境循环与无位置 PlayOneShotOnCamera 均保持原版。
- **跨图施法上下文（transpiler #1）**：`Verb_LaunchProjectile.TryCastShot` 开头"目标图≠施法者图→return false"的原版硬拒绝——transpiler 把 `[get_Thing][get_Map]` 指令对（真实 IL 实测：前置是 **ldflda** currentTarget 而非 ldfld、get_Map 是 **call** 而非 callvirt——2026-08 首版按反编译源假定 [Ldarg_0][Ldfld] 四连匹配失败的实际教训，**transpiler 锚点必须对真实 DLL 解码验证，反编译源不可信**）换为 [pop 弃地址][ldarg.0][call TargetMapForCastGate(verb)]：CWT 上下文（射击线 Prefix 验证时 `MarkCrossMapCast` 登记）命中且仍互为活跃邻居时返回施法者图放行。burst 后续发不重验证但 currentTarget 不变（图匹配即有效）；目标跨缝换图→匹配失败→原版语义中断。锚点匹配失败启动即红字（版本升级可即时发现）。
- **transpiler 纪律（2026-08 血泪教训 ×2，写新 transpiler 前必读）**：①**锚点必须对真实游戏 DLL 解码验证**（PowerShell 反射读 IL 字节），反编译源的字段/属性类型与调用形态不可信——TryCastShot 栽在 ldflda vs ldfld + call vs callvirt、瞄准角栽在 focusTarg 实为 LocalTargetInfo 而非 TargetInfo；②**改写指令必须原地 mutate opcode/operand，勿用 new CodeInstruction 替换原对象**——原指令可能是分支落点，替换丢 labels → InvalidProgramException；③**Mono DMD 对方法体内调用点替换可能就是产不出合法 IL，且 CLR 验证器对此返回 OK（拦不住！）——已两案**：瞄准角 case（指令对替换，`IL_0110: call 0x00000037`）与 Stance_Warmup.InitEffects case（2026-08，get_CenterVector3 调用点 operand 替换 + opcode 归一仍 `IL_020f: call 0x00000091` InvalidProgramException，**PatchAll 整体抛异常 = mod 加载失败**）——根因均未定位。**铁律：凡是 Prefix+ref 改参 / Postfix 后修正字段 / 整方法克隆能做到的，禁止 transpile**（DrawEquipmentAiming ref 改参、FleckMaker/MoteMaker 重定向、StanceTick Postfix 修 mote 字段全是先例）；transpiler 是最后手段，且新 transpiler 上线首测必须盯启动日志的 mod 实例化行。
- **ShotReport 跨图修正（方案 D + teleport 坐标系正修，勿回退为守卫/虚拟传送目标/克隆）**：原版 `HitReportFor` 方法体的气体扫描用目标图本地格查 caster.Map——两图尺寸不同时数值越界崩溃（2026-08 实测 IndexOutOfRangeException——每发跨图射击崩掉、弹丸不生成，双方跨图射击全体哑火，表象="谁都不索敌/不开火"）。**正修（用户铁律"不搞兜底"，2026-08）= Prefix 把 caster 经 `SeamlessVirtualTeleporter` 虚拟传送到目标图坐标系**（Position − offset，clamp 界内），原版方法体（气体/掩体/距离）全程目标图系自洽；曾用的 `GasUtility.AnyGas` 越界守卫是兜底（掩错不修系），已删。Postfix 6 字段精确写回保留（distance 统一坐标 → 两因子用原版公开 API `HitFactorFromShooter`/`GetHitChanceFactor` 重算 → 掩体在目标图以平移射手格（可越界）计算 → FieldRef 写回；覆盖 teleport clamp 近似的深目标场景）。CanSee 的气体 validator（原版 lambda 硬编码搜索者图）在 `CanSeeCrossMap` 入口包 `InBounds(hostMap)` 域（我们分段 LOS 实现内的域定义；邻图段盲烟为已记录简化）。
- **索敌 = 原版两层模型（2026-08 agent 实证勘误 + 跨图复刻，用户定夺"只 patch 获取和寻路"，勿回退为逐行为处理）**：原版真相——①感知层：`AttackTargetsCache` 按阵营**全图**注册，无距离概念；②推进层：`JobGiver_AIGotoNearestHostile`（AssaultColony duty 树第⑥位 fallback）**全图无半径**扫缓存取最近敌人 + CanReach → Goto(目标本身)；有本图建筑时第⑤位 `AITrashBuildingsDistant` 优先（"有建筑优先拆"语义）；③接战层：`AIFightEnemies → BestAttackTarget(maxDist=65)`（56 是从不生效的代码默认值，Core XML 全覆写 65/72）选 enemyTarget，65 内无需 LOS，**获取不查武器射程**（射程属于 CastPositionFinder 抵近层）；④被狙不直写 enemyTarget，只重跑 think tree。跨图 patch 面四件：①`BestAttackTarget` Postfix——本图结果非空放行（**本图优先**）；为空才扫活跃邻图缓存（统一距离/敌对/威胁失效 + **"当前可射"门控**（跨图射击线）取最近；距离口径 = caller maxDist（65），**勿加武器射程过滤**——曾把 42-65 接战圈提前拒掉，2026-08 修正）。`CanSee` Prefix 分图时分段 LOS。**跨图候选不做 fog 过滤（2026-08 实测修正，勿回退）**：可见性口径跟渲染走，看得见就打得着。②`Patch_JobGiver_AIGotoNearestHostile_CrossMap` Postfix（2026-08 推进层跨图化，超射程敌人"不索敌"的真修）：原生 null（本图无敌）→ 扫活跃邻图缓存统一坐标取最近（ThreatDisabled/AutoTargetable 镜像原版；非战斗 pawn 仅认 IsCombatant——原版 LOS 替代项是本图坐标混算不复刻）+ `CanBridgeTo` 替代 CanReach → 按原版 :49-61 复刻 Goto(目标 thing)——过缝由 StartPath 包装按**结构判据**桥接（非 playerForced 的 Goto 且 targetA Thing 在邻图；dutyTag 自标识已被 ThinkNode_Duty 覆写抹除不可用，见跨图交互架构 L3-⑤ 教训），续跑 Goto 走向目标，途中进接战圈/射程被原生 AIFightEnemies 接管；"有本图建筑优先拆"自然保留。③StartPath 包装扩 NPC 战斗 job（见跨图交互架构 L3-⑤）。④`JobGiver_AIFightEnemy` 两 patch（Wait_Combat 兜底 + ShouldLoseTarget"当前可射"保持）保留——接战圈内语义。CastPositionFinder 跨图走位候选仍为观察项（推进层已覆盖接近场景）。简化边界：威胁评分/众数仲裁未复刻（距离最近决胜）；气体校验在统一格按宿主图查（邻图段盲烟漏检）。
- **渲染与菜单**：`SeamlessTileRenderer.DrawNeighborProjectiles`（邻图 `ThingRequestGroup.Projectile` 缓存列表 → DrawPos+offset → DrawNowAt，交接前后视觉坐标连续）。
- **跨图右键 = 点击重放（2026-08 二次架构重构，用户定夺，勿回退）**：交互全链见"跨图交互架构"节——选项由原生 provider 在真邻图产出（FloatMenuContext ctor 参数注入），命令由公共函数层跨图化（CanReach 真实答案 / 虚拟传送选位 / PawnGotoAction 桥接 / StartPath 包装）。历史首版"点击复现"（GetOptions 整方法接管 + 反射手填 clickedThings + 手写注入跨图 GoHere + 多层拦截 + CanReach 安静 false）**已整体删除**，完整叙事见 `doc/第五阶段`——教训：手写注入连续漏原版目标语义（多选/鼠标容差/狩猎野物——`CanTarget` 含 `NonHumanlikeOrWildMan`）；多层拦截产生框架混算乱坐标 + 2-tick 窗口失效类时序缺陷；安静 false 使"看得见的敌人"全体禁用攻击——逐个追赶原版语义不可维护，架构层（重放 + 公共函数）才是正解。UX：野物点击弹菜单选攻击（原版狩猎语义）；选项与禁用态全原生；队形跨缝逐 pawn 保留。
- **性能口径（2026-08 定稿）**：同图早退 = 各 patch 1-2 次引用比较；弹丸热路径 = 每 tick 2 次缓存多边形包含测试（`ContainsPointAt` 6 次叉积循环零分配，内圈格首次即中）；交接检测的步终格模拟复刻 TickInterval 插值；跨图专属成本（分段 LOS ≈2× 原版走线、ShotReport ≈2× 原版计算）仅实际跨缝交火时发生。结构优势：交接后原生逐格代码零介入（对比 VMF 每调用点合并层的全图常驻成本）。
- **跨图朝向与标记修正（2026-08，`Patches_CombatVisuals.cs` + `Patch_PawnRotationTracker_CrossMapFacing`）**：视觉消费者直接读跨图目标本地坐标或被同图守卫跳过。①身体朝向：`Pawn_RotationTracker.UpdateRotation` 的 `Face(focusTarg.Thing.DrawPos)`（瞄准姿态/Job 面向两来源）→ Postfix 改朝统一坐标；②枪口瞄准角：`PawnRenderUtility.DrawEquipmentAndApparelExtras` 用 `focusTarg.Thing.DrawPos`（**LocalTargetInfo 字段**，勿写 TargetInfo）求 AngleFlat → **消费端 `DrawEquipmentAiming` Prefix+ref 改参**（经 eq.holdingOwner→Pawn_EquipmentTracker 取 pawn，修正 aimAngle 与绕 pawn 回旋的 drawLoc，放行原方法零克隆——transpiler 方案已废弃，见 transpiler 纪律③）；③瞄准 pie（2026-08 修复"指示器角度不对"之一）：`GenDraw.DrawAimPie` 内部取 `target.Thing.DrawPos` 求 facing（与①②同一坐标源的第三消费者）→ Prefix 统一坐标算 facing 后直调公开的 `DrawAimPieRaw`（顺带覆盖炮塔选中 pie；原版"仅选中显示/drawAimPie 旗标"门槛在上游不动）；**③b 瞄准三 mote（2026-08 二次，"指示器角度不对"的主凶——玩家所见瞄准进度指示器主体是 aimChargeMote 进度环而非选中才显示的 pie）**：`Stance_Warmup` 的 aimCharge/aimTarget/aimLine mote 经 `focusTarg.CenterVector3`（目标图本地坐标）算方向/位置——**纯 Prefix/Postfix 方案（曾对 InitEffects 等 4 方法做调用点替换 transpiler，Mono 下 InvalidProgramException 致 mod 加载失败，已删除——见 transpiler 纪律③第二案）**：`Patch_MoteMaker_StaticMote_CrossMap`（mote 生成重定向到统一坐标+本图，兼防目标本地格越界时 GenSpawn 崩溃）+ `Patch_StanceWarmup_StanceTick_AimMotes`（StanceTick Postfix 后修正三 mote 的 exactPosition/exactRotation/UpdateTargets）。④攻击连线：`Pawn_JobTracker.DrawLinesBetweenTargets` 原版 1020 行 `Map==pawn.Map` 守卫使跨图目标**完全不画线** → Postfix 补画（起点=路径终点/自身 → 统一坐标目标；**纯白线无末端标记，与移动同款——2026-08 二次修正：曾持续画 `DrawTargetHighlightWithLayer` 造成"指示线末端红色闪烁圆圈"（CurTargetingColor 为红色脉冲材质，原版仅瞄准 UI 期使用、执行期从不画持续标记），已删勿回退**），邻图被选 pawn 的画线 Prefix 接管全 +offset 重画；⑤行走路线：`PawnPath.DrawPath` 邻图 pawn 错位 → Prefix 整体 +offset 重画；桥接行走的**最终目的地延伸段**（TransitGoto 只画到本图 spot）由 DrawLinesBetweenTargets Postfix 按 Bridge grant 的 FinalDest 补画——移动与攻击延伸段同为纯白线（原版执行期无任何末端持续标记）；⑥fleck 反馈（2026-08）：`Patch_FleckMaker_Static*_CrossMap` 双重载 Prefix——生成在活跃邻图上的 fleck 以统一坐标重生成本图（一个公共 patch 覆盖下令反馈 FeedbackShoot/Melee、弹着等全部，邻图带视觉反馈跟随当前视图可见）；PawnGotoAction 桥接成功后补 `FeedbackGoto`（对齐原生 DraftedMove.cs:119，画在统一 gotoLoc+offset）。
- **袭击外缘生成（2026-09，`Source/Combat/SeamlessRaidOuterSpawn.cs` + `Patches_RaidOuterSpawn.cs`，待游戏内回归）**：步行类袭击从"对侧未被看见的外侧接缝"进场，不再从朝向活跃邻图的边上凭空出现。资格三分支（用户规划，对被袭击图 A 的**世界网格**一跳邻居枚举 `GetTileNeighbors` 纯拓扑——**勿用地图邻居表**，未生成邻居无链接看不到）：①一跳 B 活跃且其对侧 C 无图/休眠 → B-C 缝（跨图生成）；②一跳 B 无图/休眠 → A-B 缝（同图方向约束）；③全活跃 fallback → 任意 B-C 缝；"不活跃" = 无图或 IsDormant（**降频算活跃**，与 TileGraph 口径一致）；随机选缝、逐候选验证落地。取邻居图用 `MapParentAt(PlanetTile)?.Map` 原生口径（**勿用 TryGetMapByWorldTile**——它过滤休眠图）。**执行两形态**：同图 = ambient 计划（worker Prefix 设 / Postfix 清，Begin 前防御清陈旧覆盖异常泄漏窗口）由 `Patch_CellFinder_TryFindRandomEdgeCellWith` 4 参 Prefix 消费——候选池换 `SeamlessEdgeCells.PopulateFacingCells`（spot 按 `targetWorldTile` 过滤；**spot 朝未生成邻居也预铺**[Placer 铺全部世界邻居边，hasArrival=false 不影响取格]——这是免几何锚点四件套的关键简化），**过滤池空回落整圈池**（绝不比现状易失败，保护 EdgeWalkInDarkness 等带额外谓词模式）；跨图 = `RaidStrategyWorker.MakeLords` Postfix（原版管线里生成[Arrive :113]与建 lord[:169]是分离两步，此处 pawns/parms/lord 三全）守卫（walkIn、敌对、无 controllerPawn/attackTargets、全部 pawn 在 A 上、所建 lord 全为非工兵非破墙 `LordJob_AssaultColony`——**私有 sappers/breachers 经 FieldRefAccess 读实际 job**，siege/stage/HateChant/mod 策略天然出局放行原版）后整组迁移：预放置全有或全无（任一 pawn 无处可放 = 整体放弃，A 上已建 lord 原样保留）+ 镜像 `TryTransferPawn` 簿记无 Grant（本 tick 新生成敌人无征召/选中/许可态；`Notify_PawnLost(Vanished)` 使 A 图 lord 清空自毁）+ B 上重建 AssaultColony（镜像 ImmediateAttack 的 parms 旗标 + `inSignalLeave`/`questTag`）+ `Unthrottle(B)`。**行军零新机制（勿加 Grant/自建行军 lord）**：B 上 AssaultColony duty 树第⑥位 `GotoNearestHostile` 跨图版自动推进 → StartPath 结构判据桥接 → 过缝 → `TryAttachAssaultLord` 在 A 收编——与"殖民者跨缝逃跑后袭击者追过去"同形状（阶段5 已实测）。**patch 面 4 个（BOUND OK 基线 94）**：`IncidentWorker_Raid.TryExecuteWorker` **只 patch 基类**（RaidEnemy 覆写后首行调 base[非虚直调命中基类补丁]、RaidFriendly 不覆写虚分发直达基类；HateChanters 继承 IncidentWorker 自带独立 TryExecuteWorker = 天然排除——用户定夺 v1 不做：自带"边缘分批布点+吟唱"机制且跨图难度剧增）/ `IncidentWorker_AggressiveAnimals`（1.6 名，旧 ManhunterPack；仅同图——动物非 NPC 战斗体近战索敌不跨图无法行军）/ `IncidentWorker_GhoulAttack`（仅同图；跨图列为后续增量——其 worker 内自建无超时 AssaultColony lord，迁移点不同族）/ `RaidStrategyWorker.MakeLords`（被 SiegeMechanoid/PsychicRitualSiege 覆写不命中——本就非 AssaultColony 零损失）。Prefix 时 parms.raidArrivalMode 通常未解析（TryGenerateRaidInfo 内才选）——计划不依赖它：空投族不调 4 参边缘原语 = ambient 自然不被消费，跨图迁移在 MakeLords Postfix 处再验 walkIn。**用户定夺（2026-09-05）**：宿主 B 一视同仁含原生家族 POI 图（驻军与袭击者互动/玩家借友军当打手 = 单机涌现不管）；B 上有玩家殖民者接受涌现开打（纯随机选缝不规避）；同图方向约束仅袭击类（raid 族+发狂动物+食尸鬼，访客/旅行者/野生动物迁移不动，后续增量候选）。**已知限制（接受）**：跨缝入 A 后袭击者经 TryAttachAssaultLord 收编 canKidnap/canSteal 关（与追击链一致）；`AssaultTimeBeforeGiveUp`（26k-38k ticks）从 B 建 lord 起算含行军（~3-10k）；B 无玩家 pawn 会被 governor 重新降频（移动有 MoveCostMultiplier 补偿、AI 思考减半，接缝快速区全速）；quest/mod 读 `parms.spawnCenter` 期望 A 坐标的残余不一致（spawnCenter 保持 A 侧仅朝向用途）。日志全走 Combat 模块；设置 `raidOuterSpawnEnabled`（默认开，跨图战斗 tab）。
- 设置：`crossMapCombatEnabled`（默认开）总开关，false 时战斗语义回原版。观察项：ambientSustainer 交接后音效连续性；拦截器半径跨缝覆盖时对交接后弹丸不可见；跨天气域天气精度乘子取射手图；目标退到对图深处超出射程/LOS=不追（**门限已放宽为多边形可路由——"点远处敌人不能攻击"现在应先查射程/分段 LOS/桥接可达，而非旧的方形界内门**；邻图 muzzle flash/落地 fleck 已由 FleckMaker 公共重定向可见，2026-08）。注意：原版 `GetMultiselectAttackOption` 全员失败时 `yield return null` 会在 `GetProviderOptions` 消费循环 NRE 红字（原版潜伏 bug，平时"至少一人能攻击"掩盖；跨图场景曾使全员失败成常态）——由 `Patch_DraftedAttack_MultiselectNullGuard`（Patches_CombatTargeting.cs，Postfix null→禁用"攻击"选项）修复，保留。verbose 诊断（统一清单）：`Click replay`（重放命中）、`Cross-map drafted goto`（PawnGotoAction 桥接，**final dest = 各 pawn 的邻图队形格，多 pawn 各异为正确行为——队形跨缝逐 pawn 保留**）、`Cross-map job wrap`（StartPath 包装）、`Bridge issued`、`Seamless transfer`（传送完成）、`Bridge continuation`（落地续跑原 job）、`Cross-map shot admitted`（TryCastShot 门放行）、`Cross-map target acquired` / `Cross-map scan rejected dist=N nearestRejectedDist=M ...`（索敌命中/分桶排查）、`Cross-map approach`（推进 giver 命中）、`Cross-map combat job`（Wait_Combat 兜底命中）、`Pursuit grant` / `Pursuit lord`（追击传送/lord 接管）；**离场链插桩（2026-09 "caravans stuck on the border"排查，标签统一 `[RimExodus] [caravan-exit]`——玩家日志一眼可辨商队/访客离场行为）**：`Evacuation grant: ... lord=... duty=...`（撤离登记身份——区分商队/访客/袭击者）、`Evacuation vanilla handoff`（对端未生成放行原生）、`Evacuation force-exit`（SelfDriven/ForceExit 直接离场）、`Evacuation transfer rejected ... selfDriven=...`（传送被拒，selfDriven=true 无兜底需警惕）、`{Kind} grant dropped`（对端卸载作废）、`Evacuation chain stuck ... NO reachable exit spot`（落点隔离站桩）、`Evacuation exit search failed ... standableButUnreachable=N`（三级候选统计）、`Caravan gathering waiting ... not ready`（组队集结等待明细）、`Pawn.ExitMap:`（一切原生离场终点确认）、`Evacuation exit deferred`（2026-09-06 首跳修复配套：撤离者正走向传送点，原生离场被压到 spot 上）；偶发 NRE：野生动物落不可走格被原版 teleport 后 `Pawn_FlightTracker.Notify_JobStarted` 单次 NRE（2026-08 观察到一次，鸟自动恢复，未定位是否与我方有关）。

## 当前待办（2026-08 盘点）

**本轮兼容修复回归清单（2026-08，代码已落地待游戏内验证）**：
- GL 集成：装 GL 生成地块图出现 landform（verbose 有 `GL compat: Landform.Prepare done` 行）+ 不装 GL 零变化零红字 + 据点图（原生路径）无回归 + 生成期间开世界地图预览（GL 的 MapPreview 后台线程）后地形仍正确（EnsureContextAlive 自愈，出 `GL compat: static context lost ... re-Prepare` 警告后恢复）。
- 异暗域内并集：Anomaly 异暗（或其他 GameCondition_ForceWeather）任务期跨图跑图——强制天气在全域生效（含非宿主图）、镜头切换天气不翻转；无 Anomaly 条件的常规天气零回归。
- worldTile 收口：Odyssey 空间图（gravship 轨道）共存——表面天气不受空间图干扰、无 "All weather commonalities were zero" 黄字；VMF 共存时 tile=0 别名不再污染（同装的 transpiler 冲突仍存在，见下）。
- static current：Dev 手段令生成中途失败（如注入异常）后 IsAnyGenerating 不悬挂、后续生成不被全拒；换档读档后正常生成。

**2026-08 审计定夺记录（用户确认，长期有效）**：
- 追击场景已实测通过；**"相邻地图在 Pawn 到达边界前完成预加载"降级为非目标**——理由：传送点对端未加载本来就不工作（无危害路径），且生成速度受植被数量等大幅影响、从数据角度无法保证时限。
- 空岛 mod = **预期不兼容**（调性冲突，不修）；异象天气/外太空地图经代码核实（前两条修复覆盖）。
- **As above, So below 2（astryl.asabovesobelow2）= 预期不兼容（2026-09 用户定夺）**：多层地图 mod 必须修改地图尺寸并自定义切分，与"每 tile 一张表面基础图"的无缝假设结构性冲突（不修）；多层需求导向 MultiFloors。声明走 About.xml 原生 `<incompatibleWith>`（本仓库首次启用：`ModMetaData.meta.incompatibleWith` 为 `List<string>`、只认 packageId，装对端时原版 mod 列表原生警告）——README 与工坊介绍（中英，条目已有）同步。
- designation 前置型 9 组跨图命令**维持暂缓**（逻辑差异大、硬 patch 易出事，权威 = `doc/跨图命令适配审计.md` 三档方向）；split-target 型 6 个 provider **维持未适配**（与战斗索敌无关——战斗是 A1 直接目标型已跨图化；6 个全是 targetB 搜在本图的工作/搬运类——埋葬/棺材/收容平台/出口/灭火/入库，修复点在 targetB 搜索侧）。
- CastPositionFinder 跨图走位**不做**（AI 过缝到本图后原版走位正常工作）；迫击炮不跨缝 = **预期限制**（维持）；随行囚犯/奴隶出售记为**已知限制**（非技术不可做，v1 范围，有需求再做）。
- fog 层重叠双叠观察项**降级**（揭雾分径后接缝两侧室外区已无雾，仅室内雾邻接区可能微弱重影）。
- **VMF/VF 共存已知限制（2026-08 审计，2026-08-25 随 VF v2 机制重写更新——原三条中两条已被推翻，勿按旧口径引用）**：①~~三处 transpiler 锚点互吃~~ → **已修**：`FloatMenuMakerMap.GetOptions` 的 InBounds 冲突由我方 transpiler 降 Low 优先级 + 链式接管解决（VMF 先应用、我方接管其 InBounds 调用并回落无缝解析，双装无 "[VehicleMapFramework] Error" 红字）；**残余观察项（接受）** = `FloatMenuContext` ctor 双 Prefix 叠加——坐标数值恰好落在当前图某辆带内部图 VMF 载具占位矩形内时会被 VMF 二次改写破坏重放（需"带内部图载具停缝区 + 跨缝点击其数值重叠位"才触发，极边缘，见 VF 兼容条末段）；②VMF 的 `Reachability.CanReach` 跨图 Prefix 会按它自己的地图图连通性抢答（地块图在其图中不连通 → false），我方跨缝可达评估可能被静默否决——**维持已知限制**；③~~载具无法无缝过缝~~ → **已由 VF v2 解决**（`SeamlessVehiclesCompat` 五个手动绑定 + 复刻 VF 官方进图管线三步，见"Vehicle Framework 兼容"节；v1 时"载具踩传送点=原生出口格组队离场"已废）。tile=0 别名已由 GetMapWorldTile 收口解决。**载具适配未来方向**：`VehiclePathFollower.TryEnterNextPathCell` 同构触发 + `IsCrossMapOrderable` 放宽；VF 载具 pathgrid 在 FinalizeInit 整体构建，而我们 void 在 genStep 389 已铺——预计算天然看到最终地形，"不改动其预计算数据"的前提已满足。

**跨图战斗与交互回归清单（本轮修复后待游戏内验证；本节为权威回归清单——机制事实与勿回退要点见上文架构节，叙事与决策见 `doc/第五阶段-跨图寻路与射击.md`）**：
- **推进桥接复活（2026-08 本轮修复三件：StartPath 结构判据桥接 / MarkPursuers 补推进期 Goto 意图 / Bridge 落地 NPC 战斗体收编防"过缝秒退"——根因 dutyTag 被 ThinkNode_Duty 覆写，见跨图交互架构 L3-⑤）**。预期 verbose 链：`Cross-map approach: X` → `Cross-map job wrap: X Goto` → `Bridge issued: X` → `Seamless transfer: X` → `Bridge continuation` → `Pursuit lord: X` → 对图原生接战；殖民者跨图返回时，推进中/跨缝打不着的敌人出 `Pursuit grant` 即时过缝追击。
- **玩家侧**：①点击重放——邻图空地/物体右键 → 原生菜单（GoHere/攻击含狩猎/开采/搬运等原生产出与禁用态）、本图右键零变化、菜单开着移动鼠标不误失效（StillValid 重验证）；②移动——单选跨图右键全员桥接到点击格、多选拖拽**队形逐 pawn 保留**（每人一条 `Cross-map drafted goto`、dest 各异）、拖拽预览圆圈/标签画在正确屏幕位置、同图队形零回归；③工作命令——邻图物体下开采/砍伐/搬运/驯服令 → `Cross-map job wrap` + `Bridge continuation` 续跑完成；④战斗——跨图 FireAt（放宽门限后缝对面深处目标，射程/LOS 内即射）、近战跨图（CanReach 真实答案 → StartPath 包装 → 传送 → 原生近战）、弹道视觉跨缝连续无跳变、命中/失手/掩体数字合理（对图掩体生效）、速射武器连射稳定、深目标无 IndexOutOfRange（HitReportFor teleport 正修）；⑤混合选中（本图+邻图 pawn）点任一侧；⑥命令标记——跨图移动/射击下令 = FeedbackGoto/FeedbackShoot fleck（统一坐标）+ 纯白线**无任何末端持续标记**（原版执行期语义）。
- **AI/敌人侧**：超射程敌人经 GotoNearestHostile 跨图推进 → 过缝 → 接战圈原生开火；贴缝可射敌人直接还击（`Cross-map combat job`）不瞬断；近战 AttackMelee 目标跨图直桥；袭击者"有本图建筑优先拆"语义不变；邻图敌人主动回击 + 炮塔跨缝索敌开火；追击——跨缝甩不掉近战/打不着的远程追兵（意图三判据 + 战斗踩点资格）、落地不消失（追击 lord 接管）且持续追杀/超时正常撤退、沿缝走位的袭击者不被误甩过缝（"目标在对端"判据）、撤离链/跟随/游荡兜底零回归、狂猎动物仍不追；视觉——跨图攻击连线 + 对侧目标高亮方框、身体朝向/枪口瞄准角/瞄准 pie 与三 mote 指向真实目标、桥接行走有跨缝路线线段与终点标记、邻图被选 pawn 行走路线在本视图正确位置；休眠邻图不可射也不回击；`crossMapCombatEnabled` 关闭 = 战斗语义回原版；本图战斗 AI 零回归（安静 false 语义保持）。
- 已知边界：邻图段盲烟漏检（简化）；推进 giver 对非战斗 pawn 目标仅认 IsCombatant；菜单期 CanReserve 跨图路由未做（点击下令 job 在传送后才跑 driver，预约发生在对图，理论无冲突，观察项）；未征召 pawn 的跨图选项由原生 provider 决定（诚实禁用/启用，与原版同图行为对齐）；挖矿跨图暂不适配（用户定夺，权威 = `doc/跨图命令适配审计.md`）。
- **软休眠 + 营地 + 原生家族回归**（完整清单见 `doc/地图滚动休眠.md` 第五节）：跑图 3 跳休眠/走近唤醒、休眠图邻接显示与访问入口关闭（含殖民者栏分组框）、休眠期存读档收敛、追击者不跨休眠缝、**家园不休眠不删除（2026-08 新口径：全员离家 2+ 跳入口不消失、无 SLEEP/DELETE 日志；引力引擎营地同理）**、Dev Force Delete 后重走生成链、天气域幂等守卫（同域多活跃图天气不再 N 倍速）；本轮远行队/营地项——①全员组队远行：Dev Dormancy Report 显示远行队 tile 为源、距出发图 ≥3 跳 DELETE（日志 wt=）、不再"源空全图误删"；②设立营地：接管日志（"Camp at tile ... generated as seamless tile map"）、parent 为 MapParent_SeamlessTile、六边形/传送点正常、世界地图无 AbandonedCamp；③营地"立即重组远行队"离开后图保留（≥3 跳才删）；④三类事件（跨缝传送/组队完成/远行队进图）后下一 tick 内 SLEEP/WAKE 生效；**本轮原生家族项（2026-08 新增）**——⑤远行队访问派系据点/埋伏/遗迹后离开：图不被原版解散、≥2 眠 ≥3 `Dormancy DELETE (native family)`、Settlement 删后世界图据点仍在再访重生成驻军、图存在期间可跨缝步行进出、`dormancyEnabled` 关闭恢复原版"人走即删"；**本轮 Settlement 接入项（2026-08 新增，权威清单 = `doc/地图滚动休眠.md` 第五节第 11 条）**——⑥走近据点：原生生成日志 `Settlement at tile ... generated natively`（Settlement 不再出 skip 警告）、图被裁切可跨缝进出；敌对据点正常生成无贸易商；⑦中立据点贸易商按身价选定（`trader assigned` 日志）+ **头顶问号**（原生 ShouldShowQuestionMark 链，参考 Creepjoiner）→ 右键（同图/跨缝）→ "Talk to {名字}" → 走到 → "远行者，你想要做什么？"面板 → 交易全链（买=据点 stock、卖=同行 pawn 随身物、白银随身支付、成交物掉落贸易商身边、礼物模式、恩惠货币）+ 据 gizmo 按钮——**对话选项曾在败亡 bug 期整体消失（parent 被偷换后 `as Settlement` 落空），败亡修复后需专门验证**；⑧据点图存续期读档贸易商仍在（问号仍在）；**接缝方向揭雾**：据点图（邻接生成）从邻图跨缝看——生成方向接缝洪水揭穿整个室外连通域（围墙房间留雾）、void 带无雾（跨缝视野通透），初始地图仍原版中心揭雾；雾中贸易商不可点、揭雾后可点；世界地图远行队对据点交易零回归；删后再走近重生成重选。

**历轮修复回归观察（滚动验证中，无已知失败）**：
- 偶发全雾（2026-08 修复）：连续生成/预加载多图不再全雾、无 "Found no good central spot" 红字；传送点 1490 提前后传送触发/预加载排除/exit grid/袭击入口无回归；家园图出生点=中心附近（用户已接受）。
- genStep 提前（1400/1410→389/392）：动物不落 void、密度正常、接缝混合/岩色/屋顶跨缝一致、Settlement/site 布局正常、Fog 揭雾、manhunter/mech 任务图 pawn 不落 void、耗时无感（两次全图 pathGrid 重算）。
- 矿石入 building 层快照（isNaturalRock 判据 + SyncRockBuildingTo def 替换分支）：跨缝矿脉 def/有无连续、接缝带挖投影矿出资源正常、def 替换岩色跨缝与 T_C 主导参考为矿石 def 的实机观感。
- 重开档全面回归：渲染对齐（聚焦 C 看 A 背景无系统内偏）、跨缝三层一致（含岩色岩顶）、传送往返落点、读档后生成新图接缝混合仍工作、道路（锚点贴 void 边界）、河流接缝、撤离链/追击/跟随。

**观察项（未处理，按需推进）**：
- genStep 提前新增：Harbor 桥跨缝（外条带快照不再含 400-1400 地形写入）、污染地块接缝两侧污染变体一致性、`completelyIgnoreFertility` 植物漏进 void（肥力门无效、新序无事后清理）、贴附矩形/降落平台残余缺口处"地板盖混合"（旧序相反）、MapPreview 预览链 order 变化。
- 植物层未纳入三层参考（跨缝树缺失/多余是否显眼）；顶点楔形区（B_C 多邻居命中）三图权重合成观感；旧档首次保存后确认废弃接缝 XML 节点消失及体积下降；MutatorFinal(1600) 的 GeneratePostFog SetTerrain 覆写接缝（AncientUplink/InsectMegahive）。
- Fog(1500) 对带外圈（新实地形）揭雾验证；远行队进入出生点（Patches_CaravanEnterMap）与带外圈传送圈兼容。
- 远行队进入已删除 tile 的 parent 类型缺口（营地/Camp 已修；"人走即删"族已由原生家族接管统一覆盖——见"地图生命周期"节；残余缺口仅剩非家族的罕见 def）。
- **用户报告（2026-08 游戏内发现，`doc/TODO.md`，本轮已处置待回归）**：①家园图入口因休眠消失、且到删除距离后未删——根因 = 特权判定散落（保活无家园豁免 + 删除有类型门的不对称；删除链路本身健康），修复 = `IsProtectedHome` 保活（不休眠不删除，含引力引擎营地）；②③"人走即删"地图族（据点/扎营/埋伏/遗迹）接入滚动删除 + 家园地块不因滚动丢失——由原生家族接管（`Patches_NativeMapFamily` + governor 扩权 + `SetupNativeParentMap` 接线钩子）与家园特权一并解决；④Settlement 无缝走近进入 + 贸易商（走近 skip 日志的正式解）——本轮已实现（见"地图生命周期"节 Settlement 接入条），待游戏内回归。

**性能待办**：
- **帧预算被多图组件放大（结构性，待定夺）**：`IncrementalMapGenerator.MapComponentTick` 写作"任意图驱动全局 current"，但 FillComponents 为每张图都实例化该组件——N 张活跃图 = N×8ms 预算的 TickGeneration（实测四图 74-114ms/game-tick、模拟 ~10tps 爬行）。现状 = wall 换吞吐（出图快、模拟卡）；收窄选项：单驱动（仅 CurrentMap 的组件驱动，平滑但 wall ×N）/ 静态 per-game-tick 总预算上限（折中）。
- RandomAnimalSpawnCell_MapGen 告警刷屏（~30 条/图、约 840 行日志）：裁切图 District.TouchesMapEdge 结构性 false（方形边缘全 void；部分图六边形切点格可走则不触发）→ numDistrict=0 全采样拒绝 → fallback 随机可站格 + Warning，候选 patch = Patches_RCellFinder 接缝语义。
- 生成单帧长尾：mesh 全量重建 644-1489ms（FinishGeneration 内 RegenerateEverythingNow——跳过并依赖邻图渲染器 EnsureSectionsGenerated 懒重建的方案待调研）、RockChunks ~1049ms 单帧（原版步骤）、RocksFromGrid 岩多图 ~684ms 单帧（原版步骤）、Plants 3365-3500ms 仍是最大 CPU 项（原版成本 ~86µs/格，density=0.5 时 1399ms）。

**路线图（主文档"当前阶段计划"）**：五阶段全部实现，当前以游戏内回归和兼容修复为主。接缝参考跨删除存活不再是候选：只有实际加载 Map 才是参考，封存/删除图明确不参与；恢复时以自然重生成和当前邻图为准。残余观察项包括 AI 战斗走位级跨图选位、`Game.AddMap` 127 图上限和家园图销毁后的天气域善后。

## 存档兼容性说明

**自定义 Def 的裸字段必须显式赋值（2026-08-29 Dubs Mint Minimap 崩溃教训）**：`TerrainDef.texturePath` 是无默认值的裸字段——`RimExodus_Void` 不写即运行时为 null，Dubs Mint Minimap 的取色 `Blitter` 只防了 `def==null`/`Length==0` 没防 null → `def.texturePath.Length` NRE，且异常发生在 `Root.OnGUI`（小地图窗口 ctor 第一帧读图）会**打断整帧 GUI 绘制**（症状：底部选项卡/检查面板/小地图全部不显示、点击仍有效、日志每帧同 ref 红字）。修 = def 补 `texturePath`（`1.6/Textures/Void.png` 近黑 4×4，dontRender=true 下游戏内渲染不消费、仅第三方取色用）。纪律：新增自定义 Def 时逐字段过一遍"裸字段无默认值"清单（texturePath/uiIconPath 等），别依赖原版 configErrors 兜底——它对 `!dontRender` 才报 missing texturePath，我们恰好不触发。

**mod 已发布，修改时需考虑旧存档兼容。** 需要涉及调整存档是，必须明确知会用户并要求用户确定方案

**卸载前恢复原版兼容模式（2026-08-29 实现，待游戏内回归）**：根因定案——mod 移除后存档里 `MapParent_SeamlessTile` WorldObject 类解析失败 → `map.info.parent == null` → `Map.Tile = PlanetTile.Invalid(-1)` → `WorldGrid[-1]` 越界（每帧 UI 读 `CurrentMap.Biome` 触发循环红字；1.6 的 `MapInfo.Tile` 就是 `parent?.Tile ?? Invalid`，无独立 tile 字段），`RimExodus_Void`/传送点/岩链 def 失落为第二症状；卸载后无自愈可能——恢复必须在 mod 在场时执行。两件：
①**三层快照序列化**（`SeamlessBaseSnapshotData` 挂 `SeamlessTileManager.baseSnapshotData`，仅原生 parent 图——地块图卸载恢复必删不序列化）：terrain 全格 ushort+去重 def 表 / building·roof 稀疏；开关 `serializeBaseSnapshots`（默认开）**只门控捕获**（保存时从内存数组捕入），已捕获数据无条件照存（清除 = 设置 UI 关开关二次确认流程的显式动作 `PurgeAllSnapshots`——同时清内存数组防下次保存重捕）；读档 PostLoadInit 无条件还原内存数组（尺寸不匹配丢弃防错位）——顺带消灭 `DescribeCellReport` "读档后快照不可用"降级。
②**恢复功能**（`Source/Lifecycle/SeamlessUninstallRestore.cs`，入口 = 设置高级诊断 tab 按钮 + 确认）：前置拒绝 = 分帧生成进行中 / 任一地块图有玩家阵营 pawn（列出 tile）；CurrentMap 是地块图先切走。恢复原生图：**只回填 void 格**（接缝带混合差异属观感、原版可加载，不做全图归一；玩家不可达 void 故无建造可破坏）——terrain 按快照 `SetTerrain`（经 `Restoring` 旗标放行 `Patch_TerrainGrid_SetTerrain` 的 void 不可变守卫，唯一合法例外）+ roof `SetRoof` + 快照有岩且当前无 edifice 则 GenSpawn 重生岩石；无快照降级全图多源 BFS（`GenAdj.AdjacentCells` 口径）就近复制边界地形（roof/岩不恢复）；删 `RimExodus_SeamlessEnterSpot`/`RimExodus_VoidRockLink` thing——**两 def 均 destroyable=false，必须 DeSpawn 不可 Destroy**（红字 "Tried to destroy non-destroyable thing" 实测教训；DeSpawn 后无 holder 的 thing 不进存档，卸 mod 零残留）+ 结束 `RimExodus_TalkWithSettlementTrader`/TransitTag job + 清贸易商引用；收尾 pathGrid 全量重算 + RebuildAllRegionsAndRooms。删全部地块图走既有 `RemoveTileMap`；**残留组件节点定夺（2026-08-29，勿加回）**：map.components/game.components 里的 `RimExodus.*` 节点卸 mod 后首读档刷 "Could not find class ... use Verse.MapComponent instead"——**自愈型一次性红字**（原版空壳回落不崩，玩家读一次档再存一次档即永绝），曾实现 SaveGame Prefix/Postfix 首存剥离组件方案后被用户定夺删除（"别乱动存档机制"——patch 平时零作用纯负担，收益仅省首读档一批无害红字）。善后 = 拆影子 + `ClearAllManualDormantForRestore` + 天气域 RebindAll。完成后提示玩家**立即存档**再卸载。
- **2026-09-08 快照重建覆盖说明**：下条临时 Map 的 `<389` 重生成与销毁卫生仍是现行基础；入口已从“只补触发源图”改为统一 `EnsureNeighborSnapshots(targetTile)`，在同步 `MapGenerator.GenerateMap` Prefix 和分帧 `IncrementalMapGenerator.Start` 占用生成静态状态之前，逐张补目标 tile 周围所有实际加载参考图。完整 genStep 链先执行 `preventsGenSteps` 互斥过滤再截断；失败只 Warning，非 void 当前实况仍可用，缺失的 void 侧采样跳过；不再重建派生条带。
③**精简快照重生成（2026-08-31，勿删）**：读档后源图内存无基础三层快照（旧档无 `baseSnapshotData` / `serializeBaseSnapshots` 关闭读档）时，新图 392 混合的 `CollectNeighborRefs` 查不到条带快照参考 → **静默跳过混合**（玩家报告"旧档读档后生成的新图接缝不连续"根因）。修 = `SeamlessTileManager.GenerateTileMap` 入口（三分支之前，POI 原生/同步逃生/分帧增量统一覆盖）调 `SeamlessSnapshotRegenerator.EnsureSourceSnapshot`：缺快照时在**临时 Map**（同步段内临时加入 `Find.Maps` 再移除——`Thing.SpawnSetup` 的存在性检查是无条件 `IndexOf < 0` 无 ProgramState 逃生口，非登记图上 RocksFromGrid 的 GenSpawn 必红字+NRE[2026-08-31 首版实测教训，MapPreview"不 AddMap"先例不适用于 Playing 态 GenSpawn]；加/移都在同一次 tick 回调内，不会被 tick/渲染/存档看见，销毁 = `DisposeTempMap` 照抄 MapPreview 的 DisposeMap 卫生——全局注册表摘除[Find.TickManager.RemoveAllFromMap / sustainer.EndAllInMap，防 GL 等 mod 的 <389 步骤 spawn 的带 tick 物进入全局 tick 表持死图引用] + 共享资源归还[GlowGrid 静态光照池等逐项 Dispose] + 纯 Find.Maps.Remove——勿走 DeinitAndRemoveMap：MapDrawer.Dispose 在 sections 未生成时 NRE[sections 私有判不了 null，但临时图从不跑 mesh 重建恒 null，跳过不 Dispose 即可]，且会把临时图的 parent 误当被移除图的 parent 通知。**MapPreview 非"非登记图上 spawn"的先例（2026-08-31 对读其源码定案，references/MapPreview/.../MapPreviewGenerator.cs）：其 Patch_Verse_GenSpawn 把预览图上一切 GenSpawn 整体拦掉、岩石色从 Elevation 网格算出，spawn 存在性检查问题它从未走过；我们要真实岩体数据必须真 spawn，故走临时 AddMap + 同款销毁卫生**）上同步单帧重跑 order < 389 的地形/岩体/屋顶 genStep 子集（= 基础快照在 389 备份时点的状态；种子 = HashCombine(世界种子, tile)、RockNoises/GL shim/per-step Rand+ProgramState 全复刻 `IncrementalMapGenerator`——per-step seedPart 计数只依赖前序步，子集只删 ≥389 后续步，地形与原图确定性一致），提取三层回填源图 + `SeamStripData.CaptureAndStore` 重建条带快照（幂等：B∪T 取当前实况含玩家改动、外条带取重生成原生值）。互斥：全程持有 `mapBeingGenerated`；分帧增量/预览在飞则跳过本次（不阻塞邻图生成，混合退回静默跳过下次再补）。设置 `regenerateMissingSnapshots` 默认开。**配套语义收拢（用户定夺勿回退）**：`serializeBaseSnapshots` 关闭只影响"新快照不入档"——已捕获数据照存、内存快照总保留、**不再弹"删除已有快照"第二层确认**（`AnySerializedSnapshots`/purge 键已删，显式清除只剩卸载恢复流程的 `PurgeAllSnapshots`）；读档后缺失由本机制自动补齐闭环。mapGeneratorDef 取源图 `generatorDef`（生成时记录，缺失回落 Base_Player）。
