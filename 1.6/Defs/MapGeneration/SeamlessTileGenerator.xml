<?xml version="1.0" encoding="utf-8"?>
<Defs>

  <!--
    RimExodus 无缝地块五个 GenStepDef（不再有独立 MapGeneratorDef）。

    RimExodus_CenterCorridor (100)：中心走廊——邻接生成图在 MapGenerator.Caves 浮点网格上挖
    "本图中心 ↔ 各已生成邻居方向接缝"的星形走廊（端点 = 邻接侧接缝带按 region 分集合的随机代表
    经可达性筛选后 offset 镜像到本图），RocksFromGrid(200) 对 caves>0 格不 spawn 岩石
    （原版洞窟同一条无后效通道）。必须在 200 之前，勿后移。

    **邻居地块复用原版 Base_Player MapGeneratorDef**：WorldObjects.xml 的 RimExodus_SeamlessTileMap
    <mapGenerator> 指向 Base_Player（与 Gravship 地表降落、远行队扎营定居共用同一条链）。
    MapParent_SeamlessTile : MapParent（非 PocketMapParent），IncrementalMapGenerator 走纯基础地图路径，
    从不读 pocketMapProperties，真实 biome/elevation 来自 worldTile 的 WorldGrid 数据。
    ScenParts 等 scenario 相关 genStep 在邻居地块上无害：所有会产出东西的 ScenPart 都有
    `if (Find.GameInitData == null) return;` 守卫，邻居地块生成时 GameInitData 已为 null。

    **统一注入**：这三个 GenStepDef 通过 Patches/MapGeneration.xml 的 PatchOperationAdd 追加到
    Base_Player / Base_Faction / Encounter 的 genSteps 列表。执行顺序由 GenStepDef.order 决定（非列表序）：

      RimExodus_CoastalEdgeFill (230)：紧随 MutatorPostTerrain(220，原版 Coast mutator 铺海) 之后、
        Plants(900) 之前。补原生 Coast mutator 漏铺的海洋边（多面环海按每条边独立铺）。
        提前到 230（此前在 1801）是为了让补铺的水在 Plants(900) 之前就位——植物 spawn 时水格
        fertility=0 被跳过，避免"植物先 spawn 在土地上、随后被水覆盖导致浮在水上"。
        TileMutatorWorker_Coast 无 GenerateFinal 方法，不会在后续 MutatorFinal(1600) 覆盖。
        RocksFromGrid(200) 在 230 之前，岩石山已 spawn 为 edifice，CoastalEdgeFill 的 Stone+edifice
        守门正常工作。
      RimExodus_SeamlessTile    (389)：备份 snapshot（void 裁切前的完整矩形 topGrid）+ 按六边形铺 void
        + pathGrid 全量刷新。snapshot 记录 210/220/230 铺的地形（**2026-08-30 从 391 提前到 Roads(390)
        之前**：外条带快照只覆盖 void 侧格、道路被 TerrainGrid void 守卫拦住写不进 void 格，快照不受
        Roads 影响；Roads 改在 void 之后跑 = 道路 A* 视界内六边形外全不可走，河端点无需再延伸到
        方形边防绕行——河端点取缝中点即可，见 Patches_TileMutatorRiver）。
      RimExodus_SeamOverride    (392)：混合带卷积混合（读两端 snapshot，写本端 topGrid）+ pathGrid 刷新。
        仍在 Roads(390) 之后：B∪T 最终实况快照须含道路（后生成邻图照抄复刻道路跨缝连续）。
      RimExodus_EnterSpots     (1490)：铺传送点（从 onComplete 提前）。必须在 Fog(1500) 之前——
        Fog 的 UnfogMapFromEdge 分支依赖 CanReachMapEdge/TryFindRandomEdgeCellWith 的接缝语义 patch，
        而这些 patch 以"传送点已铺"（HasSeamEdge = listerThings 实况）为门；点在 onComplete 才铺时
        Fog 拿到的是原版方形边缘语义，在裁切图上恒不可达 → 三级候选全败 → 整图全雾（2026-08 偶发
        全雾成因之一）。铺点后 Fog/威胁步骤(1600) 期间 patch 即生效；400-1490 期间行为与旧序一致。

    **为什么 SeamlessTile/SeamOverride 在 391/392（Settlement(400) 之前，2026-08 反转旧 1400/1410）**：
    让 Settlement(400) 及之后的全部步骤在最终地形上工作——Plants(900) 的 fertility 门、Animals(1200)
    与威胁步骤(1600) 的 Standable 门天然跳过 void 格（前提：直写 topGrid 后即调 pathing.
    RecalculateAllPerceivedPathCosts，Walkable/Standable 读 PathGrid 缓存而非 terrainGrid）。
    旧序 1400/1410 下 Animals(1200) 先在将来 void 格上合法生成、再靠撤离兜底，撤离曾因 pathGrid
    过期失效（动物站 void 的历史 bug，2026-08 修正）。Fog(1500) 仍在两步之后，据最终地形揭雾不变。
    副作用（预期收益）：Patches_TerrainGrid 的 void 守卫（当前是 void 时拦新 SetTerrain）在生成期
    从空操作变为生效，400+ 的地板/污染地形写入无法在 void 格上打洞。

    **旧否决理由的重新评估（2026-08，详见 doc/地图生成步骤.md）**：旧文档以"Settlement/site BaseGen
    地板写 topGrid，snapshot 必须在其后备份；FloorFill 会覆盖混合带"为由否决 391-399。今天选址
    patch（ScatterEdgeMargin=20 / ClearRectMargin=10）已把结构推离六边形边，快照的游戏逻辑消费面
    仅剩 void 侧外条带（SeamStripData.CaptureAndStore），地板写入被拦在带外。残余风险记观察项：
    ①贴附矩形（GetOutpostRect）/降落平台残余缺口处地板可盖混合结果（旧序相反：混合盖地板）；
    ②Harbor 桥等 400-1400 地形写入不再进外条带快照（跨缝复制行为有变化）；③污染地块(890)的
    污染变体发生在混合之后，带格与对端可能不一致；④completelyIgnoreFertility 植物不受肥力门
    约束可能漏进 void（原 1400 序靠事后清理兜住）。MutatorFinal(1600) 覆写接缝的已知瑕疵与顺序无关，仍存在。

    **守卫**：genStep 类内部用 GetMapWorldTile(map) >= 0 守卫，任何有合法 worldTile 的地图都走完整链。
  -->

  <GenStepDef>
    <defName>RimExodus_CoastalEdgeFill</defName>
    <order>230</order>
    <genStep Class="RimExodus.GenStep_CoastalEdgeFill"/>
    <!-- MapPreview 集成：让本 genStep 进入预览白名单（海岸补铺在预览时可见）。
         MayRequire 用 packageId；未装 Map Preview 时此 li 整体跳过，Class 不解析，无副作用。 -->
    <modExtensions>
      <li Class="MapPreview.GenStepProperties" MayRequire="m00nl1ght.MapPreview">
        <includeInPreviews>true</includeInPreviews>
      </li>
    </modExtensions>
  </GenStepDef>

  <GenStepDef>
    <defName>RimExodus_SeamlessTile</defName>
    <order>389</order>
    <genStep Class="RimExodus.GenStep_SeamlessTile"/>
    <modExtensions>
      <li Class="MapPreview.GenStepProperties" MayRequire="m00nl1ght.MapPreview">
        <includeInPreviews>true</includeInPreviews>
      </li>
    </modExtensions>
  </GenStepDef>

  <GenStepDef>
    <defName>RimExodus_SeamOverride</defName>
    <order>392</order>
    <genStep Class="RimExodus.GenStep_SeamOverride"/>
    <modExtensions>
      <li Class="MapPreview.GenStepProperties" MayRequire="m00nl1ght.MapPreview">
        <includeInPreviews>true</includeInPreviews>
      </li>
    </modExtensions>
  </GenStepDef>

  <!-- 中心走廊：写 Caves 网格，必须在 RocksFromGrid(200) 之前（否则岩石已 spawn、只能走带后效的
       Destroy 路线）。不进 MapPreview 预览——预览纹理用 elevation≥0.7 代理画石色、不消费 Caves
       网格，进了也不生效（预览里走廊处显示岩石属已知近似，与 void 预览残留岩石同族）。 -->
  <GenStepDef>
    <defName>RimExodus_CenterCorridor</defName>
    <order>100</order>
    <genStep Class="RimExodus.GenStep_CenterCorridor"/>
  </GenStepDef>

  <!-- 传送点铺设：不进 MapPreview 预览（预览只关心地形，铺 thing 无意义）。 -->
  <GenStepDef>
    <defName>RimExodus_EnterSpots</defName>
    <order>1490</order>
    <genStep Class="RimExodus.GenStep_EnterSpots"/>
  </GenStepDef>

  <!-- 前哨保留·区域置换（2026-09 封存/重放）：本 tile 无封存记录（普通生成）时零介入。
       order 1100（2026-09 回归定夺"生成建筑的 mutator/landmark genStep 之后、pawn 与动物生成之前"）：
       身后覆盖全部常规建筑生成源（Settlement 400/401、MutatorCriticalStructures[landmark] 500、
       ScatterGroupPrefabs 950、ScatterGroup 960——含 BaseGen 从锚点铺开、绕过选址根原语的产物），
       身前避开 Animals(1200)（置换时无 pawn 在场，无需推移）。残差观察项：AncientTuneller 1650
       与 MutatorFinal 1600 的地形覆写在其后，实测污染前哨区域再按特例处理。
       首版 order 395 + 源头禁布三 patch + 延迟冲突清理已随 order 后置整体拆除（勿加回）。
       不进 MapPreview 预览（预览图无 parent，记录取不到自然早退；预览只关心地形）。 -->
  <GenStepDef>
    <defName>RimExodus_ZoneRestore</defName>
    <order>1100</order>
    <genStep Class="RimExodus.GenStep_ZoneRestore"/>
  </GenStepDef>

</Defs>
