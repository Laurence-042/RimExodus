<?xml version="1.0" encoding="utf-8"?>
<Patch>

  <!--
    RimExodus 统一注入：给所有玩家可进入的原版 MapGeneratorDef 追加五个 RimExodus genStep，
    使 Base_Player（玩家家园图（原生 parent），同时也是邻接地块 B 的 mapGenerator）、Base_Faction（派系基地）、
    Encounter（遭遇/fallback）走完全相同的海岸补铺 → void+snapshot → SeamOverride 链。

    **邻居地块 B 复用 Base_Player**：WorldObjects.xml 的 RimExodus_SeamlessTileMap <mapGenerator> 指向
    Base_Player（与 Gravship 地表降落、远行队扎营定居共用同一条链）。邻居地块不再有独立 MapGeneratorDef。
    MapParent_SeamlessTile : MapParent（非 PocketMapParent），IncrementalMapGenerator 走纯基础地图路径，
    从不读 pocketMapProperties。ScenParts 等在邻居地块无害（GameInitData==null 守卫）。

    **背景**：此前玩家家园图（原生 parent）走原生 Base_Player（无 RimExodus genStep），靠 Harmony Postfix
    GenStep_MutatorPostTerrain（order=220）在 Coast mutator 跑完后介入做海岸补铺 + void 铺设 + snapshot 备份。
    但 snapshot 在 order=220 备份太早——之后 Roads/Ruins 等仍可能改地形，导致 snapshot
    与最终地形漂移（SeamOverride 读 snapshot、写 topGrid，两者口径不一致）。
    改为 genStep 后漂移问题消除，A/B 两路径完全对称。

    **执行顺序**由 GenStepDef.order 决定（非 xml 列表序）：
      RimExodus_CoastalEdgeFill (230，紧随 MutatorPostTerrain/原版 Coast mutator 220) →
      RimExodus_SeamlessTile (389, void+snapshot+pathGrid 刷新，Roads 390 前、Settlement 400 前；2026-08-30 从 391 再提前——道路 A* 视界内六边形外不可走，河端点免延伸) →
      RimExodus_SeamOverride (392, 混合+pathGrid 刷新) →
      RimExodus_CenterCorridor (100, Caves 网格中心走廊——必须在 RocksFromGrid(200) 前) →
      RimExodus_EnterSpots (1490, 铺传送点) → Fog(1500)
    CoastalEdgeFill 提前到 230（此前在 1801）是为了让补铺的水在 Plants(900) 之前就位，避免植物浮在水上。
    SeamlessTile/SeamOverride 在 389/392（2026-08 从 1400/1410 再提前）：Settlement 及之后全部步骤在
    最终地形上工作（Plants 肥力门/Animals 与威胁步骤 Standable 门天然跳过 void——动物站 void 历史 bug
    根治）；Fog(1500) 仍在其后，flood-fill 基于最终地形算可达性（void 外条带不可达→不揭雾）。
    时序演变与残余风险详见 doc/地图生成步骤.md 反转节。
    EnterSpots 在 1490（Fog 前，2026-08 从 onComplete 提前）：Fog 的 UnfogMapFromEdge fallback 的三级
    候选依赖 CanReachMapEdge/TryFindRandomEdgeCellWith 的接缝语义 patch，而 patch 以"传送点已铺"
    （HasSeamEdge）为门——点在 onComplete 才铺时 Fog 走原版方形边缘语义，裁切图上恒不可达 →
    零揭雾、整图全雾（偶发全雾成因之一）。

    **守卫**：genStep 类内部用 GetMapWorldTile(map) >= 0 守卫，任何有合法 worldTile 的地图都走完整链。
    RimExodus 的核心交互是全局无缝——任何玩家进入的地图都应当连续、void 也切、传送点也放。

  四个 GenStepDef（RimExodus_CoastalEdgeFill/RimExodus_SeamlessTile/RimExodus_SeamOverride/RimExodus_EnterSpots）
  已在 Defs/MapGeneration/SeamlessTileGenerator.xml 定义，此处只引用 defName。
    RimWorld 先加载所有 mod 的 Defs 再跑 PatchOperation，引用时 GenStepDef 已存在。
  -->

  <!-- 全表面层 MapGeneratorDef 通配注入（2026-08，原为点名 Base_Player/Base_Faction/Encounter 三个）：
    通配覆盖 EscapeShip/Archonexus 周期/AncientStockpile/InsectLair/Anomaly（Labyrinth/Undercave/MetalHell，
    genStep 内部 GetMapWorldTile 守卫对非表面/口袋图早退，注入无害）等显式设置名单外 mapGenerator 的 def，
    以及 mod 自定义的 MapGeneratorDef（Patch 在全部 mod Defs 加载后跑，后加载者的自定义 def 同样被注入——
    mod hook 原版管线自定义结构 + 我们的裁切 genStep 双不误）。
    **排除谓词两件事，勿删**：
    ① [defName] 存在性——Abstract 基类（MapCommonBase）无 defName 但有 genSteps，通配会命中它，
      注入经 XML 继承给全部子类 = Base_Player/Encounter 等**双份注入**（389/392 重复跑）；
    ② 空间层家族（Space/Asteroid/AsteroidBasic/OrbitalItemStash/OrbitalRelay/Mechhive/SettlementPlatform
      —— 后三者同 ParentName="SpaceMapGenerator"）：GetMapWorldTile 对空间层返回 -1、genStep 内部守卫
      本会早退，但注入本身也应避免（语义上空间图不属于表面无缝网格）。
    PatchOperationAdd 对 xpath 全部命中节点逐一应用；无 <genSteps> 子节点的 def（如 SpacePocket，纯继承）
    不被命中、天然跳过。genStep 类内部用 GetMapWorldTile(map) >= 0 守卫，任何有合法表面 worldTile 的
    地图都走完整链。 -->
  <Operation Class="PatchOperationAdd">
    <xpath>/Defs/MapGeneratorDef[defName and not(defName="Space") and not(defName="Asteroid") and not(defName="AsteroidBasic") and not(defName="OrbitalItemStash") and not(defName="OrbitalRelay") and not(defName="Mechhive") and not(defName="SettlementPlatform")]/genSteps</xpath>
    <value>
      <li>RimExodus_CenterCorridor</li>
      <li>RimExodus_CoastalEdgeFill</li>
      <li>RimExodus_SeamlessTile</li>
      <li>RimExodus_SeamOverride</li>
      <li>RimExodus_EnterSpots</li>
    </value>
  </Operation>

</Patch>
